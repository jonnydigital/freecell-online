# QA/QC & Competitive Analysis Report
**Date:** 2026-03-28 | **Sites:** playfreecellonline.com, solitairestack.com
**Competitor:** online-solitaire.com (Holger Sindbaek)

---

## YESTERDAY'S CRITICAL BUGS — STATUS CHECK

### 1. Mobile Game Board Blank (Bug #1 from 03-27)
**Status: FIXED ✅**
The `DomGameShell.tsx` container at line 761 now uses `h-[100dvh]` which applies at all breakpoints, replacing the previous `min-[1180px]:h-[calc(100dvh-1rem)]` that left mobile with no explicit height. The game board should now render correctly on all viewport sizes. (Could not fully verify via browser resize due to macOS minimum window constraints, but code inspection confirms the fix.)

### 2. Google AdSense Ads Not Rendering (Bug #2 from 03-27)
**Status: ROOT CAUSE IDENTIFIED ⚠️ — Waiting on Google approval**
All ad units remain unfilled. AdSense dashboard investigation confirmed:
- Both playfreecellonline.com and solitairestack.com show **"Getting ready"** approval status in AdSense
- The AdSense account (ca-pub-3083538874906149) is still in onboarding mode (originally set up for mentalrefreshment.com)
- ads.txt is serving correctly at playfreecellonline.com/ads.txt but AdSense shows "Not found" — likely crawl lag since it was added Mar 27
- **No code fix will resolve this** — ads will start filling once Google completes site review

**Code-side improvements made:**
- Created new in-article ad unit (slot `9238758088`) in AdSense dashboard
- Updated `InContentAd` component to pass slot ID, fluid format, and in-article layout
- Ad containers now auto-collapse when unfilled (no more empty gaps)

### 3. Game Dropdown Doesn't Auto-Close (Bug #3 from 03-27)
**Status: FIXED ✅ (code-verified)**
Code inspection confirms a capture-phase `pointerdown` handler (DomGameShell.tsx lines 156-166) closes the dropdown when clicking outside. The handler listens on the document and sets `showGameDropdown(false)` when the click target is outside the dropdown container.

---

## BUGS FOUND TODAY

### 4. 8th Foundation Pile (♣) Clipped on Desktop
**Severity:** LOW → **FIXED ✅**
**Where:** playfreecellonline.com — game board top row
**What:** The club (♣) foundation pile on the far right was partially cut off by the `overflow-clip` container.
**Fix:** Changed card width CSS formula in `dom-card-styles.css` from `calc((100vw - 380px) / 7.8)` with max `135px` to `calc((100vw - 380px) / 8.6)` with max `125px`. All 8 cells now fit within the container. Verified on live deployed site.

### 5. Large Empty Gaps Between Content Sections
**Severity:** MEDIUM (hurts UX and SEO engagement)
**Where:** Homepage below the fold — between intro text and "How to Play" section (~200px gap), and between "Why Play Here?" cards and "FreeCell Variants" section (~200px gap)
**What:** These gaps are caused by `InContentAd` components rendering 90px minimum-height containers plus `my-8` margins when ads don't fill. Since ads are consistently unfilled, the page has noticeable dead zones.
**Fix applied today:** Updated `AdUnit.tsx` to collapse (return null) after 12 seconds if ad status is null/unfilled. Also updated `SidebarAdSlot` in `DomGameShell.tsx` to fully collapse the sidebar ad card after 15 seconds if unfilled. This removes the empty gaps and visible empty ad boxes.

---

## FIXES IMPLEMENTED TODAY

### Fix A: Ad Container Collapse on Unfilled Slots
**Files changed:**
- `src/components/AdUnit.tsx` — Added `MutationObserver` watching for `data-ad-status="unfilled"` attribute. Added 12-second fallback timer that collapses the component if no ad fills. When collapsed, component returns `null` (removes the empty `minHeight: 90px` container from DOM).
- `src/components/dom-freecell/DomGameShell.tsx` — Updated `SidebarAdSlot` to detect unfilled status and collapse the entire section (including border/background card) after 15 seconds. Previously, the empty card with border would remain visible even when no ad filled.

**Impact:** Eliminates ~400px of dead space on the homepage when ads don't fill. Sidebar no longer shows empty bordered boxes. When ads eventually start filling, containers render normally.

### Fix B: 8th Foundation Pile Clipping
**Files changed:**
- `src/components/dom-freecell/dom-card-styles.css` — Changed card width formula divisor from `7.8` to `8.6` and max width from `135px` to `125px`. The old formula caused 8 cards + gaps to exceed the `overflow-clip` container width, cutting off the club (♣) foundation pile.

**Impact:** All 8 top cells (4 free cells + 4 foundations) now render fully visible at all desktop viewport widths. Verified on live deployed site (playfreecellonline.com).

### Fix C: In-Content Ad Unit Configuration
**Files changed:**
- `src/components/InContentAd.tsx` — Added slot ID `9238758088` (created in AdSense dashboard), switched to `format="fluid"` and `layout="in-article"` for proper in-article ad rendering.

**Impact:** In-content ads now have proper slot IDs and use the correct AdSense in-article format. Will begin filling once AdSense site approval completes.

---

## ADDITIONAL INVESTIGATIONS

### AdSense Dashboard Findings
Visited the AdSense dashboard directly and confirmed:
- Account ca-pub-3083538874906149 is in onboarding/approval mode
- Both playfreecellonline.com and solitairestack.com show **"Getting ready"** status
- ads.txt crawl shows "Not found" despite being correctly served (crawl lag from Mar 27 deployment)
- **No code-level fix possible** — must wait for Google's site review process to complete

### Leaderboard Data Storage Investigation
- `src/lib/leaderboardClient.ts` uses deterministic seed data (hardcoded names: FreeCellFan, DailyDealer, CardShark42, AcePlayer, SolitaireKing)
- API routes (`src/app/(main)/api/leaderboard/daily/route.ts` and `submit/route.ts`) are wired for `@vercel/kv` (Redis-compatible)
- Requires `KV_REST_API_URL` environment variable — **Vercel KV store not yet provisioned**
- **Action required:** Create KV store in Vercel dashboard → link to project → env vars auto-set

### Monthly Milestone Badges
- Already fully implemented in `src/components/sidebar/SidebarDailyChallenge.tsx` with Bronze (10 wins, 🥉), Silver (20 wins, 🥈), Gold (full month, 🥇)
- Progress bar with milestone markers renders correctly in sidebar
- No additional work needed — this was already done

---

## COMPETITIVE ANALYSIS UPDATE

### Competitor Site (online-solitaire.com/freecell) — March 28 Observations:
- **March challenge calendar** still prominently displayed with milestone badges (10, 20, 31 wins) and progress bar
- **Daily leaderboard** showing real player data: "ze21" with 00:24 time, 68 moves, 92 score — active player engagement
- **Achievements section** present (Win ratio, Best time, Best moves, Best score)
- **"Signup to save your progress"** green CTA prominent in sidebar
- **Bottom toolbar:** New game, Hint, Undo, Bookmark, Download, Settings, More — well-organized
- **No visible changes** from yesterday's competitor review

### Our Improvements Since Yesterday:
- ✅ **Leaderboard populated** — Now shows 5 seeded entries (FreeCellFan, DailyDealer, CardShark42, AcePlayer, SolitaireKing) with times and moves. Much better than yesterday's empty "No scores yet" state.
- ✅ **Monthly challenge calendar** visible on both playfreecellonline.com and solitairestack.com with March 2026 dates and "0/31 March Wins" progress tracker
- ✅ **Hint system working** — Observed "Park 6♠ in free cell" toast notification on page load
- ✅ **Onboarding walkthrough** on solitairestack.com — "Welcome to FreeCell!" tooltip with Skip/Next buttons
- ✅ **Settings & Stats icons** visible in the toolbar (gear and trophy icons)

### Remaining Competitive Gaps:

| Feature | Competitor | Our Site | Priority |
|---------|-----------|----------|----------|
| Real leaderboard data | Active players (ze21, etc.) | Seeded/bot data only | HIGH |
| Working ad monetization | Freestar header bidding, video ads | AdSense unfilled on all slots | CRITICAL |
| Monthly challenge with milestone badges | Visual progress bar + trophy icons | ✅ Bronze/Silver/Gold badges + progress bar implemented | DONE |
| 320+ game variants | Massive game catalog | ~10 variants | MEDIUM |
| Account system / signup | "Signup to save progress" | No account system | MEDIUM |
| Embeddable widget | Available with embed code | Not available | LOW |
| Blog with content | Ghost CMS blog | No blog | MEDIUM |
| Video ad player | Sidebar video ads | No video ads | LOW |

---

## SEO & CONTENT OBSERVATIONS

### Strong Points:
- Extensive footer with 50+ content links covering games, strategy, tips, rules, FAQ, glossary
- "Browse all 25+ solitaire games →" CTA link present
- Each variant has dedicated content pages
- FAQ section with expandable questions on homepage

### Content Gap Opportunities:
- `/blog` — Still missing; competitor uses this for content freshness signals
- `/freecell-solver` — High search intent keyword, no dedicated page
- In-content ads now have proper slot IDs (slot `9238758088`) — will generate revenue once AdSense approves sites

---

## PRIORITIZED ACTION PLAN

### Completed Today:
- ~~Check AdSense dashboard~~ → ✅ Done. Sites in "Getting ready" status. Waiting on Google.
- ~~Create ad slot IDs for in-content ads~~ → ✅ Done. Slot `9238758088` created and wired up.
- ~~Fix 8th foundation pile clipping~~ → ✅ Done. CSS formula updated and verified on live site.
- ~~Test dropdown auto-close~~ → ✅ Done (code-verified). Capture-phase handler confirmed.
- ~~Add milestone badge visuals~~ → ✅ Already implemented (Bronze/Silver/Gold + progress bar).
- ~~Investigate leaderboard storage~~ → ✅ Done. Needs Vercel KV provisioning.

### Remaining — This Week:
1. **Provision Vercel KV store** — Create in Vercel dashboard, link to project. This enables real leaderboard data collection. (Jonathan needs to do this via Vercel dashboard.)
2. **Monitor AdSense approval** — Check daily for status change from "Getting ready" to approved.

### This Month:
3. Apply for Freestar when traffic justifies it (target: 1K+ daily users)
4. Launch blog for ongoing SEO content
5. Add more game variants (target 20+)
6. Build embeddable widget for backlink generation
7. Build account system for persistent player profiles

---

## METRICS TO TRACK
- AdSense site approval status (currently: "Getting ready")
- AdSense fill rate (currently 0% — blocked by approval)
- Daily unique visitors (target: 100K/month)
- Games played per session
- Daily challenge completion rate
- Content page bounce rate

---

## ITEMS REQUIRING JONATHAN'S ACTION
1. **Vercel KV Store** — Go to Vercel dashboard → freecell-online project → Storage → Create KV store → Link to project. This auto-sets `KV_REST_API_URL` and `KV_REST_API_TOKEN` env vars, enabling real leaderboard data.
2. **AdSense Monitoring** — Check AdSense dashboard periodically for site approval status change.

---

*Next review: 2026-03-29. All code-fixable items from today's review are closed. Remaining blockers are external (Google AdSense approval, Vercel KV provisioning).*
