# QA Report — April 24, 2026

> **Testing method.** SSR HTML audit via curl (both production domains + competitor). Interactive browser QA blocked this cycle — the Claude-in-Chrome extension required user-interactive pairing (multiple browsers connected), and the alternate `mcp__Control_Chrome__*` path returned "Google Chrome is not running" on every JS execute after navigation, so live DOM inspection was unavailable. Desktop/mobile SSR coverage is complete; client-hydration visual QA was deferred. One code change shipped and verified live.

---

## TL;DR

1. **One code change shipped and verified live.** Added a server-rendered `<h1>` to `/game/[number]` deal pages on both `playfreecellonline.com` and `solitairestack.com`. Commit `877cddd`, deploy `dpl_HRzakRptDpMJC7SdaJZFsFsNq5Pr` — READY. Live HTML now emits `FreeCell Game #1 — First MS deal`, `FreeCell Game #617 — Windows default`, etc. Prior to this fix, game pages had **zero** H1 tags in SSR HTML.
2. **Root cause of the missing H1.** `DomGameShell` is loaded via `next/dynamic` with `ssr: false`, so none of its headings appear in the server HTML; `GameShell.tsx` (the non-DOM fallback code path) has an `<h1>Freecell.</h1>` but that's not used in production. The server-rendered `GameDealInfo` article used `<h2>` for the deal title — upgraded to `<h1>` with deal label appended when the deal is in the curated catalogue.
3. **No new competitor activity.** `online-solitaire.com` HTML is byte-equivalent at the title/description/H1/schema surface to April 23. Standing gaps on our side unchanged.
4. **Homepages and FreeCell spoke remain clean.** `/`, `/freecell` (hub), and `/strategy` continue to emit their full ld+json set with stable ids, aggregateRating where relevant, and one H1 per page.

---

## Phase 1 — Visual QA

### Method limitations this cycle

- `mcp__Claude_in_Chrome__tabs_context_mcp` → **Multiple Chrome extensions connected** error; no way to pick a browser without user action in a scheduled context.
- `mcp__Claude_in_Chrome__switch_browser` → **timeout after 60s**.
- `mcp__Control_Chrome__execute_javascript` → navigation succeeded (new tab for `/game/1`), but every subsequent JS execute returned `Google Chrome is not running`, even after `osascript activate`. Root cause not diagnosed this cycle; likely an MCP-helper disconnect.
- JavaScript-from-AppleScript is disabled system-wide (`Allow JavaScript from Apple Events` off), so osascript was also unavailable as a fallback.

Result: the SSR HTML audit below is complete and live-verified; interactive hydration / mobile layout / card-dimension checks are deferred to the next cycle.

### Desktop — playfreecellonline.com (SSR HTML)

PASS.
- `/` — title and meta description present; H1 `Play FreeCell Online — Free, No Download` SSR-rendered; 5 ld+json scripts (`ld-organization`, `ld-webapplication-site`, `ld-website`, `ld-game-freecell-solitaire`, `ld-faqpage`); `Game` has `aggregateRating` 4.8/4312; canonical set to `https://playfreecellonline.com`.
- `/game/1` — after today's fix, 1 H1 in SSR HTML (`FreeCell Game #1 — First MS deal`). Schema present: Organization + WebApplication + Game (with AggregateRating + Offer) + BreadcrumbList. Title `FreeCell Game #1 - First MS deal`, meta description present, canonical set to `/game/1`.
- `/game/617` — 1 H1 in SSR HTML (`FreeCell Game #617 — Windows default`). Deal-label injection working as designed for curated deals.

### Desktop — solitairestack.com (SSR HTML)

PASS.
- `/` — 1 H1, 6 ld+json scripts: `ld-faqpage-hub`, `ld-itemlist-hub`, `ld-organization`, `ld-webapplication-hub`, `ld-webapplication-site`, `ld-website-hub`. Meta description present and accurate.
- `/freecell` spoke — 1 H1 `Play FreeCell Online — Free, No Download`. Five ld+json scripts including `ld-webapplication-freecell` (SoftwareApplication). `ld-breadcrumblist` present.
- `/game/512294` — after today's fix, 1 H1 in SSR HTML. Schema surface identical to `playfreecellonline.com/game/N` (Game + AggregateRating + BreadcrumbList). Confirms the shared `src/app/(main)/game/[number]/page.tsx` fix reaches both domains.

### Mobile — deferred

Mobile interactive QA was not run this cycle (see method limitations). Last confirmed mobile QA: **April 23 iframe proxy at 375×812** — 52 cards rendered, 39px card width, board 357px wide, no clipping, no horizontal scroll, top-bar and action bar present. No mobile-specific CSS changes have shipped since; the April 23 result is the best current state-of-the-world estimate. Rerun interactive mobile QA at the next cycle when the Chrome MCP is reachable.

---

## Phase 2 — Competitor Analysis (online-solitaire.com)

No visible changes. SSR HTML surface identical to April 23:
- Title: `Solitaire - Play Online for Free`
- H1: `Play Solitaire Online for Free (No Signup Required)`
- Meta description: `Play Solitaire online for free. No signup required. A feature-rich full-screen game with daily challenges, winnable deals, hints, and undo's`
- Schema: `AggregateRating`, `BreadcrumbList`, `ImageObject`, `ListItem`, `Offer`, `Organization`, `WebApplication`.
- Internal nav still routes to `/about`, `/blog/`, and card-image assets via client-side routing (React SPA pattern unchanged).

Standing gaps on our side (unchanged from April 20/23):
- No per-user Achievements panel.
- No in-app Signup / Download CTA (intentional — we're a no-signup product).
- No country-flag styling on leaderboard entries.
- No bookmark button on the game board.
- No PWA install CTA.

Logged in `docs/research/competitor-log.csv`.

---

## Phase 3 — SEO & Schema Audit

Spot-check results against the SEO checklist:

- **One H1 per page** — PASS on homepage, /freecell, /strategy, and (after today's fix) all `/game/[number]` pages. Previously FAIL on `/game/[number]` (0 H1s).
- **JSON-LD schema present** — PASS. Game pages emit Game + BreadcrumbList; homepage emits Organization + WebApplication + WebSite + Game + FAQPage; /freecell spoke adds SoftwareApplication + BreadcrumbList; hub homepage adds ItemList.
- **Stable JSON-LD `id=` attrs** — PASS. All scripts have stable ids (`ld-organization`, `ld-webapplication-site`, `ld-website`, `ld-schema`, `ld-faqpage`, etc.) — React 18 hydration duplication prevention from commit `67224d3` still holding.
- **AggregateRating on game pages** — PASS. 4.8/4312 visible on `/game/1`, `/game/617`, and `/game/512294` (hub). Confirms the April 23 `82174cf` fix is still live.
- **Internal links to /strategy and /how-to-play** — PASS. `GameDealInfo` emits 6 intra-site links per game page (`/famous-freecell-deals`, `/freecell-solvability`, `/freecell-mastery`, `/freecell-probability`, `/solver`, `/strategy`). Strategy hub has 105 internal links.
- **Meta descriptions unique per page** — PASS on spot-checks (homepage, /freecell, /game/1, /game/617 all have distinct descriptions via `variedDescription()` in `page.tsx`).

Sitemap — not re-audited this cycle (no changes to route ownership since last check).

---

## Phase 4 — Improvements shipped

1. **`fix(seo): add server-rendered H1 on /game/[number] deal pages`** — commit `877cddd`, deploy `dpl_HRzakRptDpMJC7SdaJZFsFsNq5Pr` READY at `1777036621183`. One file changed (`src/app/(main)/game/[number]/GameDealInfo.tsx`), +4/−3 lines. TypeScript check PASS. Live verification confirmed H1 present on `/game/1`, `/game/617`, and hub `/game/1`.

---

## Phase 5 — Backlog / remaining items

Carried over from prior cycles:

- **Mobile interactive QA** — not run today due to Chrome MCP availability. Next cycle: reproduce April 23 375×812 iframe test, confirm no regression from today's SSR-HTML change (none expected — CSS untouched).
- **solitairestack.com hub copy vs sitemap mismatch** (April 20): hub copy claims "26 distinct variants" / "20 solitaire variants documented in our catalog" but sitemap only exposes ~18 playable game slugs. Either ship the missing landing pages (Spider variants, Eight Off landing, etc.) or tighten the copy. Medium priority, not addressed today.
- **playspidersolitaireonline.com hydration duplication regression** (April 23): 12 ld+json scripts, 6 unique ids doubled. Fix lives in the sibling `playspidersolitaireonline` repo — out of scope for this freecell-online cycle, but the bug still reads as active on the `/spider` page.
- **Chrome MCP reliability** — worth asking about. Two separate Chrome MCP paths failed today (extension pairing + Control_Chrome JS execute). Reducing to one canonical browser automation path would make scheduled QA cycles more reliable.

---

## Commits this cycle

- `877cddd` — fix(seo): add server-rendered H1 on /game/[number] deal pages
