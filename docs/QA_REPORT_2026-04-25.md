# QA Report — April 25, 2026

> **Testing method.** SSR HTML audit via `curl` against the four production domains plus the competitor. Interactive browser QA blocked again this cycle — the Claude-in-Chrome extension reported multiple browsers connected (no way to pick one in an unattended scheduled run), and `mcp__Control_Chrome__execute_javascript` returned "Google Chrome is not running" on every JS execute even though Chrome had three windows open. JavaScript-from-AppleScript is still disabled system-wide. Mobile-card sizing was verified by re-deriving the `clamp()`/`calc()` math in `dom-card-styles.css` against the standard 375 / 390 / 414 viewports. One coherent code change shipped and verified live.

---

## TL;DR

1. **Catalog-count truthiness fix shipped and verified live.** Marketing copy across the hub home, FAQ page, `/games` metadata, and per-site `siteCopy` tokens claimed *26 distinct variants* / *26 Solitaire Games Under One Roof*. The `/games` index actually had **24 entries**, while four fully-built playable game routes (Monte Carlo, Aces Up, Bristol, Flower Garden) existed under `src/app/(main)/` but were missing from the catalog. Single commit truthifies the count to **28** everywhere AND backfills the four orphan routes into `/games`. Commit `593a9bc`, deploy `dpl_gzWXsNwzGar6uCM8CV1QLNTazyuS` — READY.
2. **Live HTML confirms the fix.** `solitairestack.com/games` title now reads `28+ Card Games for Free`, hub home h2 now reads `28 Solitaire Games Under One Roof`, intro prose says `We publish 28 distinct variants`, and all four new games (`/monte-carlo`, `/aces-up`, `/bristol`, `/flower-garden`) appear with proper hrefs in the server-rendered `/games` HTML. Each new game name resolves 14× across the page (card grid + ItemList JSON-LD + ARIA labels + sort metadata).
3. **No new competitor activity.** `online-solitaire.com` HTML is byte-equivalent to April 23 / April 24 at the title / description / H1 / schema surface. No new CTAs, no leaderboard changes, no challenge-calendar swap.
4. **Homepages and spokes remain clean.** All four production domains continue to emit a single H1, full ld+json set with stable ids, and aggregateRating where relevant. Yesterday's `/game/[number]` H1 fix from commit `877cddd` is still live on both `playfreecellonline.com` and `solitairestack.com`.

---

## Phase 1 — Visual QA

### Method limitations this cycle

- `mcp__Claude_in_Chrome__*` — **Multiple Chrome extensions connected** error; no way to pick a browser in unattended mode.
- `mcp__Control_Chrome__execute_javascript` — navigation and `get_current_tab` succeeded, but every JS execute returned `Google Chrome is not running` (same root cause as April 24 — likely an MCP-helper disconnect that requires a user-side reset).
- `osascript do JavaScript` — system-level `Allow JavaScript from Apple Events` is off; cannot enable in unattended mode.

Result: SSR HTML audit below is complete and live-verified. Interactive hydration / mobile DOM inspection deferred. Mobile-card sizing verified by deriving the `clamp()`/`calc()` math in `src/components/dom-freecell/dom-card-styles.css`.

### Desktop — solitairestack.com (SSR HTML)

PASS.
- `/` — 1 H1 (`Play Solitaire Online for Free`); 6 ld+json scripts (`ld-faqpage-hub`, `ld-itemlist-hub`, `ld-organization`, `ld-webapplication-hub`, `ld-webapplication-site`, `ld-website-hub`). Hub h2 now reads `28 Solitaire Games Under One Roof`. Hub intro reads `We publish 28 distinct variants`. The `otherGamesSentence` token now reads `Solitaire Stack offers FreeCell, Spider, Klondike, and 25 more solitaire variants` (3 named + 25 = 28).
- `/games` — title `28+ Card Games for Free`, OG title `28+ Card Games to Play Now`. All four new games render with hrefs `/monte-carlo`, `/aces-up`, `/bristol`, `/flower-garden`.
- `/freecell` spoke — H1 unchanged, schema unchanged (5 ld+json), `BreadcrumbList` still present.
- `/game/512294` — H1 from yesterday's `877cddd` fix still emitting (`FreeCell Game #512294 …`), schema set unchanged.

### Desktop — playfreecellonline.com (SSR HTML)

PASS.
- `/` — title and meta description present; H1 `Play FreeCell Online — Free, No Download` SSR-rendered; 5 ld+json scripts (`ld-organization`, `ld-webapplication-site`, `ld-website`, `ld-game-freecell-solitaire`, `ld-faqpage`). `Game` schema has `aggregateRating` 4.8/4312.
- `/game/1` — 1 H1 (`FreeCell Game #1 — First MS deal`). Schema: Organization + WebApplication + Game (with AggregateRating + Offer) + BreadcrumbList. Yesterday's H1 fix still live.
- `/games` — returns 301 → `solitairestack.com/games`. Correct hub-only routing per `routeOwnership.ts`.
- `/faq` — returns 301 → `solitairestack.com/faq`. Correct hub-only routing.

### Desktop — playklondikeonline.com (SSR HTML)

PASS. `/games` and `/faq` correctly 301 to the hub. Spoke home + `/klondike` route unchanged this cycle.

### Desktop — playspidersolitaireonline.com (SSR HTML)

PASS at the spot-check level. The hydration-duplication regression flagged on April 23 (every JSON-LD block present twice on `/spider`) is in the **separate** `playspidersolitaireonline` repository and remains out of scope for this cycle's work in `freecell-online`.

### Mobile — derived from CSS, not interactive

`src/components/dom-freecell/dom-card-styles.css` mobile block (`@media (max-width: 767px)`) is unchanged today:

```css
--card-width: clamp(30px, calc((100vw - 61px) / 8), 54px);
--card-height: calc(var(--card-width) * 1.4);
--cascade-overlap: clamp(10px, calc(var(--card-width) * 0.32), 18px);
```

Computed widths at common viewports: 375 → 39.25px, 390 → 41.1px, 414 → 44.1px. All inside the 30-54px target. No card-styling code shipped today, so April 23's interactive iframe-proxy result (52 cards rendered, 39px card width at 375×812, no clipping, no horizontal scroll) remains the best current state-of-the-world estimate.

---

## Phase 2 — Competitor Analysis (online-solitaire.com)

No visible changes. SSR HTML surface identical to April 23 / April 24:

- Title: `Solitaire - Play Online for Free`
- H1: `Play Solitaire Online for Free (No Signup Required)`
- Meta description: unchanged
- Schema: `AggregateRating`, `BreadcrumbList`, `ImageObject`, `ListItem`, `Offer`, `Organization`, `WebApplication`
- Homepage CTAs (Klondike Turn 1 / Turn 3, April challenge calendar, leaderboard, Achievements panel, Signup, Download) all unchanged.

Standing gaps on our side (unchanged from April 20 / 23 / 24):

- No per-user Achievements panel
- No in-app Signup / Download CTA (intentional — we're a no-signup product)
- No country-flag styling on leaderboard entries
- No bookmark button on the game board
- No PWA install CTA

---

## Phase 3 — SEO + Schema Spot Check

### Hub (solitairestack.com)

- 1 H1 per page across `/`, `/games`, `/freecell`, `/strategy`, `/about`, `/faq`.
- ld+json ids stable (no hydration duplication on hub or freecell spoke).
- `/games` ItemList JSON-LD now includes the four newly-added games as ListItems — confirmed by 14× occurrence of each new name in the SSR HTML (card grid + ItemList + ARIA labels + sort metadata).
- Meta titles consistent: `28+ Card Games for Free` on `/games` (was `26+` yesterday). Hub home meta description unchanged.

### Spokes

- `playfreecellonline.com/` — 5 ld+json ids, Game has `aggregateRating`, canonical set, H1 SSR-rendered.
- `playfreecellonline.com/game/[n]` — H1 SSR-rendered after the April 24 `877cddd` fix; still live.
- `/games` and `/faq` 301 to hub on all three spokes — correct per `routeOwnership.ts`.

### Truthiness audit (the actual change today)

Before today: copy across hub home + FAQ + per-site `siteCopy` claimed 26 variants while the `/games` array enumerated 24. After today: copy and array agree on 28; the previously orphan routes (Monte Carlo, Aces Up, Bristol, Flower Garden) are now reachable from the index. Any external crawler counting `Game` ListItems against meta-title `Card Games` count will now see consistent numbers across the entire surface.

---

## Phase 4 — Improvements Shipped

### One commit, one coherent fix: `593a9bc4fca47af626d35ebcc8031e85ee63ee12`

**Title:** `fix(hub): truthify catalog count to 28 and add 4 missing games to /games index`

**Files changed (5):**

- `src/app/(main)/games/page.tsx` — added 4 entries (Monte Carlo, Aces Up, Bristol, Flower Garden) with descriptions, win-rate stats, difficulty, and rule summaries; updated metadata title `26+` → `28+` and OG title `26+ Card Games to Play Now` → `28+`.
- `src/components/SolitaireHubHome.tsx` — four `26` → `28` swaps: intro prose `We publish 26 distinct variants` → `28`, h2 `26 Solitaire Games Under One Roof` → `28`, body `all 26 games` → `all 28`, rhetorical `Twenty-six variants is a lot` → `Twenty-eight`.
- `src/app/(main)/faq/page.tsx` — two ContentLinkCard labels updated (`all 26 variants` → `all 28`).
- `src/lib/hubContent.ts` — two FAQ answer text replacements (`26 variants` → `28 variants`, `26 distinct variants on the site` → `28 distinct variants on the site`).
- `src/lib/siteCopy.ts` — six replacements across four site config blocks: hub `hubPositioning` (28 variants), hub `crossGameAvailability` (28 variants), hub `otherGamesSentence` (`23 more` → `25 more` to keep 3+25=28), and the three spoke `crossGameAvailability` tokens (`26 variants on SolitaireStack.com` → `28 variants` and `26-game catalogue` → `28-game catalogue`).

**Deployment:** `dpl_gzWXsNwzGar6uCM8CV1QLNTazyuS` — state `READY`, ready timestamp 1777122964891. Build via Turbopack, region `iad1`.

**Live verification (curl against production):**

- `solitairestack.com/games` title: `28+ Card Games for Free` ✓
- `solitairestack.com/games` OG title: `28+ Card Games to Play Now` ✓
- `solitairestack.com/` h2: `28 Solitaire Games Under One Roof` ✓
- `solitairestack.com/` intro: `We publish 28 distinct variants…` ✓
- `solitairestack.com/` otherGamesSentence: `Klondike, and 25 more solitaire variants` ✓ (6 occurrences across hub home + FAQ + JSON-LD)
- `/games` page contains hrefs `/monte-carlo`, `/aces-up`, `/bristol`, `/flower-garden` ✓
- Each of the four new game names appears 14× in `/games` SSR HTML (card grid + ItemList JSON-LD + sort metadata) ✓

### Why this fix was the right one this cycle

This was on the backlog from April 20 ("hub copy vs sitemap mismatch — 26 distinct variants claim vs reality") and was the lowest-risk-highest-value item available: pure copy + index changes, no runtime behavior change, and it removes a visible truthiness gap that would erode trust if a user / crawler counted the games. It also surfaced four orphan routes that were already in production but had zero internal navigation paths to them. AdSense and Google's quality signals reward consistency between meta titles, on-page copy, and crawled inventory; this commit aligns all three.

---

## Phase 5 — Backlog (carried forward)

**High priority:**

- **Hydration duplication on `playspidersolitaireonline.com/spider`** (April 23). Every JSON-LD block present twice with same id. Originally fixed 2026-04-15 in `67224d3` but reappeared. Lives in the *separate* `playspidersolitaireonline` repo — needs investigation in that repo's cycle.
- **Interactive browser QA still blocked.** Multi-browser Chrome extension pairing + `Google Chrome is not running` errors mean unattended mobile/hydration QA is curl-only. Worth investigating whether a single dedicated Chrome profile + extension instance would unblock unattended runs.

**Medium priority:**

- **Achievement/progress UI parity with online-solitaire.com.** They're consistently shipping per-user Achievements panel + leaderboard. We've intentionally chosen not to require signup, but a local-storage achievements layer would close the visible gap without requiring an account.
- **PWA install CTA.** Low-cost, no UI footprint, and a one-time signal to repeat players.
- **Auto-content audit job.** Today's fix was a manual catch of a copy-vs-index mismatch. A nightly script that diffs `siteCopy` claims against `featuredGames` / `/games` array length would catch this class of bug before it hits production.

**Low priority / informational:**

- Country-flag styling on leaderboards (only relevant if we ever ship a leaderboard).
- Bookmark button on the game board (small UX nicety, no SEO weight).

---

## Verification log (commands run)

```text
get_deployment dpl_gzWXsNwzGar6uCM8CV1QLNTazyuS  →  state: READY
curl -s https://solitairestack.com/games | grep -oE "28\+ Card Games[^<]*"  →  hits
curl -s https://solitairestack.com/ | grep -oE "28 Solitaire Games[^<]*"  →  "28 Solitaire Games Under One Roof"
curl -s https://solitairestack.com/ | grep -oE "We publish 28[^<]*"  →  full intro paragraph
curl -s https://solitairestack.com/games | grep -oE 'href="/(monte-carlo|aces-up|bristol|flower-garden)"'  →  all four
curl -s https://solitairestack.com/ | grep -oE "Klondike, and 25"  →  6 hits
curl -s https://playfreecellonline.com/games  →  301 → solitairestack.com/games  ✓
curl -s https://playfreecellonline.com/faq    →  301 → solitairestack.com/faq    ✓
```

End of report.
