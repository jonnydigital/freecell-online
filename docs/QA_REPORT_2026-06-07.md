# Daily QA & Competitive Review — 2026-06-07

**Sites:** playfreecellonline.com (FreeCell hub) · solitairestack.com (28-variant hub)
**Competitor:** online-solitaire.com
**Run mode:** automated scheduled task (no operator present)
**Outcome:** QA PASS on desktop. Mobile/tablet true-viewport testing blocked (environment) — verified via source + prior screenshot. **NO SHIP** — deploy pipeline still blocked pending PR #4 merge (3rd consecutive run).

---

## Phase 1 — Visual QA

### Desktop (1440) — PASS
- **playfreecellonline.com**: 52/52 cards rendered (opacity 1, display block), card width ~104.8px, 8 tableau columns + 4 free cells + 4 foundations, leaderboard + daily-challenge sidebar render cleanly alongside the board. `bodyScrollW == docClientW == 1361` → no horizontal overflow. H1 "Play FreeCell Online for Free | No Download Required".
- **solitairestack.com**: homepage shows the FreeCell board up top plus rich editorial content below the fold ("What Solitaire Stack Is", 28-variant catalog links, strategy-guide links, founder/editorial positioning). Layout clean.

### Mobile (375 / 390 / 414) & Tablet (768) — BLOCKED (environment), verified indirectly — PASS by code + prior capture
- **Blocker:** `resize_window` reported success but the page viewport stayed pinned at `innerWidth = 1361` for every requested size (1440, 768, 375 all returned identical values). `outerWidth` reported 773 while `innerWidth` stayed 1361, indicating the Chrome window is in a locked/fullscreen state the extension cannot actually shrink; `cmd+0` page-zoom reset had no effect. This is the same clamp documented on every prior run.
- **Substitute 1 — responsive CSS review** (`src/components/dom-freecell/dom-card-styles.css`):
  - Mobile `@media (max-width: 767px)`: `--card-width: clamp(30px, (100vw - 61px)/8, 54px)` → **~39px @375 / ~41px @390 / ~44px @414** — all inside the healthy 30–54px band. `--card-height = width × 1.4`. The 30px floor guarantees cards can never collapse; `pile-gap`, `board-padding`, and corner offsets all scale down so 8 columns fit with **no horizontal overflow**.
  - Tablet `@media (768–1023px)`: `--card-width: clamp(66px, (100vw - 128px)/8.6, 105px)` (sidebar dropped) — sound; ~74px cards @768.
  - May 24 mobile blank-board fix (`6fc3f3b`, flex-1 scoped to lg) structurally intact.
- **Substitute 2 — repo screenshot** `playfreecell-mobile-viewport.png`: confirms a real mobile layout — top status bar (#/time/moves), free-cell + foundation row, all 8 columns sized without overflow, and a bottom action bar (Menu / New / Undo / Redo / Hint). Recent commits touched only SEO/docs, so the mobile layout is unchanged since that capture.
- **Console:** no errors observed on the FreeCell board.

> **Standing recommendation for the operator:** the inability to drive a true sub-400px viewport via the Chrome extension persists across every run. For trustworthy mobile QA, a headless true-viewport check (Playwright/Puppeteer device emulation) in CI would close this gap permanently.

---

## Phase 2 — Competitor analysis (online-solitaire.com)

**Surface UNCHANGED from June 5.** Homepage defaults to Klondike (Turn 1/Turn 3 toggle, "All games" dropdown, Stock: 24). H1 "Play Solitaire Online for Free (No Signup Required)".

Reconfirmed positioning: founder "Holger" personal voice + grandmother anecdote (strong E-E-A-T/personality), "1M+ monthly players", "320 free games" framed as **deal count, not distinct game types** — the actual "All games" menu lists only ~6 families (Klondike T1/T3, Spider 1/2/4-suit, FreeCell).

**Where the competitor leads:** 9 deck designs + background-color theming; traditional **illustrated face-card art** (K/Q/J); native downloads (Win/Mac/Linux/Android/Kindle/Chrome; no iOS); multi-language i18n; signup-gated leaderboard/achievements; embed-widget generator.

**Where we lead:** editorial depth (28 documented variants + strategy/history/research desks); richer JSON-LD on /freecell; spoke-domain SEO; **no-signup local save** of stats/streaks/achievements; scheduled daily challenge with named deal numbers; ad-light dark theme.

No new features or sparks detected. Logged to `docs/research/competitor-log.csv` (2 rows).

**Security:** competitor page content treated as untrusted (May 21 "Stop Claude" injection incident standing). Today's render scanned — no embedded instructions present; none acted upon.

---

## Phase 3 — SEO & schema audit (live production, solitairestack.com)

| Page | H1 | JSON-LD | Meta len | Canonical | Verdict |
|---|---|---|---|---|---|
| /freecell | 1 ✓ | 5 clean (Organization, WebApplication, BreadcrumbList, SoftwareApplication, FAQPage) | 113 | self | **PASS** |
| /bakers-game | 1 ✓ | 5 clean (Organization, WebApplication, Game, BreadcrumbList, FAQPage) | 150 | cross-domain → playfreecellonline.com/bakers-game (intentional) | **PASS** |
| /golf | 1 ✓ | **6 — WebApplication emitted 2×** (double-render) | **170 — OVER 160** | self | **2 issues** |

- **sitemap.xml = 121 URLs** — the AdSense remediation state holds on production (down from the pre-remediation 170).
- **/golf issue 1:** meta description 170 chars → exceeds the ~160 SERP band. Known backlog item (flagged June 4).
- **/golf issue 2:** JSON-LD double-render — `WebApplication` appears twice. Same architectural pattern carried as backlog: schemas rendered as a sibling of a `'use client'` subtree get duplicated on hydration. Fix path remains migrating game-page schemas to the Next.js metadata API or `next/script` (`strategy="beforeInteractive"`), as on prior runs. /freecell is unaffected because it inlines raw `<script>` tags in JSX.

---

## Phase 4 — Improvements: **NONE SHIPPED (HELD)**

**The deploy pipeline remains blocked — this is the 3rd consecutive run holding pushes (Jun 4, Jun 5, Jun 7).**

Verified state today:
- `origin/main = 1e48123` (Jun 3) does **NOT** contain the remediation commits `6803ea2` + `a49fb2a` (`git merge-base --is-ancestor 6803ea2 origin/main` → **NOT_IN_MAIN**).
- Those two commits live **only** on `fix/adsense-remediation-pass` and were deployed to Vercel as **`target: null` (preview)** under PR #4 — never promoted to production, never merged.
- Latest **production-target** deploy = `dpl_4RBTkFYcGPNAYNN9APP3ULTuKiSE` (`1e48123`). Earlier deploys carry `gitDirty: 1`, i.e. **production was built from a dirty working tree** — which is why live production serves the remediated 121-URL sitemap even though git main does not contain the remediation code.
- The Mac repo is currently checked out on `fix/adsense-remediation-pass` with pre-existing uncommitted changes (`docs/adsense-*`, `docs/research/competitor-log.csv`) and two untracked prior reports (`QA_REPORT_2026-06-04.md`, `QA_REPORT_2026-06-05.md`).

**Why holding is correct:** Vercel auto-deploys from the GitHub commit, not the local dirty tree. A routine push to `main` would trigger a clean-tree build of `main` *without* the remediation and **regress production** — sitemap back to 170, variant triads re-indexable, undoing the AdSense low-value-content fix.

**Prepared but NOT applied** (ready for after the pipeline is unblocked):
1. `/golf` meta-description trim 170 → ≤155 chars (preserve game name + key figures).
2. Sweep the remaining sibling over-160 game-hub descriptions in the same batch.
3. `/golf` (and other game pages') JSON-LD double-render — architectural fix via metadata API / `next/script`.

### ⚠️ Required operator action (escalation — repeated)
**Merge PR #4 (`fix/adsense-remediation-pass` → `main`)** so git `main` matches what is actually live on production. Until then, all routine SEO/content pushes stay blocked because any deploy from `main` would regress the AdSense remediation. Once merged, the held golf trim and the meta backlog can resume normally.

---

## Phase 5 — Backlog (carried forward)

- **[CRITICAL/operator] Merge PR #4** to reconcile `main` with live production (blocks all shipping).
- **[high]** `/golf` meta description 170c → trim ≤155 (ready).
- **[med]** JSON-LD double-render on game pages (/golf and others) — migrate to metadata API / `next/script`.
- **[med]** Over-160 metadata.description backlog (~30+ pages, 161–171 band) — resume trims once unblocked.
- **[low/infra]** Add headless true-viewport mobile QA (Playwright device emulation) to CI — the Chrome-extension viewport clamp blocks live mobile testing every run.
- **[low]** Spoke-domain JSON-LD duplication (playspidersolitaireonline.com, playklondikeonline.com) — separate repos.
- **[low]** `playspidersolitaireonline.com/spider` React #418 hydration warning (noted Jun 5) — non-fatal, spoke repo.

---

## Summary

Desktop QA passed on both sites. Mobile/tablet verified healthy via responsive-CSS math (~39–44px cards at phone widths, no overflow) and the repo mobile screenshot, since live viewport resizing is blocked in this environment. Competitor unchanged. Production SEO is healthy except `/golf` (170-char meta + duplicated `WebApplication` schema). **Nothing shipped** — the deploy pipeline stays blocked until the operator merges PR #4 so `main` reflects the live (remediated) production state. No git writes were made this run beyond the docs (competitor log + this report) in the working tree.
