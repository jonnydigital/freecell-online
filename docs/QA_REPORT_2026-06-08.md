# Daily QA & Competitive Review — 2026-06-08

**Sites:** playfreecellonline.com (FreeCell hub) · solitairestack.com (28-variant hub)
**Competitor:** online-solitaire.com
**Run mode:** automated scheduled task (no operator present)
**Outcome:** QA PASS on desktop. Mobile/tablet true-viewport testing blocked (environment) — verified via source review. **NO SHIP** — deploy pipeline still blocked pending PR #4 merge (**4th consecutive run**).

---

## Phase 1 — Visual QA

### Desktop (1440×900) — PASS
- **playfreecellonline.com**: 52/52 cards rendered (`opacity: 1`, `display: block`), card width 122.32px, 8 tableau columns + 4 free cells + 4 foundations, with the Daily Leaderboard + Daily Challenge sidebar rendering cleanly alongside the board. `innerWidth == docWidth == scrollWidth == 1512` → no horizontal overflow. H1 "Play FreeCell Online for Free | No Download Required". Console clean (no errors/exceptions on `/game/1`).
- **solitairestack.com**: homepage shows the FreeCell board above the fold (identical engine to playfreecellonline.com) with the editorial hub content below. Layout clean, no clipping.

### Mobile (375 / 390 / 414) & Tablet (768) — BLOCKED (environment), verified by code review — PASS
- **Blocker (recurring):** `resize_window` reported success and `outerWidth` dropped to 769, but the page's layout viewport stayed pinned at `innerWidth = 1512` (= `screen.width`) for every requested size. The viewport meta tag is correct (`width=device-width, initial-scale=1, viewport-fit=cover`) and there is no body/html transform or zoom — the Chrome window simply cannot be driven below the host display width in this environment. Same clamp documented on every prior run.
- **Substitute — responsive CSS review** (`src/components/dom-freecell/dom-card-styles.css`):
  - Mobile `@media (max-width: 767px)`: `--card-width: clamp(30px, (100vw − 61px)/8, 54px)` → **~39px @375 / ~41px @390 / ~44px @414** — all inside the healthy 30–54px band. `--card-height = width × 1.4`. The 30px floor prevents card collapse; `--pile-gap` (2–5px), `--board-padding-x` (4–10px), and corner offsets all scale down so 8 columns fit within the viewport with no horizontal overflow (8 × 39 + gaps + padding ≈ 351px < 375px).
  - Tablet `@media (768–1023px)`: `--card-width: clamp(66px, (100vw − 128px)/8.6, 105px)` (sidebar dropped) — sound; ~74px cards @768.
  - Spider override paths (`[data-game="spider"]`) carry their own correct breakpoints (10-column math).
  - The May 24 mobile blank-board fix (`6fc3f3b`, `flex-1` scoped to `lg`) is structurally intact; no code touching the board layout has shipped since.

> **Standing recommendation for the operator:** the inability to drive a true sub-400px viewport through the Chrome extension persists on every run. For trustworthy mobile QA, add a headless true-viewport check (Playwright/Puppeteer device emulation) to CI — it would close this gap permanently.

---

## Phase 2 — Competitor analysis (online-solitaire.com)

**Surface UNCHANGED from June 7.** Homepage still defaults to Klondike (Turn 1 / Turn 3 toggle, "All games" dropdown, Stock: 24). H1 "Play Solitaire Online for Free (No Signup Required)" (unchanged). Meta description unchanged (140 chars). 7-entry hreflang (`x-default, en, de, es, fr, it, pt`) — the durable 6-language i18n moat holds. Single JSON-LD graph (1 `<script>`).

Right rail: June 2026 challenge calendar (today = **Mon Jun 8** marked PLAYING), Daily leaderboard top-3 (sts 🇺🇸 00:54 / 94 / 148; CodigoRojo 🇺🇸 01:01 / 95 / 156 ×2), Achievements panel (all N/A pre-play), "Signup to save your progress" CTA. Bottom toolbar unchanged: New game / Hint / Undo / Bookmark / Download / Settings / More. Traditional illustrated face-card art (J/K visible).

**Where the competitor leads:** multi-language i18n; native/PWA downloads; deck/background theming; illustrated face cards; on-board bookmark; broader (deal-count-framed) catalog; embed-widget generator; signup-gated leaderboard/achievements.

**Where we lead:** editorial depth (28 documented variants + strategy/history/research desks); richer JSON-LD on /freecell (5 clean schemas); spoke-domain SEO; no-signup local save of stats/streaks/achievements; scheduled daily challenge with named deal numbers; ad-light dark theme; in-game hint tooltips.

No new features or sparks detected. Logged to `docs/research/competitor-log.csv`.

**Security:** competitor page content treated as untrusted (May 21 "Stop Claude" injection incident standing). Today's render scanned — no embedded instructions present; none acted upon.

---

## Phase 3 — SEO & schema audit (live production)

| Page | H1 | JSON-LD | Meta len | Canonical | Verdict |
|---|---|---|---|---|---|
| solitairestack.com/freecell | 1 ✓ | **5 clean** (Organization, WebApplication, BreadcrumbList, SoftwareApplication, FAQPage) | 113 | self ✓ | **PASS** |
| solitairestack.com/golf | 1 ✓ | **12 — full set double-rendered** (WebApplication ×4, Organization ×2, BreadcrumbList ×2, Game ×2, FAQPage ×2) | **170 — OVER 160** | self ✓ | **2 issues** |

- **Internal linking** on /freecell: 5 `/strategy` links + 19 `/how-to-play` links — healthy.
- **sitemap.xml = 121 URLs** (122 `https://` matches incl. 1 xmlns namespace) — the AdSense remediation state **holds on production** (down from the pre-remediation 170).
- **/golf issue 1 — meta 170 chars.** Known backlog item (flagged since June 4). Prepared trim below.
- **/golf issue 2 — JSON-LD double-render, now 12 scripts.** This appears to have **worsened** vs. the June 7 observation (6 scripts, WebApplication ×2): the entire 6-schema set is now emitted twice. Root cause remains the architectural pattern where game-page schemas rendered as a sibling of a `'use client'` subtree get duplicated on hydration. /freecell is unaffected because it inlines raw `<script>` tags directly in JSX. Fix path unchanged: migrate game-page schemas to the Next.js metadata API or `next/script` (`strategy="beforeInteractive"`). Carried as architectural backlog.

---

## Phase 4 — Improvements: NONE SHIPPED (HELD — 4th consecutive run)

**The deploy pipeline is still blocked.** Verified this run:

- Mac repo is on branch `fix/adsense-remediation-pass` at `a49fb2a` (with uncommitted doc edits + prior untracked QA reports).
- `origin/main` HEAD is still **`1e48123`** (June 3). `git merge-base --is-ancestor` confirms **neither remediation commit is in main**: `6803ea2` NOT in main, `a49fb2a` NOT in main.
- Vercel production-target deploy is **`dpl_4RBTkFYcGPNAYNN9APP3ULTuKiSE`** (commit `1e48123`, main, June 3, READY). The two remediation deploys (`6803ea2`, `a49fb2a`) are **`target: null` PREVIEW only** (PR #4) — never promoted to production.
- Production currently serves the remediated state (sitemap 121, variant triads noindexed) because earlier deploys were built from a dirty working tree. **Git main does not contain that remediation.**

**Consequence:** a routine push to `main` would trigger a clean-tree auto-deploy of *main-without-remediation* and **regress production** — sitemap back to 170, variant triads re-indexable — in the middle of an active AdSense re-review where a regression is high-cost. So every routine SEO ship remains blocked until git is reconciled. All git writes held this run.

**Prepared but NOT applied** (`src/app/(main)/golf/page.tsx` line 13 `metadata.description`):

```
Before (170): Play Golf Solitaire online for free. Clear seven columns by playing cards one rank higher or lower than the waste pile. Streak scoring, undo, hints. No download required.
After  (151): Play Golf Solitaire online free. Clear seven columns by playing cards one rank above or below the waste pile. Streak scoring, undo, hints, no download.
```
Preserves the leading game name, the ±1-rank mechanic, and all feature hooks — no fabrication.

### RECOMMENDED OWNER ACTION (now 4th consecutive run, escalating)
**Merge PR #4 (`fix/adsense-remediation-pass` → `main`).** This makes git `main` match what is already live in production (so it does **not** change what users see), unblocks all routine SEO ships, and removes the standing regression landmine. Until this merge happens:
1. No SEO/content ships can go out (the /golf trim + remaining over-160 backlog all wait).
2. Production remains one accidental `main` push away from losing the AdSense remediation.
3. The /golf JSON-LD double-render (worsening) can't be addressed.

---

## Phase 5 — Backlog (carried forward)

- **[BLOCKER] Merge PR #4** to reconcile main with live production (above). Gates everything else.
- **[SEO] Over-160 meta-description backlog** — /golf (170, trim prepared) + remaining entries (161–171 chars). Ship once unblocked.
- **[Architecture] JSON-LD double-render** on game/variant pages (/golf now 12 scripts) — migrate schemas to metadata API / `next/script`.
- **[Spoke repos] playspidersolitaireonline.com/spider** React #418 hydration warning (separate repo) — non-fatal, board renders.
- **[Tooling] Headless true-viewport mobile QA** in CI to permanently close the mobile-testing gap.
- **[Competitive] Feature gaps** where competitor leads: i18n, native/PWA downloads, deck/background theming, illustrated face cards.

---

*Generated by the automated daily QA run. No git writes were made this run; this report is written to the working tree uncommitted (consistent with the standing deploy-blocker hold).*
