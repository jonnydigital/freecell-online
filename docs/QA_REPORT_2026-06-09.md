# Daily QA & Competitive Review — 2026-06-09

Sites: **playfreecellonline.com** (FreeCell hub) · **solitairestack.com** (multi-game hub)
Competitor: **online-solitaire.com**
Method: Chrome extension (live production) + repo source review.

---

## Phase 1 — Visual QA

### Desktop (1440×900) — PASS
- **playfreecellonline.com**: 8 tableau columns + 4 free cells + 4 foundations + right sidebar (ad slot, daily leaderboard, daily challenge) all render cleanly. Card ranks/suits readable and correctly positioned. No overlap, clipping, or horizontal scroll. Live DOM: 52/52 `.dom-card` at opacity 1, display block, card width 113.95px; `innerWidth == documentElement.scrollWidth == 1440` (no overflow).
- **solitairestack.com** homepage: same FreeCell board + editorial hub below the fold. Clean.
- **solitairestack.com/pyramid** game page: renders without layout faults.

### Mobile (375 / 390 / 414) — PASS (verified via code + live DOM)
True sub-500px screenshots could **not** be captured this run — the Chrome extension pins the rendered viewport at 1440px regardless of `resize_window` (outerWidth dropped to 756 but `innerWidth`/`visualViewport`/`bodyClientWidth` stayed 1440). This is a recurring environment limitation, not a site bug; the viewport meta is correctly set (`width=device-width, initial-scale=1, viewport-fit=cover`).

Substituted authoritative checks:
- `dom-card-styles.css` `@media (max-width: 767px)`: 8-column `--card-width: clamp(30px, (100vw-61px)/8, 54px)` → **~39px @375 / ~41px @390 / ~44px @414**, all inside the healthy 30–54px band. 10-column variant `clamp(24px, (100vw-66px)/10, 44px)` → ~31px @375. `board-max-width: 100%` plus space-reserving gap math guarantees columns fit with no overflow; height = width × 1.4.
- Live DOM: all 52 cards present, opacity 1, display block.
- Responsive breakpoints present: 767 / 1023 / 1279px. Mobile bottom action bar present (`md:hidden`).

**No mobile layout bug found.**

### Tablet — PASS (code-level)
`@media (min-width: 768px) and (max-width: 1023px)`: `--card-width: clamp(66px, (100vw-128px)/8.6, 105px)` — sound, board fills without the desktop sidebar.

---

## Phase 2 — Competitor Analysis (online-solitaire.com)

**Surface UNCHANGED from June 8** (now ~5 consecutive static observations). Homepage defaults to Klondike (Turn 1 / Turn 3 toggle, "All games" dropdown, Stock: 24). Right rail: June 2026 challenge calendar (today Jun 9 highlighted, bronze/silver/gold track), daily leaderboard top-3, achievements (Win ratio / Best time / Best moves / Best score, all N/A pre-play), "Signup to save your progress." Illustrated face-card art. Bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More. No new features, redesigns, or content.

- **Gaps where they lead:** multi-language i18n (6-language hreflang), native/PWA downloads, deck/background theming, illustrated face cards, on-board bookmark, embed widget, deal-count-framed catalog.
- **Where we lead:** editorial depth (28 documented variants + strategy/history/research desks), richer JSON-LD on /freecell, spoke-domain SEO, no-signup local save of stats/streaks/achievements, named-deal daily challenge, ad-light dark theme.

Logged to `docs/research/competitor-log.csv`.

---

## Phase 3 — SEO & Schema Audit (live production)

| Page | H1 | Meta desc | JSON-LD | Canonical | Verdict |
|------|----|-----------|---------|-----------|---------|
| /freecell | 1 ✓ | 113c ✓ | 5 clean (Org+WebApp+Breadcrumb+SoftwareApp+FAQ) ✓ | self ✓ | **PASS** |
| /golf | 1 ✓ | **170c (over 160)** | **12 (double-render)** | self ✓ | 2 issues |
| /tripeaks | 1 ✓ | 151c ✓ | **12 (double-render)** | self ✓ | double-render |

- **JSON-LD double-render is widespread**, not golf-specific: /tripeaks emits the full 6-schema set twice (12 scripts) as well. Architectural backlog — ld+json is emitted in the page body and should move to the metadata API / be deduped.
- **/golf meta description 170c** still over the 160 limit (carried backlog).
- **Sitemap.xml = 121 URLs, HTTP 200** — AdSense remediation (variant triads dropped/noindexed) **holds on production**.

---

## Phase 4 — Improvements

**NO SHIP THIS RUN — all git writes held (5th consecutive run).**

The deploy blocker documented since June 4 is still active and was re-verified today:
- Mac repo is on branch `fix/adsense-remediation-pass`.
- `origin/main = 1e48123` (Jun 3). `git merge-base --is-ancestor` confirms remediation commits **6803ea2 NOT in main** and **a49fb2a NOT in main**. Both live only on PR #4, deployed as Vercel **preview** (target: null), never promoted/merged.
- Production currently serves the remediated state (sitemap 121) from an **earlier dirty-tree build, not from git main**.
- A routine push to `main` would trigger a clean-tree auto-deploy of *main-without-remediation* and **regress production** (sitemap back to 170, variant triads re-indexable) **mid-AdSense-review**.

Pushing routine SEO trims now would do real, hard-to-reverse harm, so all writes were held — consistent with the established protocol.

**Prepared but not applied** (ready to ship once unblocked):
1. /golf meta-description trim 170 → ≤155c.
2. JSON-LD dedupe pass across game/variant pages (golf, tripeaks, and siblings showing 12-script double-render).

---

## Phase 5 — Backlog / Open Items

- **🔴 OWNER ACTION (escalating, 5th run): merge PR #4 (`fix/adsense-remediation-pass` → `main`)** so git main matches live production. Changes nothing users see; unblocks all routine SEO ships and removes the production-regression landmine.
- JSON-LD double-render on game/variant pages (golf, tripeaks, +siblings) — move ld+json to metadata API / dedupe.
- /golf meta description over 160c (trim ready).
- /pyramid renders a generic cascade tableau (stock + waste + 7 columns) rather than a classic pyramid-of-cards shape — functional-fidelity question; needs owner confirmation whether the generic engine is intentional. Not a layout/clip bug.
- Competitor moats to consider: i18n, native/PWA downloads, deck/background theming, illustrated face cards, embed widget.
- Environment: Chrome extension cannot render a true sub-500px mobile viewport; mobile QA continues to rely on code + live-DOM verification.

---

## Security
All page content (own sites and competitor) treated as untrusted per the standing May 21 "Stop Claude" injection-incident protocol. Today's renders scanned; no embedded instructions present, none acted upon.
