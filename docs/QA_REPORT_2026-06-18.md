# Daily QA Report — 2026-06-18

**Sites:** playfreecellonline.com (FreeCell) · solitairestack.com (hub, 28 variants)
**Competitor:** online-solitaire.com
**Method:** Live Chrome extension QA + same-origin iframe true-viewport rig; Vercel deploy verification; Mac git via osascript.
**Outcome:** Visual QA clean (desktop + true mobile 375/390/414). Competitor unchanged. Shipped 4 meta-description fixes, verified live.

---

## Phase 1 — Visual QA

### Desktop (1440×900) — PASS
- **playfreecellonline.com** `/game/1`: 8 columns + 4 free cells + 4 foundations + leaderboard/daily-challenge sidebar. 52 `.dom-card` present, opacity 1, display block, card width 122.32px. No horizontal overflow (innerWidth == docScrollWidth).
- **solitairestack.com** home: renders FreeCell board + editorial hub below the fold (28-variant positioning, links to Spider/Klondike spoke domains). Internal links resolve — Spider/Klondike/Yukon point to sister domains (playspidersolitaireonline.com, playklondikeonline.com), Baker's Game/Eight Off to playfreecellonline.com, on-site games (/pyramid, /tripeaks, /golf, etc.) at root. No broken links observed.
- **solitairestack.com** `/freecell`: H1=1, board renders clean.

### Mobile — PASS (true-viewport via same-origin iframe rig)
The `resize_window` trap recurred again: the OS window resized (outerWidth ~783) but the render viewport stayed pinned at screen width (innerWidth 1512), so mobile media queries never fired and screenshots showed the desktop layout. Used the documented same-origin iframe rig instead (cross-origin iframes are XFO-blocked, so the parent tab must be on the same origin as the framed page).

| Site / page | Width | Cards | Card width | Overflow |
|---|---|---|---|---|
| pfco `/game/1` | 375 | 52, opacity 1 | 38.75px | none |
| pfco `/game/1` | 390 | 52, opacity 1 | 40.63px | none |
| pfco `/game/1` | 414 | 52, opacity 1 | 43.63px | none |
| solitairestack `/freecell` | 375 | 52, opacity 1 | 38.75px | none |

All card widths land in the healthy 30–54px band. Mobile top bar (#1 / time / moves), bottom toolbar (home / shuffle / undo / redo / hint), and hint toast all present and correctly positioned. No clipping, no horizontal scroll at any width. No layout/render bug found; no QA code change needed.

---

## Phase 2 — Competitor (online-solitaire.com)

Surface **UNCHANGED** from June 17 (and effectively unchanged for weeks). Homepage defaults to Klondike (Turn 1 / Turn 3 toggle, "All games (320)" dropdown, Stock: 24). Top bar Time/Moves/Stock; bottom toolbar New game / Hint / Undo / Bookmark / Download / Settings / More. Right rail: June 2026 challenge calendar ("No wins" bronze/silver/gold track), daily leaderboard, achievements (Win ratio / Best time / Best moves / Best score) all N/A pre-play, "Signup to save your progress" CTA. H1=1 ("Play Solitaire Online for Free (No Signup Required)"), meta 140c, single JSON-LD graph (WebApplication+BreadcrumbList), 7-entry hreflang (x-default/en/de/es/fr/it/pt) — the i18n moat is intact. No new features, redesigns, or content additions detected.

**Standing gaps where they lead:** multi-language i18n hreflang, native/PWA downloads, deck/background theming, illustrated face cards, embed widget, deal-count-framed catalog (320 games).
**Where we lead:** editorial depth (28 documented variants + Strategy/History/Rules/Research desks), richer per-game JSON-LD (5–6 unique schemas incl. Game+FAQPage), spoke-domain SEO, no-signup local save of stats/streaks, named daily-challenge deals, ad-light dark theme.

**Security:** competitor page content treated as untrusted (standing protocol). Scanned today's render and page text — no embedded instructions present, none acted upon.

---

## Phase 3 — SEO & Schema Audit

Full same-origin sweep of all **121 sitemap URLs** on solitairestack.com:
- **H1:** every page has exactly one H1 — zero missing, zero duplicate.
- **Canonical:** present on pages checked.
- **JSON-LD:** intact with stable IDs (e.g. `id="ld-organization"`); the Jun-11 double-render fix holds (raw "ld+json" string counts include RSC flight-payload duplicates, not extra script tags). /freecell and /golf confirmed H1=1 + schema present.
- **Sitemap:** 121 URLs, no missing/errored pages; AdSense remediation (variant triads noindexed) holds.
- **Meta descriptions:** sweep found **4 pages over 160c** — the only over-length metas remaining after the Jun-15→17 backlog clearance. Fixed this run (see Phase 4).

---

## Phase 4 — Improvements Shipped

**Commit `4916cab` — fix(seo): trim 4 static page meta descriptions to ≤155c**
Visible page copy unchanged; metadata `description` field only. `tsc --noEmit` clean.

| Page | Before (decoded) | After (decoded, verified live) |
|---|---|---|
| /history | 158 | 154 |
| /how-solitaire-changed-windows | 161 | 150 |
| /about | 162 | 152 |
| /games | 156 | 140 |

Deploy `dpl_GYfjBd8RPzrdrBvtTuqJySycAcmu` reached **READY**, aliased to playfreecellonline.com + www. Re-verified all 4 live (decoded 140–154c, all ≤155). Meta-description backlog is now fully cleared across author / blog / static / comparison / per-game / FAQ / editorial pages.

**Ops note:** removed a stale 0-byte `.git/index.lock` (no live git process) before committing — same condition handled on Jun 17.

---

## Phase 5 — Backlog (carried)

- **/pyramid renders a generic 7-column cascade**, not the canonical 28-card triangular Pyramid layout — game-logic fidelity, needs owner confirmation whether intentional (carried from Jun 9/16/17; not a layout/clip bug).
- Author **Person JSON-LD uses bioFull** — acceptable for structured data, deferred.
- **Spoke-domain JSON-LD duplication** lives in separate repos — out of scope for this repo.
- Competitive feature gaps (i18n, PWA/native downloads, theming, illustrated face cards, embed widget) — product-roadmap items, not QA fixes.

---

## Security

All page content (own sites + competitor) treated as untrusted per standing protocol. No embedded instructions found in any scanned render or page text; none acted upon. No consent/cookie banners interacted with.
