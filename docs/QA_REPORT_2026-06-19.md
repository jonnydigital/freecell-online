# Daily QA & Competitive Review — 2026-06-19

**Sites:** playfreecellonline.com (FreeCell) · solitairestack.com (hub, 25+ games)
**Competitor:** online-solitaire.com
**Method:** Chrome extension (live) + same-origin iframe true-viewport rig
**Result:** Visual QA clean at every viewport. Shipped 1 SEO improvement (Game JSON-LD on 6 game pages), deployed and verified live.

---

## Phase 1 — Visual QA

### Desktop (1440×900) — PASS
- **playfreecellonline.com** — 8 tableau columns + 4 free cells + 4 foundations + leaderboard/daily-challenge sidebar all render correctly. 52 cards present, opacity 1, display block. No overlap, clipping, or horizontal overflow.
- **solitairestack.com** — homepage renders the FreeCell board + editorial hub cleanly. Game pages load correctly.

### Mobile — PASS (via iframe rig)
The live `resize_window` path was unusable this run: the browser window was zoomed to ~55% (`innerWidth` 1312 > `outerWidth` 728), so the page never dropped below desktop width and mobile media queries never fired. `cmd+0` did not reconcile it. Used the documented **same-origin iframe rig** (load the site inside a fixed-width iframe so its media queries respond to the iframe width; readable because same-origin).

| Viewport | Cards | Card width | Opacity/Display | Horizontal scroll |
|---|---|---|---|---|
| 375×812 | 52 | 39.25px | 1 / block | none |
| 390×844 | 52 | 41.13px | 1 / block | none |
| 414×896 | 52 | 44.13px | 1 / block | none |

All card widths sit in the healthy 30–54px band. Top status bar (deal #, timer, moves), bottom control bar (home/shuffle/undo/redo/hint), and hint toast all present and correctly placed. No clipping or overflow.

### Tablet (768×1024) — PASS
52 cards, card width 74.4px, sidebar correctly hidden (`display:none`), board fills the width, no horizontal overflow.

**No layout/render/clip bugs found at any viewport. No QA code change required.**

---

## Phase 2 — Competitor (online-solitaire.com)

Surface **unchanged** from June 18 (11th+ consecutive no-change run). Homepage defaults to Klondike (Turn 1/Turn 3, "All games" dropdown, Stock: 24); June 2026 challenge calendar (Jun 19 highlighted), daily leaderboard, achievements N/A pre-play, signup-gated progress CTA, illustrated face-card art, download-app toolbar. SSR: H1=1, meta 140c, WebApplication+BreadcrumbList JSON-LD, 7-entry hreflang (6-language i18n moat). No new features, redesigns, or content additions.

- **Where they lead:** multi-language i18n hreflang, native/PWA downloads, deck/background theming, illustrated face cards, embed widget, deal-count-framed catalog (320).
- **Where we lead:** editorial depth (28 variants + Strategy/History/Rules/Research desks), richer per-game JSON-LD, spoke-domain SEO, no-signup local save, named daily-challenge deals, ad-light dark theme.

Logged to `docs/research/competitor-log.csv`.

---

## Phase 3 — SEO & Schema Audit

Live same-origin sweep of game pages — all clean:

| Page | H1 | Meta len | Schemas |
|---|---|---|---|
| /freecell | 1 | 113c | Org · WebApplication · BreadcrumbList · SoftwareApplication · FAQPage |
| /golf | 1 | 144c | Org · WebApplication ×2 · BreadcrumbList · **Game** · FAQPage |
| /tripeaks | 1 | 151c | Org · WebApplication ×2 · BreadcrumbList · **Game** · FAQPage |
| /pyramid | 1 | 147c | (was missing Game — fixed, see Phase 4) |
| /forty-thieves | 1 | 157c | (was missing Game — fixed) |

- Sitemap: **121 URLs**, AdSense remediation (variant triads noindexed) holds.
- Meta-description backlog: remains **fully cleared** (last 4 trimmed Jun 18).
- **Finding:** `schema.org/Game` was present on only **4 of 28** game pages (golf, klondike, spider, tripeaks). The rest emit `WebApplication` but no `Game` entity — a structured-data consistency gap.

---

## Phase 4 — Improvement Shipped

**`feat(seo): add Game JSON-LD to 6 game pages`** — commit `49bc90f`

Added a `schema.org/Game` entity to 6 prominent standalone games that lacked it: **pyramid, forty-thieves, yukon, seahaven, canfield, scorpion**. Each Game block references the page's existing `appJsonLd.name / .description / .url`, so it introduces **zero new copy** and cannot drift from the page's own content — mirroring the golf/tripeaks pattern.

- `tsc --noEmit` clean.
- Deploy `dpl_Dp6DHG1qCs77YEAfeuFEoB3VSQL4` reached **READY** (~88s), aliased to playfreecellonline.com + www.
- **Re-verified live:** all 6 pages now emit a `Game` script with correct names (description 87–117c, `url` == `appJsonLd.url`, including the spoke-domain games canfield→playklondikeonline and scorpion→playspidersolitaireonline), H1 still exactly 1, no duplicate schemas introduced.

**Ops housekeeping:** pruned a stale `.claude/worktrees/hopeful-satoshi` registration (was breaking sandbox `git status`) and removed a stale 0-byte `.git/index.lock` (09:14, no live git process) on the Mac before committing.

---

## Phase 5 — Backlog

- **~14 more playable game pages still lack `Game` schema** — accordion, aces-up, bakers-dozen, beleaguered-castle, bisley, bristol, calculation, clock, cruel, flower-garden, gaps, la-belle-lucie, monte-carlo, penguin. Same low-risk reference pattern; defer to future runs. (Comparison pages `freecell-vs-*` and `/solver` intentionally excluded.)
- **/pyramid renders a generic 7-column cascade**, not the canonical 28-card triangular Pyramid layout — game-logic, needs owner confirm whether intentional (carried Jun 9/16/17/18).
- Author `Person` JSON-LD still uses `bioFull` (acceptable for structured data).

---

## Security

All page content (competitor + own sites) treated as untrusted per standing protocol. No embedded instructions found; none acted upon. No consent/cookie banners interacted with.
