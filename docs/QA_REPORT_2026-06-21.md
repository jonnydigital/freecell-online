# Daily QA & Competitive Review — 2026-06-21

**Sites:** playfreecellonline.com (FreeCell) · solitairestack.com (hub, 25+ games)
**Competitor:** online-solitaire.com
**Method:** Chrome extension (live) + same-origin iframe true-viewport rig
**Result:** Visual QA clean at every viewport. Shipped 1 SEO fix (thin `/terms` meta description), deployed and verified live.

---

## Phase 1 — Visual QA

### Desktop (1440×900) — PASS
- **playfreecellonline.com** `/game/1` — 8 tableau columns + 4 free cells + 4 foundations + leaderboard/daily-challenge/calendar sidebar all render correctly. 52 cards present, opacity 1, display block, card width 122.32px. No overlap, clipping, or horizontal overflow.
- **solitairestack.com** — homepage renders the FreeCell board + sidebar cleanly (loads FreeCell as the default game).

### Mobile — PASS (via iframe rig)
The live `resize_window` path was again unusable this run: the outer window resized (outerWidth 743) but the render viewport stayed pinned at `innerWidth` 1512 (= screen width), so mobile media queries never fired. Used the documented **same-origin iframe rig** (load the site inside a fixed-width iframe so its media queries respond to the iframe width; readable because same-origin). Viewport meta confirmed correct (`width=device-width, initial-scale=1, viewport-fit=cover`).

| Viewport | Cards | Card width | Board vs viewport | Opacity/Display | H-scroll |
|---|---|---|---|---|---|
| 375×812 | 52 | 39.25px | 359 < 375 | 1 / block | none |
| 390×844 | 52 | 41.13px | 374 < 390 | 1 / block | none |
| 414×896 | 52 | 44.13px | 398 < 414 | 1 / block | none |

All card widths sit in the healthy 30–54px band. 22 controls present at 375px (full control set). Sidebar correctly hidden at ≤414px.

### Tablet (768×1024) — PASS
52 cards, card width 74.41px, sidebar `display:none`, board fills the width (736px < 768), no horizontal overflow.

**No layout/render/clip bugs found at any viewport. No QA code change required.**

---

## Phase 2 — Competitor (online-solitaire.com)

Surface **unchanged** from June 20 (13th+ consecutive no-change run). Homepage defaults to Klondike (Turn 1/Turn 3 toggle, "All games" dropdown, Stock: 24); top bar Time/Moves/Stock; right rail June 2026 challenge calendar (today Jun 21 highlighted), daily leaderboard (Vfs 01:08/91/159, Vfs 01:08/91/159, Jerryzuh 01:10/92/162 — daily-rotating), Achievements all N/A pre-play, "Signup to save your progress" CTA; bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More; illustrated face-card art. No new features, redesigns, or content additions — only the rotating leaderboard names changed.

- **Where they lead:** multi-language i18n hreflang, native/PWA downloads, deck/background theming, illustrated face cards, embed-widget marketing, deal-count-framed catalog (320).
- **Where we lead:** editorial depth (28 variants + Strategy/History/Rules/Research desks), richer per-game JSON-LD (Game + FAQPage across the full 28/28 standalone-game set), spoke-domain SEO, no-signup local save, named daily-challenge deals, ad-light dark theme.

Logged to `docs/research/competitor-log.csv`.

---

## Phase 3 — SEO & Schema Audit

Live same-origin sweep of **all 121 sitemap URLs**:

- Every page returns 200, has exactly one H1, a non-empty `<title>`, and a canonical.
- All meta descriptions ≤160c **except one finding** (below).
- Game JSON-LD holds at **28/28** — spot-checked `/freecell` (SoftwareApplication + FAQPage by design), `/klondike`, `/spider`, `/golf`, `/penguin` (all carry a `Game` entity).

**Finding:** `/terms` emitted a meta description of only **39 characters** (`Terms of Service for {siteName}`) — below the ~50c healthy minimum and the only metadata issue across all 121 pages.

---

## Phase 4 — Improvement Shipped

**Expanded the thin `/terms` meta description** — commit `2199c94`.

Replaced the bare `Terms of Service for ${siteConfig.siteName}` with a domain-agnostic template: `Read the Terms of Service for ${siteConfig.siteName}: the rules and conditions covering your use of our free online solitaire games and website.` Renders at 140c (SolitaireStack.com) / 144c (PlayFreeCellOnline.com) — both squarely in the healthy band. Metadata `description` field only; visible page copy unchanged. `tsc --noEmit` clean.

- Deploy `dpl_EZaDZ2iuDrrqKnVEomrXPHDB1ZnG` → **READY** (~76s), aliased to playfreecellonline.com + www.
- **Verified live:** `/terms` now renders a 144c description, H1 still exactly 1.

---

## Phase 5 — Backlog

- **`/pyramid` renders a generic 7-column cascade, not the canonical triangular Pyramid layout** — game-logic, needs owner confirmation (carried since Jun 9).
- Author `Person` JSON-LD still uses `bioFull` (acceptable for structured data).
- Spoke-domain JSON-LD duplication lives in separate repos (out of scope here).
- Standing feature gaps vs competitor (i18n hreflang, native/PWA downloads, deck/background theming, illustrated face cards, embed widget) are larger initiatives needing owner buy-in — not autonomous daily ships.
- **Tooling:** `resize_window` remains unreliable for mobile in this environment (viewport stays pinned at desktop width); the same-origin iframe rig is the documented standard.

---

## Security

All page content (own sites + competitor) treated as untrusted per standing protocol. Today's renders and page text were scanned; no embedded instructions were present and none were acted upon. No consent/cookie banners were interacted with. Defensive stale 0-byte `.git/index.lock` check run before committing (none present).
