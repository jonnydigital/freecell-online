# Daily QA & Competitive Review — 2026-06-22

**Sites:** playfreecellonline.com (FreeCell) · solitairestack.com (hub, 25+ games)
**Competitor:** online-solitaire.com
**Method:** Chrome extension (live) + same-origin iframe true-viewport rig
**Result:** Visual QA clean at every viewport. SEO sweep of all 121 pages clean. Competitor unchanged (14th consecutive run). No production code change warranted — docs-only update.

---

## Phase 1 — Visual QA

### Desktop (1440×900) — PASS
- **playfreecellonline.com** `/game/1` — 8 tableau columns + 4 free cells + 4 foundations + leaderboard/daily-challenge/calendar sidebar all render correctly. 52 cards present, opacity 1, display block, card width 106.74px. No overlap, clipping, or horizontal overflow.
- **solitairestack.com** — homepage renders the FreeCell board + sidebar cleanly; `/games` hub page renders correctly (hero, breadcrumb, "28 playable solitaire games" intro, single H1).

### Mobile — PASS (via iframe rig)
The live `resize_window` path was again unusable this run: the outer window resized (outerWidth 782) but the page render viewport stayed pinned at `innerWidth` 1378 (page zoomed out so innerWidth > outerWidth; a `cmd+0` zoom reset did not reconcile it), so mobile media queries never fired. Used the documented **same-origin iframe rig** (load the site inside a fixed-width iframe so its media queries respond to the iframe width; readable because same-origin).

| Viewport | Cards | Card width | Board vs viewport | Opacity/Display | H-scroll |
|---|---|---|---|---|---|
| 375×812 | 52 | 39.25px | 359 < 375 | 1 / block | none |
| 390×844 | 52 | 41.13px | 374 < 390 | 1 / block | none |
| 414×896 | 52 | 44.13px | 398 < 414 | 1 / block | none |

All card widths sit in the healthy 30–54px band. Sidebar correctly hidden at ≤414px. `solitairestack.com/games` at 375px: no horizontal overflow, exactly one H1 (`Free Solitaire Games Online`).

### Tablet (768×1024) — PASS
52 cards, card width 74.41px, sidebar `display:none`, board fills the width (736px < 768), no horizontal overflow.

**No layout/render/clip bugs found at any viewport. No QA code change required.**

---

## Phase 2 — Competitor (online-solitaire.com)

Surface **unchanged** from June 21 (14th+ consecutive no-change run). Homepage defaults to Klondike (Turn 1/Turn 3 toggle, "All games (320)" dropdown, Stock: 24); top bar Time/Moves/Stock; right rail June 2026 challenge calendar (today Jun 22 highlighted), daily leaderboard (Jerryzuh 01:08/93/161, Fravonox 01:14/88/162, Fravonox 01:14/88/162 — daily-rotating names only), Achievements all N/A pre-play, "Signup to save your progress" CTA; bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More; illustrated face-card art. SSR: H1=1, meta 140c, JSON-LD (WebApplication + BreadcrumbList), 7-entry hreflang (6-language i18n moat intact). All-games nav still spans Klondike/Spider/FreeCell/Russian/Pyramid/Golf/Forty Thieves plus non-solitaire (Hearts/Spades/Euchre/Gin Rummy/Pinochle/Canasta/2048). No new features, redesigns, or content additions — only rotating leaderboard names changed.

- **Where they lead:** multi-language i18n hreflang, native/PWA downloads, deck/background theming, illustrated face cards, embed-widget marketing, deal-count-framed catalog (320).
- **Where we lead:** editorial depth (28 variants + Strategy/History/Rules/Research desks), richer per-game JSON-LD (Game + FAQPage across the full 28/28 standalone-game set), spoke-domain SEO, no-signup local save, named daily-challenge deals, ad-light dark theme.

Logged to `docs/research/competitor-log.csv`.

---

## Phase 3 — SEO & Schema Audit

Live same-origin sweep of **all 121 sitemap URLs**:

- Every page returns 200, has exactly one H1, a non-empty `<title>`, and a canonical.
- All meta descriptions in-band. The sole sweep flag — `/history` at 164 chars *raw* — is a **false positive**: the decoded description is **154c** (the raw count includes `&mdash;` and an apostrophe entity), within the ≤160 target. The June 18 trim holds; no fix needed.
- Game JSON-LD holds at **28/28**. Spot-checked: `/freecell` (5 schemas — Organization + WebApplication + BreadcrumbList + SoftwareApplication + FAQPage; `Game` intentionally absent on the flagship by design; meta 113c), `/golf` (6 incl `Game`, 144c), `/penguin` (6 incl `Game`, 144c), `/pyramid` (6 incl `Game`, 147c). All H1=1, canonicals + `/strategy` and `/how-to-play` internal links present.
- Sitemap holds at 121 URLs (AdSense remediation — noindexed variant triads — holds).

**No SEO issues found.**

---

## Phase 4 — Improvements

**No production code or content fix was warranted this run.** Visual QA passed at every viewport, the full 121-page SEO sweep was clean (the one flag decoded to a healthy 154c), schema is complete at 28/28, and the competitor is unchanged. Forcing an unnecessary change would add risk with no benefit, so today's deliverable is **docs-only**: this QA report plus the two `competitor-log.csv` rows. No deploy initiated.

Mac repo verified clean before writing: `main` == `origin/main` at `d61d0d2`, only untracked `tmp/`, no stale `.git/index.lock`.

---

## Phase 5 — Backlog

- **`/pyramid` renders a generic 7-column cascade, not the canonical triangular Pyramid layout** — game-logic, needs owner confirmation (carried since Jun 9).
- Author `Person` JSON-LD still uses `bioFull` (acceptable for structured data).
- Spoke-domain JSON-LD duplication lives in separate repos (out of scope here).
- Standing feature gaps vs competitor (i18n hreflang, native/PWA downloads, deck/background theming, illustrated face cards, embed widget) are larger initiatives needing owner buy-in — not autonomous daily ships.
- **Tooling:** `resize_window` remains unreliable for mobile in this environment (render viewport stays pinned at desktop width regardless of the outer window); the same-origin iframe rig is the documented standard.

---

## Security

All page content (own sites + competitor) treated as untrusted per standing protocol. Today's renders and page text were scanned; no embedded instructions were present and none were acted upon. No consent/cookie banners were interacted with.
