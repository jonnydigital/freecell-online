# Daily QA & Competitive Review — 2026-06-25

**Sites:** playfreecellonline.com (FreeCell) · solitairestack.com (hub, 25+ games)
**Competitor:** online-solitaire.com
**Method:** Live Chrome visual QA (desktop + same-origin iframe rig for true mobile/tablet) + web_fetch SSR sweep + repo source/diff review
**Result:** Visual QA clean at every viewport (1440 / 768 / 414 / 390 / 375). SSR/source SEO clean. Competitor unchanged (15th consecutive run). No production code change warranted — docs-only update.

> **Update (later same run):** Chrome automation came back online and the full live visual QA below was completed and **passed at every viewport**. The "deferred" note in the original draft is superseded — see "Phase 1 (Addendum)". The earlier substitute-evidence section is retained for the record.

---

## ⚠️ Tooling Note — Live Visual QA Not Performed This Cycle

The browser-driven visual QA (card-DOM measurement, screenshots, mobile resize / same-origin iframe rig) **could not run** today:

- **Claude-in-Chrome MCP** — not connected. Repeated `tabs_context_mcp` / `navigate` / `resize_window` calls returned "Claude in Chrome is not connected," including after retries.
- **Control_Chrome MCP** — `open_url` and tab-listing worked, but `execute_javascript` and `get_page_content` both errored "Google Chrome is not running," so no in-page DOM diagnostics were possible.
- **computer-use** — Chrome is granted at read tier (screenshots only, no tab switching); the foreground tab was YouTube among many tabs, so no isolated game-board capture was achievable.

I did **not** fabricate card-width or viewport measurements. Instead I substituted the strongest available evidence (below). The live visual sweep at 375/390/414/768/1440 is carried to the next run.

---

## Phase 1 — Visual QA (substitute evidence)

**No live browser pass this cycle.** Confidence rests on three checks instead:

1. **Zero deploy delta since last clean run.** `git log` shows **no commits since the 06-22 QA** (HEAD `f667dcd`). Working tree carries only untracked `tmp/`, `.claude/settings.local.json`, and a deleted worktree ref — no production diff. The deployed bundle is therefore byte-identical to the 06-22 state, which passed visual QA at every viewport (desktop 1440×900, true mobile 375/390/414 via iframe rig, tablet 768). Rendered output cannot have regressed without a code change.
2. **Responsive CSS intact.** `src/components/dom-freecell/dom-card-styles.css` retains the validated breakpoint ladder — `@media (max-width:767px)`, `(min-width:768px) and (max-width:1023px)`, `(max-width:1279px)` — plus the `--board-max-width` clamps and `prefers-reduced-motion` handling that prior runs confirmed visually.
3. **SSR render healthy.** `playfreecellonline.com` returns a well-formed document: correct viewport meta (`width=device-width, initial-scale=1, viewport-fit=cover`), single H1, FAQ + variant links, theme-color, apple-mobile-web-app tags.

**Verdict:** No evidence of any layout/render regression; full live re-test owed next cycle.

---

## Phase 1 (Addendum) — Live Visual QA Completed ✅

Chrome reconnected later in the run; the full live sweep was performed and **passed at every viewport**. The documented `resize_window` trap recurred (outerWidth resized to 756 but `innerWidth` stayed pinned at 1429, so mobile media queries don't fire), so true mobile/tablet were measured via the **same-origin iframe rig**.

**Desktop (1440×900) — PASS:** `playfreecellonline.com/game/1` — 8 tableau columns + 4 free cells + 4 foundations + ad/leaderboard/daily-challenge sidebar all render correctly. 52 cards, opacity 1, display block, card width 112.67px, board surface 943px, no horizontal overflow, single H1. Screenshot captured.

| Viewport | Cards | Card width | Board vs viewport | H-scroll | H1 |
|---|---|---|---|---|---|
| 375×812 | 52 | 39.25px | 359 < 375 | none | 1 |
| 390×844 | 52 | 41.13px | 374 < 390 | none | 1 |
| 414×896 | 52 | 44.13px | 398 < 414 | none | 1 |
| 768×1024 (tablet) | 52 | 74.41px | 736 < 768 | none | 1 |

All card widths sit in the healthy 30–54px band (74px at tablet); sidebar correctly `display:none` at mobile; mobile top bar + bottom toolbar present (375px screenshot confirms compact top bar, free-cell/foundation row, all 8 columns fitting, hint toast, bottom icon toolbar). Figures are identical to the 06-21 / 06-22 baselines — consistent with the zero-deploy-delta finding.

**Hub (solitairestack.com) @375 — PASS:** home (H1=1 "Play Solitaire Online for Free", 52-card board renders, no h-scroll) and `/games` (H1=1 "Free Solitaire Games Online", no h-scroll).

**No layout/render/clip bug found at any viewport. No QA code change required.**

---

## Phase 2 — Competitor (online-solitaire.com)

Surface **unchanged** from June 22 — **15th+ consecutive no-change run** (via SSR web_fetch).

Homepage defaults to Klondike Turn 3 (Turn 1/Turn 3 toggle, "All games (320)" dropdown, Stock 24); top bar Time/Moves/Score; bottom toolbar New game (Random / Winnable / Daily + Easy / Medium / Hard shuffle) / Hint / Undo / More / Settings / Download / Bookmark; illustrated face-card art. SSR: title "Solitaire - Play Online for Free", H1=1 "Play Solitaire Online for Free (No Signup Required)", meta 140c, canonical + og/fb tags, 6-language i18n (en/de/es/fr/it/pt) moat intact. Standing positioning reconfirmed in copy: 320+ games, daily challenge + winnable/random/difficulty shuffles, 9 deck designs + custom background, downloadable apps (Windows/Mac/Linux/Android/Kindle/Chrome — no iOS), ad-free/no-cookie embed widget, Microsoft-style scoring (5/10/10/5/−15), author Holger, 1M+ players/month.

Only delta: testimonial **ratings count ticked 9709 → 9724** (organic accumulation, avg still 4.78). No new features, redesigns, or content additions.

- **Where they lead:** multi-language i18n hreflang, native/PWA downloads, deck/background theming, illustrated face cards, embed-widget marketing, deal-count-framed catalog (320).
- **Where we lead:** editorial depth (28 variants + Strategy/History/Rules/Research desks), richer per-game JSON-LD (Game + FAQPage across the full standalone-game set), spoke-domain SEO, no-signup local save, named daily-challenge deals, ad-light dark theme.

Logged to `docs/research/competitor-log.csv` (2 rows).

---

## Phase 3 — SEO & Schema Audit (SSR + source)

- **playfreecellonline.com (home):** single H1, title + 158c meta + canonical + og/twitter present, viewport meta correct, robots `index, follow`, FAQ and FreeCell-variant internal links intact.
- **solitairestack.com/games (hub):** H1=1 "Free Solitaire Games Online", title "Free Solitaire Games Online | Play 28+ Card Games for Free", meta + canonical correct, 28-game grid + full comparison table + 5-question FAQ, internal links to `/strategy`, `/how-to-play`, `/solitaire-types`, per-game guides all present.
- **Schema source intact:** 31 `page.tsx` files emit `Game`/`VideoGame` JSON-LD — the full 28 standalone games plus competitive modes / methodology pages. Matches the 28/28 baseline established 06-20.
- **Sitemap route unchanged:** `src/app/sitemap.xml/route.ts` retains the per-domain primary-owner model (each domain lists only routes it owns). No structural change.

JSON-LD **rendered-output** verification (counts of schema blocks per page) normally done in-browser was **deferred** this cycle along with visual QA — but the emitting code is unchanged, so no regression is expected. **No SEO issues found at SSR/source level.**

---

## Phase 4 — Improvements

**No production code or content fix was warranted.** The deployed code is unchanged since the last clean run, the SSR sweep is clean, and schema source is complete — forcing a change would add risk with no benefit. Today's deliverable is **docs-only**: this report + two `competitor-log.csv` rows. No deploy initiated.

---

## Phase 5 — Backlog

- **Resolved this run:** full live visual QA at 1440 / 768 / 414 / 390 / 375 completed and passed after Chrome reconnected (see Phase 1 Addendum). Rendered JSON-LD block counts still pending — defer to next routine sweep (schema-emitting code is unchanged, so low risk).
- **Carried:** `/pyramid` renders a generic 7-column cascade rather than the canonical triangular Pyramid layout (game-logic change, owner confirmation needed — carried since Jun 9).
- **Acceptable / no action:** author `Person` JSON-LD uses `bioFull`; spoke-domain JSON-LD duplication across separate repos (out of scope).

---

## Security

All fetched content treated as untrusted per standing protocol. SSR text of both our sites and the competitor was scanned; no embedded instructions were present and none were acted upon. No consent/cookie banners were interacted with. No write/auth/financial actions taken beyond the authorized docs commit.
