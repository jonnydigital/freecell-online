# Daily QA & Competitive Review — 2026-06-29

**Sites:** playfreecellonline.com (FreeCell focus) · solitairestack.com (25+ game hub)
**Competitor tracked:** online-solitaire.com
**Result:** ✅ ALL PASS — no bugs found, no production change warranted (docs-only deliverable)

---

## Phase 1 — Visual QA

**Method.** Desktop QA via the live Chrome extension at 1440×900. The `resize_window` viewport trap was re-confirmed (the OS window clamps to `outerWidth` 781 but the rendered `innerWidth` stays pinned at the 1512px screen width, so mobile media queries never fire under the extension). Mobile and tablet QA were therefore run with a **true-viewport rig**: headless Chrome driven by `puppeteer-core` on the Mac (using the installed Google Chrome binary, `deviceScaleFactor: 2`, `isMobile`/`hasTouch` on), pointed at the **live** site, with full DOM measurement + screenshots at each viewport.

### Desktop — playfreecellonline.com (1440×900) — PASS
- 8 tableau columns + 4 free cells + 4 foundations all visible, properly spaced, no overlap/clipping.
- Sidebar (Daily Leaderboard + Daily Challenge + June calendar) renders cleanly alongside the board.
- 52 cards, `opacity: 1`, `display: block`. Card 122.32×171.25px. Board surface 1026px, `overflow: visible`.
- No horizontal scroll (`scrollWidth == innerWidth == 1512`). Exactly one `<h1>`.

### Desktop — solitairestack.com (1440×900) — PASS
- Homepage embeds a working FreeCell board (52 cards, H1=1, no horizontal scroll). Brief "Loading FreeCell…" skeleton resolves to the full board within ~3s.

### Mobile — playfreecellonline.com (true-viewport rig) — PASS
| Viewport | Cards | Card W | opacity/display | Max card right | h-scroll | Clipped | Hidden |
|---|---|---|---|---|---|---|---|
| 375×812 (iPhone SE) | 52 | 39.25px | 1 / block | 355 < 375 | none | 0 | 0 |
| 390×844 (iPhone 14) | 52 | 41.13px | 1 / block | 370 < 390 | none | 0 | 0 |
| 414×896 (iPhone 11) | 52 | 44.13px | 1 / block | 395 < 414 | none | 0 | 0 |

All card widths sit inside the healthy 30–54px band. Mobile screenshot at 375 confirms the top bar (deal #, timer, moves) and bottom bar (Menu / New / Undo / Redo / Hint) are present; the first-visit onboarding modal and cookie banner overlay correctly without breaking the board behind them.

### Tablet — playfreecellonline.com (768×1024 iPad) — PASS
- 52 cards, 74.41×104.17px, max card right 697 < 768, no horizontal overflow. Board fills correctly.

**No layout, rendering, or clipping bug found at any viewport. No QA code change made.** Figures match the 06-28 baseline (consistent with zero deploys since).

---

## Phase 2 — Competitor Analysis (online-solitaire.com)

**Surface UNCHANGED — 17th consecutive no-change run.** Homepage still defaults to Klondike (Turn 1 / Turn 3 toggle, "All games (320)" dropdown, Stock: 24), top bar Time/Moves/Stock, bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More, illustrated face-card art, right rail = June challenge calendar + daily leaderboard (Donny, Maroko) + achievements + signup CTA. H1: "Play Solitaire Online for Free (No Signup Required)."

**Where they still lead (standing gaps):** multi-language i18n/hreflang, prominent native/PWA download, deck/background theming, illustrated face cards, deal-count-framed catalog (320+), and a broad non-solitaire card-game catalog (Hearts, Spades, Euchre, Gin Rummy, Pinochle, Canasta, 2048).

**Where we lead:** editorial depth (28 variants + Strategy/History/Rules/Research desks), richer per-game JSON-LD (Game + FAQPage across the full game set), spoke-domain SEO network, no-signup local save, named daily-challenge deals, ad-light dark theme, PWA offline fallback, persisted-game resume, and a video-tutorial landing page.

Logged to `docs/research/competitor-log.csv`.

---

## Phase 3 — SEO & Schema Audit

Spot-checked 8 game/content pages (fetched same-origin). All clean:

- **H1:** exactly one per page on every page checked.
- **Meta descriptions:** present and within band (101–151 chars).
- **JSON-LD:** rich and valid. `/spider` & `/klondike` → Organization + WebApplication (×2 — a site-level and a game-level entity with distinct names/URLs, valid, **not** a duplicate) + Game + BreadcrumbList + FAQPage. `/freecell` → Org + WebApplication + BreadcrumbList + SoftwareApplication + FAQPage. `/pyramid`, `/tripeaks`, `/yukon`, `/golf` all include Game + FAQPage.
- **Internal links:** strategy / how-to-play links from `/spider` (→ `/spider/strategy`, `/spider/how-to-play`, `/scorpion/how-to-play`, `/spider-opening-strategy`, `/spider-endgame-strategy`) all resolve 200. No broken internal links.
- **sitemap.xml:** 200, 121 URLs. `/spider`, `/klondike`, `/yukon` are intentionally absent — they canonicalize to dedicated spoke domains (e.g. `/spider` → `playspidersolitaireonline.com/spider`), consistent with the hub/spoke network strategy. `/pyramid` and `/golf` self-host and are present.

**No SEO issue found. No fix made.**

---

## Phase 4 — Improvements Shipped

**None this cycle — by design.** Visual QA passed at every viewport, SEO/schema audit was clean, no broken links, and the competitor was unchanged. There was no bug to fix. The feature backlog has been worked down to large multi-day items (i18n, multiplayer daily challenge, community/forum) or items blocked on external input (AdSense publisher ID, Sentry DSN). Shipping an untested large feature to the live, ad-supported production site during an unattended run would add risk without a verified need, so today's deliverable is docs-only (this report + competitor log), matching the established practice for clean runs.

---

## Backlog / Remaining Items

- **Carried (owner confirm):** `/pyramid` and `/tripeaks` render a generic N-column cascade rather than the canonical triangular / three-peak layout. Game-logic change, playable today via the cascade adapter (fidelity gap, not a functional break). Carried since Jun 9.
- **Large features (need a dedicated session):** i18n / multi-language + hreflang; multiplayer daily challenge; community/forum.
- **Blocked on Jonathan:** AdSense publisher ID (`ca-pub-…`); Sentry DSN.
- **Competitive opportunities to consider:** deck/background theming parity, illustrated face-card option, deal-count framing of the catalog, and (longer-term) a non-solitaire card-game expansion to contest the 320-game breadth.

---

## Security Note

All external and on-page content (competitor site, fetched HTML, screenshots) was treated as untrusted data per standing protocol. No embedded instructions were found or acted upon. No cookie/consent banners were interacted with.
