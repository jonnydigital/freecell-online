# QA Report — 2026-07-03

## Summary

Daily review run. Visual QA PASS at all viewports on both sites. Competitor unchanged. SEO audit of the newly shipped French/Spanish FreeCell routes found two issues (missing H1 on both localized play routes; non-reciprocal hreflang on Spanish pages) — both fixed and shipped in commit `74eab1d`.

## Phase 1: Visual QA (live production)

Method: live Chrome extension for desktop; same-origin iframe true-viewport rig for mobile/tablet (resize_window trap re-confirmed — window is OS-clamped/zoom-pinned, innerWidth stays 1512, cmd+0 does not reconcile; AppleScript sees a different Chrome window than the extension, so no user-window resize was attempted).

### Desktop (~1440–1512px) — PASS
- **playfreecellonline.com /game/1**: 8 tableau columns + 4 free cells + 4 foundations all visible; sidebar (ad slot, daily leaderboard, daily challenge, July calendar) renders alongside board; 52 cards in DOM; H1=1; no h-scroll (scrollWidth 1512 == innerWidth). Card corner indices readable (rotated bottom-right 6/9 confirmed as standard card design, not a bug).
- **solitairestack.com home**: FreeCell board embed + below-fold editorial content clean; H1=1 ("Play Solitaire Online for Free"); hub links (games, strategy, how-to-play, glossary, history) present.
- **solitairestack.com /spider**: redirects to playspidersolitaireonline.com/spider per hub/spoke; 10-column Spider board + stock + article content render clean.

### Mobile (iframe true-viewport rig, live site, /game/1) — PASS
| Viewport | Card width | Invisible | Clipped | H-scroll |
|---|---|---|---|---|
| 375×812 | 38.5px | 0 | 0 | no |
| 390×844 | 40.4px | 0 | 0 | no |
| 414×896 | 43.4px | 0 | 0 | no |

All 52 cards opacity 1 / display block at every size; card widths in the healthy 30–54px band; mobile top bar (#deal, timer, moves) and bottom bar (Menu/New/Undo/Redo/Hint) visible in screenshot; sidebar hidden. Consistent with 06-29/06-30 baselines.

### Tablet 768×1024 — PASS
Card width 54px, no overflow, no sidebar, mobile-style chrome; board centered.

**No layout/render/clip bugs at any viewport. No gameplay code changes needed.**

## Phase 2: Competitor (online-solitaire.com)

Surface UNCHANGED (20th+ consecutive no-change run; note: no 07-02 log entry — previous day's run appears to have been skipped). Klondike homepage, July challenge calendar w/ trophy tiers (10/20/31 wins), score-based daily leaderboard, achievements + signup CTA, macOS app promo. /freecell page standard. No new features or redesigns. Logged to docs/research/competitor-log.csv (including recovery of the uncommitted 07-01 entry found in the working tree).

Standing gaps where they lead: i18n/hreflang (we are closing this — ES + FR FreeCell routes now live), native app, deck theming, illustrated face cards, 320+ deal framing, non-solitaire games. We lead on editorial depth, per-game JSON-LD, spoke-domain SEO, no-signup local save, daily deals, ad-light dark theme, PWA offline/resume, Daily FreeCell Room.

## Phase 3: SEO & schema audit

Focus: newly shipped localization routes (commits cabe869..9c3c024, deployed since last QA) + hub pages.

- `/freecell-en-francais` — 200, H1=1, meta 148c, canonical self, hreflang fr/en/es/x-default, JSON-LD Organization+WebApplication+Article+FAQPage+BreadcrumbList. Clean.
- `/freecell-en-francais/jouer` — **H1=0 (raw HTML and after client render)** → FIXED (sr-only H1, matching /streak & /storm pattern).
- `/freecell-en-espanol` — H1=1, meta 146c, clean, but **hreflang missing fr** (FR pages point at ES; ES pages didn't point back — non-reciprocal hreflang is ignored by Google) → FIXED.
- `/freecell-en-espanol/jugar` — **H1=0** and **hreflang missing fr** → both FIXED.
- `/strategy`, `/how-to-play` — 200, H1=1, canonical → solitairestack.com per hub/spoke. Clean.
- sitemap.xml — 126 URLs (up from 105), includes both FR and both ES routes. Clean.
- Game renders fine on localized play routes (52 cards @ /jouer).

Known/accepted: `html lang="en"` on FR/ES pages (root-layout constraint; hreflang + JSON-LD inLanguage mitigate). Noted for future i18n architecture work.

## Phase 4: Improvements shipped

1. `74eab1d` — fix(seo): sr-only H1 added to `/freecell-en-francais/jouer` and `/freecell-en-espanol/jugar`; reciprocal `fr` hreflang added to both Spanish pages. TypeScript clean (`tsc --noEmit`). Deployment verified READY and re-tested live (see Phase 5 verification note).

Ops: local Mac repo was 5 commits behind origin at start with a stale `.git/index.lock` (0 bytes) blocking pulls — lock removed, fast-forwarded to `9c3c024` with autostash preserving the uncommitted 07-01 competitor-log entry.

## Backlog / carried items

- /tripeaks 3-peak layout: shipped earlier (b875234) — verify rendering fidelity next run.
- `html lang` per-locale route group (larger i18n architecture item).
- Mobile suit glyphs on overlapped cards are 7px — legible but small; consider bumping ~1–2px on mobile (low priority, cosmetic).
- Large items still blocked/deferred: full i18n hub, multiplayer, AdSense pub ID, Sentry DSN.

## Security

All external content (competitor pages, live-site DOM) treated as untrusted per standing protocol; no embedded instructions encountered; no consent banners interacted with.
