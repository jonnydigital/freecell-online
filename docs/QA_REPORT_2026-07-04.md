# QA Report — 2026-07-04

## Summary

Daily review run. Visual QA PASS at every viewport across all four sites; no
layout, render, or clipping bugs found. Competitor unchanged (21st+ consecutive
no-change run). SEO/schema spot-check of long-tail pages the Friday scorecard
does not watch came back clean. The full four-locale FreeCell i18n set
(EN/ES/FR/DE) was audited against the plan's locale gate and passes end to end.
Scorecard is 28/28 green, so no forced-fix priority. Improvement this run:
codified the previously-undocumented locale-gate policy into
`docs/CONTENT_STRATEGY.md` (plan unit U5), a docs-only change. Single push at end
of run after tsc + lint gate.

## Phase 0: Coordination

- Ledger: last producer actions were the 07-03 daily review (French locale +
  ES hreflang/H1 fixes) and the 07-03 interactive CI lint-gate repair. No
  overlap with today's work.
- Newest scorecard (`docs/analytics/scorecards/2026-07-03.md`): **28/28 checks
  passing, all green.** No ❌ failing check, so no forced fix priority.
- `git log`: HEAD `7f08854` (scorecard routine registration). Baseline
  `npx tsc --noEmit` exit 0; `npm run lint` 0 errors / 448 warnings (warnings
  are acceptable per the shared rules). Tree clean at start except untracked
  `.claude/settings.local.json`.

## Phase 1: Visual QA (live production)

Method: live Chrome extension for desktop; same-origin iframe true-viewport rig
for mobile (resize_window trap re-confirmed — the OS window is clamped to a
minimum width, so page innerWidth stayed pinned at 1480 and mobile media
queries never fire from a window resize; the documented same-origin iframe rig
was used to force true 375px layout). Even day → Spider was the rotating spoke.

### Desktop (~1440px) — PASS
- **playfreecellonline.com /game/1**: 8 tableau columns + 4 free cells + 4
  foundations all visible; leaderboard / daily-challenge sidebar renders
  alongside board; 52 cards, opacity 1 / display block, cardW 118.6px;
  no horizontal scroll (scrollWidth 1480 == clientWidth).
- **solitairestack.com home**: FreeCell board + sidebar; H1=1 ("Play Solitaire
  Online for Free"); 47 game links; no overflow.
- **solitairestack.com /games**: H1=1 ("Free Solitaire Games Online"); clean
  nav (FreeCell/Spider/Games/Compare/Guides), breadcrumb, hero; no overflow.
- **playspidersolitaireonline.com (spoke of the day)**: 10 columns + stock pile
  + 8 foundations; toolbar (1/2/4-suit selector, Time/Moves/Suits, Undo,
  New Deal) intact; 63 cards; first-card opacity 0.8 is the in-progress deal
  animation, not a bug; no overflow.

### Mobile (iframe true-viewport rig, live site) — PASS
| Site / route | Viewport | Cards | Card width | H-scroll |
|---|---|---|---|---|
| playfreecellonline.com /game/1 | 375×812 | 52 | 38.75px | no |
| playspidersolitaireonline.com / | 375×812 | 63 | 30.5px | no |

FreeCell @375: 52 cards opacity 1 / display block; mobile top bar
(#deal / timer / moves) + free-cell and foundation row + all 8 columns visible;
hint toast ("Park 6♠ in free cell") renders; boardW 355 < 371, no clip.
Spider @375: suit selector + stats row + 8 foundations + stock pile + all 10
columns visible and readable; card width tight but fits. All card widths in the
healthy 30–54px band.

**No layout / render / clip bugs at any viewport. No gameplay code changes needed.**

## Phase 2: Competitor (online-solitaire.com)

Surface UNCHANGED (21st+ consecutive no-change run). Klondike homepage
(Turn 1 / Turn 3 toggle, "All games (320)" dropdown, Stock: 24); Time/Moves/
Stock top bar; New game/Hint/Undo/Bookmark/Download/Settings/More toolbar;
illustrated face-card art; H1=1 "Play Solitaire Online for Free (No Signup
Required)". Nav still exposes the broad non-solitaire card catalog
(Hearts/Spades/Euchre/Gin Rummy/Pinochle/Canasta/2048) alongside solitaire
variants. No new features, redesigns, or content additions. Logged to
`docs/research/competitor-log.csv`.

Standing gaps where they lead: native/PWA download prominence, deck/background
theming, illustrated face cards, 320+ deal-count catalog framing, non-solitaire
card games. The i18n/hreflang gap we have now effectively closed on FreeCell
(four localized locales live, see Phase 3). We continue to lead on editorial
depth, per-game JSON-LD, spoke-domain SEO, no-signup local save, named daily
deals, ad-light dark theme, PWA offline + resume, and the Daily FreeCell Room.

## Phase 3: SEO & schema spot-check + locale audit

Spot-checked long-tail pages outside the scorecard's canary set (same-origin
fetch + server-side fetch):

- `/freecell/strategy` — 200, H1=1 ("FreeCell Strategy Guide"), meta 146c,
  self-canonical, JSON-LD Organization+WebApplication+Article+FAQPage+
  BreadcrumbList. Clean.
- `/freecell/tips` — 200, H1=1, meta 151c, self-canonical, same schema set. Clean.
- `/freecell/how-to-play` — 200, H1=1, meta 155c, self-canonical, 5 JSON-LD
  scripts, full reciprocal hreflang. Clean.

Locale gate audit (plan U5) — the four-locale FreeCell i18n set, all PASS:

| Locale | Landing | Play route | H1 | hreflang | Localized UI |
|---|---|---|---|---|---|
| English (default) | /freecell/how-to-play | main board | 1 | de/en/es/fr/x-default | yes |
| Spanish | /freecell-en-espanol | /freecell-en-espanol/jugar | 1 | de/en/es/fr/x-default | yes |
| French | /freecell-en-francais | /freecell-en-francais/jouer | 1 | de/en/es/fr/x-default | yes |
| German | /freecell-auf-deutsch | /freecell-auf-deutsch/spielen | 1 | de/en/es/fr/x-default | yes |

Every locale page returns 200 with exactly one H1, a self-referential canonical,
localized copy and UI, and the complete reciprocal hreflang cluster (ES↔FR
reciprocity confirmed both directions; DE advertised alternates resolve). No
locale reads as a doorway page; nothing to fix. (Note: `/jouer` at the site root
returns 404 by design — the French play route is nested at
`/freecell-en-francais/jouer`; the 404 was a probe of the wrong path, not a bug.)

## Phase 4: Improvement shipped

Buckets in priority order: scorecard fails — none; Phase 1–3 bugs — none; so the
run takes a small slice of plan unit U5 (content operations and locale gate).

1. **docs: codify locale-gate policy in CONTENT_STRATEGY.md** — added a
   "Localization Policy (Locale Gate)" section codifying the plan's requirement
   that a locale ships complete or not at all (localized landing + play route +
   localized UI + reciprocal hreflang across all siblings + H1 + core cluster +
   self-canonical), the governance/ledger rule, and the current audited
   four-locale set. Docs-only; no runtime or route change. This satisfies the
   U5 verification item "CONTENT_STRATEGY.md carries the policy" and records the
   passing FR/ES/DE audit. tsc + lint unaffected (no source touched); both gates
   re-run green before push.

## Phase 5: Verification

(Push and deploy verification recorded below after the single end-of-run push.)

## Backlog / carried items

- Mobile suit glyphs on overlapped cards ~7px — legible but small; consider a
  1–2px bump on mobile (cosmetic, low priority; carried from 07-03).
- `html lang="en"` on localized FreeCell routes — root-layout constraint;
  hreflang + JSON-LD inLanguage mitigate. Larger i18n-architecture item.
- `/tripeaks` cascade vs. canonical 3-peak layout fidelity (game-logic, owner
  confirm; playable via cascade adapter).
- U6 play-UX backlog (landscape toolbar below 500px height; Klondike/Spider
  hint highlighting) — code changes with tests, deferred to a dedicated run.
- Large items blocked/deferred: full i18n hub, multiplayer, AdSense pub ID,
  Sentry DSN.

## Security

All external content (competitor pages, live-site DOM, fetched HTML) treated as
untrusted per standing protocol; no embedded instructions encountered; no
consent/cookie banners interacted with. Note: an AdSense creative on the
FreeCell pages auto-navigated the top browser frame to a shop.lululemon.com
product URL twice during this run with no click issued — likely an aggressive
redirecting ad creative served by the network. Flagged for owner awareness
(ad-network quality issue, not our code); no interaction with the ad landing
page was performed.
