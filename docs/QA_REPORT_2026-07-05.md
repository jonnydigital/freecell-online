# Daily QA Report — 2026-07-05 (cowork-daily-review)

Sunday (odd day) → spoke rotation = **Klondike**. Competitor: online-solitaire.com.

## Phase 0 — Coordination
- **Ledger:** last producer runs = 07-02 spoke content (klondike/spider posts), 07-03 FR locale + hreflang/H1 fixes, 07-04 docs (locale-gate policy) + competitor log. No overlap with today's work.
- **Scorecard (latest, 2026-07-03):** **28/28 checks passing, all green.** No ❌ to prioritize.
- **Repo health at start:** `npx tsc --noEmit` = pass; `npm run lint` = 0 errors, 448 warnings (pre-existing unused-var warnings, allowed). Main was **not red**. `git log` reviewed (top: 8b7016c 07-04 daily review).

## Phase 1 — Visual QA (live production)
Method: Chrome extension on live sites for desktop; same-origin iframe true-viewport rig for mobile (documented `resize_window` trap — OS window shrinks but render `innerWidth` stays pinned ~1337, mobile media queries never fire, so the iframe rig is used for true CSS viewports).

**FreeCell — playfreecellonline.com/game/1**
- Desktop 1440×900: 8 tableau columns + 4 free cells + 4 foundations + leaderboard/daily-challenge/calendar sidebar. 52 cards, opacity 1, display block, cardW **101.98px**, no horizontal scroll. **PASS**
- Mobile 375 (rig vw 371): 52/52 cards visible, cardW **38.75px**, no h-scroll, mobile top + bottom bars present. **PASS**
- Mobile 390 (vw 386): cardW **40.63px**, 52/52. Mobile 414 (vw 410): cardW **43.63px**, 52/52. **PASS** (all widths in healthy 30–54px band)

**Hub — solitairestack.com**
- Homepage: title + H1 ("Play Solitaire Online for Free") correct, FreeCell board embed, no h-scroll. **PASS**

**Klondike (today's rotating spoke) — playklondikeonline.com**
- Desktop 1440: 7 tableau columns + stock/waste + 4 foundations, D1/D3 toggle + undo/hint/New Deal toolbar intact, H1=1, no h-scroll, 29 rendered cards all visible. **PASS**
- Mobile 375 (rig 371): 29/29 cards visible, cardW **38.75px**, toolbar present, no h-scroll. **PASS**

**Bug found (fixed today):** FreeCell **landscape mobile** (w>h, height <500px, e.g. 736×356): the board renders fine but the **top status bar (TIME / MOVES / game #) is entirely removed from the DOM** — the player loses the timer and move count in landscape. This is exactly U6's known play-UX backlog item ("landscape mobile hides timer/moves below 500px height"). See Phase 4.

## Phase 2 — Competitor
online-solitaire.com **UNCHANGED** (22nd+ consecutive no-change run). Klondike default (Turn 1/Turn 3, "All games" dropdown, Stock 24), top bar Time/Moves/Stock, bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More, right rail = July challenge calendar (today=5 highlighted) + daily leaderboard (Chiucciu/Jerryzuh) + achievements (all N/A) + signup CTA. H1=1. No new features/redesigns/content. Standing gaps where they lead: i18n breadth (we are closing — EN/ES/FR/DE FreeCell live), native-app download, deck theming, illustrated face cards, 320+ catalog framing, non-solitaire card games. Logged to docs/research/competitor-log.csv.

## Phase 3 — SEO & schema spot-check (long-tail pages not covered by scorecard canaries)
All same-origin fetch + parse. **All 4 PASS:**
- Klondike `/klondike/tips`: H1=1, meta 145c, self-canonical, JSON-LD Organization+WebApplication+BreadcrumbList+Article+FAQPage, cluster links present.
- Klondike `/klondike/draw-1-vs-draw-3`: H1=1, meta 141c, self-canonical, +Article+FAQPage+BreadcrumbList.
- Spider `/spider-suit-strategy`: H1=1, meta 148c, self-canonical, +Article+FAQPage+BreadcrumbList.
- Spider `/scorpion/tips`: H1=1, meta 151c, self-canonical, +Article+FAQPage+BreadcrumbList.
No SEO/schema issues found.

## Phase 4 — Improvement shipped (1)
**U6 slice — FreeCell landscape timer/moves readout.** `src/components/dom-freecell/DomGameShell.tsx`: added a compact floating status pill (`#gameNumber` · `timerDisplay` · `moveCount`) shown only when `isLandscapeMobile`, positioned top-center (the empty gap between free cells and foundations), mirroring the existing landscape floating undo/redo pattern. `pointer-events-none` + `aria-hidden` so it never blocks card drags and doesn't duplicate for screen readers. Renders already-computed values — no game-logic change; portrait/desktop layouts untouched.
- Scope kept to the FreeCell shell only (one small slice, per plan U6 — Klondike/Spider shells not touched today).
- Verification: `npx tsc --noEmit` pass; `npm run lint` 0 errors (448 warnings, none added). No unit test added — repo test harness is logic-only (Jest node env, `.test.ts`, no jsdom/RTL); this is a presentational conditional render, verified in-browser on the live deploy (see below).

## Phase 5 — Deploy verification
_(filled after push — Vercel READY on all 4 projects + GitHub CI green + live re-test of the landscape fix)_

## Remaining backlog
- **U6 (carry):** apply the same landscape timer/moves treatment to Klondike/Spider shells if they share the gap; Klondike/Spider hints auto-execute instead of teaching (highlight like FreeCell); generalize daily challenges beyond FreeCell.
- **Carried (game-logic, owner confirm):** /tripeaks renders generic cascade not canonical 3-peak layout (carried since Jun 9; playable via cascade adapter).
- Lint: 448 pre-existing unused-var warnings across solver/store files — safe cleanup candidate for a future polish run (non-blocking).
