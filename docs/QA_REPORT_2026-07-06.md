# Daily QA Report — 2026-07-06 (cowork-daily-review)

Monday (even day) → spoke rotation = **Spider**. Competitor: online-solitaire.com.

## Phase 0 — Coordination
- **Ledger:** last producer runs = 07-03 FR locale + hreflang/H1 fixes, 07-03 CI lint-gate repair, 07-04 docs (locale-gate policy) + competitor log, 07-05 daily review (U6 landscape status fix + QA). No overlap with today's work.
- **Scorecard (latest, 2026-07-03):** **28/28 checks passing, all green.** No ❌ to prioritize.
- **Repo health at start:** `npx tsc --noEmit` = pass; `npm run lint` = 0 errors, 448 warnings (pre-existing unused-var warnings, allowed). Main was **not red**. `git log` reviewed (top: 125022c 07-05 daily review). Since the last locale audit (07-04, 4 locales), two new FreeCell locales landed — **Italian** (1e038bb) and **Portuguese** (3b39745) — the audit target for today's Phase 4.

## Phase 1 — Visual QA (live production)
Method: Chrome extension on live sites for desktop; same-origin iframe true-viewport rig for mobile (documented `resize_window` trap — the OS window shrinks but render `innerWidth` stays pinned ~1233, mobile media queries never fire, so the iframe rig is used for true CSS viewports).

**FreeCell — playfreecellonline.com/game/1**
- Desktop 1440×900: 8 tableau columns + 4 free cells + 4 foundations + daily-leaderboard/daily-challenge/calendar sidebar. 52 cards, opacity 1, display block, cardW **89.88px**, no horizontal scroll, H1=1. **PASS**
- Mobile 375 (rig vw 375): 52/52 cards visible, cardW **39.25px**, 0 hidden, 0 clipped, maxCardRight 355<375, no h-scroll, sidebar `display:none`. **PASS**
- Mobile 390 (vw 390): cardW **41.13px**, maxRight 370. Mobile 414 (vw 414): cardW **44.13px**, maxRight 395. 52/52 each, no h-scroll. **PASS** (all widths in healthy 30–54px band)

**Hub — solitairestack.com**
- Homepage: title + H1 ("Play Solitaire Online for Free") correct, FreeCell board embed (52 cards), no h-scroll. **PASS**
- Game page `/spider`: canonical-routes to playspidersolitaireonline.com/spider (hub/spoke behavior correct), H1=1, no h-scroll. **PASS**

**Spider (today's rotating spoke) — playspidersolitaireonline.com**
- Desktop 1440: 10 tableau columns + 8 foundations + stock deck (top-right), toolbar intact (1/2/4-suit toggle, Time/Moves/Suits readout, Undo/New Deal), H1=1, no h-scroll. **PASS**
- Mobile 375 (rig vw 375): 62/62 cards visible, cardW **30.90px** (at healthy-band floor, acceptable — Spider deals 10 columns), 0 hidden, 0 clipped, maxCardRight 361<375, no h-scroll, compact mobile toolbar present. **PASS**

No visual/render/clipping bug found at any viewport. **No QA code change warranted.**

## Phase 2 — Competitor
online-solitaire.com **UNCHANGED** (23rd+ consecutive no-change run). Klondike default (Turn 1/Turn 3, "All games" dropdown, Stock 24), top bar Time/Moves/Stock, bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More, right rail = July challenge calendar (trophy tiers 10/20/31, today=6/Mo highlighted, "No wins") + Daily leaderboard (Maroko/Vfs/A333, score column) + Achievements (all N/A) + Signup CTA. H1=1. No new features/redesigns/content. Standing gaps where they lead: native-app download prominence, deck/background theming, illustrated face cards, 320+ deal-count catalog framing, non-solitaire card games. Where we lead now includes **6-locale FreeCell i18n** (EN/ES/FR/DE/IT/PT — see Phase 4) plus editorial depth, richer JSON-LD, spoke-domain SEO, no-signup save, Daily FreeCell Room. Logged to docs/research/competitor-log.csv. Content treated as untrusted per standing protocol; no embedded instructions present, none acted upon.

## Phase 3 — SEO & schema spot-check (long-tail pages not covered by scorecard canaries)
Rotated through the variant clusters. **All 3 PASS:**
- Hub `/yukon/strategy` → routes to playklondikeonline.com/yukon/strategy: H1=1, meta 149c, self-canonical, JSON-LD Organization+WebApplication+BreadcrumbList+Article+FAQPage, cluster links to /yukon/how-to-play + /yukon/tips + /klondike/*.
- Hub `/scorpion/tips` → routes to playspidersolitaireonline.com/scorpion/tips: H1=1, meta 151c, self-canonical, +Article+FAQPage+BreadcrumbList, cluster links to /spider/* + /scorpion/how-to-play.
- FreeCell `/freecell/tips`: H1=1, meta 151c, self-canonical, +Article+FAQPage+BreadcrumbList, cluster links to /freecell/how-to-play + /freecell/strategy + hub.
No SEO/schema issues found.

## Phase 4 — U5 locale-completeness audit (new locales IT + PT)
Extends the 07-04 four-locale audit (EN/ES/FR/DE) to the two locales added since. Audited both source (`alternates.languages`) and live-rendered `<link hreflang>` tags against the plan U5 gate (localized play UI + reciprocal hreflang across ALL siblings + H1 + core cluster).

- **Italian** — landing `/freecell-in-italiano`: H1=1 ("FreeCell in Italiano"), self-canonical, 7 reciprocal hreflang (it/en/es/fr/de/pt/x-default). Play route `/freecell-in-italiano/gioca`: H1=1, self-canonical, 7 hreflang, **52 playable cards** (localized play UI works). **PASS**
- **Portuguese** — landing `/freecell-em-portugues`: H1=1 ("FreeCell em Portugues"), self-canonical, 7 reciprocal hreflang. Play route `/freecell-em-portugues/jogar`: H1=1, self-canonical, 7 hreflang, **52 playable cards**. **PASS**
- **Cross-family reciprocity:** all six landing pages point to sibling landings; all six play routes point to sibling play routes with en/x-default → `/`; verified symmetric with no broken/`giocare`-style stale targets. The **6-locale FreeCell i18n family (EN/ES/FR/DE/IT/PT) is complete** and passes the completeness gate — the newest locales shipped complete, no doorway-page risk.

**Result: audit PASS, no fix warranted.** Both new locales were shipped complete. Deliverable is docs-only (no green-path bucket had an actionable fix: scorecard all-green, zero visual bugs, zero SEO issues, locales complete).

## Phase 5 — Deploy verification
Docs-only push (no source/TS changes). Gate re-run before push: `npx tsc --noEmit` pass, `npm run lint` 0 errors. Vercel + CI verification appended after push.

## Remaining backlog
- **U6 (carry, partially resolved):** the 07-05 carry "apply the landscape timer/moves treatment to Klondike/Spider shells if they share the gap" is **CLOSED** — verified today that neither spoke shell has an `isLandscapeMobile` branch; both use a `flexWrap` top bar that stays visible at short heights, so there is no equivalent hidden-status bug. Remaining U6: Klondike/Spider hints auto-execute instead of teaching (highlight like FreeCell); generalize daily challenges beyond FreeCell.
- **Deferred (per plan):** Klondike/Spider sidebars (leaderboard/achievements parity — Spider spoke sidebar currently ad-only).
- **Carried (game-logic, owner confirm):** /tripeaks renders generic cascade not canonical 3-peak layout (carried since Jun 9; playable via cascade adapter).
- Lint: 448 pre-existing unused-var warnings across solver/store files — safe cleanup candidate for a future polish run (non-blocking).
