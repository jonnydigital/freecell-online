# QA Report — 2026-07-08 (Cowork daily review)

## Phase 0 — Coordination
- Latest scorecard: `2026-07-03.md` — **28/28 checks passing, all green**. No ❌ items to prioritize.
- Ledger (last 10): 07-06 weekly pipeline shipped 2 spoke posts; 07-06/07-07 cowork runs (Spider spoke QA, FR html-lang). No overlap with today's work.
- `git log`: 07-07 landed FR html-lang fix (`2304945`) + `HtmlLang.tsx`; PWA install CTA; teach-hints for Klondike/Spider.
- Working tree clean at start (only untracked `.claude/settings.local.json`).

## Phase 1 — Visual QA

### Desktop (1440px) — live production
- **playfreecellonline.com** — ✅ 8 columns fully visible, 4 free cells + 4 foundations spaced correctly, sidebar (leaderboard + daily challenge) renders alongside board, 52 cards in DOM, `opacity:1`, no horizontal scroll (`scrollW == vw == 1440`), single H1.
- **solitairestack.com** (hub) — ✅ homepage renders, H1 present, 34 game links, no horizontal scroll, sidebar intact.
- **playspidersolitaireonline.com** (Spider spoke — even-day rotation) — ✅ 10 tableau columns, 8 foundation slots, stock pile, toolbar (1/2/4-Suit selector + Undo/Hint/New Deal) intact, Time/Moves/Suits readout, single H1, no horizontal scroll. Board renders after a short "Loading…" splash (expected).

### Mobile (375px) — ⚠️ environment limitation this run
- `resize_window` moved the OS window frame but the browser instance kept a **fixed 1440 layout viewport** (`document.documentElement.clientWidth == 1440`, mobile media queries did not match, `window.innerWidth` pinned at 1440). Live 375px screenshots were therefore not achievable in this run — this is a browser-instance limitation, **not** a site bug.
- **Static mobile-CSS regression check (repo):** `dom-card-styles.css` `@media (max-width: 767px)` sets `--card-width: clamp(30px, calc((100vw - 61px) / 8), 54px)` — exactly the expected 30–54px range, unchanged. Mobile board/card sizing rules are intact. Recommend a future run on a browser instance that honors viewport resize to re-capture live mobile screenshots.

## Phase 2 — Competitor (online-solitaire.com)
- **UNCHANGED** — 25th+ consecutive no-change run. Same Klondike default (Turn 1/Turn 3, "All games" dropdown, Stock: 24), top bar Time/Moves/Stock, bottom toolbar (New game/Hint/Undo/Bookmark/Download/Settings/More), July challenge calendar (today=8/We highlighted), daily leaderboard (Vfs sweep), Achievements N/A, signup CTA. H1=1. No new features/redesigns/content.
- Standing gaps where they lead (unchanged): native/PWA download prominence, deck/background theming, illustrated face cards, 320+ deal-count catalog framing, non-solitaire card games.
- We lead: editorial depth, richer per-game JSON-LD, spoke-domain SEO, no-signup save, named daily deals, ad-light dark theme, PWA offline + resume, Daily FreeCell Room, 6-locale FreeCell i18n.
- Security: competitor content treated as untrusted; page text scanned, no embedded instructions present, none acted upon.

## Phase 3 — SEO & schema spot-check (pages the Friday scorecard doesn't cover)
- **playfreecellonline.com/freecell/tips** — ✅ 1 H1, unique meta description, self-canonical, JSON-LD (Organization/WebApplication/BreadcrumbList/Article/FAQPage), cluster links (how-to-play/strategy/tips/1-3-cell).
- **playspidersolitaireonline.com/spider/how-to-empty-a-column** — ✅ 1 H1, unique meta description, self-canonical, JSON-LD (Organization/WebApplication/Article/FAQPage/BreadcrumbList), cluster links (how-to-play/strategy/tips/faq/etc.).
- No SEO fixes needed on spot-checked pages.

## Phase 4 — Improvement shipped (U5 completeness slice)
**fix(i18n): extend rendered-DOM `html lang` to all non-EN FreeCell locales.**
- **Finding:** The 07-07 fix added `<HtmlLang>` only to the two French pages. An audit of the hydrated DOM confirmed FR renders `lang="fr"` correctly, but the ES/DE/IT/PT landing + play routes (8 pages) had never imported `HtmlLang`, so their hydrated DOM stayed `lang="en"` — a real i18n/accessibility/SEO signal gap on live locale pages.
- **Fix:** Added the existing `HtmlLang` component (import + `<HtmlLang lang="xx" />`) to all 8 pages, matching the exact FR pattern: es → `/freecell-en-espanol` + `/jugar`, de → `/freecell-auf-deutsch` + `/spielen`, it → `/freecell-in-italiano` + `/gioca`, pt → `/freecell-em-portugues` + `/jogar`. 16 insertions across 8 files, no other changes.
- **Gates:** `npx tsc --noEmit` → exit 0. `npm run lint` → 0 errors (447 warnings, pre-existing/allowed).
- **Post-deploy verification:** confirm each locale's hydrated `document.documentElement.lang` matches its code once Vercel is READY.

## Improvements backlog (remaining)
- U6: generalize daily challenges beyond FreeCell to the two spokes (retention loop) — larger unit, future slice.
- Live mobile visual QA needs a browser instance that honors viewport resize.
- hreflang note: EN home advertises locale **play** routes (`/jugar`, `/jouer`…) while locale pages advertise **landing** routes as their siblings; worth a future consistency pass (not blocking — all targets 200 and self-canonical).

## Push
- Single push at end of run (i18n fix commit + docs commit batched into one `git push origin main`).
