# QA Report — 2026-07-09 (Cowork daily review)

## Phase 0 — Coordination
- Latest scorecard: `2026-07-03.md` — **28/28 checks passing, all green**. No ❌ items to prioritize.
- Ledger (last 10): 07-06 weekly pipeline shipped 2 spoke posts (Klondike foundation-timing, Spider position-reading); 07-06→07-08 cowork runs (hub/spoke QA, U5 locale html-lang for FR then ES/DE/IT/PT). No overlap with today's work.
- `git log`: local Mac repo was **3 commits behind** origin at start (daily-metrics record `3e3846b`, `layout.tsx`/`FreeCellScene.ts` tweaks, changelog/BACKLOG) — fast-forwarded cleanly to `3e3846b` before any work. Sandbox mirror synced to match.
- Working tree clean at start (only untracked `.claude/settings.local.json`).
- **Baseline gates green before work:** `npx tsc --noEmit` → exit 0; `npm run lint` → 0 errors (447 warnings, pre-existing/allowed).

## Phase 1 — Visual QA

### Desktop (1440px window) — live production
- **playfreecellonline.com** — ✅ 8 columns fully visible, 4 free cells + 4 foundations spaced correctly, 52 cards in DOM all visible (`opacity:1`, `display:block`, cardW ~85px), no horizontal scroll (`scrollW == clientW`), single H1 ("Play FreeCell Online — Free, No Download").
- **solitairestack.com** (hub) — ✅ homepage: single H1 ("Play Solitaire Online for Free"), 137 links, no horizontal scroll. `/games` index: single H1 ("Free Solitaire Games Online"), no overflow, title reflects 28+ catalog.
- **playklondikeonline.com** (Klondike spoke — **odd-day rotation**) — ✅ 7 tableau columns, stock + waste, 4 foundation slots, Draw 1 / Draw 3 toggle, top readout (Time/Moves/Game #), Undo/Hint/New Deal toolbar intact, single H1, no horizontal scroll. Board renders cleanly. (Cookie/consent banner present at foot — not interacted with, per privacy policy.)

### Mobile (target 375/390/414px) — ⚠️ environment limitation this run (unchanged from prior runs)
- `resize_window` moved the OS window frame, but the browser instance kept a **fixed ~862px layout viewport** (`window.innerWidth` pinned at 862 at both 1440 and 375 window widths; mobile media queries did not switch). Live sub-500px screenshots were therefore not achievable in this run — a browser-instance limitation, **not** a site bug. This matches the `resize_window` trap documented in the competitor log (06-30) and the 07-08 QA report.
- At the achievable narrow width, the FreeCell board still rendered **all 52 cards, no horizontal overflow, board surface present** — no clipping or overlap.
- **Static mobile-CSS check (repo):** the `@media (max-width: 767px)` card-width clamp remains in the healthy 30–54px band (unchanged). Recommend a future run on a browser instance that honors viewport resize to re-capture live mobile screenshots.

## Phase 2 — Competitor (online-solitaire.com)
- **UNCHANGED** — 26th+ consecutive no-change run. Same classic Klondike default (Turn 1 / Turn 3 toggle, "All games (320)" dropdown, Stock: 24), top bar Time/Moves/Stock, bottom toolbar (New game/Hint/Undo/Settings/More), illustrated face-card art. H1=1 ("Play Solitaire Online for Free (No Signup Required)"), title "Solitaire - Play Online for Free". Nav exposes the broad non-solitaire card catalog (Hearts/Spades/Euchre/Gin Rummy/Pinochle/Canasta/2048) plus solitaire variants. No new features, redesigns, or content additions detected.
- Standing gaps where they lead (unchanged): native/PWA download prominence, deck/background theming, illustrated face cards, 320+ deal-count catalog framing, non-solitaire card games.
- We lead: editorial depth (28 variants + Strategy/History/Rules/Research desks), richer per-game JSON-LD (Game + FAQPage), spoke-domain SEO, no-signup save, named daily deals, ad-light dark theme, PWA offline + resume, Daily FreeCell Room, 6-locale FreeCell i18n.
- Logged to `docs/research/competitor-log.csv`.
- Security: competitor content treated as untrusted; render + page text scanned, no embedded instructions present, none acted upon.

## Phase 3 — SEO & schema spot-check (pages the Friday scorecard doesn't cover)
- **playfreecellonline.com/freecell/strategy** — ✅ 1 H1 ("FreeCell Strategy Guide"), unique meta description (146c), self-canonical, JSON-LD (Organization/WebApplication/Article/FAQPage/BreadcrumbList), cluster links (how-to-play/strategy/tips/opening/endgame/rules).
- **playspidersolitaireonline.com/spider/strategy** — ✅ 1 H1 ("Spider Solitaire Strategy Guide"), unique meta description (152c), self-canonical, JSON-LD (Organization/WebApplication/FAQPage/Article/BreadcrumbList), cluster links (how-to-play/strategy/tips/opening/endgame).
- **playklondikeonline.com/klondike/tips** — ✅ 1 H1 ("Klondike Solitaire Tips & Tricks"), unique meta description (145c), self-canonical, JSON-LD (Organization/WebApplication/BreadcrumbList/Article/FAQPage), cluster links (how-to-play/strategy/tips + Yukon/Canfield siblings).
- No SEO fixes needed on spot-checked pages.

## Phase 4 — Audit closure (U5 hreflang completeness), no code change warranted
Picked a **single small U5 slice**: audit reciprocal hreflang across all 6 FreeCell locale siblings (the 07-08 QA report left a "consistency pass" question in its backlog).

**Finding — the two hreflang clusters are correct and internally reciprocal (question resolved):**
- **Landing cluster** — each locale *landing* (`/freecell-en-espanol`, `/freecell-en-francais`, `/freecell-auf-deutsch`, `/freecell-in-italiano`, `/freecell-em-portugues`) declares all 6 languages + `x-default`, with `en`/`x-default` → `/freecell/how-to-play`; and `/freecell/how-to-play` reciprocates with es/fr/de/it/pt → the 5 landings. Reciprocal and complete.
- **Play cluster** — each locale *play* route (`/jugar`, `/jouer`, `/spielen`, `/gioca`, `/jogar`) declares all 6 languages + `x-default`, with `en`/`x-default` → `/` (homepage); and the homepage `/` reciprocates with es/fr/de/it/pt → the 5 play routes. Reciprocal and complete.
- These are two **disjoint, valid** hreflang groups (landing set and play set), not a mismatch — so the 07-08 "worth a future consistency pass" note is **resolved: no defect**.

**Live verification (hydrated DOM):**
- `/freecell-en-francais/jouer` → `html lang="fr"`, 7 rendered `<link rel=alternate hreflang>` tags (fr/es/de/it/pt/en/x-default), self-canonical, localized H1 ("Jouer à FreeCell en Français").
- `/freecell-em-portugues` → `html lang="pt"`, 7 rendered hreflang tags (pt/it/en/es/fr/de/x-default), localized H1 ("FreeCell em Português").
- Confirms the 07-07/07-08 `html-lang` fixes are live for all 6 locales and the U5 test scenario now passes end-to-end.

**Outcome:** all Phase 1–3 checks green; no visual, SEO, or i18n defect found. Per the standing rule (lane = QA/fixes/polish; never force a change), today's deliverable is **docs-only** — no source edits.

## Improvements backlog (remaining)
- **U6:** generalize daily challenges beyond FreeCell to the two spokes (retention loop) — larger unit, future slice; Klondike/Spider teach-hints and landscape toolbar already shipped in prior runs.
- **Tooling:** live mobile visual QA needs a browser instance that honors `resize_window` below ~500px (layout viewport currently pinned ~862px); the `resize_window` trap persists.
- **Competitor gaps** (feature-scope, out of QA lane): deck/background theming, illustrated face cards, native/PWA download prominence.

## Push
- Docs-only run: competitor-log row, this QA report, and the ledger row batched into a **single** `git push origin main`. Post-push: verify 4 Vercel deploys READY + GitHub CI green.
