# QA Report — 2026-07-10 (cowork-daily-review)

Automated daily review & QA for the 4-domain solitaire network. Spoke rotation:
even day → **Spider** (playspidersolitaireonline.com).

## Phase 0 — Coordination

- **Ledger (last ~10 rows):** No overlap with other producers. Monday content
  pipeline last shipped 07-06 (klondike-foundation-timing, spider-position-reading).
  Prior daily runs 07-06→07-09 closed the U5 hreflang/locale audits.
- **Latest scorecard:** `docs/analytics/scorecards/latest.json` (2026-07-03) and
  `2026-07-03.md` — **zero ❌ failing checks** (sitemap/blog counts only). No
  Friday scorecard newer than 07-03 is committed yet; nothing to prioritize-fix.
- **git log -15:** Clean daily-review + i18n/UX commits; CI green through 07-09.
  Nothing red inherited.

## Phase 1 — Visual QA

### Desktop (1440×900) — all green
- **playfreecellonline.com** (`/game/1`): 8 columns fully visible, no overlap/
  clipping. 4 free cells + 4 foundations correctly spaced. Sidebar (leaderboard,
  daily challenge, July calendar) renders alongside board. DOM: 52 cards,
  card 113.95×159.53px, opacity 1, **no horizontal scroll** (scrollW=clientW=1440),
  single H1 ("Play FreeCell Online — Free, No Download").
- **solitairestack.com** (hub homepage): H1 present ("Play Solitaire Online for
  Free"), 29 game links, embedded playable FreeCell board (52 cards), no h-scroll.
- **playspidersolitaireonline.com** (spoke rotation): 10 columns visible, stock
  pile top-right intact, toolbar (1/2/4-Suit toggle, Undo/Hint/New Deal) sane,
  single H1, no h-scroll. Screenshot captured.

### Mobile (375px) — verified via source (live-emulation limitation this run)
- The Chrome rig's content viewport stayed pinned at `innerWidth`/`visualViewport
  = 1440` despite `resize_window` to 375×812 (window chrome resized: outerW 756,
  dpr 2), so a true 375px live render/screenshot was not obtainable today.
- **Mitigation — source verification of `dom-card-styles.css` responsive path:**
  - FreeCell mobile (≤767px): `--card-width: clamp(30px, (100vw-61px)/8, 54px)`
    → at 375px ≈ **39px** (within the 30–54px target; 8×39 + gaps fit, no overflow).
  - Spider mobile: `clamp(24px, (100vw-66px)/10, 44px)` → at 375px ≈ **31px**.
  - Wide boards (>8 cols) scale off `--cascade-count`; card-back medallion hidden
    below 767px. Breakpoints are mathematically sound.
  - Cross-check: the desktop measurement 113.95px matches its own formula
    `(1440-460)/8.6` exactly, confirming the CSS engine is applying the clamps
    correctly — only the rig's viewport emulation is stuck.
- **Conclusion:** No mobile regression indicated. Flag for next run: re-attempt a
  true 375px live screenshot once the browser rig honors resize.

## Phase 2 — Competitor (online-solitaire.com)

- **UNCHANGED — 27th+ consecutive no-change run.** Homepage still defaults to
  classic Klondike (Turn 1 / Turn 3, "All games (320)", Stock: 24); top bar
  Time/Moves/Stock; bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/
  More; illustrated face cards; right rail = July challenge calendar (tiers
  10/20/31, today=10/Fr highlighted, "No wins") + Daily leaderboard
  (YAHYA/Jerryzuh) + Achievements (all N/A) + Signup CTA. Title "Solitaire - Play
  Online for Free". Note: homepage carries **2 H1s** (their SEO, not ours). Nav
  exposes non-solitaire catalog (Hearts/Spades/Euchre/Gin/Pinochle/Canasta/2048).
- **Standing gaps where they lead:** native/PWA download prominence, deck/
  background theming, illustrated face cards, 320-deal catalog framing,
  non-solitaire games.
- **Where we lead:** editorial depth (28 variants + Strategy/History/Rules/
  Research desks), richer per-game JSON-LD (Game + FAQPage), spoke-domain SEO,
  no-signup local save, named daily deals, ad-light dark theme, PWA offline +
  resume, Daily FreeCell Room, 6-locale FreeCell i18n (EN/ES/FR/DE/IT/PT).
- **Security:** competitor content treated as untrusted; render + page text
  scanned, no embedded instructions present, none acted upon.

## Phase 3 — SEO & schema spot-check (long-tail, not covered by scorecard canaries)

- **solitairestack.com/golf/how-to-play** — 1 H1 ("How to Play Golf Solitaire"),
  unique meta desc (144 chars), self-canonical, JSON-LD
  Organization+WebApplication+FAQPage+Article+BreadcrumbList, 39 cluster links. ✅
- **solitairestack.com/bisley/tips** — 1 H1 ("Bisley Solitaire Tips & Tricks"),
  unique meta desc (149 chars), self-canonical, full JSON-LD, 44 cluster links. ✅
- **/canfield/strategy** — 301-consolidates to
  **playklondikeonline.com/canfield/strategy** (intended cross-domain
  consolidation, R3). Destination: 1 H1, meta desc 132 chars, self-canonical to
  the Klondike spoke, full JSON-LD, 23 cluster links, not a 404. ✅
- No SEO issues found.

## Phase 4 — Improvement shipped (1)

**SEO gap fix — `/achievements` page metadata (U5(c) polish slice).**
Source audit of all 245 `(main)` page routes found every metadata-bearing page
has a description; only two lacked any metadata — `lab/dom-freecell` (dev lab,
non-indexed, intentional) and **`/achievements`**. The latter is `FREECELL_ONLY`
and **listed in `sitemap.xml` (priority 0.5, indexable)** yet exported no title,
description, or keywords — search engines got a fallback title and no meta
description. Added a `Metadata` export (title "FreeCell Achievements | Track Your
Badges & Milestones", accurate description, keywords), matching the
`leaderboard`/`about` sibling convention. Canonical is inherited from the root
layout's self-referential `alternates.canonical: './'`, so none was added.
Validated: `npx tsc --noEmit` exit 0; `npm run lint` 0 errors (447 warnings,
pre-existing, allowed).

## Remaining backlog

- Re-attempt true 375px live mobile screenshot once the browser rig honors
  `resize_window` (blocked this run).
- U6 play-UX backlog (Klondike/Spider hint-highlighting parity; daily-challenge
  generalization to spokes) — untouched, awaiting a dedicated slice.
- U7 off-site legitimacy / U8 monetization ladder — operational, not daily-lane.

## Push & deploy verification

Single push at end of run (see run summary). Vercel deploy READY + GitHub CI
status recorded post-push.
