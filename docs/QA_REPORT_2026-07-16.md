# QA Report — 2026-07-16 (Cowork daily review)

**Outcome:** Network all-green. No bugs found across any viewport; no SEO/schema
defects in the long-tail spot-check; competitor unchanged. **No code shipped
and no push made this run** — see "Blocker" below.

Spoke rotation: even day → **Spider** (playspidersolitaireonline.com).

---

## Phase 0 — Coordination

- **Latest scorecard:** `docs/analytics/scorecards/2026-07-10.md` — **28/28 checks
  passing, all green.** No ❌ failing checks, so no forced #1 fix priority today.
- **Ledger:** last daily-review row is 2026-07-14. Weekly content pipeline last
  shipped 2026-07-13 (klondike-dead-game-detection, spider-solitaire-suit-completion).
  No topic/route overlap with my QA lane.
- **git log:** last commit `b5b9ece` (2026-07-14 daily review — P0 mobile occlusion
  fix on both spokes). Nothing landed 07-15 → 07-16.
- **Found from an aborted 2026-07-15 run:** an uncommitted `2026-07-15` row in
  `docs/research/competitor-log.csv` and a leftover `.git/index.lock`
  (undeletable from the sandbox: "Operation not permitted"), but **no** committed
  07-15 QA report or ledger row. The 07-15 competitor row is valid history and was
  **preserved**, not discarded.

## Phase 1 — Visual QA (all-green)

| Viewport | Target | Result |
|---|---|---|
| Desktop 1440 | playfreecellonline.com | 8 columns + 4 free cells + 4 foundations well-spaced; sidebar (ad / daily leaderboard / daily challenge) intact; single H1; no horizontal scroll. Clean. |
| Mobile 375 (iframe rig) | playfreecellonline.com | 52 cards, cardW **38.5px** (in 30–54px range), opacity 1, 0 cards offscreen, top bar + bottom toolbar visible, no h-scroll. Clean. |
| Desktop 1440 | solitairestack.com (hub) home | Embedded FreeCell board renders; single H1 "Play Solitaire Online for Free"; canonical correct; 34 game links; no h-scroll. Clean. |
| Desktop 1440 | solitairestack.com/tripeaks (game page) | 3-peak board deals correctly; single H1; canonical correct; JSON-LD Game + FAQPage + BreadcrumbList. Clean. |
| Desktop 1440 | playspidersolitaireonline.com (rotation) | 10 columns + 8 foundations + stock; toolbar (Undo/Hint/New Deal) and mode chips (1/2/4 Suit) intact; no h-scroll. Clean. |
| Mobile 375 (iframe rig) | playspidersolitaireonline.com (rotation) | 63 cards, cardW **30.3px** (at the 30px floor of the range), 0 cards offscreen, no h-scroll. **07-14 P0 fix HOLDS:** mode chips (1/2/4) render on a dedicated second row and all three test `tappable:true` / unoccluded via elementFromPoint. Clean. |

**Note:** `resize_window` did not shrink the content viewport (stayed 1443px), so
mobile QA used the same same-origin 375px iframe rig prior runs used. Screenshots
captured at every viewport. 390/414 not separately re-run: 375 (the tightest
constraint) passed and cards only grow wider above it.

## Phase 2 — Competitor (online-solitaire.com)

**UNCHANGED — 33rd+ consecutive no-change run.** Klondike default (Turn 1 / Turn 3,
"All games (320)", Stock 24); top bar Time/Moves/Stock; bottom toolbar New game /
Hint / Undo / Bookmark / Download / Settings / More; right rail July calendar
(today=16/Th highlighted, "No wins"), daily leaderboard (Jerryzuh / ENIO / Jerryzuh,
country flags + Time/Moves/Score columns), Achievements all N/A, "Signup to save"
CTA. H1=1. Structured data still only **WebApplication + BreadcrumbList site-wide**
(no per-page Article/FAQPage) — confirms our per-page JSON-LD lead. No new features.
Logged to `docs/research/competitor-log.csv`. Competitor content treated as
untrusted; page scanned, no embedded instructions present or acted upon.

## Phase 3 — SEO & schema spot-check (all-green)

Rotated to three long-tail pages the Friday scorecard has never covered:

| Page | H1 | Canonical | Meta desc | JSON-LD | Cluster links |
|---|---|---|---|---|---|
| solitairestack.com/beleaguered-castle | 1 | correct | 158 chars | Organization / WebApplication×2 / Game / BreadcrumbList / FAQPage | own how-to-play + strategy present |
| solitairestack.com/monte-carlo | 1 | correct | 152 chars | same set | own how-to-play + strategy + tips present |
| solitairestack.com/cruel | 1 | correct | 155 chars | same set | own how-to-play + strategy + tips present |

**On the two `WebApplication` entities:** inspected the raw JSON-LD — they are
legitimately distinct: a site-level app ("Solitaire Stack" @ root, from the global
layout) plus a per-page game app (e.g. "Cruel Solitaire" @ /cruel) alongside a
matching `Game` entity. This is **not** the 07-13 defect (there, editorial
*comparison* pages wrongly carried a game `WebApplication` pointing at the site
root). These are real game pages with correct per-page URLs, so **no fix warranted.**

## Phase 4 — Improvements

**None shipped.** Priority buckets 1 (scorecard ❌) and 2 (Phase 1–3 bugs) are
**empty** — everything green. Buckets 3 (a U5/U6 plan slice) and 4 (competitor
gaps) are code changes, and **no code can be shipped this run** because the push
channel is unavailable (see Blocker). Shipping an unpushable code change would
leave the working tree dirty — the exact failure mode of the aborted 07-15 run —
so it was correctly deferred.

## Phase 5 — Validation

- `npx tsc --noEmit` → **exit 0** (type-clean).
- `npm run lint` → **exit 0**, 445 warnings / **0 errors** (green per the rule).
- No code changed, so state equals the last green CI commit `b5b9ece`.

---

## Blocker — could not commit or push this run

This was an **unattended scheduled run**, and every write path to the repo's git
history is unavailable in that context:

1. **Sandbox has no push credentials.** `git push --dry-run origin main` →
   `could not read Username for 'https://github.com'` (exit 128). No `GH_TOKEN` /
   `GITHUB_TOKEN` in env and no `gh` CLI.
2. **Documented Mac-git path needs interactive approval.** The task specifies
   `osascript` on the Mac (GitHub keychain creds), but that requires computer-use
   `request_access`, which can't be approved with no user present; Terminal is also
   a "click"-tier app (typing blocked), so commands can't be typed into it.
3. **Leftover `.git/index.lock`** from the aborted 07-15 run cannot be removed from
   the sandbox ("Operation not permitted"), which independently blocks local git
   index writes.

**Left uncommitted in the working tree** (recoverable — the next run's Phase 0 reads
LEDGER/competitor-log by file content, so coordination is preserved):

- `docs/research/competitor-log.csv` — 07-15 (preserved) + 07-16 (this run) rows
- `docs/QA_REPORT_2026-07-16.md` — this report
- `docs/content-pipeline/LEDGER.md` — this run's ledger row

**Recommended follow-up for Jonathan (or the next attended run):** remove
`.git/index.lock`, then commit the three docs files above and
`git push origin main` once. Nothing here is red — a plain docs push.

## Remaining backlog (unchanged)

- U5 — French/6-locale completeness audit slices (play-UI, reciprocal hreflang).
- U6 — play-UX polish backlog (mobile-portrait cascade tuning).
- Standing competitor gaps (lower priority): deck/background theming, illustrated
  face cards, native/PWA download prominence, mobile star-rating feedback loop.
