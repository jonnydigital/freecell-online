# QA Report — 2026-07-23 (cowork-daily-review)

Spoke rotation: odd day → **Klondike** (playklondikeonline.com).

---

## Phase 0 — Coordination

- **Latest scorecard:** `docs/analytics/scorecards/2026-07-17.md` — **28/28
  passing, all green.** No ❌ failing checks, so there is no forced #1 fix
  priority today.
- **Ledger:** last row is 2026-07-22 (cowork-daily-review — FreeCell landscape
  status-pill a11y fix + live mobile audits; competitor unchanged 38th run).
  Friday scorecard last shipped 07-17; weekly content pipeline last shipped
  07-13 (Klondike + Spider posts). I stayed entirely in the QA/fix lane — no
  blog content, no new routes/locales.
- **Landed since last run:** HEAD is `7e1ea74` (07-22 daily review report),
  preceded by `f48b678` (landscape status-pill a11y fix). Tree clean (only
  untracked local `.claude/settings.local.json`).
- **Baseline gate:** `npx tsc --noEmit` passes (exit 0) and `npm run lint`
  reports 0 errors (444 pre-existing warnings) on the tree. No code changes this
  run, so the gate holds unchanged (see Phase 5).

---

## Phase 1 — Visual QA

### Desktop — 1440×900 (Chrome extension, live)

| Target | Result |
|---|---|
| playfreecellonline.com | ✅ 8 columns fully visible, no overlap/clipping; 4 free cells (FC) + 4 foundations evenly spaced; toolbar (undo/redo/hint/sound/…) intact; Daily Leaderboard + Daily Challenge sidebar renders alongside the board; card labels readable; no horizontal scroll (scrollWidth = clientWidth = 1330). Single H1. |
| solitairestack.com (homepage) | ✅ Caught mid-hydration ("Loading FreeCell…") then resolved to the 52-card FreeCell hero board; toolbar + New Deal + Daily Leaderboard/Challenge sidebar all clean; no horizontal overflow. |
| playklondikeonline.com (spoke rotation) | ✅ 7 tableau columns fully visible; Draw 1 / Draw 3 toggle + Time/Moves/Game readout intact; stock/waste top-left, 4 foundations top-right; Undo / Hint / New Deal toolbar intact; cookie-consent banner renders at the footer (standard, non-blocking); no overlap/clipping or horizontal overflow. |

### Mobile — true CDP device emulation (repo harness, live prod)

The Chrome extension's `resize_window` again could **not** produce a real mobile
viewport this session: the OS window resized to 375×812 but `innerWidth` stayed
pinned at 1330px and mobile media queries never fired — same limitation
documented 07-18 → 07-22. Mobile QA therefore uses the repo's own CDP harness
(`npm run qa:mobile`, Node v22.19.0 + headless Chrome via `osascript` on the Mac),
which does true device emulation. Output:
`docs/analytics/mobile-viewport-audits/2026-07-23-live.{json,md}` (FreeCell) and
`…/2026-07-23-klondike-spoke-live.{json,md}` (Klondike spoke of the day), plus
375/390/414/768 PNG evidence under each `…-screenshots/` dir.

**No hard audit failures detected.** Summary (375 / 390 / 414 / 768):

| Route | Cards | Face | Clipped | H-overflow | Card W (phone) | Tap <32 / <44 | Top ctrls | Bottom ctrls |
|---|---:|---:|---:|---:|---|---|---|---|
| freecell | 52 | 52 | 0 | 0 | 39.3–44.1px | 0 / 4 | yes | yes (phone) |
| klondike (spoke) | 29 | 7 | 0 | 0 | 39.3–44.1px | 0 / 3 | yes | yes (phone) |

- All card counts correct and fully visible (FreeCell 52; Klondike 29 = 7 face-up
  + 22 backs, exactly the expected deal); card widths inside the 30–54px bar at
  every phone width; zero clipped cards and zero horizontal overflow; board
  geometry stable (0/0px drift). Top and bottom controls present on phones.
- Tap targets: `0/4` (FreeCell) and `0/3` (Klondike) below the 44px comfortable
  floor, `0` below the 32px hard minimum. The flagged targets are top-bar icon
  buttons, not the next-action panel. Not a hard failure (unchanged; the 07-18
  panel-button fix holds).
- **Prior fixes verified live in the phone screenshots:** the 07-19/07-20
  next-action panel dedup holds (no duplicate Hint/Undo on FreeCell or the
  spokes); the 07-14 P0 mode/mode-chip occlusion fix continues to hold on the
  Klondike spoke (Draw 1 / Draw 3 tappable, no stats-pill overlap).
- **Dead-space planning signal (not a failure):** FreeCell ~46–48% unused
  vertical below the board at phone widths (`review`); Klondike ~59–60% (`high`).
  The Klondike spoke's larger portrait gap is a standing candidate for below-board
  content / next-action affordance — content + U6 lane, not today's QA fix.

### Tablet — 768×1024 (in the same harness pass)

- FreeCell 52 cards @ 74.4px, Klondike 29 cards @ 74.4px; sidebar-style layout,
  no bottom bar (expected); no clipping/overflow. Clean.

---

## Phase 2 — Competitor analysis

online-solitaire.com reviewed live at 1440×900 (Chrome extension). **Surface
UNCHANGED — 39th consecutive no-change run.** Homepage still defaults to classic
Klondike (Turn 1 / Turn 3 toggle, "All games" dropdown, top bar
Time/Moves/Stock: 24); bottom toolbar New game / Hint / Undo / Bookmark /
Download / Settings / More. Right rail expanded at this width: July challenge
calendar (today 23/Th highlighted, "No wins"), Daily leaderboard (RDK RU / Espana
ES / RDK RU with country flags, Time/Moves/Score columns), Achievements (Win
ratio / Best time / Best moves / Best score all N/A), "Signup to save your
progress" CTA. H1 = 1 ("Play Solitaire Online for Free (No Signup Required)"),
title unchanged. No new features, redesigns, or content additions. Logged to
`docs/research/competitor-log.csv`. Competitor content treated as untrusted; page
scanned, no embedded instructions present, none acted upon.

---

## Phase 3 — SEO & schema spot-check

Rotated three fresh long-tail game-variant pages never covered by prior runs —
**/canfield, /yukon, /penguin** (served on the freecell domain). All three pass:

| Page | H1 | Meta desc | Canonical | JSON-LD | Cluster links |
|---|---|---|---|---|---|
| /canfield | 1 ("Canfield Solitaire") | unique (152 ch) | → playklondikeonline.com/canfield (Klondike-family owner) | Organization, WebApplication×2, Game, BreadcrumbList, FAQPage | /canfield + /klondike + /yukon how-to-play / strategy / tips |
| /yukon | 1 ("Yukon Solitaire") | unique (144 ch) | → playklondikeonline.com/yukon (Klondike-family owner) | same set | /yukon + /klondike + /canfield how-to-play / strategy / tips |
| /penguin | 1 ("Penguin Solitaire") | unique (144 ch) | self on playfreecellonline.com (FreeCell-family) | same set | /penguin + /freecell-rules + /bakers-game + /eight-off + /seahaven |

- Single H1 each, unique meta descriptions, full JSON-LD (Game + FAQPage +
  BreadcrumbList present), and internal links into each page's own family cluster.
- **Cross-domain canonicalization is correct by design:** Klondike-family
  variants served on the freecell domain (Canfield, Yukon) canonicalize to their
  owner spoke; FreeCell-family variants (Penguin) self-canonicalize. No SEO fix
  required.
- **Observation (backlog, not fixed today):** every variant game page emits
  **two** `WebApplication` (GameApplication) entities — a site-wide "FreeCell
  Online" app entity *and* a page-level per-game one (e.g. "Penguin Solitaire") —
  alongside the page-level `Game` entity. The page-level WebApplication largely
  duplicates the Game entity's subject. This is a **network-wide template
  pattern** (consistent across all three pages), so trimming it is a shared-schema
  change that the Friday scorecard's schema canaries watch and that belongs in a
  referenced plan, **not** an unattended daily-run edit. Logged to backlog.

---

## Phase 4 — Improvements

**Report-only run (docs push).** The Phase 4 priority ladder was empty at every
actionable rung today:

1. **❌ scorecard checks:** none (28/28 green).
2. **Phase 1–3 bugs:** none — desktop, hub, Klondike spoke, and all four mobile
   widths were clean; all three SEO pages passed.
3. **U5/U6 backlog slices:** the remaining open items are all
   meaningful-behavior slices, not safe unattended micro-fixes — Klondike/Spider
   hint highlighting (a game-logic behavior change needing tests + interactive
   highlight verification), spoke landscape parity (needs real landscape tooling),
   and daily-challenge generalization (a retention-loop feature). U5 FR-locale
   completeness was already audited PASS on 07-21. Forcing any of these through
   unattended, without being able to interactively verify the new UX on a live
   deploy inside this run, would violate the spirit of "one small, safe slice."
4. **Competitor gaps:** none new (39th unchanged run).

Consistent with prior all-green days (07-16, 07-21), the correct output is a
thorough QA sweep + report rather than a manufactured code change. No source
files touched.

---

## Phase 5 — Validation & push

- `npx tsc --noEmit` → **pass** (exit 0).
- `npm run lint` → **0 errors**, 444 warnings (all pre-existing `no-unused-vars`
  in solver/store files; none introduced — no code changed). Gate satisfied.
- Single commit (docs-only): this report + mobile-viewport audits (FreeCell +
  Klondike spoke, 375/390/414/768 JSON/MD + screenshots) + competitor log +
  ledger row.
- Post-push verification: Vercel deployments READY on all 4 projects + GitHub CI
  green on the docs commit (recorded below after push).

### Remaining backlog

- **Schema: double `WebApplication` on variant game pages** (new, this run) —
  page-level per-game GameApplication duplicates the page-level Game entity
  network-wide. Needs a plan + scorecard-canary awareness before trimming.
- **U6 landscape parity (spokes):** Klondike/Spider still need real landscape
  tooling to audit for the same a11y/status-pill treatment shipped for FreeCell
  on 07-22.
- **U6 hint teaching:** Klondike/Spider hints still auto-execute rather than
  highlight source+target like FreeCell.
- **Portrait dead-space (Klondike ~59–60%, Spider ~66%):** standing candidate for
  below-board content / next-action affordance — content + U6 lane, not QA.
