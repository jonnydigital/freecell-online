# QA Report — 2026-07-24 (cowork-daily-review)

Spoke rotation: even day → **Spider** (playspidersolitaireonline.com).

---

## Phase 0 — Coordination

- **Latest scorecard:** `docs/analytics/scorecards/2026-07-17.md` — **28/28
  passing, all green.** No ❌ failing checks, so there is no forced #1 fix
  priority today.
- **Ledger:** last row is 2026-07-23 (cowork-daily-review — report-only, Klondike
  spoke live mobile audits, SEO canfield/yukon/penguin clean, competitor
  unchanged 39th run). Friday scorecard last shipped 07-17; weekly content
  pipeline last shipped 07-13 (Klondike + Spider posts). I stayed entirely in the
  QA/fix lane — no blog content, no new routes/locales. (Today is Friday; the
  Friday scorecard is a separate cloud producer and is not run here.)
- **Landed since last run:** HEAD is `aea0f45` (07-23 daily review report). Tree
  clean apart from untracked local `.claude/settings.local.json`.
- **Baseline gate:** `npx tsc --noEmit` passes (exit 0) and `npm run lint`
  reports **0 errors** (444 pre-existing warnings) on the tree. No code changes
  this run, so the gate holds unchanged (see Phase 5).

---

## Phase 1 — Visual QA

### Desktop — 1440×900 (Chrome extension, live)

| Target | Result |
|---|---|
| playfreecellonline.com | ✅ 8 columns fully visible, no overlap/clipping; 4 free cells (FC) + 4 foundations evenly spaced; toolbar (undo/redo/hint/sound/…) intact; Daily Leaderboard + Daily Challenge sidebar renders alongside the board; card labels readable; no horizontal scroll (scrollWidth = clientWidth = 1512). 52 cards in DOM, opacity 1. Single H1 "Play FreeCell Online — Free, No Download". |
| solitairestack.com (homepage) | ✅ Caught mid-hydration ("Loading FreeCell…") then resolved to the 52-card FreeCell hero board; toolbar + New Deal + Daily Leaderboard/Challenge sidebar all clean; no horizontal overflow. |
| solitairestack.com/games (hub game page) | ✅ Header nav (FreeCell/Spider/Games/Compare/Guides + Play CTA) intact; "Free Solitaire Games Online" hero; breadcrumb Home / Free Solitaire Games Online; "28 playable games, 32,000 numbered FreeCell deals" copy; no overlap or horizontal overflow. |
| playspidersolitaireonline.com (spoke rotation) | ✅ 10 tableau columns fully visible; 8 suit-foundation slots top-left; face-down stock pile top-right; 1 Suit / 2 Suits / 4 Suits mode chips + Time/Moves/Suits (0/8) readout + Undo / Hint / New Deal toolbar all intact; face-up cards readable, 63 cards in DOM; no overlap/clipping or horizontal overflow. |

### Mobile — true CDP device emulation (repo harness, live prod)

The Chrome extension's `resize_window` again could **not** produce a real mobile
viewport this session: the OS window resized to 375×812 but `innerWidth` stayed
pinned at 1512px and mobile media queries never fired — same limitation
documented 07-18 → 07-23. Mobile QA therefore uses the repo's own CDP harness
(`npm run qa:mobile`, Node v22.19.0 + headless Chrome via `osascript` on the Mac;
the sandbox mirror only had an x86-64 headless build, unrunnable on this aarch64
box, so the harness was driven on the Mac where real Chrome lives). Output:
`docs/analytics/mobile-viewport-audits/2026-07-24-live.{json,md}` (FreeCell
domain, all four game engines) and
`…/2026-07-24-spider-spoke-live.{json,md}` (Spider spoke of the day), plus
375/390/414/768 PNG evidence under the matching `…-screenshots/` dirs.

**No hard audit failures detected.** Summary across 375 / 390 / 414 / 768:

| Route (domain) | Cards | Face | Clipped | H-overflow | Card W (phone) | Tap <32 / <44 | Top ctrls | Bottom ctrls |
|---|---:|---:|---:|---:|---|---|---|---|
| freecell (freecell) | 52 | 52 | 0 | 0 | 39.3–44.1px | 0 / 0 | yes | yes (phone) |
| klondike (freecell) | 29 | 7 | 0 | 0 | 39.3–44.1px | 0 / 0 | yes | yes (phone) |
| spider (freecell) | 62–63 | 10 | 0 | 0 | 30.9–34.8px | 0 / 0 | yes | yes (phone) |
| forty-thieves (freecell) | 41 | 40 | 0 | 0 | 31.9–35.8px | 0 / 0 | yes | no |
| spider (spider spoke) | 62–63 | 10 | 0 | 0 | 30.9–34.8px | 0 / 0 | yes | yes (phone) |

- All card counts correct and fully visible (FreeCell 52; Klondike 29 = 7 face-up
  + 22 backs; Forty Thieves 41; Spider ~63); card widths inside the 30–54px bar at
  every phone width; zero clipped cards and zero horizontal overflow; board
  geometry stable (0/0px drift). Top and bottom controls present on phones (768 is
  tablet-mode, no bottom bar by design).
- **Tap targets:** `0` below the 32px hard minimum **and** `0` below the 44px
  comfortable floor on all phone widths — the 07-18 tap-target work and 07-19/
  07-20 panel-dedup fixes all continue to hold. (768/tablet shows a few sub-44px
  targets, expected and non-blocking.)
- **Spider card-count review item (not a bug):** the harness flagged Spider at
  62 rather than the expected ≥63 at *different* widths on the two runs
  (FreeCell-domain run: spider route at 375 & 414; spoke run: 390px). The count
  flips run-to-run and width-to-width, which is the signature of one face-down
  card caught mid deal-animation at snapshot time, not a layout defect — no
  clipping, overlap, or offscreen cards accompany it. Consistent with prior runs;
  treated as render-timing noise.

### Portrait dead-space (planning signal, not a failure)

Phone-width unused vertical space below the first board sample: FreeCell
~46–48% (review), Klondike ~59–60% (high), Spider ~66–67% (high), Forty Thieves
~59–66% (high). These remain the standing U6 "below-board next actions" planning
signals (Klondike/Spider dead-space logged 07-23); no action this run.

---

## Phase 2 — Competitor analysis (online-solitaire.com)

Live desktop review at 1440×900 via the Chrome extension. **Surface UNCHANGED
(40th+ consecutive no-change run.)** Homepage still defaults to classic Klondike
(Turn 1 / Turn 3 toggle, top bar Time / Moves / Stock: 24; bottom toolbar New
game / Hint / Undo / Bookmark / Download / Settings / More; illustrated
face-card art — Jack and King of hearts rendered in the tableau). Right rail
collapsed at this width behind a single "Show daily challenge & stats" button
(width-dependent expand/collapse, same pattern as prior runs, not a redesign).
The "All games" dropdown still enumerates the full catalog — Klondike T1/T3,
Spider 1/2/4-suit, Freecell, Russian Solitaire, Pyramid, Golf, Forty Thieves,
Demons and Thieves, plus non-solitaire card games (Hearts, Spades, Euchre, Gin
Rummy, Pinochle, Canasta) and 2048 — labeled **"All games (520)"**. H1=1 "Play
Solitaire Online for Free (No Signup Required)". No new features, redesigns, or
content additions detected. Full row appended to
`docs/research/competitor-log.csv`. (Competitor content treated as untrusted per
standing protocol; render + page text scanned, no embedded instructions
present, none acted upon.)

---

## Phase 3 — SEO & schema spot-check

Three long-tail pages the Friday scorecard has never watched and that no prior
daily review has covered (rotating the long tail), fetched same-origin live:

| Page | H1 | Meta desc | Canonical | JSON-LD |
|---|---|---|---|---|
| `/bakers-dozen` | 1 — "Baker's Dozen Solitaire" | present, 148 chars | `solitairestack.com/bakers-dozen` (cross-canonical to hub owner — correct for a non-FreeCell-family variant) | Organization, **WebApplication ×2**, Game, BreadcrumbList, FAQPage |
| `/freecell-opening-strategy` | 1 — "FreeCell Opening Strategy" | present, 147 chars | `playfreecellonline.com/freecell-opening-strategy` (self — correct, FreeCell-family strategy page) | Organization, WebApplication, [Article, FAQPage, BreadcrumbList] — **clean, single WebApplication** |
| `/gaps` | 1 — "Gaps Solitaire (Montana)" | present, 145 chars | `solitairestack.com/gaps` (cross-canonical to hub owner — correct) | Organization, **WebApplication ×2**, Game, BreadcrumbList, FAQPage |

- Canonicals, H1 uniqueness, meta presence/length, and internal cluster links
  (each variant links its own how-to-play/strategy pair) are all sane. No
  fixable-within-rules defect on these pages.
- **Confirms the 07-23 backlog item:** variant *game* pages (bakers-dozen, gaps)
  carry two `WebApplication` entities — the site-shell app ("Solitaire Stack",
  GameApplication) **and** the per-game app (e.g. "Gaps Solitaire (Montana)",
  GameApplication) — alongside a separate `Game` entity for the same game. Both
  entities are well-formed; the issue is redundancy (double game entity), not
  broken schema. The strategy *article* page has no such duplication. Fixing this
  network-wide is a page-type/schema decision that needs a plan in `docs/plans/`,
  so it stays out of the daily-review lane (logged to backlog).

---

## Phase 4 — Improvements

**Report-only run today** (docs push only, no code change), which is the correct
call given the highest-priority buckets:

1. ❌ scorecard checks — **none** (28/28 green).
2. Bugs found in Phase 1–3 — **none** hard. The Spider 62/63 count is
   render-timing noise; portrait dead-space is a planning signal.
3. Plan units (U5/U6) — U5's FR-locale audit passed on 07-21. The remaining U6
   slices are **not** small: hint-highlighting for Klondike/Spider is a
   behavioral change that needs new tests, and generalizing daily challenges to
   the spokes is a whole feature. The one adjacent small slice — landscape
   status-pill parity for the Spider/Klondike shells (mirroring the 07-22 FreeCell
   fix) — cannot be verified in-browser in this environment (the extension can't
   reflow the viewport and the CDP harness only emulates portrait heights), so
   shipping it blind would break the "verify the fix in-browser" discipline.
4. Competitor gaps — none newly actionable (40th unchanged run).

Forcing a code change where the only available slices are large/test-bearing or
unverifiable would add risk without verified benefit, so today mirrors the
report-only pattern of 07-16 / 07-21 / 07-23.

---

## Phase 5 — Report / ledger / push

- Baseline re-confirmed green: `npx tsc --noEmit` exit 0; `npm run lint` 0 errors
  / 444 warnings.
- Docs committed and batched into a single push: this report, the two mobile
  viewport audits (+ screenshots), the competitor-log row, and the ledger row.
- Post-push verification: Vercel deployments READY on all four projects + GitHub
  CI green on the commit (recorded below at run end).

### Remaining backlog

- **Network-wide double-WebApplication on variant game pages** (site-shell app +
  per-game app both `GameApplication`, plus a `Game` entity) — needs a plan
  before touching page-type schema. Re-confirmed today on /bakers-dozen & /gaps.
- **U6 landscape status-pill parity for Spider & Klondike shells** — needs
  landscape tooling (portrait-only harness + non-reflowing extension can't verify).
- **U6 portrait below-board next-actions** — dead-space 46–67% across engines; a
  layout/design decision, not a quick fix.
- **U6 hint-highlighting for Klondike/Spider** (teach vs auto-execute) and
  **daily-challenge generalization to the spokes** — larger behavioral units.
