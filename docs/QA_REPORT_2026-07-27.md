# Daily QA Report — 2026-07-27 (Monday)

Run: `cowork-daily-review`. Odd day → Klondike is the rotating spoke of the day.
Network status at start: scorecard `2026-07-24` **28/28 passing** (no ❌ to fix). Last landed commit `12f25d2` (07-26 FreeCell toolbar a11y + audits). Working tree clean apart from untracked `.claude/settings.local.json`.
Coordination: LEDGER read (last 10 rows) — the Monday content routine ships 2 spoke blog posts today; stayed out of content lane. No overlap.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension, live production)
- **playfreecellonline.com** — 8 tableau columns fully visible, 4 free cells + 4 suit foundations correctly spaced, right sidebar (Daily Leaderboard + Daily Challenge calendar) renders alongside the board, toolbar intact, card labels readable, no overlap/clipping/horizontal scroll (`scrollWidth == clientWidth == 1440`), H1×1. ✅
- **solitairestack.com** (hub) — homepage renders the default FreeCell board + leaderboard + Daily Challenge cleanly (H1×1 "Play Solitaire Online for Free"); `/games` index intact (H1 "Free Solitaire Games Online", 28+ catalog, correct spoke-domain outbound links); hub `/pyramid` game page renders 29 cards, single H1, no horizontal scroll; branded 404 (`/games/spider-solitaire` mis-slug) renders correctly with "Deal a New Game" CTA. ✅
- **playklondikeonline.com** (rotating spoke) — see mobile harness below; board renders 29 cards, 7 cascades, toolbar intact across widths. ✅

### Mobile (true-viewport CDP harness — `scripts/mobile-viewport-audit.mjs`, Node 22.19, live)
Note: the Chrome extension `resize_window` resizes the OS window but the page viewport stays pinned at 1440 (`innerWidth` unchanged after resize to 375), so true mobile verification is done via the repo's dependency-free CDP harness, as in prior runs.

Audited **playfreecellonline.com** (freecell/klondike/spider/forty-thieves) and **playklondikeonline.com** (klondike) at 375 / 390 / 414 / 768:
- Full card counts (freecell 52, klondike 29, spider 63, forty-thieves 41), **0 clipped**, **0 horizontal overflow**, **0 blocked controls**, **0 sub-44px tap targets** on phone widths (375/390/414), card widths within the 30–54px band (spider 30.9px @375 → 61.3px @768; klondike 39.3px @375 → 74.4px @768), board stability 0px, top and bottom bars present on phones.
- One benign transient: spider @414 on the network run sampled 62 cards / 52 backs vs expected 63/53 — the same deal-animation timing race seen 07-25/07-26. **Verified non-reproducible:** three fresh spider@414 re-runs all returned 63 cards / 53 backs / 10 face / 10 cascades, 0 clipped, 34.8px card width. Not a real defect.
- Portrait dead-space 42–53% flagged as "review" candidates — a planning signal for below-board content, **not** a hard failure.
- Screenshots captured for both bases (`docs/analytics/mobile-viewport-audits/2026-07-27-live-screenshots/` and `…-klondike-spoke-live-screenshots/`).

### Tablet (768×1024)
Board fills correctly without the sidebar across all four engines (harness 768 rows) — no clipping. ✅

**No hard visual/layout failures on any viewport.**

## Phase 2 — Competitor (online-solitaire.com)
UNCHANGED — **43rd consecutive no-change run**. Homepage still defaults to classic Klondike (Turn 1 / Turn 3 toggle, "All games" dropdown, top bar Time/Moves/Stock: 24, illustrated face-card art); bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More; "All games (520)" catalog enumerates Klondike T1/T3, Spider 1/2/4-suit, Freecell, Russian Solitaire, Pyramid, Golf, Forty Thieves, Demons and Thieves + non-solitaire card games. H1×1 "Play Solitaire Online for Free (No Signup Required)"; title and meta description unchanged. No new features, redesigns, or content. Standing gaps and leads unchanged from prior logs. Competitor content treated as untrusted; render + page text scanned, no embedded instructions present, none acted upon. Logged to `docs/research/competitor-log.csv`.

## Phase 3 — SEO & schema spot-check
Three fresh long-tail pages the scorecard/recent runs have never covered:
- `playklondikeonline.com/klondike-probability` — H1×1 "Klondike Probability: What the Math Says"; JSON-LD Article + WebPage + BreadcrumbList + Organization/WebSite/SearchAction; self-canonical; unique meta; in-cluster links (endgame/opening strategy). ✅
- `playfreecellonline.com/famous-freecell-game-numbers` — H1×1 "Famous FreeCell Game Numbers"; JSON-LD Article + WebPage + FAQPage + BreadcrumbList; self-canonical; unique meta; cluster link. ✅
- `playklondikeonline.com/klondike-cheat-sheet` — H1×1 "Klondike Solitaire Cheat Sheet"; self-canonical; unique meta; cluster links — **but JSON-LD carried only BreadcrumbList**, no Article/WebPage. Its sibling cheat-sheet pages (`freecell-cheat-sheet`, `spider-cheat-sheet`) both emit Article + WebPage. Schema-parity gap → fixed in Phase 4.

## Phase 4 — Improvement shipped (1)
**SEO/schema: add Article + WebPage JSON-LD to `klondike-cheat-sheet`.** The page is a bylined editorial reference (AuthorByline "The Rules Desk", published 2026-04-12, OpenGraph `type: article`) but emitted only a `BreadcrumbList`, unlike its two sibling cheat-sheet pages which both carry an `Article` graph with `mainEntityOfPage` WebPage. Added a matching `Article` node (headline/description mirrored from the page metadata, author "The Rules Desk" with `/authors/the-rules-desk` URL, publisher = site, datePublished/dateModified 2026-04-12, `mainEntityOfPage` → self URL) alongside the existing breadcrumb, using the already-imported `absoluteUrl`/`siteConfig`. Layout-neutral, no visible change. `npx tsc --noEmit` clean; `eslint` 0 errors (445 pre-existing warnings).

## Remaining backlog
- Audit whether any other non-cheat-sheet reference pages network-wide are missing Article/WebPage schema (spot-check surfaced one; a fuller sweep is a future slice).
- U6: Klondike/Spider hints auto-execute instead of teaching (highlight like FreeCell) — behavior change, needs tests.
- U6: portrait dead-space 42–53% on phone widths — below-board next-action/contextual content opportunity (planning).
- Network-wide double-`WebApplication` on variant game pages — needs a plan (out of the daily lane).
- U5: FR-locale editorial depth + GSC resubmission/recrawl monitoring.
