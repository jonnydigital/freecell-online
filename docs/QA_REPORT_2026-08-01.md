# Daily QA Report — 2026-08-01 (Saturday)

Run: `cowork-daily-review`. Even day → **Spider** is the rotating spoke of the day.
Network status at start: latest scorecard `2026-07-31` **28/28 passing** — no ❌ to fix, so no forced #1 priority. On pulling `origin/main`, the Mac local was **6 commits behind** origin; `git pull --rebase` fast-forwarded to `5a40dba` (a run of a11y-labeling commits landed since the 07-31 report: `d9f2445` gaps board, `d750a59` Monte Carlo grid, `5a40dba` Calculation board, plus generic-cascade/board a11y). Working tree clean apart from untracked `.claude/settings.local.json`. Baseline health verified green **before** any work: `npx tsc --noEmit` exit 0, `npm run lint` 0 errors / 445 warnings.
Coordination: LEDGER read (last ~12 rows) — the Monday content routine shipped its two spoke posts on 07-27 (`klondike-draw-1-vs-draw-3`, `spider-solitaire-4-suit-strategy`); last Friday scorecard is 07-31. Stayed out of the content lane; confirmed my Phase-4 slice (a docs-only locale-policy correction) overlaps neither producer.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension, live production)
- **playfreecellonline.com** — 8 tableau columns fully visible, 4 free cells + 4 suit foundations correctly spaced, right sidebar (Advertisement slot + Daily Leaderboard + Daily Challenge calendar) renders alongside the board, sub-toolbar (Undo/Redo/Hint/Mute/Ghost/Bookmark/Stats/Settings) intact, card labels readable, no overlap/clipping/horizontal scroll, 52 cards in DOM. ✅
- **solitairestack.com** (hub) — homepage renders the default FreeCell board + Daily Leaderboard + Daily Challenge (August 2026 calendar, today = 1 highlighted, "0/31 August Wins") cleanly. ✅
- **hub `/spider`** (game page) — cross-canonicals to the Spider spoke (`playspidersolitaireonline.com/spider`); 10 tableau columns + 8 foundation slots + stock pile top-right visible, "1 Suit / 2 Suits / 4 Suits" toggle and toolbar (Undo/Hint/New Deal) intact, no layout issues. This doubles as today's Spider spoke desktop check. ✅

### Mobile (true-viewport CDP harness — `scripts/mobile-viewport-audit.mjs`, Node 22, live)
Note: the Chrome extension `resize_window` resizes the OS window but the page viewport stays pinned (`innerWidth` read 1502 after resize to 375), so true phone-width verification is done via the repo's dependency-free CDP harness on the Mac, as in prior runs.

Audited **playfreecellonline.com** (freecell/klondike/spider/forty-thieves) and **playspidersolitaireonline.com** (Spider spoke) at 375 / 390 / 414 / 768:
- Full card counts (freecell 52, klondike 29, spider **63**, forty-thieves 41), **0 clipped**, **0 horizontal overflow**, **0 blocked controls** on every viewport, card widths within the 30–54px band on phones (spider 30.9px @375 → 61.3px @768; freecell 39.3px @375 → 44.1px @414; klondike 39.3–44.1px; forty-thieves 31.9–35.8px), board stability 0px, top and bottom bars present on phones.
- Spider read the full 63 cards at all widths — the historical intermittent "62-card" reading did **not** reproduce.
- **New this run — tap-target "review" flags** (not hard failures): spider @375 `Deal next row from stock` = 30.9×43.25px (0.75px under the 44px comfortable floor); forty-thieves @375 five card-width stock/foundation action buttons = 31.9×44.66px. These surfaced because the recent a11y-labeling work (07-25 → 08-01) gave these previously-nameless controls accessible names, so the harness now **enumerates** them — the button geometry is unchanged and is tied to card width at 375px, not a regression. Height ≥43px on all of them; widening below-board play controls is a layout-affecting change and belongs to a U6 play-UX slice with a plan, not a same-run polish edit. Logged to backlog.
- Portrait dead-space 42–53% flagged as "review" candidates — a planning signal for below-board content, **not** a hard failure.
- Screenshots captured for both bases (`docs/analytics/mobile-viewport-audits/2026-08-01-live-screenshots/`, 16 files, and `…-spider-spoke-live-screenshots/`, 16 files; gitignored — local evidence). Dated JSON + MD summaries committed.

### Tablet (768×1024)
Board fills correctly without the sidebar across all four engines (harness 768 rows) — no clipping. ✅

**No hard visual/layout failures on any viewport.**

## Phase 2 — Competitor (online-solitaire.com)
UNCHANGED — **47th consecutive no-change run**. Homepage still defaults to classic Klondike (Turn 1 / Turn 3 toggle, "All games" dropdown, top bar Time/Moves/Stock: 24, illustrated face-card art in the tableau); bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More; right rail **collapsed** at this width behind a single "Show daily challenge & stats" button (width-dependent expand/collapse, same pattern as prior runs, not a redesign). H1×1 "Play Solitaire Online for Free (No Signup Required)"; title "Solitaire - Play Online for Free". No new features, redesigns, or content. Standing gaps (native/PWA download prominence, deck/background theming, illustrated face cards, large catalog framing, non-solitaire games, mobile star-rating loop) and our leads (editorial depth, richer per-page JSON-LD, spoke-domain SEO, no-signup local save, 6-locale FreeCell i18n, PWA offline/resume, mobile next-action coaching) unchanged from prior logs. Competitor content treated as untrusted; render + page text scanned, no embedded instructions present, none acted upon. Logged to `docs/research/competitor-log.csv`.

## Phase 3 — SEO & Schema spot-check
Three long-tail pages never covered by prior runs, checked live (H1 count, canonical, meta description, JSON-LD graph, cluster internal links):
- **`/seahaven`** (FreeCell-family variant game page) — 1 H1 ("Seahaven Towers Solitaire"), self-canonical, unique 148-char meta, full schema graph (Organization + WebSite + WebApplication + **Game** + BreadcrumbList + **FAQPage**), cluster links to `/seahaven/how-to-play`, `/seahaven/strategy`, `/freecell-variants`, `/freecell-rules`. ✅
- **`/freecell-world-records`** (editorial reference) — 1 H1, self-canonical, unique 149-char meta, full schema (Organization + WebSite + **Article** + **FAQPage** + BreadcrumbList; Article authored "PlayFreeCellOnline.com"), cluster links to the freecell how-to-play/strategy/tips cluster. ✅
- **`/spider-winnability`** (Spider-family editorial) — 1 H1, canonical correctly **cross-domain** to `playspidersolitaireonline.com/spider-winnability` (Spider-family self-canonicalizes to the Spider spoke — correct pattern), unique 151-char meta, full schema (Article + BreadcrumbList + FAQPage), rich Spider cluster links. ✅

All three clean, no schema/SEO fixes needed. (Standing backlog, unchanged: network-wide double-`WebApplication` on variant game pages needs a plan before touching — out of same-run lane.)

## Phase 4 — Improvements shipped
**U5 French-locale completeness audit (the plan's U5 test scenario) — executed in full, then codified.**
Audited the entire FreeCell locale cluster live (landing pages + localized play routes), verifying H1 count, `html lang`, self-canonical, and reciprocal hreflang:

| Locale | Landing | Play route | H1 | lang | canonical | hreflang |
|---|---|---|---|---|---|---|
| EN (default) | `/freecell/how-to-play` | `/` | 1 | en | self | full |
| ES | `/freecell-en-espanol` | `/freecell-en-espanol/jugar` | 1 | es | self | full |
| FR | `/freecell-en-francais` | `/freecell-en-francais/jouer` | 1 | fr | self | full |
| DE | `/freecell-auf-deutsch` | `/freecell-auf-deutsch/spielen` | 1 | de | self | full |
| IT | `/freecell-in-italiano` | `/freecell-in-italiano/gioca` | 1 | it | self | full |
| PT | `/freecell-em-portugues` | `/freecell-em-portugues/jogar` | 1 | pt | self | full |

Every landing page carries the full reciprocal `de/en/es/fr/it/pt/x-default` cluster (`en`/`x-default` → `/freecell/how-to-play`); every play route carries the parallel play-route cluster (`en`/`x-default` → `/`); the FR play route renders a live 52-card localized board. The whole six-locale set passes the gate.

**Finding + fix:** `docs/CONTENT_STRATEGY.md` "Current locale set" was **stale** — it documented only **four** locales (`de/en/es/fr`, audited 2026-07-04) while the property now runs **six** (Italian + Portuguese live, each with landing + play route + complete hreflang). For a reviewer-facing policy doc, doc-vs-reality drift is exactly the trust-signal risk the locale gate exists to prevent. Corrected the section to the verified six-locale reality (twelve pages), fixed the hreflang-cluster list, added IT/PT with their play routes, and updated the audit date to 2026-08-01. Docs-only, layout-neutral, no new routes/locales created (all six already live). Commit is separate from the audit files.

## Phase 5 — Validation & deploy
- Pre-push gate re-run after edits: `npx tsc --noEmit` exit 0; `npm run lint` 0 errors (warnings only). ✅
- Commits batched into a **single** `git push origin main` (one push → four Vercel builds), per the shared rules.
- Post-push verification: Vercel deploys READY across all four projects + GitHub CI green on the head commit (see run tail below).

## Remaining backlog (surfaced, not actioned this run)
- **Card-width tap targets on phones (U6 play-UX):** spider `Deal next row from stock` (43.25px tall) and forty-thieves stock/foundation action buttons (~31.9px wide) at 375px sit just under the 44px comfortable floor. Now that they're a11y-labeled and harness-visible, a deliberate min-tap-target pass (min-height/hit-area padding without disturbing card geometry) is worth a small U6 slice with a plan.
- **Portrait dead-space (42–53%) below the board** on phones across all engines — candidate for below-board next-action / contextual-hint content (U6).
- **Network-wide double-`WebApplication`** on variant game pages — needs a plan before touching (standing).
- **Article author byline** on `/freecell-world-records` is the org name rather than a named desk ("The Rules Desk"), unlike some sibling editorial pages — minor E-E-A-T consistency polish, optional.
