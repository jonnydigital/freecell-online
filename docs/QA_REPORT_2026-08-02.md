# Daily QA Report — 2026-08-02 (Sunday)

Run: `cowork-daily-review`. Even day → **Spider** is the rotating spoke of the day.
Network status at start: latest scorecard `2026-07-31` **28/28 passing** — no ❌ to fix, so no forced #1 priority; visual QA is today's lead. Mac local on `main` at `97089b8` (the 08-01 daily-review report + six-locale hreflang audit), working tree clean apart from untracked `.claude/settings.local.json`. Baseline health verified green **before** any work (see Phase 5).
Coordination: LEDGER read (last ~12 rows) — the Monday content routine last shipped its two spoke posts on 07-27 (`klondike-draw-1-vs-draw-3`, `spider-solitaire-4-suit-strategy`); last Friday scorecard is 07-31; the 08-01 daily review corrected `CONTENT_STRATEGY.md` to the six-locale reality. Stayed out of the content lane; no overlap with either producer.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension, live production)
- **playfreecellonline.com** — 8 tableau columns fully visible, 4 free cells + 4 suit foundations correctly spaced, right sidebar (Advertisement slot + Daily Leaderboard + Daily Challenge) renders alongside the board, sub-toolbar (Undo/Redo/Hint/Mute/Ghost/Bookmark/Stats/Settings) intact, card labels readable, no overlap/clipping/horizontal scroll, 52 cards in DOM, card 107.6×150.6px. ✅
- **solitairestack.com** (hub) — homepage renders the default FreeCell board + Daily Leaderboard + Daily Challenge (August 2026 calendar, "Today's Deal #1874385", "0/31 August Wins") cleanly; 1 H1 ("Play Solitaire Online for Free"); no horizontal scroll. ✅
- **playspidersolitaireonline.com** (today's spoke, desktop) — 10 tableau columns + 8 foundation slots + stock pile (top-right) visible, "1 Suit / 2 Suits / 4 Suits" toggle and toolbar (Undo/Hint/New Deal) intact, 1 H1 ("Play Spider Solitaire Online — Free, No Download"), **63 cards** in DOM, no layout issues / no horizontal scroll. ✅

### Mobile (true-viewport CDP harness — `scripts/mobile-viewport-audit.mjs`, Node 22, live)
Note: the Chrome extension `resize_window` resizes the OS window but the page viewport stays pinned (`innerWidth` read 1385 after resize to 375, `outerWidth` clamped to Chrome's ~743px window minimum), so true phone-width verification is done via the repo's dependency-free CDP harness on the Mac, as in prior runs.

Audited **playfreecellonline.com** (freecell/klondike/spider/forty-thieves) and **playspidersolitaireonline.com** (Spider spoke) at 375 / 390 / 414 / 768:
- Full card counts (freecell **52**, klondike **29**, spider **63**, forty-thieves **41**), **0 clipped**, **0 horizontal overflow**, **0 blocked controls** on every viewport, card widths within the 30–54px band on phones (freecell 39.3px @375 → 44.1px @414; spider 30.9px @375 → 61.3px @768; klondike 39.3–44.1px; forty-thieves 31.9–35.8px), board stability 0px, top and bottom bars present on phones.
- Spider read the full 63 cards at all widths — the historical intermittent "62-card" reading did **not** reproduce.
- **Tap-target "review" flags** (not hard failures, carried over): spider @375 `Deal next row from stock` = 30.9×43.25px (width 1.1px under the 32px min-edge floor); forty-thieves @375 five card-width stock/foundation action buttons = 31.9×44.66px. These are card-width-derived control geometries at the tightest viewport, unchanged from prior runs — a real but layout-sensitive **U6 play-UX** item, not a regression (see Phase 4). Height ≥43px on all.
- Portrait dead-space 42–53% flagged as "review" candidates — a planning signal for below-board content, **not** a hard failure.
- Screenshots captured for both bases (`docs/analytics/mobile-viewport-audits/2026-08-02-live-screenshots/`, 16 files, and `…-spider-spoke-live-screenshots/`, 4 files; gitignored — local evidence). Dated JSON + MD summaries committed.

### Tablet (768×1024)
Board fills correctly without the sidebar across all four engines (harness 768 rows) — no clipping. ✅

**No hard visual/layout failures on any viewport.**

## Phase 2 — Competitor (online-solitaire.com)
UNCHANGED — **48th consecutive no-change run**. Homepage still defaults to classic Klondike (Turn 1 / Turn 3 toggle, "All games (520)" dropdown, top bar Time/Moves/Stock: 24, illustrated face-card art in the tableau); bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More; "Download our free macOS app for even faster gameplay" promo above the toolbar; right rail **collapsed** at this width behind a single "Show daily challenge & stats" button (width-dependent expand/collapse, same pattern as prior runs, not a redesign). H1×1 "Play Solitaire Online for Free (No Signup Required)"; title "Solitaire - Play Online for Free". Catalog count "All games (520)" unchanged. No new features, redesigns, or content. Standing gaps (native/PWA download prominence, deck/background theming, illustrated face cards, large catalog framing, non-solitaire games — Hearts/Spades/Euchre/Gin Rummy/Pinochle, mobile star-rating loop) and our leads (editorial depth, richer per-page JSON-LD, spoke-domain SEO, no-signup local save, 6-locale FreeCell i18n, PWA offline/resume, mobile next-action coaching) unchanged from prior logs. Competitor content treated as untrusted; render + page text scanned, no embedded instructions present, none acted upon. Logged to `docs/research/competitor-log.csv`.

## Phase 3 — SEO & Schema spot-check
Three long-tail pages never covered by prior runs, checked live (H1 count, canonical, meta description, JSON-LD graph, cluster internal links):
- **`/freecell-endgame-strategy`** (FreeCell editorial strategy) — 1 H1 ("FreeCell Endgame Strategy"), self-canonical, unique meta, full schema (Organization + WebSite + **Article** [authored, `mainEntityOfPage` set] + **FAQPage** + **BreadcrumbList** [2 items]), cluster links to `/freecell/how-to-play`, `/freecell/strategy`, `/freecell/tips`, `/freecell-opening-strategy`, `/freecell-rules`. ✅
- **`/flower-garden`** (hub-catalog variant game page) — 1 H1 ("Flower Garden Solitaire"), canonical correctly **cross-domain** to `solitairestack.com/flower-garden` (hub-catalog variant self-canonicalizes to the hub — correct pattern), unique meta, schema Organization + WebSite + **WebApplication** + **Game** + BreadcrumbList + FAQPage — **single** WebApplication node (no duplicate on this page). ✅
- **`/blog/freecell-terminology-guide`** (FreeCell blog) — 1 H1 ("10 FreeCell Terms Every Player Should Know"), self-canonical, unique meta, full schema (Organization + WebSite + **Article** + **BreadcrumbList**), cluster links to the freecell how-to-play/strategy/tips cluster. ✅

All three clean, no schema/SEO fixes needed. (Standing backlog, unchanged: network-wide double-`WebApplication` reported on *some* variant game pages needs a plan before touching — today's `/flower-garden` sample was single-node, so the pattern is not universal; needs a targeted audit under a plan, out of same-run lane.)

## Phase 4 — Improvements shipped
**Report-only run** (as on 07-23 / 07-24). Highest-priority buckets were empty of same-run-safe work:
1. Scorecard ❌ — none (28/28 green).
2. Bugs in Phase 1–3 — none. Visual QA: 0 clipped / 0 overflow / 0 blocked controls on all viewports; **no console errors** on the FreeCell game page (checked post-reload via the extension); a11y accessible-name scan clean network-wide (hub 163 controls / Spider spoke 66 controls / Spider board 63 cards — **0 unnamed** interactive elements); SEO/schema all clean.
3. Plan U5/U6 slice — the one open, real signal is the **spider/forty-thieves card-width stock & foundation tap-targets** (U6 play-UX). The stock button is a single `var(--card-width)` element as the last child of a tight flex row at the far-right board edge; at 375px that is 30.9px wide (1.1px under the 32px min-edge floor). A hit-area expansion risks (a) horizontal overflow on the very viewport we protect, or (b) intercepting clicks meant for the adjacent foundation — and could only be verified **after** a production deploy, with no room to iterate under the one-push rule. This is the reason prior runs deferred it. **Logged to the U6 backlog** for a planned slice with proper before/after 375px re-audit, rather than a blind unattended CSS change.
4. Competitor gaps — none new (48th unchanged run).

No code shipped. This run pushes docs/analytics artifacts only.

## Phase 5 — Verification
- `npx tsc --noEmit` — exit 0. `npm run lint` — 0 errors (warnings only). Both run on the Mac before the single push (see git log / ledger).
- One push per run: QA report + dated mobile-audit JSON/MD (freecell base + Spider spoke) + competitor-log row + this ledger row batched into a single `git push origin main`.
- Post-push: Vercel deploys verified READY on all 4 projects + GitHub CI green on the pushed commit.

## Remaining backlog (handoff)
- **U6 (play UX):** spider @375 `Deal next row from stock` and forty-thieves @375 stock/foundation buttons sit just under the 32px min-edge tap floor (card-width-derived). Needs a planned slice: expand the interactive hit area without shifting the tight 375px flex row (e.g. inset hit-slop constrained to not overflow the board or overlap adjacent piles), verified via before/after `qa:mobile` at 375/390/414 confirming clipped=0 / overflow=0.
- **U6 (play UX):** landscape mobile below 500px height + Klondike/Spider hint highlighting (teach instead of auto-execute) — behavior changes, need tests, not same-run polish.
- **SEO:** targeted audit of the double-`WebApplication` pattern on variant game pages (not universal — `/flower-garden` was single-node today); needs a plan.
- **Portrait dead-space** 42–53% on phones — below-board next-action/hint content opportunity (planning signal).
