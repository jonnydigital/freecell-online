# Daily QA Report — 2026-08-05 (Wednesday)

Network status: **green.** 28/28 on the latest scorecard (2026-07-31), no
failing checks to inherit. Baseline gate confirmed before any change:
`npx tsc --noEmit` clean, `npm run lint` 0 errors / 444 warnings. Rotating
spoke of the day (odd date): **Klondike**.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension, live production)

- **playfreecellonline.com** — 8 cascade columns fully visible, 4 free cells +
  4 foundations evenly spaced, right sidebar (Daily Leaderboard + Daily
  Challenge + August calendar) renders alongside the board. 52 `.dom-card` in
  DOM, single H1, no horizontal scroll.
- **solitairestack.com (hub)** — homepage clean: single H1 "Play Solitaire
  Online for Free", embedded FreeCell hero board, editorial sections
  ("What Solitaire Stack Is"), 137 internal links, no horizontal scroll. Game
  page checked: hub `/spider` 308-redirects to the Spider spoke
  (`playspidersolitaireonline.com/spider`, canonical points to spoke) — 10
  columns, full 63 cards, 8 foundations, 1/2/4-suit toggle, single H1.
- **playklondikeonline.com (rotating spoke)** — stock + waste + 4 foundations
  spaced, 7 staggered tableau columns, Draw 1 / Draw 3 toggle, Undo/Hint/New
  Deal toolbar intact, 29 cards, single H1, no horizontal scroll. (Cookie
  banner present; left untouched — no consent action taken.)

### Mobile (true-viewport CDP harness — `scripts/mobile-viewport-audit.mjs`, Node 22, live)

Ran against live `playfreecellonline.com` at 375 / 390 / 414 / 768 across all
four engines (freecell, klondike, spider, forty-thieves), with screenshots.
Artifact: `docs/analytics/mobile-viewport-audits/2026-08-05-live.json` + `.md`
(+ `2026-08-05-live-screenshots/`).

- **0 clipped, 0 horizontal overflow, 0 blocked controls** at every viewport,
  every engine. Board stability 0/0px everywhere.
- Full card counts render: FreeCell **52/52** (card widths 39.25–74.41px),
  Klondike **29** (7 face), Spider **63** (10 face), Forty-thieves **41** (40
  face). All within the 30–54px phone card-width expectation (768 wider by
  design).
- Visually confirmed FreeCell-375, Klondike-375, Spider-375 screenshots: top
  bar (deal#/timer/moves), bottom bar (Menu/New/Undo/Redo/Hint), and the
  next-action coaching panel all render intact; no clipping.
- Known review flags (non-regressions, already on the U6 backlog): Spider@375
  and Forty-thieves@375 show ~11 card-width stock/foundation controls below the
  44px comfortable tap-target on the narrow edge (30.89 / 31.89px wide). These
  are the layout-sensitive card-width buttons deferred to a planned U6 slice,
  identical to prior runs. Portrait dead-space ≥45% on several phone widths is a
  planning signal, not a failure.

### Tablet (768×1024)

Covered by the harness pass: board fills correctly, 0 clipped / 0 overflow, no
bottom bar by design at that width.

## Phase 2 — Competitor (online-solitaire.com)

Live desktop review. Surface **UNCHANGED — 49th consecutive no-change run.**
Klondike-default hero (Turn 1 / Turn 3), "All games (520)" catalog, standard
bottom toolbar (New game/Hint/Undo/Bookmark/Download/Settings/More), collapsed
daily-challenge rail, single H1. Non-solitaire catalog (Hearts/Spades/Euchre/
Gin Rummy/Pinochle) reconfirmed. No new features, redesigns, or content.
Standing lead/gap analysis unchanged. Logged to `docs/research/competitor-log.csv`.
(Competitor content treated as untrusted; page scanned, no embedded
instructions acted upon.)

## Phase 3 — SEO & Schema spot-check

Three never-before-covered long-tail pages, all clean:

- **`/yukon`** (→ Klondike spoke, cross-canonical correct) — single H1, unique
  meta, JSON-LD Organization + WebSite + WebApplication + Game + BreadcrumbList
  + FAQPage; cluster links to `/yukon/how-to-play`, `/strategy`, `/tips`.
- **`/freecell-mistakes-to-avoid`** — single H1, unique meta, self-canonical,
  JSON-LD Article + FAQPage + BreadcrumbList (Article emitted as a JSON array;
  headline + mainEntityOfPage WebPage present). Cluster links intact.
- **`/klondike-endgame-strategy`** (→ Klondike spoke) — single H1, unique meta,
  cross-canonical correct, JSON-LD Organization + WebSite + BreadcrumbList +
  Article; cluster links to `/klondike/how-to-play`, `/strategy`, `/tips`,
  `/faq`, `/klondike-opening-strategy`, `/klondike-probability`.

## Phase 4 — Improvements shipped

**U5 locale-gate slice (docs-only, layout-neutral).** Ran the U5 French-locale
completeness audit and extended it to the full FreeCell locale set. Verified
live: all **12** locale pages (6 landings + 6 play routes) return 200, each with
exactly one H1 and the complete seven-entry reciprocal hreflang cluster
(`de/en/es/fr/it/pt/x-default`). Reciprocity confirmed bidirectionally — the
French landing + play route resolve the full cluster, and the German play route
points `fr` back at `/freecell-en-francais/jouer`; rendered per-locale `lang`
correct (fr/de). Recorded the re-verification in
`docs/CONTENT_STRATEGY.md` ("Current locale set" audit date + a dated
re-verification note) and logged one new observation to the U6 backlog: the raw
SSR `<html lang>` serves the default `en` and is corrected to the locale on
hydration (JS-rendering crawlers see the right value; emitting localized `lang`
in server HTML would be a small future hardening — root-layout change, out of a
same-run-safe slice).

No code/UX slice today: no scorecard failures, no visual/SEO bugs found, and the
only open play-UX signal (spider/forty-thieves card-width tap-targets) is a
layout-sensitive U6 item that needs a planned slice, not a same-run edit.

## Phase 5 — Verification

- Pre-push gate re-run after the docs change: `npx tsc --noEmit` clean;
  `npm run lint` 0 errors.
- Single push per run (one `git push origin main`) batching the docs commits.
- Post-push: Vercel deployments verified READY on all four projects; GitHub CI
  verified green on the pushed commit.

## Remaining backlog (handoff)

- **U6 — Spider / Forty-thieves card-width tap-targets** (stock + foundation
  buttons ~31–32px wide on 375px phones, below the 44px comfortable floor).
  Layout-sensitive; needs a planned slice, not a daily micro-edit.
- **U6 — SSR `<html lang>` localization** (new today): serve the localized
  `lang` attribute in server HTML for the locale pages rather than correcting it
  on hydration. Root-layout change.
- **Housekeeping** — stray untracked `docs/analytics/mobile-viewport-audits/
  2026-08-03-live.json/.md` from an incomplete prior run remain in the working
  tree (no matching ledger row); left in place, not committed.
- Standing competitor gaps (theming, illustrated face cards, PWA/native download
  prominence, non-solitaire catalog) — no change, informational only.
