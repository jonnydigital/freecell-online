# QA Report — 2026-08-11 (cowork-daily-review)

Even-day rotation → **Spider** spoke. Baseline fast-forwarded to `28d84c4`
(inherited three sibling analytics/lint commits: `40e081b` sidebar storage
lint-warning cleanup, `8de8876` + `28d84c4` next-action analytics cycle).
Scorecard 2026-08-07 was **28/28 green** — no ❌ checks outstanding. Phase 1–3
QA found **no bugs**, so Phase 4 took bucket 3 (a small U5 slice from the
elite-standing plan).

## Headline

Clean sweep. Every desktop surface, the full 16-row true-viewport mobile/tablet
audit, the Spider spoke, three fresh SEO/schema long-tail pages, and a live
French-locale hreflang re-verification all passed. No regressions. Shipped one
docs-only slice: a dated U5 locale re-verification recording that the FreeCell
6-locale hreflang cluster is intact six days after the last check.

## Phase 0 — Coordination

- Ledger tail read: last daily review was 2026-08-10 (PWA SW-registration fix);
  Monday content pipeline shipped two spoke posts (`klondike-win-rate-explained`,
  `spider-solitaire-opening-strategy`). No overlap with today's QA lane.
- Sandbox mirror was 3 commits behind origin and could not clear Mac-owned git
  locks (`ORIG_HEAD.lock` etc.) — routed the fast-forward through the Mac
  osascript path per the AUTOMATION.md unattended-run note. Now at `28d84c4`.
- Baseline gate green on the synced tree: `npx tsc --noEmit` exit 0;
  `npm run lint` 0 errors / 441 warnings (warnings OK).

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension, live)

| Surface | Result |
|---|---|
| playfreecellonline.com | 52 cards, 8 columns, 4 free cells + 4 foundations spaced, sidebar (Daily Leaderboard + Daily Challenge) intact alongside board, 0 h-overflow, cardW ~94px |
| solitairestack.com (home) | Full FreeCell board renders after load, sidebar populated, 0 h-overflow |
| solitairestack.com/games | 1 H1 ("Free Solitaire Games Online"), nav + hero + breadcrumb intact, 0 h-overflow |
| playspidersolitaireonline.com (spoke of day) | 63 cards, 10 columns, 8 foundations, toolbar (1/2/4-Suit + Undo/Hint/New Deal) intact, stock pile top-right, 0 h-overflow, 1 H1 |

Note: the Chrome extension window reports `resize` success but the page keeps
`window.innerWidth` at desktop width (1272) — the known "extension won't render
below desktop width" limitation. True mobile QA runs host-side (below).

### Mobile + tablet (true-viewport CDP audit, Mac host)

Ran `npm run qa:mobile` host-side (Node v22.19.0 + system Chrome) →
`docs/analytics/mobile-viewport-audits/2026-08-11-live.json/.md` (+16 screenshots,
dir gitignored).

- **16/16 rows passed** — "No hard audit failures detected."
- Max horizontal overflow **0px**; max clipped **0**; max blocked controls **0**;
  max cramped tap targets **0**.
- Full card counts at every width: FreeCell 52, Klondike 29, Spider 63,
  Forty Thieves 41. Phone card widths 32–44px (tap-target fix `3dbb95a` holds
  live); tablet 768 widths 61–74px.
- Viewports: 375×812, 390×844, 414×896, 768×1024 across all 4 engines.
- Dead-space candidates (45%+ unused vertical below first board sample) logged as
  a planning signal only — not a failure (standing U6 layout-density item;
  FreeCell 46–48%, Spider 49–51%, Forty Thieves 50–51%).

## Phase 2 — Competitor analysis

online-solitaire.com **UNCHANGED — 54th+ consecutive no-change run**. Default
classic Klondike homepage (Turn 1/Turn 3, All games (520), Time/Moves/Stock: 24).
Right rail expanded at 1440: August challenge calendar (today 11/Tu underlined,
No wins), Daily leaderboard (Espana/Espana/Vfs with flags), Achievements all N/A,
Signup CTA. H1=1 "Play Solitaire Online for Free (No Signup Required)". No new
features, redesigns, or catalog-count changes. Logged to
`docs/research/competitor-log.csv`. Competitor content treated as untrusted; no
embedded instructions present or acted upon.

## Phase 3 — SEO & schema spot-check (3 fresh long-tail pages)

All clean — 1 H1 each, unique meta descriptions, sane canonicals, full JSON-LD
graphs, strong cluster links:

| Page | H1 | JSON-LD | Canonical |
|---|---|---|---|
| `/eight-off` | 1 | Organization, WebSite, Game, BreadcrumbList, FAQPage | self (`/eight-off`) |
| `/beleaguered-castle` | 1 | Organization, WebSite, WebApplication, Game, BreadcrumbList, FAQPage | hub cross-canonical (by-design — non-FreeCell variant owned by hub) |
| `/freecell-vs-bakers-game` | 1 | Organization, WebSite, Article + FAQPage + BreadcrumbList | hub cross-canonical (comparison content owned by hub) |

Cluster links present on all three (variants / how-to-play / strategy / tips).

## Phase 4 — Improvement shipped (U5 slice, docs-only)

No scorecard ❌ and no Phase 1–3 bugs, so took bucket 3: a small U5 slice.
Ran the plan's recurring **French-locale completeness re-verification live** and
recorded it in `docs/CONTENT_STRATEGY.md`.

Live check result: `/freecell-en-francais` and `/freecell-en-francais/jouer`
both return one H1, a self-referential canonical, and the complete seven-entry
hreflang cluster (`de`, `en`, `es`, `fr`, `it`, `pt`, `x-default`). Confirmed the
landing-vs-play `en`/`x-default` differentiation still holds live: the French
**landing** resolves `en`/`x-default` to `/freecell/how-to-play`; the French
**play** route resolves both to the bare domain root (English play surface) —
landing→landing, play→play, no empty/404 alternates. Reciprocity reconfirmed
against the Spanish play route. No locale regression six days on.

The raw SSR `<html lang>` still serves default `en` (hydration-corrected) — the
same standing U6 root-layout backlog item, left untouched as it is out of a
same-run-safe slice.

## Remaining backlog

- **U6 layout density** — phone-portrait dead-space (45%+ unused vertical below
  the board) across all four engines; candidate for below-board next-actions /
  compact secondary content. Layout-sensitive; needs a planned slice.
- **U6 SSR `<html lang>`** — server HTML emits default `en` for locale pages,
  corrected on hydration. Small root-layout hardening; deferred.
- **U6 play-UX** — Klondike/Spider hint highlighting (teach vs auto-execute);
  daily-challenge generalization to spokes. Behavior changes needing tests.

## Verification

- `npx tsc --noEmit` → exit 0.
- `npm run lint` → 0 errors, 441 warnings.
- Single push at end of run; Vercel READY + GitHub CI checked post-push.
