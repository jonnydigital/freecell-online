# QA Report — 2026-08-13 (Cowork daily review)

**Producer:** cowork-daily-review
**Scope:** visual QA (desktop + true-viewport mobile), competitor watch, SEO/schema spot-check, one docs-only improvement slice.
**Result:** All green. No regressions, no bugs, no scorecard failures. One plan-gate docs slice shipped.

## Phase 0 — Coordination

- Read `docs/AUTOMATION.md`, last ~12 ledger rows, newest scorecard (`2026-08-07.md`), and `git log`.
- **Newest scorecard = 28/28 checks passing, all green** — no `❌ FAILING` check to prioritize.
- Rebased onto `origin/main`: local was 6 commits behind. Inherited a sibling producer's mobile next-action QA hardening (`80ce3f6`, `e553d55`, `3b066d8`, `14e041e`, `445d7fd`) plus `c7e8342` "Guard root layout static rendering". They already shipped 2026-08-13 next-action audit artifacts, so this run did not duplicate that area.
- Monday content pipeline shipped two spoke posts (`klondike-win-rate-explained`, `spider-solitaire-opening-strategy`) on 2026-08-10 — spot-checked here (Phase 3), not overlapped.
- Spoke rotation: 2026-08-13 is odd → **Klondike** spoke QA this run.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension, live production)

- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4 foundations correctly spaced, sidebar (Daily Leaderboard + Daily Challenge + ad slot) renders alongside the board, no horizontal scroll (`scrollWidth === innerWidth`), exactly one H1 ("Play FreeCell Online — Free, No Download"). Toolbar intact.
- **solitairestack.com** — homepage FreeCell shell loads clean after the "Loading FreeCell…" splash; identical board + sidebar. `/games` catalog page: nav, hero, breadcrumb, and content all render correctly.
- **playklondikeonline.com** (rotation) — 29 cards, 7 columns, stock/waste + 4 foundations, toolbar intact, **0/22 unnamed buttons**, no horizontal scroll, one H1. Cookie consent banner present (Decline/Accept) — standard, not touched.

### Mobile + tablet (true-viewport CDP audit via Mac host, live production)

Ran `npm run qa:mobile --screenshots --base=https://playfreecellonline.com` host-side (the Chrome extension cannot render a true sub-desktop viewport — resizing the window to 375px left the page viewport at 1329px/101px cards, the documented limitation). Artifact: `docs/analytics/mobile-viewport-audits/2026-08-13-live.json` + `.md` (16 rows, screenshots captured).

- **16-row full pass, all 4 engines × 375/390/414/768.** No hard audit failures.
- Card counts full and correct every viewport: **FreeCell 52 / Klondike 29 / Spider 63 / Forty Thieves 41**.
- **0 clipped cards, 0 horizontal overflow px, 0 blocked interactive controls, 0 cramped tap targets, 0 console errors, 0 runtime exceptions.**
- Phone card widths 32–44px (min 32.09 Spider@375, up to 44.13 FreeCell@414) — confirms the `3dbb95a` tap-target fix holds live.
- Standing (non-failing) signals: portrait "unused vertical space" 42–61% flagged `review`/`ok` (tracked U6/dead-space planning item, not a regression); Forty Thieves shows no mobile bottom-control bar at phone widths — by design (it is not a mobile next-action engine; the next-action health section only asserts FreeCell/Klondike/Spider, all matched with `missing none; disabled none`).

## Phase 2 — Competitor analysis

- **online-solitaire.com — UNCHANGED (56th consecutive no-change run).** Desktop 1440. Homepage defaults to classic Klondike (Turn 1 / Turn 3, All games (520) dropdown, top bar Time/Moves/Stock: 24, illustrated court-card art). Right rail collapsed at this width behind "Show daily challenge & stats"; bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More.
- H1 = 1 ("Play Solitaire Online for Free (No Signup Required)"), title/meta unchanged, catalog count 520 unchanged. No new features, redesigns, or content.
- **Security:** competitor content treated as untrusted; rendered DOM + body text scanned, no embedded instructions present, none acted upon.
- Logged to `docs/research/competitor-log.csv`.

## Phase 3 — SEO & schema spot-check

Three long-tail pages the Friday scorecard does not cover (rotating the tail; all previously unchecked):

- **playklondikeonline.com/blog/klondike-win-rate-explained** (Monday's new post) — 1 H1, JSON-LD Organization + WebSite + **Article + BreadcrumbList**, self-canonical, meta 155 chars, strong cluster links (`/klondike/how-to-play`, `/strategy`, `/tips`, blog siblings, hub `/tips`). Clean.
- **playspidersolitaireonline.com/blog/spider-solitaire-opening-strategy** (Monday's new post) — 1 H1, Organization + WebSite + **Article + BreadcrumbList**, self-canonical, meta 148 chars, strong cluster links. Clean.
- **solitairestack.com/scorpion** — correctly cross-redirects hub → **playspidersolitaireonline.com/scorpion** (Spider-family ownership) with self-canonical on the owner spoke. 1 H1, full graph Organization + WebSite + **WebApplication + Game + BreadcrumbList + FAQPage**, meta 147 chars, cluster links (`/scorpion/how-to-play`, `/strategy`, `/tips` + spider siblings + blog). Clean.

No SEO defects found.

## Phase 4 — Improvements

Buckets 1 (scorecard fails), 2 (Phase 1–3 bugs), and 4 (new competitor gaps) are all empty this run. Bucket 3 (plan units): took one small **U6 root-layout / U5 locale-gate** docs slice.

- **`docs/CONTENT_STRATEGY.md`** — Verified against today's `c7e8342` static-rendering guard whether it closed the standing SSR `<html lang>` observation. **It does not:** a same-origin no-store fetch of `/freecell-en-francais` still returns `<html lang="en">` in server HTML, hydration-corrected to `fr`. Source-confirmed the cause (single shared App-Router root layout hard-coding `lang="en"`; locale only in pathname, unreadable by a static root layout). Elevated the item from "small future hardening" to **plan-gated**: a real fix requires either making the root layout dynamic (regressing `c7e8342`) or a locale route group (new route family) — both need a `docs/plans/` reference per the automation rules. Docs-only, layout-neutral, no new routes/locales/types.

## Phase 5 — Validation

- `npx tsc --noEmit` — pass.
- `npm run lint` — pass (0 errors).
- Single push at end of run (one push → 4 Vercel builds). Verified Vercel deploys READY on all 4 projects and GitHub CI green on the commit.

## Remaining backlog / signals

- **U6 (plan-gated):** SSR `<html lang>` per-locale hardening — now blocked on a plan; do not attempt a dynamic-render or route-group fix without one (would conflict with `c7e8342`).
- **U6 (planning):** portrait unused-vertical-space (42–61% phone widths) remains a measured opportunity, not a bug.
- **Forty Thieves mobile:** no bottom-control bar at phone widths — confirm intended (secondary engine, no next-action UX) or add to backlog if parity is desired.
- Next lever per plan: content cadence + off-site placements (U7).
