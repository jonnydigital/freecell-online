# QA Report — 2026-08-14 (Cowork daily review)

**Producer:** cowork-daily-review
**Scope:** visual QA (desktop + true-viewport mobile), competitor watch, SEO/schema spot-check, one small code-polish improvement slice.
**Result:** All green. No regressions, no bugs, no scorecard failures. One lint-polish code slice shipped.

## Phase 0 — Coordination

- Read `docs/AUTOMATION.md`, last ~12 ledger rows, newest scorecard (`2026-08-07.md`), and `git log`.
- **Newest scorecard = 28/28 checks passing, all green** — no `❌ FAILING` check to prioritize.
- Rebased the Mac host onto `origin/main`: local was 1 commit behind. Inherited `c2be494` "Add mobile next-action QA coverage summary" (overnight `openclaw-heartbeat`/nightly producer — touched `scripts/mobile-viewport-audit.mjs` +12 lines and added a `2026-08-14-overnight-next-action-coverage-smoke` audit). Did not duplicate that next-action coverage work.
- Monday content pipeline shipped two spoke posts (`klondike-win-rate-explained`, `spider-solitaire-opening-strategy`) on 2026-08-10 — not re-touched.
- Spoke rotation: 2026-08-14 is even → **Spider** spoke QA this run.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension, live production)

- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4 foundations correctly spaced, sidebar (Daily Leaderboard + Daily Challenge) renders alongside the board, no horizontal scroll (`scrollWidth === innerWidth === 1448`), card 114.9×160.8px, opacity 1. Toolbar intact.
- **solitairestack.com** — homepage FreeCell shell loads clean after the "Loading FreeCell…" splash (caught mid-load on first paint, confirmed rendered after a 2s settle); identical board + sidebar. `/games` catalog page: nav, hero ("Free Solitaire Games Online"), breadcrumb, and content all render correctly.
- **playspidersolitaireonline.com** (rotation) — 63 cards, 10 columns, 8 foundation slots + stock, toolbar intact (1/2/4-Suit selector, Undo/Hint/New Deal), **0/22 unnamed buttons**, no horizontal scroll, win-modal CSS not triggered but board layout sane.

### Mobile + tablet (true-viewport CDP audit via Mac host, live production)

Ran `npm run qa:mobile -- --base=https://playfreecellonline.com --screenshots` host-side (the Chrome extension cannot render a true sub-desktop viewport — resizing the window to 375px left the page viewport at 1448px with 114.9px cards, the documented limitation). Artifact: `docs/analytics/mobile-viewport-audits/2026-08-14-live.json` + `.md` (16 rows, screenshots captured; screenshot dir gitignored).

- **16-row full pass, all 4 engines × 375/390/414/768.** No hard audit failures.
- Card counts full and correct every viewport: **FreeCell 52 / Klondike 29 / Spider 63 / Forty Thieves 41**.
- **0 clipped cards, 0 horizontal overflow px, 0 blocked interactive controls, 0 cramped tap targets, 0 console errors, 0 runtime exceptions** on every row.
- Phone card widths 32–44px (min 32.09 Spider@375, up to 44.13 FreeCell@414), tablet 61–74px — confirms the `3dbb95a` tap-target fix holds live.
- Top controls visible on all rows; bottom controls visible on all phone rows (tablet 768 uses the sidebar layout, bottom bar intentionally absent).
- **Spider@414 read a clean 63** this run (the known intermittent DOM-mount 62-read did not recur).
- Planning signal (not a failure): portrait dead-space candidates persist at 45–51% unused vertical below the first board sample on phones (FreeCell 46–48%, Spider 49–51%, Forty Thieves 49–51%). Logged as a standing U6 play-UX planning input; acting on it (below-board next actions / contextual content) is a layout change that needs a `docs/plans/` reference, so not taken this run.

## Phase 2 — Competitor watch (online-solitaire.com)

**UNCHANGED — 57th+ consecutive no-change run.** Live desktop 1440×900. Homepage defaults to classic Klondike (Turn 1 / Turn 3 toggle, All games (520) dropdown, Time/Moves/Stock: 24 top bar, illustrated court-card art). H1=1 "Play Solitaire Online for Free (No Signup Required)", title "Solitaire - Play Online for Free", meta unchanged, catalog count 520 unchanged. Bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More; right rail collapsed behind "Show daily challenge & stats" at this width (width-dependent, not a redesign). No new features, redesigns, or content detected. Standing gaps where they lead (download prominence, deck/background theming, illustrated face cards, 520-catalog framing, non-solitaire card games, single-surface mobile controls) unchanged; our leads (editorial depth, richer per-page JSON-LD, spoke SEO, no-signup save, named daily deals, PWA offline+resume, 6-locale FreeCell i18n, mobile next-action coaching, post-win rating) unchanged. Competitor content treated as untrusted; DOM + page text scanned, no embedded instructions present, none acted upon. Logged to `docs/research/competitor-log.csv`.

## Phase 3 — SEO & schema spot-check (3 fresh long-tail pages, never previously covered)

All clean — 1 H1, self-canonical, unique meta, strong cluster links:

- **solitairestack.com/blog/best-solitaire-games-for-seniors** (hub Article) — `Organization + WebSite + Article + BreadcrumbList`, self-canonical, meta 152 chars, 41 cluster links.
- **solitairestack.com/accordion** (hub game page) — full `Organization + WebSite + WebApplication + Game + BreadcrumbList + FAQPage` graph, self-canonical, meta 137 chars, 45 cluster links.
- **playspidersolitaireonline.com/blog/spider-solitaire-empty-columns** (Spider spoke Article) — `Organization + WebSite + Article + BreadcrumbList`, self-canonical on the owner domain, meta 153 chars, 28 cluster links.

Incidental: `solitairestack.com/russian-solitaire` returns the expected SPA 404 (canonical `/_not-found`, H1 "404") — not a real route, no sitemap entry, so not a defect. No SEO/schema fixes required.

## Phase 4 — Improvement (1 slice)

Buckets 1 (scorecard `❌`) and 2 (QA bugs) were empty — everything green — so I took a small, same-run-safe code-polish slice (in-lane: polish of existing code, no new routes/types/locales, no behavior or layout change).

**`chore(lint): remove verified-dead unused type imports` (commit 248a7b5).** Removed 8 genuinely-unused named imports flagged by `@typescript-eslint/no-unused-vars`, each verified dead (single occurrence = import line only) before removal:

- `src/lib/dom-klondike/useDomKlondikeStore.ts` — `KlondikeGameState`, `KlondikeMove`
- `src/lib/dom-spider/useDomSpiderStore.ts` — `SpiderGameState`, `Suit`
- `src/solver/solver.ts` — `Location`, `Card`, `Suit`, `Color` (dropped the now-empty `../engine/Card` import line)

Lint warning count dropped **441 → 433**; `npx tsc --noEmit` and `npm run lint` both green (0 errors) before and after.

## Phase 5 — Validation & push

- `npx tsc --noEmit` → pass (exit 0).
- `npm run lint` → 0 errors, 433 warnings (down from 441).
- Two descriptive commits batched into a single `git push origin main` (code cleanup + this docs/report/ledger/competitor/mobile-audit commit).
- Post-push verification: Vercel deployments READY on all 4 projects + GitHub CI green on the pushed commit (see ledger note).

## Remaining backlog

- **U6 portrait dead-space** (45–51% unused vertical on phones) — quantified again today; needs a `docs/plans/` entry before any below-board layout change.
- **U6 SSR `<html lang>`** — root layout hard-codes `lang="en"`; plan-gated (dynamic render regresses `c7e8342`, or a locale route group is a new route family).
- Lint warnings remain at 433 (mostly unused-vars in solver/engine internals + a few unused `eslint-disable` directives) — future incremental cleanup, not urgent.
- Strategic priority remains traffic/off-site (U7) + monetization (U8), per the plan — not in this producer's lane.
