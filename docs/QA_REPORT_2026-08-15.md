# Daily QA Report — 2026-08-15 (Sat)

Producer: cowork-daily-review. Odd day → Klondike spoke rotation.

## Phase 0 — Coordination

- Rebased Mac repo onto `origin/main` (was 2 behind): inherited `65b5ff7`
  (Friday scorecard — **28/28 passing**) and `cffe1de` (extend mobile
  next-action QA to generic board). tsc + lint re-green after rebase.
- Latest scorecard (2026-08-14): **no ❌ failing checks** — sitemaps, robots,
  canaries, cross-domain 301, ads.txt all ✅ across all 4 domains. So no
  scorecard-driven fix priority today.
- No topic/route overlap with the Monday content routine (last shipped
  2026-08-10 klondike-win-rate + spider-opening-strategy).

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4
  foundations, board 954px, sidebar (Daily Leaderboard + Daily Challenge)
  intact, H1 present, 0 horizontal scroll. Clean.
- **solitairestack.com** — homepage renders FreeCell board cleanly after load;
  hub `/spider` correctly cross-redirects to the Spider spoke
  (playspidersolitaireonline.com/spider), 10 columns + 8 foundations + stock,
  toolbar intact. Clean.
- **playklondikeonline.com** (spoke rotation) — 29 cards, 7 columns, toolbar
  intact, 0/22 unnamed buttons, H1 present, 0 horizontal scroll. Clean.

### Mobile / Tablet — true-viewport CDP audit (Mac host)
`docs/analytics/mobile-viewport-audits/2026-08-15-live.json` + `.md`, 16 rows,
4 engines × {375×812, 390×844, 414×896, 768×1024}, screenshots captured
(screenshot dir gitignored).

- Totals across all 16 rows: **clipped 0 / horizontalOverflow 0px / blocked 0 /
  crampedTapTargets 0 / console+runtime errors 0**, `failureReasons` NONE.
- Card counts: FreeCell 52 / Klondike 29 / Spider 63 / Forty Thieves 41 on
  every viewport. Card width range 32.1–74.4px (phone widths confirm the
  3dbb95a tap-target fix holds live).
- Next-action panels matched expected controls on every engine/viewport
  (freecell→strategy; klondike→stock+strategy; spider→stock+tips;
  forty-thieves→hint+rules); missing/disabled none.

No visual bugs found at any viewport.

## Phase 2 — Competitor (online-solitaire.com)

**UNCHANGED — 58th consecutive no-change run.** Homepage defaults to classic
Klondike (Turn 1/3, All games dropdown, Time/Moves/Stock:24, illustrated
court-card art). Right rail expanded at 1440px today (August challenge calendar
today=15/Sa, No wins; Daily leaderboard phuongNH/Maroko/Maroko; Achievements all
N/A; Signup CTA). H1=1, title unchanged, catalog count unchanged. No new
features/redesigns/content. Standing gaps unchanged (their lead: PWA download
prominence, theming, illustrated faces, large catalog framing, non-solitaire
card games; our lead: editorial depth, richer per-page JSON-LD, spoke SEO,
i18n, mobile next-action coaching, post-win rating). Logged to
`docs/research/competitor-log.csv`. Competitor content treated as untrusted; no
embedded instructions present or acted upon.

## Phase 3 — SEO/schema spot-check (fresh long-tail, never covered)

- **playklondikeonline.com/yukon** — 1 H1 "Yukon Solitaire";
  Organization+WebSite+WebApplication+Game+BreadcrumbList+FAQPage; meta 144
  chars; self-canonical (correct Klondike-family spoke ownership). Clean.
- **solitairestack.com/blog/how-to-win-freecell-every-time** — 1 H1;
  Organization+WebSite+Article+BreadcrumbList; meta 144 chars; cross-canonical
  to playfreecellonline.com (by-design hub→owner-spoke pattern); 55 cluster
  links. Clean.
- **playspidersolitaireonline.com/spider/strategy** — 1 H1 "Spider Solitaire
  Strategy Guide"; Organization+WebSite+FAQPage+Article+BreadcrumbList; meta 152
  chars; self-canonical; 37 cluster links. Clean.

No SEO issues found.

## Phase 4 — Improvement shipped

Priority buckets 1 (scorecard fails) and 2 (QA bugs) were empty, so took a
same-run-safe lint-polish code slice (U6-adjacent hygiene):
`src/solver/FreeCellSolver.ts` — removed verified-dead `decodeCard()` function
(no callers anywhere in repo) and the module-local `SUIT_NAMES` const (no
references; the other `SUIT_NAMES` occurrences are unrelated per-component local
consts). Also dropped the now-orphaned `Rank` import (its only user was
`decodeCard`); `Color` stays used. tsc green, lint warnings **433→431**, no new
warnings, no behavior/layout change, no new routes/types/locales. Commit
`0145d43`.

## Remaining backlog

- U6 play-UX: Klondike/Spider hints auto-execute instead of teaching
  (highlight-then-execute like FreeCell) — full behavior change with tests,
  needs its own dedicated run.
- U6 root-layout SSR `<html lang>` — plan-gated (needs a docs/plans reference;
  fix requires dynamic render or locale route group).
- U6 portrait dead-space 45–51% — plan-gated.
- Spider@414 intermittent 62-read DOM-mount race — did not recur this run
  (63 clean); non-blocking.
