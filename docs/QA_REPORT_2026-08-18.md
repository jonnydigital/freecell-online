# Daily QA Report — 2026-08-18 (Tue)

Producer: cowork-daily-review. Even day → Spider spoke rotation.

## Phase 0 — Coordination

- Mac repo up to date with `origin/main` at `00ef612` (2026-08-16 daily review).
  No sibling commits to rebase this run (2026-08-17 Monday content routine had no
  new daily-review commit intervening).
- Latest scorecard (2026-08-14): **28/28 passing, no ❌ failing checks** —
  sitemaps, robots, canaries, cross-domain 301, ads.txt all ✅ across all 4
  domains. No scorecard-driven fix priority today.
- No topic/route overlap with the Monday content routine (last shipped
  2026-08-10: klondike-win-rate-explained + spider-solitaire-opening-strategy)
  or the Friday scorecard routine (last ran 2026-08-14).
- Baseline before any edits: `npx tsc --noEmit` clean, `npm run lint` 0 errors /
  425 warnings.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4
  foundations, sidebar (Daily Leaderboard + Daily Challenge) renders alongside
  the board, 0 horizontal scroll, 1 H1. Toolbar (undo/redo/hint/sound/ghost/
  bookmark/trophy/settings) intact. Card labels readable. Clean.
- **solitairestack.com** — homepage renders the FreeCell board cleanly; 1 H1
  "Play Solitaire Online for Free", 138 links, 0 horizontal scroll. Clean.
- **playspidersolitaireonline.com** (spoke rotation) — 63 cards, 10 cascades,
  8 spade foundations, stock pile top-right, toolbar intact (1/2/4 Suit,
  Undo/Hint/New Deal), 0/22 unnamed buttons, 0 horizontal scroll. Clean.

### Mobile / Tablet — true-viewport CDP audit (Mac host)
`docs/analytics/mobile-viewport-audits/2026-08-18-live.json` + `.md`, 20 rows,
5 engines × {375×812, 390×844, 414×896, 768×1024}, screenshots captured
(screenshot dir gitignored).

- Totals across all 20 rows: **clipped 0 / max horizontal overflow 0px /
  blocked controls 0 / cramped tap targets 0 / runtime exceptions 0 /
  console+log errors 0**. No hard audit failures detected.
- Card counts: FreeCell 52 / Klondike 29 / Spider 63 / Forty Thieves 41 /
  Yukon 52 on every viewport. Card width range 32.1–74.4px (phone widths
  32–44px confirm the 3dbb95a tap-target fix holds live).
- Top + bottom bars visible on every row; next-action panels matched expected
  controls on every phone viewport (freecell→strategy; klondike→stock+strategy;
  spider→stock+tips; forty-thieves→hint+rules; yukon→hint+rules); missing/
  disabled none.
- Verified live FreeCell 375×812 screenshot: 8 columns + 4 FC + 4 foundations,
  readable labels, "Start with a safe move" next-action panel, bottom nav
  (Menu/New/Undo/Redo/Hint) intact.
- Note: the Chrome-extension `resize_window` path did not produce a true mobile
  viewport this run (`innerWidth` stayed 1323 after resize to 375). Per the
  AUTOMATION.md unattended-run note, fell back to the verified Mac-host CDP
  harness (`npm run qa:mobile`), which spawns real Chrome at true device
  widths — the authoritative mobile evidence.

No visual bugs found at any viewport.

## Phase 2 — Competitor (online-solitaire.com)

**UNCHANGED — 60th consecutive no-change run.** Homepage defaults to classic
Klondike (Turn 1/3 toggle, All games (520) dropdown, Time/Moves/Stock:24,
foundations + Redeal slot). Right rail expanded at this width (August challenge
calendar with today=18 highlighted, Daily leaderboard, Achievements N/A, Signup
CTA). Bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/More. H1=1
"Play Solitaire Online for Free (No Signup Required)"; title "Solitaire - Play
Online for Free"; meta description unchanged ("Play classic Solitaire online for
free…300+ free solitaire games."). All games (520) catalog count unchanged. No
new features/redesigns/content. Standing gaps unchanged (their lead: PWA download
prominence, deck/background theming, illustrated faces, large catalog framing
(520), non-solitaire card games; our lead: editorial depth, richer per-page
JSON-LD, spoke SEO, 6-locale i18n, mobile next-action coaching, post-win deal
rating). Logged to `docs/research/competitor-log.csv`. Competitor content treated
as untrusted; no embedded instructions present or acted upon.

## Phase 3 — SEO/schema spot-check (fresh long-tail, never covered)

- **playfreecellonline.com/blog/freecell-terminology-guide** — 1 H1 "10 FreeCell
  Terms Every Player Should Know"; Organization+WebSite+Article+BreadcrumbList;
  meta 155 chars; self-canonical; 6 internal links incl. cluster
  (freecell/how-to-play, strategy). Clean.
- **solitairestack.com/blog/how-52-cards-produce-hundreds-of-games** — 1 H1
  "How 52 Cards Produce Hundreds of Solitaire Games: A Design Taxonomy";
  Organization+WebSite+Article+BreadcrumbList; meta 140 chars; self-canonical
  (hub owner); 11 internal links. Clean.
- **playklondikeonline.com/blog/klondike-foundation-timing** — 1 H1 "Foundation
  Timing in Klondike: The Rule That Stops You Blocking Your Own Game";
  Organization+WebSite+Article+BreadcrumbList; meta 157 chars; self-canonical
  (correct Klondike-spoke ownership); 8 internal links, 6 cluster links. Clean.

All three: unique titles, unique meta descriptions, one H1, self-canonical on the
correct owner domain, proper Article graph, strong intra-cluster linking. No SEO
issues found.

## Phase 4 — Improvement shipped

Priority buckets 1 (scorecard ❌) and 2 (today's QA bugs) were empty, so took a
same-run-safe lint-polish code slice (well-precedented when scorecard is green
and no bug is found):

- **`chore(lint)` — removed verified-dead `Rank` / `SUIT_SYMBOLS` imports** from
  `src/game/PyramidScene.ts`, `src/game/ScorpionScene.ts`,
  `src/game/SeahavenScene.ts` (commit `d107b25`). `Rank` was import-line-only
  (0 other references) in all three; `SUIT_SYMBOLS` was dead in Pyramid and
  Seahaven but **kept in Scorpion** (still used at line 916,
  `SUIT_SYMBOLS[suit]`) — matching lint's exact per-file flags. `Card` and
  `Suit` stay (used). tsc green before and after; lint warnings 425 → 420 (−5),
  0 errors, no new warnings, no behavior/layout change, no new
  routes/types/locales. This completes the Phaser-scene dead-import sweep begun
  2026-08-16 (Spider/TriPeaks/Yukon).

## Phase 5 — Remaining backlog

- **U6 (play-UX):** portrait dead-space 42–61% below the first board sample on
  phone viewports (9 candidate rows today) remains a planning signal for
  below-board next actions / contextual hints — plan-gated, not a hard failure.
  Klondike/Spider hint auto-execute→highlight (teach like FreeCell) and a generic
  daily-challenge retention loop for the two spokes remain the larger U6 items.
- **U5 (locale gate):** FreeCell 6-locale set holds; no new locale work this run
  (content creation is the Monday routine's lane).
- **U6 SSR `<html lang>`:** root layout still hard-codes `lang="en"`; a fix needs
  either dynamic rendering (regresses c7e8342) or a locale route group (new
  route family) — plan-gated, unchanged.
- **Remaining lint warnings (420):** the leftover unused-var / unused
  eslint-disable warnings (siteConfig gameName, useDomFreecellStore GameState,
  TestBridge/React-hooks disable directives) are candidate future polish slices.

No regressions. tsc + lint green. Single push at end of run.
