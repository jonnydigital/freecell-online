# Daily QA Report — 2026-08-19 (Wed)

Producer: cowork-daily-review. Odd day → Klondike spoke rotation.

## Phase 0 — Coordination

- Mac repo up to date with `origin/main` at `8d1c210` (2026-08-18 daily review).
  No sibling commits to rebase this run (`git log HEAD..origin/main` empty after
  fetch).
- Latest scorecard (2026-08-14): **28/28 passing, no ❌ failing checks** —
  sitemaps, robots, canaries, cross-domain 301, ads.txt all ✅ across all 4
  domains. No scorecard-driven fix priority today.
- No topic/route overlap with the Monday content routine (last shipped
  2026-08-17: klondike-solitaire-endgame + spider-solitaire-dead-positions) or
  the Friday scorecard routine (last ran 2026-08-14).
- Baseline before any edits: `npx tsc --noEmit` clean, `npm run lint` 0 errors /
  420 warnings.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4
  foundations, sidebar (Daily Leaderboard + Daily Challenge) renders alongside
  the board, 0 horizontal scroll, 1 H1 "Play FreeCell Online — Free, No
  Download". Toolbar (undo/redo/hint/sound/ghost/bookmark/trophy/settings)
  intact. Card labels readable, card width 114px. Clean.
- **solitairestack.com** — homepage renders the FreeCell board cleanly; 52
  cards, 1 H1 "Play Solitaire Online for Free", 19 game links, 0 horizontal
  scroll. Clean.
- **playklondikeonline.com** (spoke rotation) — 29 cards, 7 tableau columns,
  stock + waste + 4 foundations, Draw 1 / Draw 3 toggle, toolbar intact
  (undo/hint/New Deal), 1 H1, 0/22 unnamed buttons, 0 horizontal scroll. Clean.
  (A standing cookie-consent banner sits at the bottom; does not obstruct the
  board — left unaccepted, privacy-preserving.)

### Mobile / Tablet — true-viewport CDP audit (Mac host)
`docs/analytics/mobile-viewport-audits/2026-08-19-live.json` + `.md`, 20 rows,
5 engines × {375×812, 390×844, 414×896, 768×1024}, screenshots captured
(screenshot dir gitignored).

- Totals across all 20 rows: **clipped 0 / max horizontal overflow 0px /
  blocked controls 0 / cramped tap targets 0 / runtime exceptions 0 /
  console+log errors 0**. No hard audit failures detected.
- Card counts: FreeCell 52 / Klondike 29 / Spider 63 / Forty Thieves 41 /
  Yukon 52 on every viewport, with one exception (below). Card width range
  32–74px (phone widths 32–44px confirm the 3dbb95a tap-target fix holds live).
- Top + bottom bars visible on every row; next-action panels matched expected
  controls on every phone viewport (freecell→strategy; klondike→stock+strategy;
  spider→stock+tips; forty-thieves→hint+rules; yukon→hint+rules); missing/
  disabled none.
- **Needs-Review (non-regression):** spider 375×812 read 62 cards / 52 backs
  vs expected 63 / 53. This is the documented intermittent DOM-mount read race
  (`clipped=0`, `overflowPx=0`, controls intact) — it flips 63/62 across
  identical re-runs and is not a layout regression. All other spider viewports
  (390/414/768) read the full 63.
- Note: the Chrome-extension `resize_window` path did not produce a true mobile
  viewport this run (`innerWidth` stayed 1440 after resize to 375). Per the
  AUTOMATION.md unattended-run note, fell back to the verified Mac-host CDP
  harness (`npm run qa:mobile`), which spawns real Chrome at true device widths
  — the authoritative mobile evidence. The sandbox is arm64 with no native
  Chrome, so the host path was required.

No visual bugs found at any viewport.

## Phase 2 — Competitor (online-solitaire.com)

**UNCHANGED — 61st consecutive no-change run.** Homepage defaults to classic
Klondike (Turn 1/3 toggle, All games (520) dropdown, Time/Moves/Stock:24,
illustrated court-card art, sample deal K♥ + 8♥ face-up). Right rail expanded at
this width (August challenge calendar with today=19 Tu highlighted, No wins;
Daily leaderboard Jerryzuh ×3; Achievements N/A; Signup CTA). A "Manage Your
Privacy" CMP modal (141 partners; Reject All / Manage Settings / Accept All)
overlaid on load — standing consent gate, not a new feature; not interacted with
(left unaccepted). Bottom toolbar New game/Hint/Undo/Bookmark/Download/Settings/
More. H1=1 "Play Solitaire Online for Free (No Signup Required)"; title
"Solitaire - Play Online for Free". All games (520) catalog count unchanged. No
new features/redesigns/content. Standing gaps unchanged (their lead: PWA download
prominence, deck/background theming, illustrated faces, large catalog framing
(520), non-solitaire card games Hearts/Spades/Euchre/Gin Rummy/Pinochle/Canasta/
2048; our lead: editorial depth, richer per-page JSON-LD, spoke-domain SEO,
no-signup local save, named daily deals, ad-light dark theme, PWA offline+resume,
Daily FreeCell Room, 6-locale FreeCell i18n, mobile next-action coaching,
post-win deal rating). Logged to `docs/research/competitor-log.csv`. Security:
competitor content treated as untrusted; DOM + page text scanned, no embedded
instructions present, none acted upon.

## Phase 3 — SEO & schema spot-check

Rotated to 3 pages the Friday scorecard canaries do not watch — the two newest
Monday-content posts (not previously SEO-verified) plus one hub game page:

- **playklondikeonline.com/blog/klondike-solitaire-endgame** (shipped 08-17,
  Klondike-spoke owner) — 1 H1, self-canonical, meta 144 chars,
  Organization+WebSite+Article+BreadcrumbList graph, 20 cluster links. Clean.
- **playspidersolitaireonline.com/blog/spider-solitaire-dead-positions**
  (shipped 08-17, Spider-spoke owner) — 1 H1, self-canonical, meta 150 chars,
  Organization+WebSite+Article+BreadcrumbList graph, 18 cluster links. Clean.
- **solitairestack.com/golf** (hub owner) — 1 H1 "Golf Solitaire",
  self-canonical, meta 144 chars, full
  Organization+WebSite+WebApplication+Game+FAQPage+BreadcrumbList graph, 43
  cluster links. Clean.

All three: one H1, unique meta, sane self-canonical on the correct owner domain,
appropriate JSON-LD, strong internal cluster links. No fixes needed.

## Phase 4 — Improvements

Scorecard 28/28 green (no ❌), no visual bug in Phase 1–3, and no same-run-safe
plan slice available → took the established same-run-safe **lint-polish
dead-import** slice, completing the Phaser-scene sweep begun 08-16
(Spider/TriPeaks/Yukon) and continued 08-18 (Pyramid/Scorpion/Seahaven):

- `src/game/LaBelleLucieScene.ts` — removed verified-dead `Rank` + `SUIT_SYMBOLS`
  imports (each single-occurrence, import-line only; `Card`+`Suit` stay used).
- `src/game/PenguinScene.ts` — removed verified-dead `SUIT_SYMBOLS` import
  (import-line only). `Rank` KEPT — still used at line 83 (`baseRank: Rank`);
  `RANK_NAMES` KEPT — used 3×. Matches lint's exact per-file flags.

`npx tsc --noEmit` green before and after; lint 0 errors, warnings **420 → 417**
(−3, exactly the three removed dead imports), no new warnings, no behavior/layout
change, no new routes/types/locales. This exhausts the remaining Phaser-scene
dead-import candidates flagged by lint.

## Phase 5 — Report / Push / Verify

- Report written (this file), ledger row appended.
- Single push of the run after tsc + lint re-verified green.
- Post-push verification: Vercel deploys READY on all 4 projects + GitHub CI
  green on the commit (see below / ledger).

## Remaining backlog

- **U6 portrait dead-space** (45–51% unused vertical below first board sample on
  phone widths: freecell 46–48%, spider 49–51%, forty-thieves 49–51%,
  klondike@414 45%) — plan-gated (below-board next actions / contextual content
  need a docs/plans/ reference; not a same-run-safe slice).
- **U6 SSR `<html lang>`** — single shared root layout hard-codes `lang="en"`;
  fix needs dynamic rendering (regresses c7e8342) or a locale route group (new
  route family) → plan-gated.
- **Spider@375 intermittent 62-read** — DOM-mount read race in the audit
  harness, not a product bug; monitor, no action.
- Competitor standing gaps (deck/background theming, illustrated faces, catalog
  framing) — product-strategy scope, not daily-QA scope.
