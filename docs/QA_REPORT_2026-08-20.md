# Daily QA Report — 2026-08-20 (Thu)

Producer: cowork-daily-review. Even day → Spider spoke rotation.

## Phase 0 — Coordination

- Mac repo at `origin/main` `ceef0c5` (2026-08-19 daily review) before this run;
  no sibling commits to rebase (`git log HEAD..origin/main` empty after fetch).
- Latest scorecard (2026-08-14): **28/28 passing, no ❌ failing checks** —
  sitemaps, robots, canaries, cross-domain 301/308, ads.txt all ✅ across all 4
  domains. No scorecard-driven fix priority today.
- No topic/route overlap with the Monday content routine (last shipped
  2026-08-17: klondike-solitaire-endgame + spider-solitaire-dead-positions) or
  the Friday scorecard routine (last ran 2026-08-14).
- Baseline before any edits: `npx tsc --noEmit` clean, `npm run lint` 0 errors /
  417 warnings.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4
  foundations, sidebar (Daily Leaderboard + Daily Challenge) renders alongside
  the board, 0 horizontal scroll (scrollWidth == clientWidth 1244), 1 H1 "Play
  FreeCell Online — Free, No Download". Toolbar (undo/redo/hint/sound/ghost/
  bookmark/trophy/settings) intact. Card labels readable, card width 91px. Clean.
- **solitairestack.com** — homepage renders the FreeCell board; 52 cards, 1 H1
  "Play Solitaire Online for Free", 0 horizontal scroll. Clean.
- **playspidersolitaireonline.com** (spoke rotation, even day) — 10 tableau
  columns, 8 foundations, stock pile top-right, 1/2/4-Suit selector, toolbar
  intact (Undo/Hint/New Deal), 1 H1 "Play Spider Solitaire Online — Free, No
  Download", 22 buttons / 0 unnamed, 0 horizontal scroll, clipped 0. Card width
  100px. Clean. (Live DOM read landed 62 cards mid-deal at `opacity 0.8`; the
  true-viewport Mac-host audit below reads the full 63 — the documented
  intermittent DOM-mount read race, not a regression.)

### Mobile / Tablet — true-viewport CDP audit (Mac host)
`docs/analytics/mobile-viewport-audits/2026-08-20-live.json` + `.md`, 20 rows,
5 engines × {375×812, 390×844, 414×896, 768×1024}, screenshots captured
(screenshot dir gitignored).

- Totals across all 20 rows: **clipped 0 / max horizontal overflow 0px /
  blocked controls 0 / cramped tap targets 0 / runtime exceptions 0 /
  console+log errors 0**. No hard audit failures detected.
- Card counts full on **every** viewport this run: FreeCell 52 / Klondike 29 /
  **Spider 63 (incl. 414 — no 62-read recurrence)** / Forty Thieves 41 /
  Yukon 52. Card width range 39–74px (phone widths 39–44px confirm the 3dbb95a
  tap-target fix holds live). scrollWidth == viewport width on all 20 rows.
- Top + bottom bars visible on every row; next-action panels matched expected
  controls 27/27 (100%) on every phone viewport (freecell→strategy;
  klondike→stock+strategy; spider→stock+tips; forty-thieves→hint+rules;
  yukon→hint+rules); missing/disabled none.
- Note: the Chrome-extension `resize_window` path again did not produce a true
  mobile viewport (`innerWidth` stayed 1244 after resize to 375, card width
  still 91px desktop). Per the AUTOMATION.md unattended-run note, fell back to
  the verified Mac-host CDP harness (`npm run qa:mobile`), which spawns real
  Chrome at true device widths — the authoritative mobile evidence. The sandbox
  is arm64 with no native Chrome, so the host path was required.

No visual bugs found at any viewport.

## Phase 2 — Competitor (online-solitaire.com)

**UNCHANGED — 62nd consecutive no-change run.** Homepage defaults to classic
Klondike (Turn 1/3 toggle, All games (520) dropdown, Time/Moves/Stock:24,
foundations club/heart/spade/diamond + Redeal slot; board captured mid-deal
pre-render, controls stable). Bottom toolbar New game/Hint/Undo/Bookmark/
Download/Settings/More. H1=1 "Play Solitaire Online for Free (No Signup
Required)"; title "Solitaire - Play Online for Free"; meta description unchanged.
All games (520) catalog count unchanged. No new features/redesigns/content.
Standing gaps unchanged (their lead: PWA download prominence, deck/background
theming, illustrated faces, large catalog framing (520), non-solitaire card
games Hearts/Spades/Euchre/Gin Rummy/Pinochle/Canasta/2048; our lead: editorial
depth, richer per-page JSON-LD, spoke-domain SEO, no-signup local save, named
daily deals, ad-light dark theme, PWA offline+resume, Daily FreeCell Room,
6-locale FreeCell i18n, mobile next-action coaching, post-win deal rating).
Logged to `docs/research/competitor-log.csv`. Security: competitor content
treated as untrusted; DOM + page text scanned, no embedded instructions present,
none acted upon.

## Phase 3 — SEO & schema spot-check

Rotated to 3 pages the Friday scorecard canaries do not watch — one per site
tier, none previously covered in the ledger:

- **solitairestack.com/canfield** (hub) — 1 H1 "Canfield Solitaire", 308
  cross-domain redirect + self-canonical to the Klondike-family owner
  `playklondikeonline.com/canfield` (by-design consolidation, same pattern as
  beleaguered-castle), meta 152 chars, full
  Organization+WebSite+WebApplication+Game+BreadcrumbList+FAQPage graph, 13
  cluster links. Clean.
- **playspidersolitaireonline.com/spider/tips** (Spider-spoke owner) — 1 H1
  "Spider Solitaire Tips & Tricks", self-canonical, meta 156 chars,
  Organization+WebSite+Article+FAQPage+BreadcrumbList graph, 22 cluster links.
  Clean.
- **playfreecellonline.com/freecell/tips** (FreeCell owner) — 1 H1 "FreeCell
  Tips & Tricks", self-canonical, meta 151 chars,
  Organization+WebSite+Article+FAQPage+BreadcrumbList graph, 28 cluster links.
  Clean.

All three: one H1, unique meta, sane canonical on the correct owner domain,
appropriate JSON-LD, strong internal cluster links. No fixes needed.

## Phase 4 — Improvements

Scorecard 28/28 green (no ❌), no visual bug in Phase 1–3, and no same-run-safe
plan slice available → took the established same-run-safe **lint-polish
dead-import** slice. Lint still flagged the two remaining Phaser scenes carrying
the same dead symbols the 08-16→08-19 sweep cleared elsewhere:

- `src/game/GolfScene.ts` — removed verified-dead `Rank` + `SUIT_SYMBOLS`
  imports (each single-occurrence, import-line only; `Card`+`Suit` stay used).
- `src/game/KlondikeScene.ts` — removed verified-dead `Rank` + `SUIT_SYMBOLS`
  imports (each single-occurrence, import-line only; `Card`+`Suit` stay used).

`npx tsc --noEmit` green before and after; lint 0 errors, warnings **417 → 413**
(−4, exactly the four removed dead imports), no new warnings, no behavior/layout
change, no new routes/types/locales. This closes out the Phaser-scene
dead-import sweep across all engine scenes. (Residual per-file locals — Klondike
`move`/`e`, Golf none, Penguin `overlap` — are unused *local variables*, not
dead imports; left as-is, riskier than an import-line removal and out of a
same-run-safe slice.)

## Phase 5 — Report / Push / Verify

- Report written (this file), ledger row appended.
- Single push of the run after tsc + lint re-verified green.
- Post-push verification: Vercel deploys READY on all 4 projects + GitHub CI
  green on the commit (see ledger).

## Remaining backlog

- **U6 portrait dead-space** (~42–48% unused vertical below the first board
  sample on phone widths: freecell 46–48%, and comparable on the other engines)
  — plan-gated (below-board next actions / contextual content need a
  docs/plans/ reference; not a same-run-safe slice).
- **U6 SSR `<html lang>`** — single shared root layout hard-codes `lang="en"`;
  fix needs dynamic rendering (regresses c7e8342) or a locale route group (new
  route family) → plan-gated.
- **Spider intermittent 62-read** — DOM-mount read race in the live Chrome read
  / audit harness, not a product bug; monitor, no action.
- Competitor standing gaps (deck/background theming, illustrated faces, catalog
  framing) — product-strategy scope, not daily-QA scope.
