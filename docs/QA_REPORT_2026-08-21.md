# Daily QA Report — 2026-08-21 (Fri)

Producer: cowork-daily-review. Odd day → Klondike spoke rotation.

## Phase 0 — Coordination

- Mac repo started at `f883978` (2026-08-20 daily review). `git fetch` showed
  remote 1 ahead: `f6b12f3` **docs(scorecard): weekly property health 2026-08-21
  — 28/28 passing** (today's Friday scorecard routine). Rebased onto it before
  any edits (fast-forward, 0 local ahead).
- Latest scorecard (2026-08-21): **28/28 passing, no ❌ failing checks** —
  sitemaps, robots, canaries, cross-domain 301/308, ads.txt all ✅ across all 4
  domains. Klondike +1 URL/+1 blog and Spider +1 URL/+1 blog vs 08-14 (the
  Monday 08-17 content pipeline shipped klondike-solitaire-endgame +
  spider-solitaire-dead-positions). No scorecard-driven fix priority today.
- No topic/route overlap with the Monday content routine (last shipped
  2026-08-17) or the Friday scorecard routine (ran today, 08-21).
- `git log --oneline -15` reviewed: the Phaser-scene dead-import lint sweep
  closed 08-20 (Golf/Klondike, the last two engine scenes).
- Baseline before any edits: `npx tsc --noEmit` clean, `npm run lint` 0 errors /
  413 warnings.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4
  foundations, sidebar (Daily Leaderboard + Daily Challenge) renders alongside
  the board, 0 horizontal scroll (scrollWidth == innerWidth 1512), 1 H1 "Play
  FreeCell Online — Free, No Download". Toolbar (undo/redo/hint/sound/ghost/
  bookmark/trophy/settings) intact. Card labels readable. Clean.
- **solitairestack.com** — homepage renders the FreeCell board (52 cards, 1 H1
  "Play Solitaire Online for Free", 0 hscroll); `/games` index 1 H1 "Free
  Solitaire Games Online", 0 hscroll, catalog links intact. Clean.
- **playklondikeonline.com** (spoke rotation, odd day) — 29 cards, 7 tableau
  columns, stock + waste, 4 foundations, Draw 1 / Draw 3 selector, toolbar
  (undo/hint/New Deal) intact, 1 H1 "Play Klondike Solitaire Online — Free, No
  Download", 22 buttons / 0 unnamed, 0 horizontal scroll. Win-modal CSS not
  triggered (fresh deal); board sane. Clean. (Standard cookie/consent banner
  present, left unaccepted per privacy policy.)

### Mobile / Tablet — true-viewport CDP audit (Mac host)
`docs/analytics/mobile-viewport-audits/2026-08-21-live.json` + `.md`, 20 rows,
5 engines × {375×812, 390×844, 414×896, 768×1024}, screenshots captured
(screenshot dir gitignored). Chrome-extension `resize_window` again did **not**
yield a true mobile viewport (innerWidth stayed 1512, card width 122px =
desktop), so the AUTOMATION.md-documented Mac-host CDP harness is the
authoritative mobile evidence.

- **20/20 rows passed, 0 need review.** Max horizontal overflow 0px, max cramped
  tap targets 0, max blocked controls 0, runtime exceptions 0, console/log
  errors 0.
- Full card counts every viewport: freecell 52 / klondike 29 / spider 63 /
  forty-thieves 41 / yukon 52. Spider @375 read full 63 (no intermittent
  62-read recurrence this run).
- Card widths 32–74px across phone→tablet (all within the 30–54px phone range +
  tablet 61–74px), confirming the `3dbb95a` tap-target fix holds.
- Expected next-action panels: **27/27 matched (100%)**, 0 missing, 0 disabled,
  every engine × phone viewport.
- Portrait dead-space candidates: 9 phone rows at 45–51% unused vertical space
  (standing U6 planning signal, plan-gated — not a failure).

## Phase 2 — Competitor analysis (online-solitaire.com)

- **UNCHANGED — 63rd consecutive no-change run.** Homepage defaults to classic
  Klondike (Turn 1 / Turn 3 toggle, All games (520) dropdown, top bar
  Time/Moves/Stock: 24, club/heart/spade/diamond foundations). Bottom toolbar
  New game / Hint / Undo / Bookmark / Download / Settings / More. 1 H1 "Play
  Solitaire Online for Free (No Signup Required)", title "Solitaire - Play Online
  for Free". Same 141-partner "Manage Your Privacy" CMP modal on load (left
  unaccepted). No new features, redesigns, or content additions.
- Standing gaps where they lead: native/PWA download prominence, deck/background
  theming, non-solitaire card games (Hearts/Spades/Euchre/Gin/Pinochle/Canasta/
  2048), large 520-game catalog framing. We lead: editorial depth (28 variants +
  Strategy/History/Rules/Research), richer per-page JSON-LD, spoke-domain SEO,
  no-signup local save, 6-locale FreeCell i18n, mobile next-action coaching.
- Logged to `docs/research/competitor-log.csv`. Competitor content treated as
  untrusted; no embedded instructions present, none acted upon.

## Phase 3 — SEO & schema spot-check (long-tail, off-scorecard)

Three fresh pages the Friday scorecard never covers, rotated off recently-logged
URLs — all clean (1 H1, self-canonical, unique meta, cluster links present):

- **playfreecellonline.com/freecell/strategy** — 1 H1 "FreeCell Strategy Guide",
  Article + FAQPage + BreadcrumbList (+Organization + WebSite) graph,
  self-canonical, 146-char meta, 31 cluster links.
- **solitairestack.com/aces-up** (hub game) — 1 H1 "Aces Up Solitaire",
  WebApplication + Game + FAQPage + BreadcrumbList (+Org + WebSite),
  self-canonical (own host), 152-char meta, 45 cluster links.
- **playklondikeonline.com/klondike/tips** (klondike spoke) — 1 H1 "Klondike
  Solitaire Tips & Tricks", Article + FAQPage + BreadcrumbList, self-canonical
  (own host), 145-char meta, 24 cluster links.

No SEO/schema defects found; no fix needed.

## Phase 4 — Improvements

- Scorecard fails: none (28/28). Phase 1–3 bugs: none. So Phase 4 took a
  same-run-safe lint-polish code slice (matching the established pattern):
- **`src/lib/dom-freecell/useDomFreecellStore.ts`** — removed the verified-dead
  unused `GameState` type import from the `@/engine/FreeCellEngine` import line.
  Confirmed import-line-only before removal (all other `GameState` grep hits are
  the unrelated `saveGameState`/`clearGameState` storage helpers). `tsc` clean
  before + after; lint warnings 413 → 412, 0 errors, no new warnings, no
  behavior/layout change, no new routes/types/locales.

## Phase 5 — Validation & push

- `npx tsc --noEmit` exit 0; `npm run lint` 0 errors / 412 warnings (down 1).
- Separate descriptive commits, single `git push origin main` at end of run.
- Post-push verification: Vercel deploys READY on all 4 projects + GitHub CI
  green on the pushed commit (recorded below).

## Remaining backlog

- **U6 play-UX**: portrait dead-space 45–51% on phone widths (9 rows) — surface
  below-board next actions / contextual hints. Plan-gated (docs/plans/
  2026-07-03-001).
- **U6 root layout**: shared App-Router root layout hard-codes `<html lang="en">`
  for all locales (SSR serves `lang="en"` on `/freecell-en-francais`,
  hydration-corrected to `fr`). Fix needs dynamic rendering or a locale route
  group — plan-gated.
- **U5 French-locale completeness**: last live re-verification held (reciprocal
  7-entry hreflang, self-canonical, localized play UI). No new locale work
  without a docs/plans/ reference.
