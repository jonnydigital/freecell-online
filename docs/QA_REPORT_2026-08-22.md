# Daily QA Report — 2026-08-22 (Sat)

Producer: cowork-daily-review. Even day → Spider spoke rotation.

## Phase 0 — Coordination

- Mac repo + sandbox mirror both at `eae6b3c` (2026-08-21 daily review). No
  remote drift; clean working tree apart from three pre-existing untracked
  artifacts (`.claude/settings.local.json`, `docs/analytics/mobile-viewport-audits/2026-08-03-live.*`)
  that predate this run and were left untouched.
- Latest scorecard (2026-08-21): **28/28 passing, no ❌ failing checks** —
  sitemaps, robots, canaries, cross-domain 301/308, ads.txt all ✅ across all 4
  domains. No scorecard-driven fix priority today.
- No topic/route overlap with the Monday content routine (last shipped
  2026-08-17: klondike-solitaire-endgame + spider-solitaire-dead-positions) or
  the Friday scorecard routine (ran 2026-08-21).
- `git log --oneline -15` reviewed: the Phaser-scene dead-import lint sweep
  closed 08-20; 08-21 removed a dead `GameState` import in the dom-freecell store.
- Baseline before any edits: `npx tsc --noEmit` clean, `npm run lint` 0 errors /
  412 warnings.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4
  foundations, sidebar (Daily Leaderboard + Daily Challenge with calendar,
  today=22 highlighted) renders alongside the board, 0 horizontal scroll
  (scrollWidth == innerWidth 1244), 1 H1 "Play FreeCell Online — Free, No
  Download". Toolbar (undo/redo/hint/sound/ghost/bookmark/trophy/settings)
  intact. Card labels readable. Clean.
- **solitairestack.com** — homepage renders the FreeCell board (52 cards,
  populated Daily Leaderboard, Daily Challenge), 1 H1 "Solitaire Stack ...",
  0 hscroll. Game page `/tripeaks` — three peaks + stock/waste (23 stock, 1
  waste, 0 cleared), tableau row, toolbar (undo/hint/New Deal/settings) intact,
  no clipping. Clean.
- **playspidersolitaireonline.com** (spoke rotation, even day) — 63 cards,
  10 tableau columns, 8 foundation slots, stock pile top-right, 1/2/4-Suit
  selector, toolbar (Undo/Hint/New Deal) intact, 1 H1 "Play Spider Solitaire
  Online — Free, No Download", 0 horizontal scroll. Win-modal CSS not triggered
  (fresh deal); board sane. Clean.

### Mobile / Tablet — true-viewport CDP audit (Mac host)
`docs/analytics/mobile-viewport-audits/2026-08-22-live.json` + `.md`, 20 rows,
5 engines × {375×812, 390×844, 414×896, 768×1024}, screenshots captured
(screenshot dir gitignored). Chrome-extension `resize_window` again did **not**
yield a true mobile viewport (innerWidth stayed 1244, card width 91px =
desktop), so the AUTOMATION.md-documented Mac-host CDP harness is the
authoritative mobile evidence.

- **20/20 rows passed, 0 need review.** Max horizontal overflow 0px, max cramped
  tap targets 0, max blocked controls 0, runtime exceptions 0, console/log
  errors 0.
- Full card counts every viewport: freecell 52, klondike 29, spider 63
  (**incl. 375 — no 62-read recurrence today**), forty-thieves 41, yukon 52.
  Card widths 32–74px across viewports.
- Expected next-action panels 27/27 matched (100% coverage), 0 missing, 0
  disabled — freecell strategy; klondike stock+strategy; spider stock+tips;
  forty-thieves hint+rules; yukon hint+rules.
- Portrait dead-space candidates: 9 rows at 45–51% unused vertical space. This
  is the standing **U6** planning signal (below-board next-action / compact
  secondary content), plan-gated — not a hard failure.

## Phase 2 — Competitor analysis

- **online-solitaire.com** (Chrome extension, 1440×900): **UNCHANGED, 64th
  consecutive no-change run.** Homepage defaults to classic Klondike (Turn 1 /
  Turn 3 toggle, All games dropdown, top bar Time/Moves/Stock: 24), right rail
  August challenge calendar (trophy tiers 10/20/31, today=22 Sa, No wins) +
  Daily leaderboard + Achievements (all N/A) + Signup CTA, bottom toolbar New
  game/Hint/Undo/Bookmark/Download/Settings/More. Title "Solitaire - Play Online
  for Free". Same 141-partner "Manage Your Privacy" CMP modal on load, left
  unaccepted (privacy-preserving). No new features / design / content.
- Standing gaps where they lead: native/PWA download prominence, deck/background
  theming, illustrated face cards, large multi-game catalog framing, non-solitaire
  card games, single-surface mobile controls. We lead: editorial depth, richer
  per-page JSON-LD, spoke-domain SEO, no-signup local save, named daily deals,
  ad-light dark theme, PWA offline+resume, 6-locale FreeCell i18n, mobile
  next-action coaching.
- Logged to `docs/research/competitor-log.csv`. Competitor content treated as
  untrusted; DOM + text scanned, no embedded instructions acted upon.

## Phase 3 — SEO & schema spot-check

Three never-before-covered long-tail pages, one per site cluster (rotating the
long tail; distinct from the Friday scorecard canaries):

- **solitairestack.com/blog/best-solitaire-games-for-brain-training** (hub-owned)
  — 1 H1, meta 149 chars unique, self-canonical, JSON-LD Organization + WebSite
  + Article + BreadcrumbList, 9 internal / 4 cluster links. Clean.
- **playklondikeonline.com/blog/klondike-vegas-scoring-math** (klondike spoke) —
  1 H1, meta 153 chars unique, self-canonical, JSON-LD Organization + WebSite +
  Article + BreadcrumbList, 4 internal / 2 cluster links. Clean.
- **playspidersolitaireonline.com/blog/spider-solitaire-difficulty-guide**
  (spider spoke) — 1 H1, meta 143 chars unique, self-canonical, JSON-LD
  Organization + WebSite + Article + BreadcrumbList, 7 internal / 5 cluster
  links. Clean.

All three: exactly one H1, unique meta, sane self-canonical, full schema graph,
strong cluster interlinking. No SEO fixes required.

## Phase 4 — Improvements

Priority buckets: (1) scorecard ❌ — none; (2) today's QA bugs — none; (3) plan
units U5/U6 — took a same-run-safe polish slice; (4) competitor gaps — none new.

- **Lint-polish: removed a verified-dead unused `eslint-disable` directive** in
  `src/game/TestBridge.ts` (line 54, `// eslint-disable-next-line
  @typescript-eslint/no-explicit-any`). ESLint's own `reportUnusedDisableDirectives`
  flagged it — `no-explicit-any` is already off for this bridge file, so the
  directive suppressed nothing. Removal is behavior- and layout-neutral, adds no
  new route/type/locale.
- Validation: `npx tsc --noEmit` clean before + after; `npm run lint` 0 errors,
  warnings **412 → 411**, TestBridge.ts no longer flagged, no new warnings.

## Phase 5 — Report / ledger / push

- This report + `docs/analytics/mobile-viewport-audits/2026-08-22-live.{json,md}`
  (+ screenshots, dir gitignored) + `docs/research/competitor-log.csv` row +
  ledger row + the TestBridge.ts one-line fix, batched into a single push.
- Post-push verification: Vercel deploys READY on all 4 projects + GitHub CI
  green — recorded below.

## Remaining backlog

- **U6** portrait dead-space (45–51% on phone widths, 9 rows) — plan-gated;
  below-board next-action / compact secondary content is the standing lever.
- **U6** SSR `<html lang>` observation (shared root layout hard-codes `lang="en"`,
  hydration-corrects per locale) — plan-gated; a fix needs dynamic rendering or
  a locale route group.
- **U5** French-locale completeness — periodic re-verification; no regressions
  observed.
- Remaining lint warnings: 411 (0 errors). A few more verified-dead
  `eslint-disable` directives remain (WinScreen.tsx exhaustive-deps) as future
  same-run-safe polish candidates.
