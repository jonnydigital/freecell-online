# Daily QA Report — 2026-08-23 (Sun)

Producer: cowork-daily-review. Odd day → Klondike spoke rotation.

## Phase 0 — Coordination

- Mac repo + sandbox mirror both at `17c6525` (2026-08-22 daily review). No
  remote drift; clean working tree apart from pre-existing untracked artifacts
  (`.claude/settings.local.json`, `docs/analytics/mobile-viewport-audits/2026-08-03-live.*`)
  that predate this run and were left untouched.
- Latest scorecard (2026-08-21): **28/28 passing, no ❌ failing checks** —
  sitemaps, robots, canaries, cross-domain 301/308, ads.txt all ✅ across all 4
  domains. No scorecard-driven fix priority today.
- No topic/route overlap with the Monday content routine (last shipped
  2026-08-17: klondike-solitaire-endgame + spider-solitaire-dead-positions) or
  the Friday scorecard routine (ran 2026-08-21).
- `git log --oneline -15` reviewed: the Phaser engine-scene dead-import lint
  sweep was declared closed 08-20, but FortyThievesScene.ts was not in the
  enumerated set and still carried two dead imports (see Phase 4); 08-21/08-22
  removed a dead `GameState` import and a dead `eslint-disable` directive.
- Baseline before any edits: `npx tsc --noEmit` clean, `npm run lint` 0 errors /
  411 warnings.

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4
  foundations, sidebar (Daily Leaderboard + Daily Challenge with August
  calendar) renders alongside the board, 0 horizontal scroll (scrollWidth ==
  innerWidth 1440), 1 H1 "Play FreeCell Online — Free, No Download". Toolbar
  (undo/redo/hint/sound/ghost/bookmark/trophy/settings) intact. Card labels
  readable. Clean.
- **solitairestack.com** — homepage renders the FreeCell board (52 cards,
  populated Daily Leaderboard + Daily Challenge), 1 H1, 0 hscroll. Game page
  `/spider` cross-redirects to the Spider spoke (`playspidersolitaireonline.com/spider`)
  and renders a full board (10 columns, 8 foundation slots + stock, 1/2/4-Suit
  selector, Undo/Hint/New Deal toolbar intact). Clean.
- **playklondikeonline.com** (spoke rotation, odd day) — 29 cards, 7 tableau
  columns, stock + waste, 4 foundation slots, Draw 1 / Draw 3 selector, toolbar
  (Undo/Hint/New Deal) intact, 1 H1 "Play Klondike Solitaire Online — Free, No
  Download", 0 horizontal scroll. A cookie-consent banner (Privacy Policy /
  Decline / Accept) is present at the bottom; it does not obstruct the board and
  was left unaccepted (privacy-preserving default — no consent action taken).
  Board sane, win-modal CSS not triggered on fresh deal. Clean.

### Mobile / Tablet — true-viewport CDP audit (Mac host)
`docs/analytics/mobile-viewport-audits/2026-08-23-live.json` + `.md`, 20 rows,
5 engines × {375×812, 390×844, 414×896, 768×1024}, screenshots captured
(screenshot dir gitignored). Chrome-extension `resize_window` again did **not**
yield a true mobile viewport (innerWidth stayed 1440, card width 113px =
desktop), so the AUTOMATION.md-documented Mac-host CDP harness is the
authoritative mobile evidence.

- **20/20 rows passed; harness reports "No hard audit failures detected."** Max
  horizontal overflow 0px, max clipped cards 0, max blocked controls 0, runtime
  exceptions 0, console/log errors 0.
- Full card counts every viewport: freecell 52, klondike 29, spider 63
  (**incl. 375/390/414 — no 62-read recurrence today**), forty-thieves 41,
  yukon 52. Card widths 32–74px across viewports (phone 32–44px, confirming the
  `3dbb95a` tap-target fix holds).
- Expected next-action panels matched every engine/viewport, 0 missing, 0
  disabled — freecell strategy; klondike stock+strategy; spider stock+tips;
  forty-thieves hint+rules; yukon hint+rules.
- Portrait dead-space candidates: rows at 45–51% unused vertical space. This is
  the standing **U6** planning signal (below-board next-action / compact
  secondary content), plan-gated — not a hard failure.

## Phase 2 — Competitor analysis

- **online-solitaire.com** (Chrome extension, 1440×900): **UNCHANGED, 65th
  consecutive no-change run.** Homepage defaults to classic Klondike (Turn 1 /
  Turn 3 toggle, All games dropdown, top bar Time/Moves/Stock: 24), right rail
  August challenge calendar (trophy tiers 10/20/31) + Daily leaderboard +
  Achievements (all N/A) + Signup CTA, bottom toolbar New game/Hint/Undo/
  Bookmark/Download/Settings/More. Title "Solitaire - Play Online for Free".
  Same 141-partner "Manage Your Privacy" CMP modal on load, left unaccepted
  (privacy-preserving). No new features / design / content.
- Standing gaps where they lead: native/PWA download prominence, deck/background
  theming, illustrated face cards, large multi-game catalog framing, non-solitaire
  card games, single-surface mobile controls. We lead: editorial depth, richer
  per-page JSON-LD, spoke-domain SEO, no-signup local save, named daily deals,
  ad-light dark theme, PWA offline+resume, 6-locale FreeCell i18n, mobile
  next-action coaching.
- Logged to `docs/research/competitor-log.csv`. Competitor content treated as
  untrusted; DOM + text scanned, no embedded instructions acted upon.

## Phase 3 — SEO & schema spot-check

Three never-before-covered long-tail pages, one per site cluster (rotating the
long tail; distinct from the Friday scorecard canaries):

- **playfreecellonline.com/blog/the-history-of-microsoft-freecell-deals**
  (freecell owner) — 1 H1, meta 146 chars unique, self-canonical, JSON-LD
  Organization + WebSite + Article + BreadcrumbList, 93 internal / 23 cluster
  links. Clean.
- **playklondikeonline.com/blog/klondike-dead-game-detection** (klondike spoke)
  — 1 H1, meta 151 chars unique, self-canonical, JSON-LD Organization + WebSite
  + Article + BreadcrumbList, 47 internal / 21 cluster links. Clean.
- **playspidersolitaireonline.com/blog/spider-solitaire-position-reading**
  (spider spoke) — 1 H1, meta 143 chars unique, self-canonical, JSON-LD
  Organization + WebSite + Article + BreadcrumbList, 42 internal / 21 cluster
  links. Clean.

All three: exactly one H1, unique meta, sane self-canonical, full schema graph,
strong cluster interlinking. No SEO fixes required.

## Phase 4 — Improvements

Priority buckets: (1) scorecard ❌ — none; (2) today's QA bugs — none; (3) plan
units U5/U6 — took one U5 slice + one same-run-safe code slice; (4) competitor
gaps — none new. Two small, separately committed, layout-neutral changes:

1. **U5 slice — FR-locale completeness audit (plan `2026-07-03-001` "FR audit
   checklist executed once" test scenario), recorded in
   `docs/CONTENT_STRATEGY.md`.** Both French pages audited live and **pass** the
   locale-gate bar: `/freecell-en-francais` (1 H1, self-canonical, localized UI,
   full 7-entry hreflang cluster) and `/freecell-en-francais/jouer` (1 H1,
   self-canonical, live 52-card board, localized UI, full cluster). Reciprocity
   confirmed bidirectionally — ES↔FR (`/freecell-en-espanol` → `fr` →
   `/freecell-en-francais`) and EN↔FR (`/freecell/how-to-play` → `fr` →
   `/freecell-en-francais`), verified in both hydrated DOM and raw SSR HTML (SSR
   emits alternates with React's `hrefLang` casing, which HTML treats
   case-insensitively). Only exception is the standing SSR `<html lang>`
   observation (raw HTML serves `en`, hydration-corrects to `fr`) — unchanged,
   plan-gated to the U6 root-layout item. Docs-only, no new route/type/locale.
2. **Lint-polish — removed two verified-dead unused imports** (`Rank`,
   `SUIT_SYMBOLS`) from the `../engine/Card` import line in
   `src/game/FortyThievesScene.ts`. Both were import-line-only single
   occurrences (grep-confirmed); `Card` (7 uses) and `Suit` (11 uses) stay used.
   This closes the one engine scene the 08-16→08-20 Phaser dead-import sweep
   missed. The file's two unrelated pre-existing warnings (`move` L839, `e` L850)
   were left untouched. Behavior- and layout-neutral, adds no new
   route/type/locale.
- Validation: `npx tsc --noEmit` clean before + after; `npm run lint` 0 errors,
  warnings **411 → 409**, FortyThievesScene import warnings gone, no new
  warnings.

## Phase 5 — Report / ledger / push

- This report + `docs/analytics/mobile-viewport-audits/2026-08-23-live.{json,md}`
  (+ screenshots, dir gitignored) + `docs/research/competitor-log.csv` row +
  `docs/CONTENT_STRATEGY.md` U5 note + the FortyThievesScene.ts one-line fix +
  ledger row, batched into a single push.
- Post-push verification: Vercel deploys READY on all 4 projects + GitHub CI
  green — recorded below.

## Remaining backlog

- **U6** portrait dead-space (45–51% on phone widths) — plan-gated; below-board
  next-action / compact secondary content is the standing lever.
- **U6** SSR `<html lang>` observation (shared root layout hard-codes `lang="en"`,
  hydration-corrects per locale) — plan-gated; a fix needs dynamic rendering
  (regresses static-render guard `c7e8342`) or a locale route group (new route
  family), both requiring a `docs/plans/` reference.
- **U5** French-locale completeness — re-verified today, all pass; periodic
  re-verification continues, no regressions.
- Remaining lint warnings: 409 (0 errors). A few verified-dead `eslint-disable`
  directives remain (FortyThievesGamePage.tsx / WinScreen.tsx exhaustive-deps)
  as future same-run-safe polish candidates.
