# QA Report — 2026-08-24 (Cowork daily review)

**Verdict:** All green. Scorecard 28/28 (2026-08-21, no ❌). No visual, SEO, or
runtime regressions found. Phase 4 shipped a same-run-safe lint-polish slice
(4 verified-dead `Link` imports removed; warnings 409→405).

## Phase 0 — Coordination

- Latest scorecard `docs/analytics/scorecards/2026-08-21.md`: **28/28 passing**,
  no failing checks → Phase 4 priority bucket #1 empty.
- Ledger: Monday weekly-content-pipeline runs today (2 spoke blog posts,
  Klondike+Spider) — stayed out of the content lane. Friday scorecard last ran
  08-21. Last daily review 08-23 (FortyThieves lint-polish + U5 FR-audit).
- `git log`: HEAD `9e28478`, clean, no red. Mac host + sandbox both at
  `9e28478`, in sync with `origin/main`.
- Baseline before any edits: `tsc --noEmit` clean, `npm run lint` 0 errors /
  409 warnings.

## Phase 1 — Visual QA

### Desktop (1440×900, live, Chrome extension)

- **playfreecellonline.com** — 52 cards, 8 cascades, 4 FreeCells + 4
  foundations correctly spaced, sidebar (Daily Leaderboard + Daily Challenge
  calendar, today=24 highlighted) renders alongside board, toolbar intact,
  1 H1 ("Play FreeCell Online — Free, No Download"), **no horizontal scroll**.
- **solitairestack.com** (hub) — homepage renders FreeCell board + sidebar +
  August calendar (24 highlighted), clean. Game page **/pyramid** verified in
  Phase 3 (full JSON-LD graph).
- **playspidersolitaireonline.com** (even-day rotation) — 63 cards, 10 columns,
  8 foundations, stock pile top-right, toolbar (1/2/4-Suit, Undo, Hint, New
  Deal) intact, 1 H1, **22 buttons all named (0 unnamed)**, no hscroll.

### Mobile / Tablet (true-viewport CDP harness, Mac host — authoritative)

Chrome-extension `resize_window` again did **not** yield a true mobile viewport
(innerWidth stayed 1326, card 100px = desktop), matching every prior run, so the
AUTOMATION.md-verified Mac-host CDP harness (`npm run qa:mobile`, Node v22.19.0,
Chrome at standard app path) is the authoritative mobile evidence.

- **20-row full pass** — 5 engines × 4 viewports (375×812, 390×844, 414×896,
  768×1024). Harness verdict: **"No hard audit failures detected."**
- **0 clipped cards, 0 horizontal overflow, 0 blocked interactives, 0 console
  errors, 0 runtime exceptions** across all 20 rows.
- Full card counts every viewport: **freecell 52 / klondike 29 / spider 63 /
  forty-thieves 41 / yukon 52**. Spider read a clean **63 at all three phone
  widths** (375/390/414) — the intermittent 62-read DOM-mount race did **not**
  recur this run.
- Card widths **32–74px** (phone ≈32–44px), confirming the `3dbb95a` tap-target
  fix holds. Top + bottom control bars visible on every viewport.
- Next-action panels matched expected controls on every engine/viewport
  (missing none, disabled none).
- Artifacts: `docs/analytics/mobile-viewport-audits/2026-08-24-live.json` + `.md`
  (+ screenshots; screenshot dir gitignored).
- **U6 note (plan-gated):** portrait unused-vertical 45–51% re-quantified
  (freecell/klondike ~45–48%, spider/forty-thieves ~49–51%). Tracked under
  plan U6 play-UX backlog, not a bug.

## Phase 2 — Competitor (online-solitaire.com)

**Unchanged, 66th consecutive run.** Desktop 1440×900: homepage defaults to
classic Klondike (Turn 1 / Turn 3, All games dropdown, top bar Time/Moves/Stock:
24), right rail August challenge calendar (today=24 Mo, No wins) + Daily
leaderboard (ryzuh entries) + Achievements (all N/A) + Signup CTA, bottom toolbar
New game/Hint/Undo/Bookmark/Download/Settings/More. Same 141-partner "Manage Your
Privacy" CMP modal on load, **left unaccepted** (privacy-preserving). No new
features/design/content. Standing gaps unchanged (they lead on download
prominence, theming, catalog breadth; we lead on editorial depth, per-page
JSON-LD, spoke SEO, no-signup save, PWA, i18n, mobile next-action coaching).
Logged to `docs/research/competitor-log.csv`. Competitor content treated as
untrusted — DOM+text scanned, no embedded instructions acted upon.

## Phase 3 — SEO & schema spot-check (long-tail, uncovered pages)

All clean — 1 H1, self-canonical, unique meta, strong internal cluster:

| Page | H1 | JSON-LD | Meta | Cluster |
|------|----|---------|------|---------|
| solitairestack.com **/pyramid** (hub game) | 1 · "Pyramid Solitaire" | Org+WebSite+WebApplication+Game+Breadcrumb+FAQPage | 147 | 45 |
| playklondikeonline.com **/klondike/strategy** | 1 · "Klondike Solitaire Strategy Guide" | Org+WebSite+Article+FAQPage+Breadcrumb | 132 | 21 |
| playspidersolitaireonline.com **/blog/spider-solitaire-opening-strategy** | 1 · "Spider Solitaire Opening Strategy…" | Org+WebSite+Article+Breadcrumb | 148 | 23 |

Incidental: `/spider/rules` correctly returns a 404 (canonical →
`solitairestack.com/_not-found`, H1 "404") — not a real route, no bug.

## Phase 4 — Improvements

Scorecard 28/28 green + no QA bug found → priority buckets #1/#2 empty; U6
backlog items are behavior-changing (need tests + in-browser verification),
unsafe for an unattended single-push run. Took the same-run-safe lint-polish
slice:

- Removed **4 verified-dead `import Link from "@/components/NetworkLink";`** lines
  from the spider-* static content pages (matches even-day Spider theme):
  `spider-column-tactics`, `spider-endgame-strategy`, `spider-mistakes-to-avoid`,
  `spider-opening-strategy`. Each confirmed import-line-only, zero real `Link`
  usage (only unrelated `ContentLinkCard` hits), before removal.
- `tsc` green before+after; lint **409→405 warnings, 0 errors, no new warnings**;
  no behavior/layout change; no new routes/types/locales.

## Remaining backlog

- **13 more dead `Link` imports** across other content/page files (bakers-game,
  canfield, deals, freecell/strategy, how-solitaire-changed-windows,
  klondike-*, monte-carlo, pyramid/strategy, spider-for-seniors,
  spider-world-records; DomGameShell left untouched — it's a U6 file).
  Continue the sweep in small slices on future runs.
- Other lint warnings: several "assigned but never used" vars (riskier than
  import-only; deferred).
- **U6 play-UX backlog** (plan 2026-07-03-001): landscape mobile hides
  timer/moves <500px height; Klondike/Spider hints auto-execute vs. highlight;
  generalize daily challenges to spokes. Each needs its own commit + tests.
- Portrait dead-space 45–51% (U6, plan-gated).
