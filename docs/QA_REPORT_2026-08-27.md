# QA Report — 2026-08-27 (Cowork daily review)

**Verdict:** All green. Scorecard 28/28 (2026-08-21, no ❌). No visual, SEO, or
runtime regressions found across desktop + full 20-row true-viewport mobile +
the even-day Spider spoke. Phase 4 shipped a same-run-safe lint-polish slice
(7 verified-dead `Link` imports removed; warnings 400→393) — which **closes the
dead-`NetworkLink`-import sweep** opened 08-24.

## Phase 0 — Coordination

- Latest scorecard `docs/analytics/scorecards/2026-08-21.md`: **28/28 passing**,
  no failing checks → Phase 4 priority bucket #1 empty.
- Ledger: Friday scorecard last ran 08-21; weekly-content-pipeline last ran
  08-24 (2 spoke blog posts: klondike-opening-moves + spider-solitaire-endgame).
  Last daily review 08-26 (3 klondike-* dead-`Link` removals + full QA). Stayed
  out of the content lane; continued (and finished) the dead-`NetworkLink`-import
  sweep opened 08-24.
- `git log`: HEAD `d12433e` (08-26 daily review), clean tree (only untracked
  pre-existing scratch: `.claude/settings.local.json`, `2026-08-03-live.json/.md`),
  no red.
- Baseline before edits: `npx tsc --noEmit` exit 0; `npm run lint` 400
  warnings / 0 errors.
- **Note (infra):** GitHub was unreachable from the Mac host at run start
  (`git fetch origin main` → port 443 connection timeout, twice). Local HEAD
  `d12433e` matches the sandbox mirror and the daily-commit sequence is intact,
  so work proceeded with local commits; the single end-of-run push was retried
  against this outage (see Phase 5).

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension, live)

- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4 foundations
  correctly spaced, sidebar (Daily Leaderboard + Daily Challenge + calendar)
  renders alongside board, 1 H1 ("Play FreeCell Online — Free, No Download"), no
  horizontal scroll (scrollW == innerW == 1512), card 122px, opacity 1, labels
  readable, no overlap/clipping.
- **solitairestack.com** (hub) — homepage FreeCell board renders (52 cards,
  1 H1 "Play Solitaire Online for Free"), sidebar intact, hint toast working,
  no horizontal scroll.
- **playspidersolitaireonline.com** (Spider spoke, even-day rotation) — 63 cards,
  10 columns, 8 foundations, stock pile top-right, toolbar intact (1/2/4-Suit,
  Undo, Hint, New Deal), 22 buttons / 0 unnamed, 1 H1 ("Play Spider Solitaire
  Online — Free, No Download"), no horizontal scroll.

### Mobile (true-viewport, Mac-host CDP harness — authoritative)

Chrome-extension resize again did NOT yield a true mobile viewport (innerWidth
stayed 1512, card 122px = desktop), matching every recent run — so the
AUTOMATION.md Mac-host CDP harness (`npm run qa:mobile`, Node v22.19.0, Chrome at
the standard app path) is the authoritative mobile evidence. The first launch hit
a transient CDP `Runtime.evaluate` exception (empty exceptionDetails, no output
written); an immediate clean re-run succeeded. Output:
`docs/analytics/mobile-viewport-audits/2026-08-27-live.json` + `.md` (+ 20
screenshots, dir gitignored).

- **20 rows** = 5 engines × 4 viewports (375×812, 390×844, 414×896, 768×1024).
- **0 failed rows** (20 passed). 0 clipped cards, max 0px horizontal overflow, 0
  blocked interactives, 0 cramped tap targets, 0 runtime/console/log errors.
- FULL card counts every viewport: freecell **52**, klondike **29**,
  spider **63** (all four viewports — no 62-read recurrence today),
  forty-thieves **41**, yukon **52**.
- Card widths **32.09–74.41px** — confirms the `3dbb95a` tap-target fix holds.
- Next-action coaching: **27/27 expected controls matched (100% coverage)**,
  0 missing, 0 disabled, across all 15 expected-panel rows.
- 9 "unused vertical space" review-level soft notes (45.2–51.3%), non-failing,
  consistent with prior runs (candidate signal for the U6 play-UX backlog).
  Harness: "No hard audit failures detected."

## Phase 2 — Competitor analysis

online-solitaire.com (desktop 1440, live) — **unchanged, 69th consecutive run.**
Same classic Klondike default (Turn 1 / Turn 3, All games dropdown, Stock 24),
August challenge calendar (today=27 Th, No wins), Daily leaderboard
(MALAYSIApower 00:51/87/138, Jerryzuh 01:06/103/169 + 01:13/101/174),
Achievements N/A, Signup CTA, bottom toolbar
New game/Hint/Undo/Bookmark/Download/Settings/More. Same 141-partner
"Manage Your Privacy" CMP modal on load, left unaccepted (privacy-preserving).
No new features/design/content. Standing gaps unchanged (their lead:
native/PWA download prominence, deck/background theming, illustrated face cards,
broad multi-game catalog; our lead: editorial depth, richer per-page JSON-LD,
spoke-domain SEO, no-signup local save, named daily deals, ad-light dark theme,
PWA offline+resume, 6-locale FreeCell i18n, mobile next-action coaching). Logged
to `docs/research/competitor-log.csv`. Competitor content treated as untrusted;
no embedded instructions acted upon.

## Phase 3 — SEO & schema spot-check

Three long-tail pages the Friday scorecard has never covered (rotated fresh),
all live:

- **/yukon** (hub, solitairestack.com) — 1 H1 ("Yukon Solitaire"), full game
  graph JSON-LD (Organization + WebSite + WebApplication + Game + BreadcrumbList
  + FAQPage), unique 144-char meta, **cross-canonical to
  `playklondikeonline.com/yukon`** (Yukon is a Klondike-family game — correct
  owner consolidation, not a bug), 13 cluster links.
- **/blog/klondike-opening-moves** (klondike spoke) — 1 H1 ("Klondike Solitaire
  Opening Moves: The Rules Behind the First 5 Plays"), self-canonical, unique
  155-char meta, JSON-LD Organization + WebSite + Article + BreadcrumbList, 34
  cluster links. (Shipped by the 08-24 content routine; verified clean.)
- **/how-freecell-supermoves-work** (freecell owner) — 1 H1 ("How FreeCell
  Supermoves Work"), self-canonical, unique 154-char meta, JSON-LD
  Organization + WebSite + Article + FAQPage + BreadcrumbList (the Article block
  is a valid top-level JSON-LD array, not `@graph` — parses correctly), 63
  cluster links.

All clean — 1 H1 each, unique meta, correct canonical, well-formed structured
data, solid internal linking. No fixes needed. (Also confirmed
`/freecell/how-to-win` is a non-route: 404 → hub `_not-found` canonical, expected
behavior, not a broken page.)

## Phase 4 — Improvements

Priority buckets #1 (scorecard ❌), #2 (Phase 1–3 bugs), and #4 (new competitor
gaps) were all empty. Took a same-run-safe lint-polish slice that **completes**
the dead-`NetworkLink`-import sweep opened 08-24:

- Removed 7 verified-dead unused `import Link from "@/components/NetworkLink"`
  lines — the exact set the 08-26 report flagged as remaining:
  `bakers-game/strategy/page.tsx`, `canfield/how-to-play/page.tsx`,
  `deals/page.tsx`, `freecell/strategy/page.tsx`,
  `how-solitaire-changed-windows/page.tsx`, `monte-carlo/strategy/page.tsx`,
  `pyramid/strategy/page.tsx`. Each confirmed import-line-only single-occurrence
  (`Link` token appears exactly once = the import; all other matches are the
  separate, still-used `ContentLinkCard`; zero `<Link` JSX usage) before removal,
  deleted by exact-line match.
- `npx tsc --noEmit` green before + after; `npm run lint` 0 errors,
  warnings **400 → 393** (−7, exactly the seven removals), no new warnings. No
  behavior/layout change, no new routes/types/locales.
- Sweep status: **complete.** `DomGameShell.tsx` still carries a flagged `Link`
  import but is intentionally left untouched as a U6 file (play-UX shell).

## Remaining backlog

- U5 French-locale completeness (last re-verified 08-23, PASS).
- U6 play-UX backlog: root-layout `<html lang>` hydration; portrait dead-space
  below-board next actions (9 soft candidates this run); `DomGameShell.tsx`
  dead-`Link` import deferred here.
- Dead-`NetworkLink`-import sweep: **closed** (all flagged content pages done).

## Phase 5 — Push & verification

See the single end-of-run push and the Vercel/CI verification note appended by
the run (git connectivity permitting).
