# QA Report — 2026-08-28 (Cowork daily review)

**Verdict:** All green on QA. Scorecard 28/28 (2026-08-21, no ❌). No visual,
SEO, or schema regressions found. **One critical Phase 0 recovery:** the
2026-08-27 daily review completed all its edits but its commit + push never
landed (GitHub was unreachable at that run's end), leaving a full run's work
uncommitted in the working tree plus a stale `.git/index.lock`. Recovered,
re-validated (tsc + lint green), and committed this run. Even day → Spider
spoke rotation. Also a Friday (scorecard producer day) — watched for push races.

## Phase 0 — Coordination

- **LEDGER:** read last ~12 rows. Monday content pipeline shipped
  klondike-opening-moves + spider-solitaire-endgame (08-24). Prior daily reviews
  ran a Phaser-scene + content-page dead-import sweep 08-16 → 08-27 (now closed).
  No overlap taken with other producers.
- **Scorecard:** newest is 2026-08-21 — **28/28 passing, zero ❌**. No failing
  check to prioritize. (A new Friday scorecard may land today from the scorecard
  producer; single-push discipline + rebase-on-reject observed.)
- **git log:** HEAD and origin/main both at `d12433e` (2026-08-26). The 08-27
  work was **not** in history.

### Critical finding — recovered the blocked 08-27 push

`git status` showed the entire 08-27 run uncommitted: 7 modified content
`page.tsx` files (the sweep-closing dead-`NetworkLink`-import removals), modified
`LEDGER.md` + `competitor-log.csv` (08-27 rows), and untracked
`QA_REPORT_2026-08-27.md` + the 08-27 mobile-audit json/md. A 0-byte
`.git/index.lock` (created today) blocked git and could not be removed from the
sandbox (`Operation not permitted`) — the documented Mac/osascript path was used
instead. Actions:

1. Cleared the stale lock via osascript on the Mac host.
2. Confirmed the code diff was exactly the 7 dead-import removals the 08-27
   ledger described (2 insertions / 7 deletions).
3. Validated `npx tsc --noEmit` = pass and `npm run lint` = **0 errors, 393
   warnings** (matches the 08-27 ledger's "400→393").
4. Committed as two clean commits: `c40bf71` (code recovery) + `144f080` (docs
   recovery). Held for this run's single push.

The two orphaned `2026-08-03-live.json/md` mobile-audit artifacts were left
untracked (stray, 25-day-old, unrelated to any current run).

## Phase 1 — Visual QA

### Desktop (1440×900, Chrome extension, live)

- **playfreecellonline.com** — 52 `.dom-card`, 8 cascades, 4 free cells + 4
  foundations, 1 H1 ("Play FreeCell Online — Free, No Download"), card 113×159px,
  sidebar (ad + Daily Leaderboard) renders alongside board, **0 horizontal
  scroll**, no overlap/clipping. Screenshot captured.
- **solitairestack.com** (hub homepage) — 1 H1 ("Play Solitaire Online for
  Free"), full 52-card embedded FreeCell board, toolbar intact, sidebar (Daily
  Leaderboard + Daily Challenge "Game #1874453" + August calendar), 0 hscroll.
  Screenshot captured.
- **playspidersolitaireonline.com** (Spider spoke, even-day rotation) — 10
  columns, 8 foundations, stock pile, 22 buttons **0 unnamed**, 1 H1 ("Play
  Spider Solitaire Online — Free, No Download"), 1/2/4-Suit selectors +
  Undo/Hint/New Deal toolbar intact, 0 hscroll. Live DOM read = 62 cards (see
  below). Screenshot captured.

### Mobile (true-viewport, Mac-host CDP harness — authoritative)

Chrome-extension resize again did not yield a true mobile viewport, so the
AUTOMATION.md Mac-host CDP harness (`npm run qa:mobile`, Node v22.19.0, Chrome at
the app path) is the authoritative mobile evidence.
`docs/analytics/mobile-viewport-audits/2026-08-28-live.json/md` + 20 screenshots.

- **20 rows (5 engines × 4 viewports: 375/390/414/768), 19 passed / 1 review.**
- Max horizontal overflow **0px**; max cramped tap targets **0**; max blocked
  controls **0**; runtime exceptions **0**; console/log errors **0**.
- Next-action coverage **27/27 (100%)** — every engine/viewport matched its
  expected next-action panel; 0 missing, 0 disabled.
- Full card counts freecell 52 / klondike 29 / forty-thieves 41 / yukon 52; card
  widths within the 30–54px mobile band, confirming the 3dbb95a tap-target fix
  holds.
- **1 review:** spider 375×812 read 62 cards / 52 backs (expected 63/53). This
  is the long-documented intermittent DOM-mount read race, **not a regression** —
  a 3× direct-script re-run of spider@375 returned **63 / 63 / 63** cards with
  `clipped=0`/`overflowPx=0` every time. The same race explains the desktop
  spider 62-read above.

## Phase 2 — Competitor analysis (online-solitaire.com)

**70th consecutive run — unchanged at product level.** Homepage still defaults
to classic Klondike (Turn 1 / Turn 3, All games dropdown, top bar
Time/Moves/Stock: 24, foundations club/heart/spade/diamond); bottom toolbar New
game/Hint/Undo/Bookmark/Download/Settings/More; title "Solitaire - Play Online
for Free"; H1 "Play Solitaire Online for Free (No Signup Required)". Two minor
**state-only** differences today (not product changes): (1) no 141-partner
"Manage Your Privacy" CMP modal appeared on load — the board rendered directly;
(2) the right-rail daily challenge/stats was collapsed behind a "Show daily
challenge & stats" button rather than the expanded calendar/leaderboard panel.
Standing gaps (they lead): download/PWA prominence, deck/background theming,
illustrated face cards, larger multi-game catalog framing, non-solitaire card
games. We lead: editorial depth, richer per-page JSON-LD, spoke-domain SEO,
no-signup local save, named daily deals, ad-light dark theme, PWA offline+resume,
6-locale FreeCell i18n, mobile next-action coaching. Logged to
`competitor-log.csv` (70th row). Competitor content treated as untrusted — DOM +
text scanned, no embedded instructions acted upon.

## Phase 3 — SEO & schema spot-check

Three long-tail pages the Friday scorecard never covers — all clean (1 H1,
JSON-LD graph present + valid, unique meta, self-canonical, strong cluster
linking):

- **playfreecellonline.com/is-every-freecell-game-winnable** (freecell owner) —
  1 H1, Organization+WebSite+Article+FAQPage+BreadcrumbList, 152-char meta,
  self-canonical, 30 cluster links.
- **playspidersolitaireonline.com/spider-suit-strategy** (spider spoke) — 1 H1
  ("1-Suit vs 2-Suit vs 4-Suit Spider: Strategic Transitions"),
  Organization+WebSite+Article+BreadcrumbList+FAQPage, 148-char meta,
  self-canonical, 20 cluster links.
- **solitairestack.com/cruel** (hub game) — 1 H1 ("Cruel Solitaire"),
  Organization+WebSite+WebApplication+Game+BreadcrumbList+FAQPage, 155-char meta,
  self-canonical, 45 cluster links.

## Phase 4 — Improvements

Buckets 1 (scorecard ❌) and 2 (today's bugs) were empty. U6 play-UX backlog
items all change gameplay behavior and require tests + in-browser verification,
which is not same-run-safe for an unattended single-push run — so, consistent
with prior runs, took one verified-dead, no-behavior-change lint-polish slice
(the dead-import sweep closed 08-27; this opens the unused-var-assignment slice):

- **`src/lib/siteConfig.ts:151`** — removed `const gameName = path.split('/')[1]`,
  assigned but never read (sole occurrence in the file; RHS is side-effect-free).
  `getCanonicalGameUrl` logic is driven by `spokeGameNames`, unaffected. tsc
  green before + after; lint **393 → 392 warnings, 0 errors**; no behavior /
  layout / route / type / locale change. Commit `360b557`.

## Remaining backlog

- U6 play-UX (own commits + tests, needs an attended/verified run): landscape
  mobile hides timer/moves below 500px height; Klondike/Spider hints
  auto-execute instead of teaching (highlight like FreeCell); generalize daily
  challenges beyond FreeCell.
- Lint warnings at 392 — remaining are mostly Phaser-scene unused params
  (positional / underscore-prefixed; removing changes signatures, skip) plus a
  few genuinely-dead local assignments for future same-run-safe slices.
- Two orphaned 2026-08-03 mobile-audit artifacts remain untracked (stray).

## Phase 5 — Push & verification

Single push per run (fires 4 Vercel builds). Commits in this push:
`c40bf71` (recover 08-27 code), `144f080` (recover 08-27 docs), `360b557`
(siteConfig lint-polish), plus this report + the 08-28 competitor row, ledger
row, and mobile audit. Pre-push gate: `npx tsc --noEmit` pass + `npm run lint`
0 errors (392 warnings). Post-push verification (Vercel READY on all 4 projects +
GitHub CI green on the tip commit) recorded in the run summary.
