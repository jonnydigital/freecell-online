# QA Report — 2026-09-01 (Cowork daily review)

**Result: all green. No bugs found. One lint-polish code slice shipped.**

Repo synced to `fdb6cbb` at run start (fast-forwarded past the Monday content
routine's `klondike-sequence-building` + `spider-solitaire-1-suit-strategy`
spoke posts — no overlap taken). Latest scorecard `2026-08-28` = 28/28 passing,
so no ❌ fix priority. Spoke rotation today (odd day) = **Klondike**.

## Phase 1 — Visual QA

### Desktop (Chrome extension, live production)
- **playfreecellonline.com** `/game/1` — 52 cards, 8 cascades, 4 free cells,
  4 foundations, 1 H1 ("Play FreeCell Online — Free, No Download"), no
  horizontal scroll, sidebar toggle present, toolbar intact. ✅
- **solitairestack.com** (hub) — homepage renders the FreeCell board (52 cards,
  8 cols, 4 FC / 4 foundations), 1 H1, 138 links, no h-scroll. ✅
- **playklondikeonline.com** (Klondike spoke, odd-day rotation) — 29 cards,
  7 tableau columns, 4 foundations, stock+waste, Draw 1 / Draw 3 toggle,
  22 buttons / 0 unnamed, 1 H1, no h-scroll. Cookie-consent banner present at
  the bottom (Decline / Accept), non-obstructing, left unaccepted. ✅

### Mobile + tablet (Mac-host CDP harness, true viewport)
`npm run qa:mobile --base=https://playfreecellonline.com --screenshots`
→ `docs/analytics/mobile-viewport-audits/2026-09-01-live.json` + `.md`
(+ 20 screenshots, dir gitignored). **"No hard audit failures detected."**

5 engines × 375 / 390 / 414 / 768, full card counts every viewport, scrollWidth
== viewport width every row (0 horizontal overflow), 0 clipped:

| engine | cards (all 4 vw) | card width range |
|--------|------------------|------------------|
| freecell | 52 | 39.25–74.41px |
| klondike | 29 | 39.25–74.41px |
| spider | 63 (no 62-read recurrence) | 32.09–61.28px |
| forty-thieves | 41 | 32.09–62.06px |
| yukon | 52 | 39.25–74.41px |

Phone-width card widths land in the 32–44px tap-target band (confirms the
`3dbb95a` tap-target fix still holds); tablet 768 widths ~61–74px. Next-action
health: matched every engine/viewport, none missing, none disabled. Portrait
dead-space candidates (45%+ unused vertical below the board) logged as a U6
planning signal, not a failure. Chrome-extension resize again did not yield a
true mobile viewport (innerWidth stayed 806 on the 1440 window), so the Mac-host
harness remains the authoritative mobile evidence per AUTOMATION.md.

## Phase 2 — Competitor (online-solitaire.com)

Unchanged **71st consecutive run**. Classic Klondike default (Turn 1 / Turn 3,
All games, Time/Moves/Stock 24), toolbar New game/Hint/Undo + Download CTA/
Settings/More, right ad rail. Title "Solitaire - Play Online for Free", H1 "Play
Solitaire Online for Free (No Signup Required)". CMP dialog present today
("Do not sell or share my personal information" + "Terms of Content Use"), board
rendered behind it, left unaccepted. No new features/design/content. Logged to
`docs/research/competitor-log.csv`. (Competitor content treated as untrusted;
DOM+text scanned only, no embedded instructions acted upon.)

## Phase 3 — SEO / schema spot-check (3 fresh long-tail pages)

- **solitairestack.com/scorpion** → 308 cross-canonical to
  `playspidersolitaireonline.com/scorpion` (spider spoke owns the Scorpion
  variant); self-canonical there, 1 H1, 147-char meta,
  Org+WebSite+WebApplication+Game+BreadcrumbList+FAQPage, 10 cluster links. ✅
- **playklondikeonline.com/blog/klondike-sequence-building** (Monday-shipped) —
  1 H1, 152-char meta, self-canonical, Org+WebSite+Article+BreadcrumbList,
  23 cluster links. ✅
- **playspidersolitaireonline.com/blog/spider-solitaire-1-suit-strategy**
  (Monday-shipped) — 1 H1, 157-char meta, self-canonical,
  Org+WebSite+Article+BreadcrumbList, 19 cluster links. ✅

All clean: one H1 each, unique meta, sane canonical, valid JSON-LD.

## Phase 4 — Improvement shipped (1)

Scorecard green + no QA bug found, so took a same-run-safe lint-polish slice on
today's rotation engine:

- `src/game/KlondikeScene.ts` — `handleDrawFromStock()` bound
  `const move = this.engine.drawFromStock()` but never read `move`. The RHS
  mutates engine state (draws from stock), so the call was **kept** and only the
  dead binding dropped (`this.engine.drawFromStock();`). Verified `move` had no
  other use in scope (the other "move" tokens are the unrelated `moveExecuted`
  emit + `getMoveCount()`). tsc green before+after; lint 392 → 391 warnings,
  0 errors, no new warnings; no behavior/layout change; no new
  routes/types/locales. Commit `194a523`.

## Remaining backlog

- U6 play-UX: portrait below-board dead-space (45–51% on spider/forty-thieves
  phone widths) remains an open enhancement candidate.
- Unused-var-assignment lint-polish continues (~a handful of verified-safe
  `no-unused-vars` warnings remain, e.g. PenguinScene `overlap`, KlondikeScene
  `e`); take one small verified slice per run.
- Competitor gaps unchanged (deck/background theming, illustrated face cards,
  download prominence) — none pursued this run.
