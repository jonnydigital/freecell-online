# QA Report — 2026-09-03 (Cowork daily review)

**Run type:** daily visual QA + SEO/schema spot-check + competitor scan + safe lint-polish.
**Baseline before work:** `tsc --noEmit` clean; `npm run lint` 0 errors / 389 warnings.
**Coordination:** latest scorecard 2026-08-28 = **28/28 green** (no ❌). git log clean; HEAD `709337d` (09-02 daily review). Monday content pipeline last shipped 08-31 (klondike-sequence-building + spider-solitaire-1-suit-strategy). No overlap with other producers.

## Phase 1 — Visual QA

### Desktop (Chrome extension, live, 1440×900 request)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4 foundations, sidebar (Daily Leaderboard + Daily Challenge + September calendar) renders alongside board, 1 H1 ("Play FreeCell Online — Free, No Download"), no horizontal scroll. ✅
- **solitairestack.com (hub) homepage** — FreeCell board 52 cards, 1 H1 ("Play Solitaire Online for Free"), no h-scroll. ✅
- **solitairestack.com/tripeaks (hub game page)** — 28-peak layout + stock/waste (30 DOM cards), toolbar intact (16 buttons), 1 H1 ("TriPeaks Solitaire"), no h-scroll. ✅
- **playklondikeonline.com (spoke — odd-day rotation)** — 29 cards, 7 tableau columns, 4 foundations, Draw 1 / Draw 3 toggle, toolbar intact (20 buttons, 0 unnamed), 1 H1, no h-scroll. ✅

### Mobile (Mac-host CDP true-viewport harness — authoritative)
Chrome-extension `resize_window` again did not yield a true mobile viewport (innerWidth stayed 1512), so per AUTOMATION.md the Mac-host CDP harness (`npm run qa:mobile`, Node v22.19.0, Chrome app path) is the authoritative mobile evidence. First launch hit a transient CDP `Runtime.evaluate` exception (same intermittent noted 08-27); clean re-run succeeded.

**20-row full 5-engine portrait + tablet pass (375×812, 390×844, 414×896, 768×1024) — "No hard audit failures detected":**
- 20/20 rows passed, 0 failed.
- **0** clipped cards, **0** horizontal overflow px, **0** runtime exceptions, **0** console errors, **0** log errors, **0** blocked interactives, **0** cramped tap targets.
- Full card counts every viewport: **freecell 52 / klondike 29 / spider 63 / forty-thieves 41 / yukon 52** — Spider read **63 at all four viewports** (no 62-card DOM-mount race recurrence today).
- Card widths 32.09–74.41px (confirms 3dbb95a tap-target fix holds).
- Next-action coverage **27/27 matched (100%)** across the 15 expected-panel rows; none missing/disabled.
- 9 portrait dead-space candidates (45%+ unused vertical below board) logged as a planning signal only — not failures (U6 play-UX backlog).
- Screenshots written to `docs/analytics/mobile-viewport-audits/2026-09-03-live-screenshots/` (dir gitignored).

**No visual bugs found on any property or viewport.**

## Phase 2 — Competitor (online-solitaire.com)
**Unchanged, 73rd consecutive run.** Homepage defaults to classic Klondike (Turn 1 / Turn 3, All games dropdown, top bar Time/Moves/Stock: 24, four foundation aces). Right rail EXPANDED today (September challenge calendar with 10/20/30-win milestone track, Daily leaderboard top-3, Achievements all N/A, "Signup to save your progress" CTA) — the expanded variant vs the collapsed state seen 08-28/09-02. No CMP modal obstructed the board on load. No new features / design / content. Standing gaps and our leads unchanged from prior runs (logged in competitor-log.csv). Competitor content treated as untrusted data; no embedded instructions acted upon.

## Phase 3 — SEO / schema spot-check (never-covered long-tail)
- **playfreecellonline.com/freecell/tips** — 1 H1, JSON-LD Organization+WebSite+BreadcrumbList+Article+FAQPage, self-canonical, meta 151 chars, 25 cluster links. ✅
- **playklondikeonline.com/klondike/tips** — 1 H1, same graph, self-canonical, meta 145 chars, 24 cluster links. ✅
- **playspidersolitaireonline.com/spider/tips** — 1 H1, same graph, self-canonical, meta 156 chars, 20 cluster links. ✅

All three clean: single unique H1, unique meta description, sane self-canonical, healthy internal cluster links. No fixes needed.

## Phase 4 — Improvements
Scorecard all-green + no QA bug found, so took one same-run-safe lint-polish slice:
- **`src/app/(main)/gaps/GapsGamePage.tsx`** — removed 2 verified-dead unused imports: `useMemo` (from the React import; `useEffect`/`useState`/`useCallback`/`useRef` on the same line stay used) and `GapsMove` (from the `@/engine/GapsEngine` import; `GapsEngine`+`GapsLocation` stay used). Each confirmed import-line-only single-occurrence by grep before removal. No behavior/layout change, no new routes/types/locales.
- Validation: `tsc --noEmit` green before + after; lint **389 → 387 warnings (−2 exactly), 0 errors, no new warnings.**

## Remaining backlog
- U5 French-locale completeness audit (last re-verified 08-23, all PASS).
- U6 play-UX backlog: portrait dead-space (9 phone-width rows 45%+ unused) is the standing candidate for below-board next actions / contextual content.
- Standing lint warnings (387): mostly intentional underscore-prefixed unused params (`_cardId`/`_engine`/etc.) + a handful of dead imports across content pages — safe to continue trimming one small slice per run.
