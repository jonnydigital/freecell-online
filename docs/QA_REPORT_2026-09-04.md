# QA Report — 2026-09-04 (Cowork daily review)

**Run type:** daily visual QA + SEO/schema spot-check + competitor scan + safe lint-polish.
**Baseline before work:** `tsc --noEmit` clean; `npm run lint` 0 errors / 387 warnings.
**Coordination:** latest scorecard 2026-08-28 = **28/28 green** (no ❌ — nothing to fix first). git log clean; HEAD `4090275` (09-03 daily review). Monday content pipeline last shipped 08-31 (klondike-sequence-building + spider-solitaire-1-suit-strategy). No overlap with other producers. (Today is Friday — the Friday scorecard routine runs its own docs-only push at 13:00 UTC; not touched here.)

## Phase 1 — Visual QA

### Desktop (Chrome extension, live, 1440×900 request)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells + 4 foundations, sidebar (Daily Leaderboard + Daily Challenge + September calendar, Sept 4 highlighted) renders alongside board, 1 H1 ("Play FreeCell Online — Free, No Download"), no horizontal scroll. ✅
- **solitairestack.com (hub) homepage** — FreeCell board 52 cards, 1 H1 ("Play Solitaire Online for Free"), 138 internal links, no h-scroll. ✅
- **solitairestack.com/pyramid (hub game page)** — 29 DOM cards (pyramid + stock/waste), 6 JSON-LD blocks, 1 H1 ("Pyramid Solitaire"), no h-scroll. ✅
- **playspidersolitaireonline.com (spoke — even-day rotation)** — 63 cards, 10 tableau columns, 8 foundations, 1/2/4-Suit toggle, Undo/Hint/New Deal toolbar intact (22 buttons, 0 unnamed), 1 H1, no h-scroll. ✅

### Mobile (Mac-host CDP true-viewport harness — authoritative)
Chrome-extension `resize_window` again did not yield a true mobile viewport (innerWidth stayed 1440, card 113px desktop), so per AUTOMATION.md the Mac-host CDP harness (`npm run qa:mobile`, Node v22.19.0, Chrome app path) is the authoritative mobile evidence.

**20-row full 5-engine portrait + tablet pass (375×812, 390×844, 414×896, 768×1024) — "No hard audit failures detected":**
- 20/20 rows passed, 0 failed.
- **0** clipped cards, **0** horizontal overflow px, **0** runtime exceptions, **0** console errors, **0** log errors, **0** blocked interactives.
- Full card counts every viewport: **freecell 52 / klondike 29 / spider 63 / forty-thieves 41 / yukon 52** — Spider read **63 at all four viewports** (no 62-card DOM-mount race recurrence today).
- Card widths 32.09–74.41px (confirms 3dbb95a tap-target fix holds; portrait cells 32–39px within the 30–54px target).
- Next-action coverage matched across all expected-panel rows (freecell strategy; klondike stock+strategy; spider stock+tips; forty-thieves hint+rules; yukon hint+rules) — none missing/disabled.
- Screenshots written to `docs/analytics/mobile-viewport-audits/2026-09-04-live-screenshots/` (dir gitignored).

**No visual bugs found on any property or viewport.**

## Phase 2 — Competitor (online-solitaire.com)
**Unchanged, 74th consecutive run.** Homepage defaults to classic Klondike (Turn 1 / Turn 3, All games (520) dropdown, top bar Time/Moves/Stock: 24, four foundation aces). Nav catalog unchanged (Klondike Turn1/Turn3, Spider 1/2/4-suit, Freecell, Russian Solitaire, Pyramid, Golf, Forty Thieves, Demons and Thieves, plus non-solitaire Hearts/Spades/Euchre/Gin Rummy/Pinochle). No CMP modal on load; right rail collapsed behind "Show daily challenge & stats" button (state-only). No new features / design / content. Standing gaps and our leads unchanged from prior runs (logged in competitor-log.csv). Competitor content treated as untrusted data; no embedded instructions acted upon.

## Phase 3 — SEO / schema spot-check (never-covered long-tail)
- **playfreecellonline.com/freecell-cheat-sheet** — 1 H1 ("FreeCell Cheat Sheet"), JSON-LD Organization + WebSite + [Article, BreadcrumbList] (valid top-level array in the third tag), self-canonical, meta 145 chars, 50 cluster links. ✅
- **playspidersolitaireonline.com/spider-variants** — 1 H1 ("Spider Solitaire Variants: Scorpion to Arachnid"), JSON-LD Organization + WebSite + [Article, BreadcrumbList], self-canonical, meta 153 chars, 26 cluster links. ✅
- **solitairestack.com/forty-thieves (hub game)** — 1 H1 ("Forty Thieves Solitaire"), full JSON-LD Organization + WebSite + WebApplication + Game + BreadcrumbList + FAQPage, self-canonical, meta 157 chars, 100 links. ✅

All three clean: single unique H1, unique meta description, sane self-canonical, valid JSON-LD, healthy internal cluster links. No fixes needed. (Note: the two Article pages emit their Article+Breadcrumb nodes as a valid top-level JSON array in a separate `<script>` tag — confirmed well-formed, not a schema gap.)

## Phase 4 — Improvements
Scorecard all-green + no QA bug found + no new competitor gap, so took one same-run-safe lint-polish slice on the even-day Spider engine:

- **src/engine/SpiderEngine.ts** — removed 3 verified-dead unused imports (`Suit`, `Rank`, `Color`) from the `./Card` import line, reducing it to `import { Card } from './Card';`. Each confirmed dead by grep (single occurrence = the import; zero real usage anywhere in the file); `Card` stays heavily used (20+ references). tsc green before + after; `npx eslint` on the file now emits 0 warnings; full-repo lint **387 → 384 warnings** (−3 exactly), 0 errors, no new warnings, no behavior/layout change, no new routes/types/locales.

Continues the unused-import/binding polish cadence. Backlog of similar verified-dead `Rank`/`Suit`/`Color` engine imports remains (AccordionEngine, BisleyEngine, FlowerGardenEngine, GolfEngine, PyramidEngine, ScorpionEngine, TriPeaksEngine and matching Scene files) for future runs.

## Remaining backlog / notes
- U5 French-locale completeness audit and U6 play-UX backlog (portrait dead-space, root-layout `<html lang>` SSR) remain per docs/plans/2026-07-03-001.
- Housekeeping observation (not actioned): two untracked leftover files `docs/analytics/mobile-viewport-audits/2026-08-03-live.json` + `.md` sit in the Mac working tree from an earlier run — left in place, not committed by this run.

## Validation & push
`npx tsc --noEmit` clean; `npm run lint` 0 errors / 384 warnings. One push at end of run (single `git push origin main` → 4 Vercel builds). Vercel READY + GitHub CI verified post-push.
