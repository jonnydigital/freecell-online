# QA Report — 2026-09-05 (Saturday, odd-day → Klondike spoke rotation)

Producer: cowork-daily-review. Scope: visual QA sweep, competitor check, SEO/schema
spot-check, one same-run-safe fix. One push at end of run.

## Phase 0 — Coordination

- **AUTOMATION.md / LEDGER**: read. Recent producers — Monday content routine shipped
  `klondike-sequence-building` + `spider-solitaire-1-suit-strategy` (08-31); Friday
  scorecard shipped `2026-09-04.md`; prior daily reviews ran the unused-import polish
  cadence (last: SpiderEngine Suit/Rank/Color, 09-04). No overlap with today's work.
- **Newest scorecard** (`2026-09-04.md`): **28/28 checks passing, all green.** No ❌
  FAILING check → priority bucket 1 empty.
- **git log**: on `main` @ 6ac3267 at run start; nothing unexpected landed.
- **Working tree**: clean (only gitignored `.claude/settings.local.json` + two stale
  untracked `2026-08-03` mobile-audit files from a prior run, left untouched — not mine).
- **Baseline**: `npx tsc --noEmit` exit 0; `npm run lint` 384 warnings / **0 errors**.

## Phase 1 — Visual QA

Chrome extension was **offline** this run; used the built-in browser pane for desktop
QA and the AUTOMATION.md **Mac-host CDP harness** for authoritative true-viewport mobile
QA (the documented, verified path — Chrome-extension resize never yields a true mobile
viewport anyway).

### Desktop (1440×900, built-in browser pane, live production)

| Target | Result |
|---|---|
| **playfreecellonline.com** | 52 cards, 8 cascades, 4 free cells, 4 foundations, 1 H1, sidebar renders (Daily Leaderboard + Daily Challenge, today "5" highlighted in Sept calendar), **0 clipped, no h-scroll**. Welcome walkthrough modal + cookie banner = normal states (banner left unaccepted per privacy). |
| **solitairestack.com (hub home)** | 52 cards, 1 H1 "Play Solitaire Online for Free", 140 links, 6 JSON-LD blocks, no h-scroll. |
| **solitairestack.com/golf (hub game)** | 37 cards, 1 H1, 6 JSON-LD, 10 buttons (0 unnamed), toolbar intact, no h-scroll. |
| **playklondikeonline.com (spoke, odd-day rotation)** | 29 cards, 7 columns, 4 foundations, Draw1/Draw3 toggle, 22 buttons (0 unnamed), 0 clipped, no h-scroll, toolbar (undo/hint/New Deal) intact, 1 H1. |

### Mobile + tablet (Mac-host CDP harness — 5 engines × 375 / 390 / 414 / 768)

- **20/20 rows: "No hard audit failures detected."**
- Full card counts every viewport: **freecell 52, klondike 29, spider 63, forty-thieves 41,
  yukon 52** — spider 63 at all four viewports incl. 375 (no 62 intermittent DOM-mount read).
- **0 clipped, 0 horizontal overflow, 0 cramped tap targets** across all rows.
- Card width range **32.09–74.41px** — confirms the 3dbb95a tap-target fix holds.
- Top controls visible 20/20; next-action expectation misses **0/20**; 0 blocked interactive.
- **0 runtime exceptions, 0 console errors, 0 log errors.**
- Informational (non-failing, unchanged from prior runs): `smallTapTarget` count 183 and
  `bottomControlsVisible` 9/20 (bottom bar below initial fold on tall portrait viewports,
  reachable by scroll), plus 48–51% "unused vertical space (review)" — these are the known
  U6 dead-space/landscape-toolbar planning signals, not regressions.
- Artifacts: `docs/analytics/mobile-viewport-audits/2026-09-05-live.json` + `.md`
  (20 rows + 20 screenshots; screenshot dir gitignored).

## Phase 2 — Competitor (online-solitaire.com)

**Unchanged — 75th consecutive run.** Same H1 "Play Solitaire Online for Free (No Signup
Required)", same nav catalog (Klondike Turn1/Turn3, Spider 1/2/4-suit, Freecell, Russian
Solitaire, Pyramid, Golf, Forty Thieves, Demons and Thieves, "All games (520)", plus
non-solitaire Hearts/Spades/Euchre/Gin Rummy/Pinochle/Canasta/2048), same Download CTA,
CMP present on load (left unaccepted). No new features/design/content vs prior runs.
Standing gaps where they lead (native/PWA download prominence, deck/background theming,
illustrated face cards, 520-game catalog framing, non-solitaire card games) are
longstanding, not new. Logged to `docs/research/competitor-log.csv`.

## Phase 3 — SEO & schema spot-check (3 fresh long-tail pages, never previously covered)

| Page | H1 | Canonical | Meta | JSON-LD | Cluster |
|---|---|---|---|---|---|
| `playfreecellonline.com/freecell-for-beginners` (freecell owner) | 1 | self | 146 | Org+WebSite+Article+BreadcrumbList+**HowTo** | 34 |
| `playklondikeonline.com/klondike-fewest-moves` (klondike spoke) | 1 | self | 151 | Org+WebSite+Article+FAQPage+BreadcrumbList | 21 |
| `solitairestack.com/beleaguered-castle` (hub game) | 1 | self | 158 | Org+WebSite+WebApplication+Game+BreadcrumbList+FAQPage | 45 |

All clean: one H1 each, self-canonical, unique meta descriptions, appropriate JSON-LD
graph per page type, healthy cluster interlinking. **No SEO issues found.**

## Phase 4 — Improvement shipped (1 slice)

Priority buckets 1 (failing checks) and 2 (bugs) were empty, so took a same-run-safe
lint-polish slice aligned to today's Klondike rotation:

- **`src/engine/KlondikeEngine.ts`** — removed the verified-dead unused `Color` import
  from the `./Card` import line (`import { Card, Suit, Rank, Color }` →
  `import { Card, Suit, Rank }`). `Color` was single-occurrence (import line only, grep
  count 1 → 0 after removal); `Card` (16 uses), `Suit` (12), `Rank` (3) all stay used.
  tsc green before + after; lint **384 → 383 warnings, 0 errors**, no new warnings; no
  behavior/layout change; no new routes/types/locales. Commit `ea7b4a3`.
  (Continues the unused-import polish cadence. Matching dead `Rank/Suit/Color` /
  `SUIT_SYMBOLS` imports remain across the other engines + scenes —
  Accordion/AcesUp/Bisley/Bristol/Canfield/FlowerGarden/Golf/Pyramid/Scorpion/TriPeaks
  engines and their scene files — for future runs.)

## Remaining backlog

- U6 play-UX (unattended, planning-signal only today): landscape/portrait bottom-bar
  visibility on tall viewports (9/20 rows), 48–51% dead vertical space, Klondike/Spider
  hint-highlighting parity, daily-challenge generalization to spokes.
- U5 FR-locale completeness audit (not touched this run).
- Ongoing unused-import polish across the remaining engine/scene files.

## Validation & push

- `npx tsc --noEmit` exit 0; `npm run lint` 383 warnings / 0 errors (post-fix).
- Single push at end of run (Vercel READY + GitHub CI verification appended to ledger).
