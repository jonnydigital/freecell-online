# QA Report — 2026-09-06 (Sunday, even-day → Spider spoke rotation)

Producer: cowork-daily-review. Scope: visual QA sweep, competitor check, SEO/schema
spot-check, one same-run-safe fix. One push at end of run.

## Phase 0 — Coordination

- **AUTOMATION.md / LEDGER**: read. Recent producers — Monday content routine shipped
  `klondike-sequence-building` + `spider-solitaire-1-suit-strategy` (08-31); Friday
  scorecard shipped `2026-09-04.md`; prior daily reviews ran the unused-import polish
  cadence (last: KlondikeEngine `Color`, 09-05). No overlap with today's work.
- **Newest scorecard** (`2026-09-04.md`): **28/28 checks passing, all green.** No ❌
  FAILING check → priority bucket 1 empty.
- **git log**: on `main` @ edcbd20 at run start; pulled `--rebase` (already up to date);
  nothing unexpected landed since last run.
- **Working tree**: only two stale untracked `2026-08-03` mobile-audit files from a prior
  run, left untouched — not mine.
- **Baseline**: `npx tsc --noEmit` exit 0; `npm run lint` **383 warnings / 0 errors**.

## Phase 1 — Visual QA

Chrome extension **online** this run — used for desktop QA. Chrome-extension resize again
did **not** yield a true mobile viewport (innerWidth stayed 1440, card 113px = desktop),
so used the AUTOMATION.md **Mac-host CDP harness** as authoritative true-viewport mobile
evidence (the documented, verified path).

### Desktop (1440×900, Chrome extension, live production)

| Target | Result |
|---|---|
| **playfreecellonline.com** | 52 cards, 8 cascades, 4 free cells (FC), 4 foundations, 1 H1 "Play FreeCell Online — Free, No Download", sidebar renders (Advertisement + Daily Leaderboard + Daily Challenge), **0 clipped, no h-scroll** (scrollW 1440 = innerW). Toolbar intact. |
| **solitairestack.com (hub home)** | 52 cards (FreeCell default), 1 H1 "Play Solitaire Online for Free", 140 links, 6 JSON-LD blocks, no h-scroll; today "6" highlighted in Sept calendar, "Play Today" deal #1904180. |
| **spider spoke (playspidersolitaireonline.com/spider, even-day rotation)** | 63 cards, 10 columns, 8 foundations, 1/2/4-Suit selector, 16 buttons, toolbar (Undo/Hint/New Deal) intact, 1 H1, 6 JSON-LD, no h-scroll. (Hub `/spider` correctly 308s to the spider spoke.) |

### Mobile + tablet (Mac-host CDP harness — 5 engines × 375 / 390 / 414 / 768)

- **20/20 rows: "No hard audit failures detected."**
- Full card counts every viewport: **freecell 52, klondike 29, spider 63, forty-thieves 41,
  yukon 52** — spider 63 at all four viewports incl. 375 (no 62 intermittent DOM-mount read).
- **0 clipped, 0 horizontal overflow** across all 20 rows; all cards visible (vis == count).
- Card width range **32.09–74.41px** (phones 32.09–44.13px; tablet 61.28–74.41px) —
  confirms the 3dbb95a tap-target fix holds.
- Top controls visible 20/20; next-action expectation misses **0/20**; 0 blocked interactive.
- **0 runtime exceptions, 0 console errors, 0 log errors.**
- Informational (non-failing, unchanged from prior runs): `bottomControlsVisible` false on
  9/20 rows (bottom bar below initial fold on tall portrait viewports + forty-thieves/yukon
  + all 768 tablet rows, reachable by scroll) and 45–51% "unused vertical space (review)" —
  the known U6 dead-space/landscape-toolbar planning signals, not regressions.
- Artifacts: `docs/analytics/mobile-viewport-audits/2026-09-06-live.json` + `.md`
  (20 rows + 20 screenshots; screenshot dir gitignored via `*.png`).

## Phase 2 — Competitor (online-solitaire.com)

**Unchanged — 76th consecutive run** (Chrome extension, live, 1440×900). Same H1 "Play
Solitaire Online for Free (No Signup Required)", same nav catalog (Klondike Turn1/Turn3,
Spider 1/2/4-suit, Freecell, Russian Solitaire, Pyramid, Golf, Forty Thieves, Demons and
Thieves, "All games (520)", plus non-solitaire Hearts/Spades/Euchre/Gin Rummy/Pinochle),
same Download CTA, top bar Time/Moves/Stock:24. STATE (not product) note: no CMP modal on
load today; right rail EXPANDED (September challenge calendar day-6 "No wins" + 10/20/30
milestone track, Daily leaderboard top-3, Achievements all N/A, "Signup to save" CTA). No
new features/design/content vs prior runs. Standing gaps where they lead (native/PWA
download prominence, deck/background theming, illustrated face cards, 520-game catalog
framing, non-solitaire card games, single-surface mobile controls) are longstanding, not
new. Logged to `docs/research/competitor-log.csv`.

## Phase 3 — SEO & schema spot-check (3 fresh long-tail pages, never previously covered)

| Page | H1 | Canonical | Meta | JSON-LD | Cluster |
|---|---|---|---|---|---|
| `playfreecellonline.com/freecell-vs-klondike` (cross-game comparison, hub-owned) | 1 | self → `solitairestack.com/freecell-vs-klondike` | 146 | Org+WebSite+Article+FAQPage+BreadcrumbList | 49 |
| `playspidersolitaireonline.com/spider-opening-strategy` (spider spoke) | 1 | self | 152 | Org+WebSite+Article+BreadcrumbList+FAQPage | 17 |
| `solitairestack.com/canfield` (hub game → Klondike-family) | 1 | self → `playklondikeonline.com/canfield` | 152 | Org+WebSite+WebApplication+Game+BreadcrumbList+FAQPage | 12 |

All clean: one H1 each, sane canonicals (comparison content self-canonicals to the hub;
Canfield cross-canonicals to the Klondike spoke as a Klondike-family variant), unique meta
descriptions, appropriate JSON-LD graph per page type, healthy cluster interlinking.
**No SEO issues found.**

## Phase 4 — Improvement shipped (1 slice)

Priority buckets 1 (failing checks) and 2 (bugs) were empty, so took a same-run-safe
lint-polish slice aligned to today's Spider rotation (Scorpion is a Spider-family engine):

- **`src/engine/ScorpionEngine.ts`** — removed two verified-dead unused imports: `Rank`
  from the `./Card` import line (`import { Card, Suit, Rank }` → `import { Card, Suit }`)
  and `MoveEntry` from the `./MoveHistory` import line
  (`import { MoveHistory, MoveEntry }` → `import { MoveHistory }`). Each confirmed
  single-occurrence (import line only, grep count 1 → 0 after removal); `Card` (18 uses),
  `Suit` (used as type at 3 sites: `suit: Suit`), `MoveHistory` (5 uses) all stay used.
  Per-file eslint now **0 warnings**. tsc green before + after; full lint **383 → 381
  warnings, 0 errors** (−2 exactly), no new warnings; no behavior/layout change; no new
  routes/types/locales.
  (Continues the unused-import polish cadence. Matching dead `Rank/Suit/Color` /
  `SUIT_SYMBOLS` imports remain across other engines + scenes —
  Accordion/AcesUp/Bisley/Bristol/Canfield/FlowerGarden/Golf/Pyramid/TriPeaks
  engines and their scene files, plus a dead `toBristolLoc` local function in
  BristolGamePage.tsx — for future runs.)

## Remaining backlog

- U6 play-UX (unattended, planning-signal only today): bottom-bar visibility on tall
  viewports (9/20 rows), 45–51% dead vertical space, Klondike/Spider hint-highlighting
  parity, daily-challenge generalization to spokes.
- U5 FR-locale completeness audit (not touched this run).
- Ongoing unused-import polish across the remaining engine/scene files.

## Validation & push

- `npx tsc --noEmit` exit 0; `npm run lint` 381 warnings / 0 errors (post-fix).
- Single push at end of run (Vercel READY + GitHub CI verification appended below).
