# QA Report — 2026-09-07 (Monday, odd-day → Klondike spoke rotation)

Producer: cowork-daily-review. Scope: visual QA sweep, competitor check, SEO/schema
spot-check, one same-run-safe fix. One push at end of run.

## Phase 0 — Coordination

- **AUTOMATION.md / LEDGER**: read (last ~10 rows). Recent producers — Monday content
  routine shipped `klondike-sequence-building` + `spider-solitaire-1-suit-strategy`
  (08-31); Friday scorecard shipped `2026-09-04.md`; prior daily reviews have run the
  unused-import polish cadence (last: ScorpionEngine `Rank`+`MoveEntry`, 09-06). No
  overlap with today's work chosen.
- **Newest scorecard** (`2026-09-04.md`): **28/28 checks passing, all green.** No ❌
  FAILING check → priority bucket 1 empty.
- **git log** (`-15`): on `main` @ `f0d31ef` at run start; nothing unexpected landed
  since the last daily review.
- **Baseline**: `npx tsc --noEmit` exit 0; `npm run lint` **381 warnings / 0 errors**
  (matches the 09-06 report's ending count).

## Phase 1 — Visual QA

Chrome extension **online** this run — used for desktop QA. Chrome-extension resize again
did **not** yield a true mobile viewport (innerWidth stayed 1512, card 122px = desktop),
so used the AUTOMATION.md **Mac-host CDP harness** as the authoritative true-viewport
mobile evidence (the documented, verified path).

### Desktop (1440×900, Chrome extension, live production)

| Target | Result |
|---|---|
| **playfreecellonline.com** | 52 cards, 8 cascades, 4 free cells, 4 foundations; 1 H1 ("Play FreeCell Online — Free, No Download"); sidebar (Daily Leaderboard + Daily Challenge + September calendar, day 7 highlighted) renders alongside board; 0 horizontal scroll. ✅ |
| **solitairestack.com** (hub homepage) | FreeCell board 52 cards; 1 H1 ("Play Solitaire Online for Free"); 140 links; 6 JSON-LD blocks; 0 horizontal scroll. ✅ |
| **playklondikeonline.com** (spoke, odd-day rotation) | 29 cards, 7 tableau columns, 4 foundations; Draw 1 / Draw 3 toggle; 22 buttons, 0 unnamed; 1 H1 ("Play Klondike Solitaire Online — Free, No Download"); 5 JSON-LD; toolbar (undo/hint/new deal) intact; 0 horizontal scroll. Cookie banner present, non-obstructing, left unaccepted per action-policy. ✅ |

### Mobile + tablet (Mac-host CDP harness, true viewport, live production)

20-row full pass, 5 engines × 4 viewports (375 / 390 / 414 / 768):

- **19 passed, 1 needs-review.** Max horizontal overflow **0px**; max cramped tap
  targets **0**; max blocked controls **0**; **runtime exceptions 0**; console/log
  errors **1** (see below); next-action **27/27 = 100% coverage**, 0 missing / 0 disabled.
- **Full card counts every viewport**: freecell 52, klondike 29, spider 63,
  forty-thieves 41, yukon 52.
- **Card widths**: phones 32.09–44.13px, tablet 61.28–74.41px — all within the phone
  30–54px target, confirming the `3dbb95a` tap-target fix still holds.
- **Needs-review (not a regression)**: `spider 390×844` read **62** cards (expected ≥63)
  — the known intermittent DOM-mount read race documented in AUTOMATION.md. Re-ran the
  single cell 3× directly → **63 / 63 / 63, clipped 0, overflow 0** every time. Confirmed
  intermittent read timing, not a layout/content defect.
- **Console/log error (triage-only, 0 runtime)**: `klondike 414×896` logged one
  `Failed to load resource: net::ERR_FAILED`. Traced live via Chrome network panel to
  Vercel infra endpoints — `/.well-known/vercel/jwe` (503) and a HEAD `/` probe (503);
  the document GET renders the full board (29 cards, all controls). This is a
  Vercel-injected deployment-protection/toolbar endpoint, not a first-party asset —
  benign, gameplay unaffected, recorded for triage consistent with prior runs.

## Phase 2 — Competitor Analysis

**online-solitaire.com — UNCHANGED, 77th consecutive run.** Desktop 1440×900, live via
Chrome extension. Homepage defaults to classic Klondike (Turn 1 / Turn 3 toggle,
All games (520) dropdown, top bar Time 00:00 / Moves 0 / Stock: 24, foundations
club/heart/spade/diamond); bottom toolbar New game / Hint / Undo + Bookmark / Download
CTA / Settings / More. Title "Solitaire - Play Online for Free", H1 "Play Solitaire
Online for Free (No Signup Required)". Nav catalog unchanged (Klondike Turn1/Turn3,
Spider 1/2/4-suit, Freecell, Russian Solitaire, Pyramid, Golf, Forty Thieves, Demons and
Thieves, plus non-solitaire Hearts/Spades/Euchre/Gin Rummy/Pinochle). State-only note:
no CMP modal on load; right rail collapsed behind "Show daily challenge & stats" (the
collapsed variant, as 08-28 / 09-02 / 09-04). No new features / design / content.
Logged to `docs/research/competitor-log.csv`. Competitor content treated as untrusted;
DOM + text scanned, no embedded instructions acted upon.

## Phase 3 — SEO & Schema Spot-Check

Three never-before-covered long-tail pages (rotating the long tail, one per owning
domain), live:

| Page | H1 | Meta | Canonical | JSON-LD | Cluster links |
|---|---|---|---|---|---|
| `/freecell-world-records` (freecell owner) | 1 ("FreeCell World Records") | 149 chars, unique | self → playfreecellonline.com | Organization, WebSite, Article, FAQPage, BreadcrumbList | 28 |
| `/klondike-vegas-scoring` (klondike spoke) | 1 ("Klondike Vegas Scoring: Strategy and Economics") | 146 chars, unique | self → playklondikeonline.com | Organization, WebSite, Article, BreadcrumbList | 22 |
| `/spider-mastery` (spider spoke) | 1 ("Spider Solitaire Mastery: The Complete Guide") | 155 chars, unique | self → playspidersolitaireonline.com | Organization, WebSite, Article, BreadcrumbList, FAQPage | 23 |

All three: exactly one H1, unique meta description in-range, sane self-canonical to the
owning domain, appropriate JSON-LD graph, healthy internal linking into each page's
cluster. No fixes needed.

## Phase 4 — Improvements

Priority buckets: (1) scorecard ❌ — empty (28/28 green); (2) QA bugs — none (spider 62
is the known intermittent race, klondike 503 is Vercel infra); (3) plan units U5/U6;
(4) competitor gaps. Took a same-run-safe **lint-polish** slice continuing the documented
unused-import sweep.

- **`src/engine/AccordionEngine.ts`**: removed 3 verified-dead unused imports —
  `Suit` + `Rank` from the `./Card` import line (→ `import { Card } from './Card';`) and
  `MoveEntry` from the `./MoveHistory` import line (→ `import { MoveHistory } from './MoveHistory';`).
  Each confirmed single-occurrence by grep (appears only on its import line, zero real
  usage); `Card` (8 uses) and `MoveHistory` (5 uses) stay used. ESLint flags were
  authoritative (`no-unused-vars`). Per-file eslint now **0 problems**; `tsc` green
  before + after; full lint **381 → 378 warnings (−3 exactly)**, 0 errors, no new
  warnings, no behavior/layout change, no new routes/types/locales.
- Remaining flagged dead Card-family imports for future runs: AcesUp / Bisley / Bristol
  (MoveEntry) / Canfield / Golf / Pyramid / TriPeaks engines, plus a dead `toBristolLoc`
  local fn in BristolGamePage.tsx (all still lint-warning-level only).

## Phase 5 — Report / Ledger / Push / Verify

- This report written; ledger row appended; `2026-09-07-live.json` + `.md` mobile-audit
  artifacts + 20 screenshots (dir gitignored) written; competitor log row appended.
- Pre-push gate: `npx tsc --noEmit` exit 0 **and** `npm run lint` 378 warnings / **0
  errors** — both pass.
- Single push at end of run (one push = 4 Vercel builds). Post-push: verify Vercel
  deploys READY on all 4 projects + GitHub CI green on the commit.

## Backlog / Remaining

- U5 French-locale completeness audit and U6 play-UX backlog remain open (plan
  `2026-07-03-001`); intentionally not touched in an unattended run beyond the safe
  lint-polish slice.
- Intermittent `spider` DOM-mount card-count read race persists (harmless; re-run
  confirms 63). Vercel `/.well-known/vercel/jwe` 503 appears in harness console logs
  intermittently — infra, not a site defect.
