# QA Report — 2026-09-08 (Tuesday, even-day → Spider spoke rotation)

Producer: cowork-daily-review. Scope: visual QA sweep, competitor check, SEO/schema
spot-check, one same-run-safe fix. One push at end of run.

## Phase 0 — Coordination

- **AUTOMATION.md / LEDGER**: read (last ~10 rows). Recent producers — Monday content
  routine shipped `klondike-sequence-building` + `spider-solitaire-1-suit-strategy`
  (08-31) and a later batch `klondike probability thinking` + `spider column tactics`
  (spoke-exclusive, `32b3905`); Friday scorecard shipped `2026-09-04.md`; prior daily
  reviews have run the unused-import polish cadence (last: AccordionEngine
  `Suit`+`Rank`+`MoveEntry`, 09-07). No overlap with today's work chosen.
- **Newest scorecard** (`2026-09-04.md` / `latest.json`): **28/28 checks passing, all
  green.** No ❌ FAILING check → priority bucket 1 empty.
- **git log** (`-15`): on `main` @ `634ed5a` at run start; Mac host up-to-date with
  `origin/main` (behind 0 / ahead 0); nothing unexpected landed since the last daily
  review.
- **Baseline**: `npx tsc --noEmit` exit 0; `npm run lint` **378 warnings / 0 errors**
  (matches the 09-07 report's ending count).

## Phase 1 — Visual QA

Chrome extension **online** this run — used for desktop QA. Chrome-extension resize again
did **not** yield a true mobile viewport (innerWidth stayed 1440, card 113.95px =
desktop layout), so used the AUTOMATION.md **Mac-host CDP harness** as the authoritative
true-viewport mobile evidence (the documented, verified path).

### Desktop (1440×900, Chrome extension, live production)

| Target | Result |
|---|---|
| **playfreecellonline.com** | 52 cards, 8 cascades, 4 free cells, 4 foundations; 1 H1 ("Play FreeCell Online — Free, No Download"); sidebar (Daily Leaderboard + Daily Challenge + September calendar, day 8 highlighted) renders alongside board; card labels readable; 0 horizontal scroll. ✅ |
| **solitairestack.com** (hub homepage) | FreeCell board 52 cards; 1 H1 ("Play Solitaire Online for Free"); 140 links; 6 JSON-LD blocks; 0 horizontal scroll. (Transient "Loading FreeCell…" splash on first paint, resolved to full board on settle.) ✅ |
| **playspidersolitaireonline.com/spider** (spoke, even-day rotation) | via hub `/spider` 308 → spoke; 63 cards, 10 tableau columns, 8 foundations; 1 / 2 / 4-Suit toggle; 16 buttons (Undo / Hint / New Deal toolbar intact); 1 H1 ("Play Spider Solitaire Online — Free, 1, 2 & 4 Suit"); 6 JSON-LD; 0 horizontal scroll; win-modal CSS sane (no stray overlay on load). ✅ |

### Mobile + tablet (Mac-host CDP harness, true viewport, live production)

20-row full pass, 5 engines × 4 viewports (375 / 390 / 414 / 768):

- **20 passed, 0 failed.** Max horizontal overflow **0px**; max cramped tap targets
  **0**; max blocked controls **0**; **runtime exceptions 0**; **console errors 0**;
  **log errors 0**; next-action **27/27 = 100% coverage**, 0 missing / 0 disabled.
- **Full card counts every viewport**: freecell 52, klondike 29, spider 63,
  forty-thieves 41, yukon 52 — **spider read 63 at all four viewports incl. 375, no
  62-recurrence today.**
- **Card widths**: phones 32.09–44.13px, tablet 61.28–74.41px — all within the phone
  30–54px target, confirming the `3dbb95a` tap-target fix still holds.
- **Planning signals (not failures)**: 9 dead-space candidates and 183 small-tap-target
  counts (below the preferred 44px but not overlapping/cramped) — the known **U6**
  play-UX backlog signals, consistent with prior runs; scrollWidth == viewport width on
  every row.
- Tablet (768×1024): board fills correctly, no sidebar, 0 overflow across all 5 engines.

## Phase 2 — Competitor Analysis

**online-solitaire.com — UNCHANGED, 78th consecutive run.** Desktop 1440×900, live via
Chrome extension. Homepage defaults to classic Klondike (Turn 1 / Turn 3 toggle, All
games (520) dropdown, top bar Time 00:00 / Moves 0 / Stock: 24, foundations
club/heart/spade/diamond); bottom toolbar New game / Hint / Undo + Bookmark / Download
CTA / Settings / More. Title "Solitaire - Play Online for Free", H1 "Play Solitaire
Online for Free (No Signup Required)". Nav catalog unchanged (Klondike Turn1/Turn3,
Spider 1/2/4-suit, Freecell, Russian Solitaire, Pyramid, Golf, Forty Thieves, Demons and
Thieves, plus non-solitaire Hearts/Spades/Euchre/Gin Rummy/Pinochle/Canasta/2048).

- **State (not product) note today**: right rail collapsed behind "Show daily challenge
  & stats" button (the collapsed variant, as 08-28 / 09-02 / 09-04 / 09-07); GoDaddy ad
  in right rail; CMP present, left unaccepted per action-policy.
- **No new features / design / content** vs prior runs.
- **Standing gaps where they lead**: native/PWA download prominence, deck/background
  theming, illustrated face cards, large multi-game catalog framing (520), non-solitaire
  card games, single-surface mobile controls.
- **Where we lead**: editorial depth (28 variants + desks), richer per-page JSON-LD,
  spoke-domain SEO, no-signup local save, named daily deals, ad-light dark theme, PWA
  offline+resume, 6-locale FreeCell i18n, mobile next-action coaching.
- **Security**: competitor content treated as untrusted; DOM + text scanned only, no
  embedded instructions acted upon.

Logged to `docs/research/competitor-log.csv` (78th row).

## Phase 3 — SEO & Schema Spot-Check

Three fresh long-tail pages the Friday scorecard has never covered (rotated across all
three owners; the scorecard already covers canaries network-wide, not rebuilt here):

| Page | Owner | H1 | Canonical | Meta | JSON-LD | Internal links |
|---|---|---|---|---|---|---|
| `/blog/history-of-freecell` | freecell owner | 1, unique | self | 150 | Organization + WebSite + Article + BreadcrumbList | 95 |
| `/eight-off` | hub → freecell owner | 1, unique | cross-canonical → `playfreecellonline.com/eight-off` (Eight Off is a FreeCell-family game; correct family ownership) | 142 | Organization + WebSite + Game + BreadcrumbList + FAQPage | 9 |
| `/blog/spider-solitaire-dead-positions` | spider spoke | 1, unique | self | 150 | Organization + WebSite + Article + BreadcrumbList | 43 |

All three: exactly one H1, unique meta description present, sane canonical, appropriate
JSON-LD for page type, internal links into the page's cluster. **No SEO/schema issues
found.**

## Phase 4 — Improvements

Priority order — bucket 1 (scorecard ❌) empty (28/28 green); bucket 2 (QA bugs) empty
(desktop + mobile + SEO all clean); so took a **same-run-safe lint-polish slice**
continuing the unused-import polish cadence.

- **`src/engine/CanfieldEngine.ts`** — removed 2 verified-dead unused imports `Suit` +
  `Color` from the `./Card` import line (`import { Card, Suit, Rank, Color }` →
  `import { Card, Rank }`). Each confirmed dead by the authoritative ESLint
  `@typescript-eslint/no-unused-vars` flag (1:16 `Suit`, 1:28 `Color`) **and** a
  whole-word grep excluding the import line (`Suit` 0 uses, `Color` 0 uses); `Card`
  (23 uses) + `Rank` (2 uses) stay used. Per-file ESLint now 0 problems.
- **Validation**: `npx tsc --noEmit` green before + after; full lint **378 → 376
  warnings (−2 exactly), 0 errors**, no new warnings; no behavior / layout / route /
  type / locale change.
- **Remaining candidates for future runs** (verified-flagged this run, left untouched):
  `GolfEngine` (`Rank`), `PyramidEngine` (`Suit`+`Rank`), `TriPeaksEngine` (`Rank`),
  plus the previously-noted `AcesUp`/`Bisley`/`Bristol` engines and the dead
  `toBristolLoc` fn in `BristolGamePage.tsx`.

## Phase 5 — Report / Push / Verify

- QA report (this file), 20-row mobile-audit JSON + MD, competitor row, ledger row.
- Two descriptive commits, batched into a single `git push origin main` (one push =
  four Vercel builds).
- Post-push verification: Vercel deploys READY on all 4 projects + GitHub CI green on
  the pushed commit (recorded below after push).

## Remaining backlog

- **U6 play-UX** (planning signals from the harness): 183 small-tap-target counts and
  9 dead-space candidates across engines — tracked, not acted on (needs a referenced
  U6 slice, not an ad-hoc daily fix).
- **U5** French-locale completeness audit — not started this run.
- **Lint cadence**: 376 warnings remain (0 errors); dead Card-family imports continue
  across the engines listed in Phase 4.
- Intermittent spider DOM-mount read race (62 vs 63) — did not recur today; keep
  monitoring.
