# QA Report — 2026-09-13 (Cowork daily review)

**Run type:** automated daily review & QA. Odd day → Klondike spoke rotation.
**Baseline at run start:** `npx tsc --noEmit` green; `npm run lint` 370 warnings / 0 errors; working tree clean (09-12 landed properly); in sync with origin/main; scorecard 2026-09-11 = 28/28 green.

## Phase 0 — Coordination

- Read `docs/AUTOMATION.md`, last ~10 ledger rows, newest scorecard (2026-09-11, 28/28 green — no ❌ to prioritize), and `git log --oneline -15`.
- No overlap with the Monday content routine or Friday scorecard routine. My lane: QA, fixes, polish.
- No lost-run recovery needed this run (unlike 09-10/09-11): 09-12 run committed + pushed cleanly.

## Phase 1 — Visual QA

### Desktop (true 1440×900 via built-in browser pane; Chrome-extension viewport capped at 606px this run, as recurring)

| Surface | Result |
|---|---|
| playfreecellonline.com | 52 cards, 8 cascades, 4 free cells + 4 foundations spaced, **sidebar renders alongside board** (Daily Leaderboard + Sept challenge calendar, **day-13 highlighted**), 1 H1, 0 h-scroll. Onboarding modal + cookie banner present, non-obstructing (CMP left unaccepted). |
| solitairestack.com (hub home) | 52 cards, 1 H1, 140 links, 6 JSON-LD, 0 h-scroll. |
| solitairestack.com/forty-thieves (hub game page) | 41 cards, 10 columns, stock + waste, 4 foundations, toolbar intact, 1 H1, 6 JSON-LD, 0 unnamed buttons, 0 h-scroll. |
| playklondikeonline.com (odd-day spoke) | 29 cards, 7 columns, stock + waste, 4 foundations, Draw1/Draw3 toolbar intact, 1 H1, 5 JSON-LD, 22 buttons / 0 unnamed, 0 h-scroll, win-modal CSS sane. |

### Mobile (authoritative Mac-host CDP harness — 5 engines × 375/390/414 + tablet 768)

- **20 rows: 19 passed, 1 need-review.** Max horizontal overflow **0px**; console/log errors **0**; JS errors **0**; clipped **0**; blocked controls **0**.
- **Full card counts every viewport:** freecell 52 / klondike 29 / spider 63 / forty-thieves 41 / yukon 52.
- **Card widths 32.09–74.41px** (phones 32–44px) — confirms the 3dbb95a tap-target fix holds.
- **Expected next-action health: 15/15 matched, 0 missing, 0 disabled** across every engine/viewport.
- **Dead-space candidates** 45–51% on phone widths (freecell/klondike/spider/forty-thieves) = known **U6 planning signals**, not failures.
- **The lone need-review:** spider 390×844 read 62 cards (expected 63), `clipped=0` `overflow=0` — the **known intermittent DOM-mount read race** documented in AUTOMATION.md. Confirmed benign: spider@375 and spider@414 both read 63 in the same pass, and a direct 3× re-run of spider@390 returned **63 / 63 / 63**. Not a regression.
- Corroborated in the built-in browser at a true 375px viewport: freecell 52 cards / 39.25px / 0 clipped / 0 h-scroll; klondike spoke 29 cards / 39.25px / 0 clipped / 0 h-scroll, 1 H1.
- Artifacts: `docs/analytics/mobile-viewport-audits/2026-09-13-live.json` + `.md` (+ 20 screenshots, dir gitignored).

### Tablet (768×1024, in the harness)

- Board fills correctly without the sidebar (e.g. yukon 768: 52 cards, 74.41px, 0 clipped, 41.7% dead-space = n/a).

## Phase 2 — Competitor analysis

- online-solitaire.com **UNCHANGED — 83rd consecutive run.** Classic Klondike default, Turn1/Turn3, All games (520) dropdown, top bar Time/Moves/Stock:24, bottom toolbar New game/Hint/Undo + Bookmark/Download/Settings/More, macOS-app download CTA. Title unchanged; H1 "Play Solitaire Online for Free (No Signup Required)".
- STATE (not product) note: no CMP modal on load; right rail **collapsed** behind "Show daily challenge & stats" (vs expanded on 09-12) = state-only.
- No new features/design/content. Logged to `docs/research/competitor-log.csv`.

## Phase 3 — SEO & schema spot-check (3 pages never previously covered)

| Page | Result |
|---|---|
| playfreecellonline.com/freecell-opening-strategy (freecell owner) | 1 H1, JSON-LD Organization+WebSite+Article+FAQPage+BreadcrumbList, meta 147 chars unique, self-canonical, 26 cluster links. Clean. |
| solitairestack.com/blog/mental-skills-that-make-great-solitaire-players (hub) | 1 H1, JSON-LD Organization+WebSite+Article+BreadcrumbList, meta 152 chars unique, self-canonical, 44 cluster links. Clean. |
| playspidersolitaireonline.com/blog/spider-solitaire-suit-completion (spider spoke) | 1 H1, JSON-LD Organization+WebSite+Article+BreadcrumbList, meta 154 chars unique, self-canonical, 21 cluster links. Clean. |

No SEO/schema issues found.

## Phase 4 — Improvements shipped

Priority buckets: (1) scorecard ❌ — none (28/28 green); (2) QA bugs — none (spider read is a confirmed benign race); (3) plan open units — deferred (U5/U6 slices are larger than a same-run-safe change); (4) competitor gaps — none newly actionable. → Took a **same-run-safe lint-polish slice** per the established cadence.

- **`src/engine/AcesUpEngine.ts`** — removed the verified-dead unused `Suit` import (`import { Card, Suit, Rank }` → `import { Card, Rank }`). Confirmed single-occurrence by ESLint @typescript-eslint/no-unused-vars [14:16] + whole-word grep (import line only, zero real usage); `Card` (8 refs) and `Rank` (3 refs) stay used. Per-file eslint now 0 problems. tsc green before+after; full lint **370→369 warnings (−1 exactly)**, 0 errors, no new warnings, no behavior/layout/route/type/locale change. Commit `33c627b`.
- Remaining verified-flagged unused-import candidates for future runs: `BristolEngine[Suit 11:16]` + dead `toBristolLoc` fn in `BristolGamePage.tsx`.

## Phase 5 — Report / ledger / push / verify

- This report + ledger row + competitor-log + 09-13 mobile audit artifacts committed; single `git push origin main` at end of run.
- Post-push verification (Vercel READY on all 4 projects + GitHub CI green) recorded below.

### Remaining backlog

- U5 French-locale completeness audit and U6 play-UX backlog (dead-space below-board utilization, bottom-control-below-fold) — planning signals only; no action this run.
- Pre-existing orphaned untracked artifacts `docs/analytics/mobile-viewport-audits/2026-08-03-live.json/.md` (never committed by their originating run) — left as-is, not this run's output.

## Deploy verification

(Appended after the single push — see below.)
