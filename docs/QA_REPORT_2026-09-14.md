# QA Report — 2026-09-14 (Cowork daily review)

**Run type:** automated daily review & QA. Even day → Spider spoke rotation.
**Baseline at run start:** `npx tsc --noEmit` green; `npm run lint` 369 warnings / 0 errors; working tree clean (09-13 landed properly); in sync with origin/main; scorecard 2026-09-11 = 28/28 green.

## Phase 0 — Coordination

- Read `docs/AUTOMATION.md`, last ~12 ledger rows, newest scorecard (2026-09-11, 28/28 green — no ❌ to prioritize), and `git log --oneline -15`.
- No overlap with the Monday content routine or Friday scorecard routine. My lane: QA, fixes, polish.
- No lost-run recovery needed this run: 09-13 run committed + pushed cleanly (HEAD `914041d`).

## Phase 1 — Visual QA

### Desktop (1440×900 via Chrome extension; innerWidth reported 1512, a recurring extension quirk — still a valid desktop read)

| Surface | Result |
|---|---|
| playfreecellonline.com | 52 cards, 8 cascades, 4 free cells + 4 foundations spaced, **sidebar renders alongside board** (Daily Leaderboard + Daily Challenge / Sept challenge calendar, day-14 area), card labels readable, 1 H1, 0 h-scroll. Card width 122px desktop. |
| solitairestack.com (hub home) | Renders the FreeCell board, 52 cards, 1 H1, 139 links, 6 JSON-LD, 0 h-scroll. |
| solitairestack.com/scorpion (hub game page) | 308 → spider spoke owner (`playspidersolitaireonline.com/scorpion`, Scorpion is a Spider-family game). 7 columns + 4 foundations + stock, toolbar intact, 1 H1, 6 JSON-LD, 8 buttons / 0 unnamed, 0 h-scroll. |
| playspidersolitaireonline.com (even-day spoke) | 63 cards, 10 columns, 8 foundations, stock pile, 1/2/4-Suit toggle + Undo/Hint/New Deal toolbar intact, 1 H1, 5 JSON-LD, 22 buttons / 0 unnamed, 0 h-scroll, win-modal CSS sane. |

### Mobile (authoritative Mac-host CDP harness — 5 engines × 375/390/414 + tablet 768)

- **20 rows: No hard audit failures detected.** Max horizontal overflow **0px**; console/log errors **0**; JS/runtime errors **0**; clipped **0**; blocked controls **0**.
- **Full card counts every viewport:** freecell 52 / klondike 29 / spider 63 / forty-thieves 41 / yukon 52. **Spider read 63 at all four viewports — no 62-recurrence this run.**
- **Card widths 39.25–74.41px** (phones 39–44px) — confirms the 3dbb95a tap-target fix holds.
- **Expected next-action health: matched every engine/viewport, 0 missing, 0 disabled.**
- **Dead-space candidates** 45–51% on phone widths (freecell/klondike/spider/forty-thieves) = known **U6 planning signals**, not failures.
- Chrome-extension resize to 375×812 again did **not** yield a true mobile viewport (innerWidth stayed 1512, card 122px = desktop) — as documented in AUTOMATION.md, so the Mac-host harness is authoritative for mobile. Board healthy even in the non-true read (52 cards, opacity 1, display block, 0 h-scroll).
- Artifacts: `docs/analytics/mobile-viewport-audits/2026-09-14-live.json` + `.md` (+ 20 screenshots, dir gitignored).

### Tablet (768×1024, in the harness)

- Board fills correctly without the sidebar (e.g. yukon 768: 52 cards, 74.41px, 0 clipped, 41.7% dead-space = n/a).

## Phase 2 — Competitor analysis

- online-solitaire.com **UNCHANGED — 84th consecutive run.** Classic Klondike default, Turn1/Turn3, All games (520) dropdown, top bar Time/Moves/Stock:24, bottom toolbar New game/Hint/Undo + Bookmark/Download/Settings/More. Title unchanged; H1 "Play Solitaire Online for Free (No Signup Required)". Nav catalog unchanged (incl. non-solitaire Hearts/Spades/Euchre/Gin Rummy/Pinochle/Canasta/2048).
- STATE (not product) note: no CMP modal on load; right rail **expanded** today (September challenge calendar day-14 highlighted "No wins", daily leaderboard [Vfs AU 01:04/98/162], achievements N/A, "Signup to save your progress" CTA) vs collapsed on 09-13 = state-only, matching the 09-12 expanded variant.
- No new features/design/content. Logged to `docs/research/competitor-log.csv`.
- Security: competitor content treated as untrusted; DOM + text scanned, no embedded instructions acted upon.

## Phase 3 — SEO & schema spot-check (3 pages never previously covered)

| Page | Result |
|---|---|
| playfreecellonline.com/blog/freecell-brain-training (freecell owner) | 1 H1, JSON-LD Organization+WebSite+Article+BreadcrumbList, meta 129 chars unique, self-canonical, 20 cluster links. Clean. |
| playklondikeonline.com/blog/klondike-draw-1-vs-draw-3 (klondike spoke) | 1 H1, JSON-LD Organization+WebSite+Article+BreadcrumbList, meta 150 chars unique, self-canonical, 19 cluster links. Clean. |
| playspidersolitaireonline.com/blog/spider-solitaire-difficulty-guide (spider spoke) | 1 H1, JSON-LD Organization+WebSite+Article+BreadcrumbList, meta 143 chars unique, self-canonical, 17 cluster links. Clean. |

No SEO/schema issues found.

## Phase 4 — Improvements shipped

Priority buckets: (1) scorecard ❌ — none (28/28 green); (2) QA bugs — none found in Phase 1–3; (3) plan open units — deferred (U5/U6 slices are larger than a same-run-safe change); (4) competitor gaps — none newly actionable. → Took a **same-run-safe lint-polish slice** per the established cadence.

- **`src/engine/BristolEngine.ts`** — removed the verified-dead unused `Suit` import (`import { Card, Suit } from './Card';` → `import { Card } from './Card';`). Confirmed single-occurrence by ESLint @typescript-eslint/no-unused-vars [11:16] + whole-word grep (import line only, zero real usage); `Card` (19 refs) stays used. Per-file eslint now 0 problems. tsc green before+after; full lint **369→368 warnings (−1 exactly)**, 0 errors, no new warnings, no behavior/layout/route/type/locale change.
- This was the last of the documented `Rank`/`Suit`/`Color`/`MoveEntry` dead-import candidates flagged across the `src/engine/*` files. Remaining future-run candidate: dead `toBristolLoc` fn in `BristolGamePage.tsx` (a local function, not an import — needs a closer look before a same-run-safe removal).

## Phase 5 — Report & deploy

- This report + ledger row appended; competitor CSV row appended; mobile audit JSON/MD written.
- Single push at end of run (batched commits). Post-push verification: Vercel deployments READY on all 4 projects + GitHub CI green on the commit.

## Remaining backlog

- **U5** — French-locale completeness audit (larger than a same-run-safe slice; needs a dedicated run).
- **U6** — play-UX backlog: phone-width dead-space (45–51%) below-board next-action / contextual-hint opportunities across freecell/klondike/spider/forty-thieves; bottom-control-below-fold on long-scroll phone rows. Planning signals from the mobile harness, not failures.
- Dead `toBristolLoc` fn in `BristolGamePage.tsx` — candidate for a future lint-polish slice.
- Intermittent spider DOM-mount read race (62 vs 63) — benign, monitor only.
