# QA Report — 2026-09-18 (Cowork daily review)

**Run outcome: all-green sweep.** Desktop QA (FreeCell + hub + Spider spoke),
true-viewport mobile (20/20 rows pass), and tablet all clean — no bugs. Three
long-tail SEO/schema spot-checks clean. Competitor unchanged (88th run). Phase 4
shipped one same-run-safe polish slice: removed the last documented dead-code
candidate (`toBristolLoc` in BristolGamePage.tsx). tsc + lint green
(368 → 366 warnings, 0 errors). Single push at end of run.

---

## Phase 0 — Coordination

- Day parity: **even → Spider spoke** rotation for Phase 1.
- Latest scorecard (`2026-09-11.md`): **28/28 green, no ❌ checks** → Phase 4
  priority #1 (scorecard fails) empty.
- Ledger: last committed daily run was **09-17** (shipped the dup-WebSite JSON-LD
  dedupe `6d7c110` + recovered 09-15/09-16). No untracked prior runs. No lane
  overlap with the Monday content pipeline or Friday scorecard.
- `git log --oneline -15` reviewed. Mac repo clean and up to date with
  `origin/main` at HEAD `07f9087` (pull --rebase: already up to date).

## Phase 1 — Visual QA

### Desktop (Chrome extension, 1440 viewport)
- **playfreecellonline.com** (`/game/1`): 52 cards, 8 cascades, 4 free cells + 4
  foundations, sidebar (ad slot + daily leaderboard) alongside board, **1 H1**
  ("Play FreeCell Online — Free, No Download"), no hscroll (scrollW 1440),
  4 JSON-LD, self-canonical. Clean. (screenshot captured)
- **solitairestack.com** (hub home): **1 H1** ("Play Solitaire Online for Free"),
  139 links, 5 JSON-LD, 52-card FreeCell board mounts, sidebar (leaderboard +
  daily challenge + Sept calendar), no hscroll. Clean. (screenshot captured)
- **playspidersolitaireonline.com** (even-day spoke): 63 cards, 10 tableau
  columns, 8 foundation slots, stock pile, 1/2/4-Suit toggle, Undo/Hint/New Deal
  toolbar intact, **22 buttons / 0 unnamed**, **1 H1**, self-canonical, 4 JSON-LD,
  no hscroll, win-modal not triggered (fresh deal) — CSS sane. Clean. (screenshot
  captured)

### Mobile (authoritative: Mac-host CDP harness — `npm run qa:mobile`)
- Full **5-engine × 4-viewport** (375 / 390 / 414 / 768) pass:
  **20/20 rows passed, 0 need review. "No hard audit failures detected."**
- Max horizontal overflow **0px**, cramped tap targets **0**, blocked controls
  **0**, runtime exceptions **0**, console errors **0**, log errors **0**.
- Full card counts stable at **every** viewport: freecell **52** / klondike **29**
  / spider **63** (all 4 viewports incl 375 — **no 62-read recurrence this run**)
  / forty-thieves **41** / yukon **52**.
- Card widths on phones **32.09–44.13px** (spider 32.09 / freecell·klondike·yukon
  39.25–44.13 / forty-thieves 32.09–36) — confirms the `3dbb95a` tap-target fix
  holds; tablet (768) 61.28–74.41px.
- Next-action coverage **27/27 (100%)**, 0 missing / 0 disabled across all
  next-action rows.
- 9 portrait dead-space candidates (45–51% unused vertical, phone widths) =
  **known U6 planning signals, not failures.**
- Artifacts: `docs/analytics/mobile-viewport-audits/2026-09-18-live.json` + `.md`
  (+ 20 screenshots, dir gitignored).

### Tablet (768×1024, in the harness)
- freecell 52 / klondike 29 / spider 63 / forty-thieves 41 / yukon 52 — board
  fills without sidebar, 0 overflow / 0 clipped every route.

### Extension mobile-viewport note
- Chrome-extension `resize_window` to 375×812 again did **not** yield a true
  mobile viewport (innerWidth stayed 1440, card 113.95px = desktop), so the
  Mac-host CDP harness remains authoritative for mobile. Consistent with every
  prior run.

## Phase 2 — Competitor analysis

- **online-solitaire.com — UNCHANGED, 88th consecutive run.** Title "Solitaire -
  Play Online for Free". Classic Klondike board, Turn 1 / Turn 3, All games
  dropdown, September challenge calendar (day 18 highlighted, "No wins" banner),
  daily leaderboard (Fravonox KR, Jerryzuh CZ ×2), Achievements N/A, Signup-to-save
  CTA, New game/Hint/Undo bottom-left, Bookmark/Download/Settings/More toolbar.
  Right rail **expanded** today (state-only, matches 09-12/09-14 expanded days).
- No new features / design / content vs prior runs. No new gaps to act on.
- Logged to `docs/research/competitor-log.csv`. (Competitor content treated as
  untrusted data; DOM + text scanned, no embedded instructions acted upon.)

## Phase 3 — SEO & schema spot-check

Three long-tail pages the Friday scorecard never covers (rotated fresh), one per
property:

- **playfreecellonline.com/blog/is-every-freecell-game-winnable** (freecell owner):
  1 H1, meta 138 chars, self-canonical, JSON-LD Org+WebSite+Article+BreadcrumbList,
  25 cluster links. **Single WebSite node** (confirms the 09-17 dedupe holds on
  interior pages).
- **solitairestack.com/blog/best-solitaire-games-for-seniors** (hub owner): 1 H1,
  meta 152 chars, self-canonical, Org+WebSite+Article+BreadcrumbList, 1 WebSite
  node, 46 cluster links.
- **playspidersolitaireonline.com/blog/spider-solitaire-position-reading** (spider
  spoke): 1 H1, meta 143 chars, self-canonical, Org+WebSite+Article+BreadcrumbList,
  1 WebSite node, 22 cluster links.

All clean — 1 H1 each, unique meta descriptions, sane self-canonicals, exactly one
WebSite node per page. No fixes needed.

## Phase 4 — Improvements

Priority order: (1) scorecard ❌ — none; (2) Phase 1–3 bugs — none; (3) plan units.
With no fails and no bugs, took **one same-run-safe polish slice** from the
documented backlog:

- **Removed the dead `toBristolLoc` function** from
  `src/app/(main)/bristol/BristolGamePage.tsx` (lines 30–41). This was the last
  remaining candidate flagged in the engine/page dead-code cleanup cadence
  (documented across the 09-07…09-14 ledger rows as "dead `toBristolLoc` local fn
  … not an import, needs closer look").
- Verified dead: single occurrence (definition only, **zero call sites** by grep),
  authoritatively flagged by ESLint `@typescript-eslint/no-unused-vars` at 30:10.
- The `BristolLocation` import **stays used** (line 108, `let fromLoc:
  BristolLocation`), so the import line is untouched.
- Clears **2 warnings** (30:10 unused-vars + 30:32 no-explicit-any on the same
  function). No behavior / layout / route / type / locale change.
- **tsc `--noEmit` exit 0**; full lint **368 → 366 warnings, 0 errors**, no new
  warnings.

## Phase 5 — Report

- This report + competitor CSV row + mobile audit artifacts + the Bristol fix,
  batched into separate commits and a **single push**.
- Ledger row appended.
- Post-push verification: Vercel deployments READY on all 4 projects + GitHub CI
  green on the pushed commit (recorded below after push).

## Remaining backlog

- **U6 play-UX:** 9 portrait dead-space candidates (45–51% unused vertical on
  phones) — below-board next-actions / contextual hints / compact secondary
  content. Planning signal, not a failure.
- **U5 content/locale gate:** French-locale completeness audit (hreflang
  reciprocity + core cluster) — not yet sliced; a future small slice.
- **Bristol `any`-type cleanup:** remaining `no-explicit-any` warnings in
  BristolGamePage.tsx (81:46, 81:55, 119:46, 119:55) + unused `s` (120) and
  `_cardId` (174) — deeper than a dead-code removal, defer for a focused slice.
- Engine/page dead-import + dead-function cleanup cadence: **complete** as of this
  run.
