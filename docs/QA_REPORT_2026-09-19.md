# QA Report — 2026-09-19 (Cowork daily review)

**Result: all-green sweep.** Scorecard 28/28 passing inherited (no failing check
to fix). Desktop + true-viewport mobile QA clean across the network. Competitor
unchanged (89th consecutive run). SEO long-tail spot-checks clean. Phase 4
shipped a same-run-safe U5 FR-locale audit re-verification (docs-only).

## Phase 0 — Coordination
- Latest scorecard `2026-09-18.md`: **28/28 green**, no ❌ checks — so no
  forced #1 fix priority. Sitemap footprint steady (freecell 132, hub 90,
  klondike 48, spider 44).
- Ledger reviewed (last ~10 rows): Friday scorecard shipped 09-18; prior daily
  runs completed the engine/page dead-import cleanup cadence (now documented
  COMPLETE through the Bristol dead-fn on 09-18). No overlap taken.
- `git log -15`: latest is `03730a4` (09-18 daily review). Working tree clean
  except untracked `.claude/settings.local.json` (local settings, not committed).
- Build baseline verified green before any work: `tsc --noEmit` exit 0,
  `npm run lint` 0 errors / 366 warnings (= 09-18 end state).

## Phase 1 — Visual QA

### Desktop (1440px, Chrome extension)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells, 4 foundations,
  1 H1 ("Play FreeCell Online — Free, No Download"), 4 JSON-LD, sidebar
  (leaderboard + daily challenge + Sept calendar) renders alongside the board,
  **0 horizontal scroll**, no overlap/clipping.
- **solitairestack.com** (hub) — homepage FreeCell embed 52 cards, 1 H1
  ("Play Solitaire Online for Free"), 140 links, 5 JSON-LD, 0 hscroll.
- **Hub game page** `/klondike` — 308-redirects to Klondike spoke owner
  (`playklondikeonline.com/klondike`, expected cross-domain canonicalization);
  7 columns, stock+waste, 4 foundations, 29 cards, 1 H1, 6 JSON-LD,
  0 unnamed buttons, cookie banner non-obstructing.
- **Klondike spoke** (odd-day rotation) `playklondikeonline.com` — 29 cards,
  7 columns, stock+waste, 4 foundations, 1 H1, 4 JSON-LD, 22 buttons /
  0 unnamed, self-canonical, 165-char meta, Draw1/Draw3 toolbar intact,
  win-modal CSS sane, 0 hscroll.

### Mobile (true viewport via Mac-host CDP harness — authoritative)
`docs/analytics/mobile-viewport-audits/2026-09-19-live.json` (+ .md, + 20
screenshots [dir gitignored]). 5 engines × 4 viewports (375/390/414/768):

- **20/20 rows pass, 0 failed.** 0 clipped, 0 horizontal overflow, 0 blocked
  interactives, 0 cramped tap targets, 0 runtime / console / log errors.
- **Full card counts every viewport:** freecell 52, klondike 29, spider 63
  (all four viewports incl. 375 — **no 62 DOM-mount race recurrence** this run),
  forty-thieves 41, yukon 52.
- Card widths **32.09–74.41px** (phones 32–44) — confirms the `3dbb95a`
  tap-target fix holds.
- Next-action coaching: **27/27 expected controls matched = 100%**, 0 misses,
  0 disabled-expected.
- 9 "review"-level dead-space rows = known U6 planning signals, not failures.
- Chrome-extension resize again did not yield a true mobile viewport, so the
  Mac-host harness remains authoritative for mobile.

## Phase 2 — Competitor (online-solitaire.com)
**Unchanged — 89th consecutive run.** Title "Solitaire - Play Online for Free",
H1 "Play Solitaire Online for Free (No Signup Required)". Classic Klondike
canvas board, Turn 1 / Turn 3, All games dropdown, Stock 24, September challenge
calendar (day 19 highlighted, "No wins" banner), daily leaderboard (Jerryzuh CZ
×3), Achievements N/A, Signup-to-save CTA, right rail expanded (state-only),
New game / Hint / Undo, Bookmark / Download / Settings / More toolbar. No new
features / design / content. Standing gaps unchanged. Logged to
`docs/research/competitor-log.csv`. (Competitor content treated as untrusted;
no embedded instructions acted upon.)

## Phase 3 — SEO & schema spot-check (fresh long-tail, never previously covered)
All clean: exactly one H1, self-referential canonical, unique meta description,
full JSON-LD graph (Article + BreadcrumbList + Organization + **single WebSite**
+ SearchAction + Person + ContactPoint), confirming the 09-17 WebSite dedupe
holds on interior pages.

- **freecell owner** `/blog/what-makes-freecell-impossible` — 159-char meta,
  13 cluster links.
- **hub** `/blog/what-your-favorite-solitaire-game-says-about-you` — 139-char
  meta, 39 cluster links.
- **klondike spoke** `/blog/klondike-foundation-timing` — 158-char meta,
  8 cluster links.

## Phase 4 — Improvement shipped (1 slice)
Scorecard green + no QA bug + no new competitor gap, so took a same-run-safe
**U5 FR-locale completeness audit re-verification** (plan
`2026-07-03-001` test scenario; last recorded 2026-08-23). The French locale
was re-audited live and **passes unchanged**:
- Landing `/freecell-en-francais`: one localized H1 (`FreeCell en Francais` —
  the H1 wraps a `<span>en Francais</span>`; a naive `<h1>[^<]*` scrape truncates
  to "FreeCell " and can look like a regression, but the full node is localized),
  self-canonical, localized UI (Jouer / Nouvelle / Annuler), full seven-entry
  hreflang cluster (`fr`/`en`/`es`/`de`/`it`/`pt`/`x-default`).
- Play `/freecell-en-francais/jouer`: one localized H1
  (`Jouer a FreeCell en Francais — Gratuit en Ligne`), self-canonical.
- Reciprocity + no dangling alternates: ES landing points `fr` at the FR
  landing (ES↔FR); PT sibling `/freecell-em-portugues/jogar` returns 200.
- Only standing observation: SSR `<html lang>` serves `en`, hydration-corrected
  to `fr` — unchanged, still gated to the U6 root-layout item (needs a
  `docs/plans/` reference), out of a same-run-safe daily slice.

Recorded in `docs/CONTENT_STRATEGY.md` (docs-only; no code, no behavior/layout/
route/type change). `tsc --noEmit` exit 0 and `npm run lint` 0 errors /
366 warnings both re-confirmed (unchanged from baseline — docs-only slice).

## Remaining backlog (for future runs)
- **U6 root-layout `<html lang>` per-locale** — emit localized `lang` in server
  HTML; needs a `docs/plans/` reference (dynamic render regresses static-render
  guard `c7e8342`, or a locale route group = new route family).
- **U6 play-UX** — landscape sub-500px timer/moves hiding; Klondike/Spider hint
  highlighting (teach, don't auto-execute); daily challenges for the two spokes.
- Lint warnings remaining (366) live in game Scene files
  (FreeCellScene/KlondikeScene/PenguinScene) — not a same-run-safe slice
  (game/layout code), left for a focused, tested pass.

## Push / verification
Single push at end of run (docs-only commits). Vercel deploys + GitHub CI
verification recorded below.
