# QA Report — 2026-09-20 (Cowork daily review)

**Result: all-green sweep.** Scorecard 28/28 passing inherited (no failing check
to fix). Desktop + true-viewport mobile QA clean across the network. Competitor
unchanged (90th consecutive run). SEO long-tail spot-checks clean. Phase 4
shipped a same-run-safe lint-polish slice (one verified-dead unused local in the
Penguin scene; 366 → 365 warnings).

## Phase 0 — Coordination
- Latest scorecard `2026-09-18.md`: **28/28 green**, no ❌ checks — so no forced
  #1 fix priority. Sitemap footprint steady (freecell 132, hub 90, klondike 48,
  spider 44).
- Ledger reviewed (last ~10 rows): Friday scorecard shipped 09-18; the daily
  runs completed the engine/page dead-import cleanup cadence (documented COMPLETE
  through the Bristol dead-fn on 09-18); 09-19 re-verified the U5 FR-locale audit.
  No overlap taken with the Monday content routine or Friday scorecard.
- `git log -15`: latest is `287ad80` (09-19 daily review). Working tree clean
  except untracked `.claude/settings.local.json` (local settings, not committed).
- Build baseline verified green before any work: `tsc --noEmit` exit 0,
  `npm run lint` 0 errors / 366 warnings (= 09-18/09-19 end state).

## Phase 1 — Visual QA

### Desktop (1440px, Chrome extension; innerWidth 1512 extension quirk)
- **playfreecellonline.com** — 52 cards, 8 cascades, 4 free cells, 4 foundations,
  1 H1 ("Play FreeCell Online — Free, No Download"), 4 JSON-LD, self-canonical,
  150-char meta, sidebar (leaderboard + daily challenge) renders alongside the
  board, **0 horizontal scroll**, no overlap/clipping.
- **solitairestack.com** (hub) — homepage FreeCell embed 52 cards, 1 H1
  ("Play Solitaire Online for Free"), 139 links, 5 JSON-LD, self-canonical,
  0 hscroll.
- **Hub game page** `/yukon` — 308-redirects to Klondike spoke owner
  (`playklondikeonline.com/yukon`, expected cross-domain canonicalization);
  52 cards, 7 columns, 4 foundations, 1 H1 ("Yukon Solitaire"), 6 JSON-LD,
  0 unnamed buttons, self-canonical, cookie banner non-obstructing (board fully
  visible above it), 0 hscroll.
- **Spider spoke** (even-day rotation) `playspidersolitaireonline.com` — 63 cards,
  10 columns, 8 foundations, 1 H1 ("Play Spider Solitaire Online — Free, No
  Download"), 4 JSON-LD, 22 buttons / 0 unnamed, self-canonical, 168-char meta,
  1/2/4-Suit toolbar intact (Undo/Hint/New Deal), win-modal CSS sane, 0 hscroll.

### Mobile (true viewport via Mac-host CDP harness — authoritative)
`docs/analytics/mobile-viewport-audits/2026-09-20-live.json` (+ .md, + 20
screenshots [dir gitignored]). 5 engines × 4 viewports (375/390/414/768):

- **20/20 rows pass, 0 failed.** 0 clipped, 0 horizontal overflow, 0 blocked
  interactives, 0 cramped tap targets, 0 runtime / console / log errors —
  harness verdict: "No hard audit failures detected."
- **Full card counts every viewport:** freecell 52, klondike 29, spider 63
  (all four viewports incl. 375 — **no 62 DOM-mount race recurrence** this run),
  forty-thieves 41, yukon 52.
- Card widths **32.09–74.41px** (phones 32–44) — confirms the `3dbb95a`
  tap-target fix holds.
- Next-action coaching: **27/27 expected controls matched = 100%**, 0 misses,
  0 disabled-expected (15 rows carry a visible next-action panel).
- 9 "review"-level dead-space rows (45–51% unused vertical) = known U6 planning
  signals, not failures.
- Chrome-extension resize again did not yield a true mobile viewport (innerWidth
  stayed 1512, card 122px desktop), so the Mac-host harness remains authoritative
  for mobile.

## Phase 2 — Competitor (online-solitaire.com)
**Unchanged — 90th consecutive run.** Title "Solitaire - Play Online for Free",
H1 "Play Solitaire Online for Free (No Signup Required)". Classic Klondike canvas
board, Turn 1 / Turn 3, All games dropdown, Stock 24, "Show daily challenge &
stats" button (right rail collapsed today = state-only, vs 09-18/09-19 expanded),
New game/Hint/Undo bottom-left, Bookmark/Download/Settings/More toolbar, no CMP
modal on load, 75 links. No new features/design/content. Standing gaps where they
lead (download prominence, deck/background theming, illustrated face cards,
520-catalog framing, non-solitaire card games) and where we lead (editorial
depth, richer per-page JSON-LD, spoke-domain SEO, no-signup local save, named
daily deals, ad-light dark theme, PWA offline+resume, 6-locale FreeCell i18n,
mobile next-action coaching) are unchanged. Logged to
`docs/research/competitor-log.csv`. Competitor content treated as untrusted data.

## Phase 3 — SEO & schema spot-check (3 fresh long-tail pages, never previously covered)
- **freecell owner** `/blog/freecell-vs-klondike` — 1 H1 ("FreeCell vs Klondike:
  Which Solitaire Game Is Better?"), JSON-LD Organization + WebSite + Article +
  BreadcrumbList, **single WebSite node**, self-canonical, 136-char meta.
- **hub** `/blog/how-52-cards-produce-hundreds-of-games` — 1 H1, JSON-LD
  Organization + WebSite + Article + BreadcrumbList, single WebSite node,
  self-canonical, 140-char meta, 98 internal links.
- **spider spoke** `/blog/spider-solitaire-when-to-deal` — 1 H1 ("When Should You
  Deal a New Row in Spider Solitaire?"), JSON-LD Organization + WebSite + Article
  + BreadcrumbList, single WebSite node, self-canonical, 147-char meta, 41
  internal links.
- All three clean: one H1, unique meta, self-canonical, **single WebSite node**
  each — confirms the 09-17 dup-WebSite dedupe holds on interior pages. No fixes
  needed.

## Phase 4 — Improvement shipped (same-run-safe lint-polish slice)
Buckets in priority order were empty at the top: (1) no ❌ scorecard checks
(28/28 green), (2) no bugs found in Phase 1–3, (3) U6 backlog items are behavior
changes needing tests + in-browser verification (too large for an unattended
same-run-safe slice) and the U5 FR audit was re-verified 09-19, (4) no new
competitor gaps. So Phase 4 took a same-run-safe lint-polish slice:

- **`src/game/PenguinScene.ts`** — removed the verified-dead unused local
  `const overlap = this.getCurrentOverlap();` (line 1349, drag-settle path).
  Confirmed unused by authoritative ESLint `@typescript-eslint/no-unused-vars`
  [1349:15] — the value is never read in its block. `getCurrentOverlap()` is an
  idempotent memoized getter (populates `this.cachedOverlap` only when null) and
  is called at three other live sites (369, 1154, 1206), so dropping this dead
  call changes no rendering/layout behavior. `tsc --noEmit` exit 0 before+after,
  full lint **366 → 365 warnings** (−1 exactly), 0 errors, no new warnings, no
  behavior/layout/route/type/locale change.

## Phase 5 — Validation & push
- `npx tsc --noEmit` exit 0; `npm run lint` 0 errors / 365 warnings.
- Two descriptive commits (docs QA + lint-polish) batched into a **single push**.
- Post-push: Vercel deploys verified READY on all 4 projects + GitHub CI green.

## Remaining backlog (not blocking)
- U6 play-UX items (landscape mobile timer/moves below 500px height; Klondike/
  Spider hint highlighting to teach rather than auto-execute; generalize daily
  challenges to spokes) — each needs its own commit with tests + in-browser
  verification; not a same-run-safe unattended slice.
- Remaining lint warnings are `@typescript-eslint/no-explicit-any` (122),
  `react/no-unescaped-entities` (77), and non-trivial `no-unused-vars` cases
  needing a deeper focused slice.
- 9 mobile dead-space (45–51% unused vertical) rows = U6 landscape/spacing signal.
