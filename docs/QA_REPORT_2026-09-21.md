# QA Report — 2026-09-21 (Cowork daily review)

Odd-day rotation → **Klondike** spoke of the day. Baseline at run start:
`git status` clean (only untracked `.claude/settings.local.json`), tsc exit 0,
lint 0 errors / 365 warnings (= 09-20 baseline). Latest scorecard 2026-09-18 =
**28/28 green**, no ❌ checks → no scorecard-priority fix. Monday content
pipeline lane (spoke blog posts) left untouched — QA/fix/polish only.

## Phase 1 — Visual QA

### Desktop (1440, Chrome extension; innerWidth read true 1440 this run)
- **playfreecellonline.com** — 52 cards / 8 cascades, 4 FreeCells + 4
  foundations properly spaced, sidebar (Daily Leaderboard + Daily Challenge)
  renders alongside board, 1 H1 (`Play FreeCell Online — Free, No Download`),
  0 horizontal scroll, 4 JSON-LD, single WebSite node, self-canonical, meta 150ch.
- **solitairestack.com** (hub homepage) — FreeCell board 52 cards, 1 H1
  (`Play Solitaire Online for Free`), 140 links, 5 JSON-LD, single WebSite,
  self-canonical, 0 hscroll.
- **solitairestack.com/pyramid** (hub game page) — 28-card pyramid + stock/waste,
  toolbar intact (24 stock / 28 pyramid / 2 recycles), 1 H1 (`Pyramid Solitaire`),
  6 JSON-LD, self-canonical, 0 hscroll. The lone "unnamed" button carries
  `title="Draw from stock"` (has an accessible name — filter false positive, not a gap).
- **playklondikeonline.com** (spoke, odd-day rotation) — 29 cards / 7 columns,
  stock+waste + 4 foundations, Draw 1 / Draw 3 toggle, toolbar intact, 1 H1
  (`Play Klondike Solitaire Online — Free, No Download`), 4 JSON-LD, single
  WebSite, self-canonical, meta 165ch. Cookie banner non-obstructing at bottom,
  left unaccepted. Win-modal CSS sane.

### Mobile + tablet (Mac-host CDP true-viewport harness — authoritative)
Chrome-extension resize to 375 again did **not** yield a true mobile viewport
(innerWidth stayed 1440, card 113.95px desktop) — confirming the standing
finding; Mac-host harness is authoritative. `2026-09-21-live` run:

- **20/20 rows pass**, 0 need-review, 0 fail — "No hard audit failures detected".
- 0 clipped, 0 overflow across every engine × viewport (375 / 390 / 414 / 768).
- Full card counts at every viewport: freecell 52 / klondike 29 / **spider 63
  (all 4 viewports, no 62-recurrence)** / forty-thieves 41 / yukon 52.
- Card widths 32.09–74.41px (phones 32–44px) → 3dbb95a tap-target fix holds.
- Next-action health 15/15 portrait matched, 0 missing / 0 disabled.
- Portrait dead-space 45–51% on the usual freecell/spider/forty-thieves rows =
  known U6 planning signal, not a failure.
- Artifacts: `docs/analytics/mobile-viewport-audits/2026-09-21-live.json` + `.md`
  (+ 20 screenshots, dir gitignored).

## Phase 2 — Competitor (online-solitaire.com)

**Unchanged, 91st consecutive run.** Title `Solitaire - Play Online for Free`,
H1 `Play Solitaire Online for Free (No Signup Required)`. Classic Klondike canvas,
Turn 1 / Turn 3, All games dropdown, Stock 24, "Show daily challenge & stats"
button (right rail COLLAPSED = state-only, matches 09-20), no CMP modal on load,
76 links. No new features/design/content. Logged to
`docs/research/competitor-log.csv`. Competitor content treated as untrusted —
DOM/text scanned only, no embedded instructions acted upon.

## Phase 3 — SEO / schema spot-check (fresh long-tail, never previously covered)

All three clean — 1 H1, valid JSON-LD graph, single WebSite, self-canonical,
unique meta, healthy internal cluster linking; 09-17 dup-WebSite dedupe holds interior:

- **playklondikeonline.com/blog/klondike-stock-cycle-trap** (spoke, today's
  rotation) — 200, 1 H1, Organization + WebSite + Article + BreadcrumbList,
  single WebSite, self-canonical, meta 135ch, 24 cluster links.
- **playfreecellonline.com/how-freecell-supermoves-work** (freecell owner) —
  200, 1 H1, Organization + WebSite + Article + FAQPage + BreadcrumbList,
  single WebSite, self-canonical, meta 154ch, 25 cluster links.
- **solitairestack.com/blog/which-solitaire-games-are-most-skill-based** (hub) —
  200, 1 H1, Organization + WebSite + Article + BreadcrumbList, single WebSite,
  self-canonical, meta 142ch, 45 cluster links.

## Phase 4 — Improvement shipped

Priority buckets 1 (scorecard ❌) and 2 (Phase 1–3 bugs) both empty; scorecard
28/28 green, no QA bug. Took a same-run-safe lint-polish slice:

- **`src/app/(main)/deals/DealsExplorer.tsx`** — removed the verified-dead unused
  `type Difficulty` import from the `@/lib/curatedDeals` import line
  (`{ ...communityDeals, type Difficulty, type DealInfo }` →
  `{ ...communityDeals, type DealInfo }`). Confirmed single-occurrence by
  authoritative ESLint `@typescript-eslint/no-unused-vars` [8:72] + whole-word
  grep (appears only on the import line, zero real usage); the other named
  imports (`famousDeals`/`beginnerDeals`/`expertDeals`/`communityDeals`/`DealInfo`)
  all stay used. Per-file eslint now 0 problems; tsc `--noEmit` exit 0
  before + after; full lint 365→364 warnings (−1 exactly), 0 errors, no new
  warnings; no behavior/layout/route/type/locale change. Commit `14a10ec`.

## Remaining backlog

- Lint warnings 364 (0 errors). Engine/page dead-import cadence is complete;
  remaining are `any`-types (~122), no-unescaped-entities (~77), deeper
  no-unused-vars (incl. `absoluteUrl` dead in 4 below-fold components, dead
  `_cardId` underscore signature params — need a focused non-import slice).
- U6 play-UX backlog: 45–51% portrait dead-space below board (next-action /
  contextual-hint placement) — planning signal, needs a plan slice.
- Standing competitor gaps where they lead (unchanged): native/PWA download
  prominence, deck/background theming, illustrated face cards, 520-catalog
  framing, non-solitaire card games, single-surface mobile controls.
