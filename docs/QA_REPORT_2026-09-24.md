# QA Report — 2026-09-24 (cowork-daily-review)

**Producer:** cowork-daily-review · **Rotation:** even day → Spider spoke
**Baseline inherited:** tsc exit 0 · lint 0-err/362-warn (matches 09-23 ledger)
**Shipped:** FreecellBelowFold dead-import polish (−1 warning → 361) · competitor 94th · SEO long-tail 3-page spot-check clean

---

## Phase 0 — Coordination

- **LEDGER:** last producer runs = 09-23 (FortyThievesScene dead-vars polish, +09-22 recovery), 09-21 DealsExplorer, Monday content routine shipped `klondike-draw-1-stock-cycling` + `spider-solitaire-how-to-improve` (commit 625be55). No overlap taken.
- **Latest scorecard (2026-09-18):** **28/28 green, zero ❌**. No scorecard-driven fix priority today.
- **git log -15:** clean history through 09-23 `dfdd708`. Working tree clean at start (only untracked `.claude/settings.local.json`).

## Phase 1 — Visual QA

### Desktop (1440px, Chrome extension — innerWidth true 1440 this run)
- **playfreecellonline.com:** 52 cards, cardW 113.95px, 8 cascades + 4 FreeCells + 4 foundations spaced, toolbar intact, sidebar (ad + daily leaderboard + daily challenge) alongside board, 1 H1 "Play FreeCell Online — Free, No Download", 0 hscroll, 4 JSON-LD, self-canonical. **PASS**
- **solitairestack.com (hub):** homepage embeds FreeCell board + leaderboard/challenge/Sept-calendar sidebar; 1 H1 "Play Solitaire Online for Free", 140 links, 5 JSON-LD, self-canonical, 0 hscroll, 0 unnamed buttons. **PASS**
- **playspidersolitaireonline.com (spoke, even-day rotation):** 63 cards, cardW 104.22px, 10 columns + 8 foundations + stock top-right, 1/2/4-Suit selector, toolbar intact (Undo/Hint/New Deal), 22 buttons/0 unnamed, 1 H1, 4 JSON-LD, self-canonical, 0 hscroll, win-modal CSS sane. **PASS**

### Mobile — TRUE viewport (Mac-host CDP harness; Chrome-extension resize confirmed non-authoritative again — window→375 but innerWidth stayed 1440/113.95px desktop)
- Full 5-engine × 4-viewport (375/390/414/768) run: **20/20 rows passed, 0 need-review (hard), "No hard audit failures detected."**
- 0 clipped · 0 horizontal overflow · 0 blocked controls · 0 cramped tap targets · 0 runtime/console/log errors.
- Next-action coverage **27/27 (100%)**, 0 missing / 0 disabled expected controls.
- FULL card counts every viewport: freecell 52 / klondike 29 / spider 63 (all 4 viewports incl. 375 — **no 62-recurrence**) / forty-thieves 41 / yukon 52.
- Card widths 32.09–74.41px (phones 32–44px) — confirms 3dbb95a tap-target fix holds.
- 9 portrait dead-space candidates (45–52% unused vertical) = known **U6 planning signal, not a failure**.
- Screenshots: 20 captured in `docs/analytics/mobile-viewport-audits/2026-09-24-live-screenshots/` (dir gitignored). Spider@375 visually verified: 10 columns, 8 foundations, stock, suit selector, "Build a clean run" Deal/Tips next-action panel, bottom nav bar intact, no clipping.

### Tablet (768×1024, in the harness set)
- All 5 engines fill correctly without the sidebar; 0 overflow/clipped. **PASS**

**No visual bugs found.**

## Phase 2 — Competitor Analysis

- **online-solitaire.com: UNCHANGED, 94th consecutive run.** Title "Solitaire - Play Online for Free", H1 "Play Solitaire Online for Free (No Signup Required)". Classic Klondike canvas, Turn 1/Turn 3, All games dropdown, Stock 24, "Show daily challenge & stats" (right rail COLLAPSED = state-only, matches 09-20/21/23), New game/Hint/Undo bottom-left, Bookmark/Download/Settings/More toolbar, no CMP modal, 76 links. No product change.
- Logged to `docs/research/competitor-log.csv` (row 303). Competitor content treated as untrusted; DOM+text scanned only, no embedded instructions acted upon.

## Phase 3 — SEO & Schema Spot-Check (pages the Friday scorecard does not cover)

Rotated onto the two freshly-shipped Monday-content spoke posts + one uncovered freecell long-tail:
- **playklondikeonline.com/blog/klondike-draw-1-stock-cycling** (NEW): 1 H1, self-canonical, meta 143 chars, JSON-LD Organization+WebSite+Article+BreadcrumbList, **single WebSite node**, 28 cluster links. **CLEAN**
- **playspidersolitaireonline.com/blog/spider-solitaire-how-to-improve** (NEW): 1 H1, self-canonical, meta 151 chars, Org+WebSite+Article+BreadcrumbList, single WebSite, 24 cluster links. **CLEAN**
- **playfreecellonline.com/blog/freecell-terminology-guide:** 1 H1, self-canonical, meta 155 chars, Org+WebSite+Article+BreadcrumbList, single WebSite, 27 cluster links. **CLEAN**

All 3 confirm the 09-17 dup-WebSite dedupe holds on interior pages; unique metas, sane canonicals. No SEO fixes needed.

## Phase 4 — Improvements (1 slice)

Scorecard green + no QA bug ⇒ same-run-safe lint-polish slice (dead-code cadence):
- **`src/components/FreecellBelowFold.tsx`** — removed verified-dead unused `absoluteUrl` import from the `@/lib/siteConfig` line (`import { absoluteUrl, gameUrl }` → `import { gameUrl }`). Confirmed single-occurrence by ESLint no-unused-vars [7:10] + whole-word grep (import line only, zero real usage); sibling `gameUrl` stays used (line 82). tsc --noEmit exit 0 before+after; full lint **362→361 warnings (−1 exactly)**, 0 errors, no new warnings, no behavior/layout/route/type/locale change.
- Note: identical dead `absoluteUrl` imports remain in KlondikeBelowFold/SpiderBelowFold/SolitaireHubHome and dead `CtaSection` in deals/solver/spider pages — future single-file polish candidates.

## Tooling note (self-inflicted, cleaned)

To run the true-viewport mobile harness in the Linux sandbox (no system Chrome), a Chrome-for-Testing binary was installed. A first install attempt extracted a 602MB `chrome/` dir into the repo root before being redirected to `/tmp`; this polluted `npm run lint` (scanned bundled JS → 1 spurious `no-this-alias` error + ~200 warnings). The stray dir was **removed from the Mac host** (`rm -rf chrome`), eslint cache cleared, and the gate re-verified clean (tsc 0 / lint 0-err/361-warn). The dir was untracked and never staged, so it never entered a commit. Harness binary + extracted libs live only in `/tmp` (sandbox-ephemeral).

## Remaining backlog

- U6 play-UX: portrait dead-space (45–52%) → below-board next-action / contextual content (planning, not a failure).
- U5 FR-locale: last re-verified 09-19, passes; standing SSR `<html lang>=en` (hydration-corrected) gated to U6 root-layout item.
- Long-tail lint-polish: remaining verified-dead `absoluteUrl`/`CtaSection` imports across sibling components/pages.

## Validation summary

- `npx tsc --noEmit` → exit 0
- `npm run lint` → 0 errors, 361 warnings (−1 vs 362 baseline)
- Single push at end of run; Vercel READY + GitHub CI verification appended below.
