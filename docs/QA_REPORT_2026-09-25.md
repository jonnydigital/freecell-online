# QA Report — 2026-09-25 (cowork-daily-review)

**Result:** All-green sweep. Desktop + true-viewport mobile (20-row Mac-host CDP
harness) + Klondike spoke (odd-day rotation) all clean; competitor unchanged
95th run; SEO long-tail spot-check clean; Phase 4 shipped a same-run-safe
lint-polish slice clearing the 09-24-flagged dead-`absoluteUrl` backlog across
3 components. No QA bugs found. One push.

## Phase 0 — Coordination

- Read AUTOMATION.md, last ~12 LEDGER rows, newest scorecard, `git log -15`.
- Newest scorecard = **2026-09-18, 28/28 passing, 0 ❌** (the Friday scorecard
  routine also fires today 13:00 UTC — watched for its docs-only push; single
  push at run end, rebase if it lands first).
- No failing checks, so Phase 4 falls to a plan-cadence slice (bucket 3).
- No overlap with the Monday content routine (last content: 09-22 Klondike
  draw-1 stock cycling + Spider improvement framework).

## Phase 1 — Visual QA

### Desktop (1440px, Chrome extension — innerWidth reported 1512, extension quirk)
- **playfreecellonline.com/game/1** — 52 cards, 8 cascades, 4 free cells, 4
  foundations, sidebar (ad + daily leaderboard + daily-challenge calendar,
  today=25 highlighted) alongside board; 1 H1, 4 JSON-LD, 31 btn / 0 unnamed,
  0 h-scroll, self-canonical. No overlap/clipping.
- **solitairestack.com** (hub homepage) — embedded FreeCell 52 cards, 1 H1, 5
  JSON-LD, 139 links, 24 btn / 0 unnamed, 0 h-scroll, self-canonical.
- **solitairestack.com/tripeaks** (hub game page) — 30 dom-cards, 3 peaks +
  stock/waste + tableau row, toolbar intact (New deal/Hint/Undo), 1 H1, 6
  JSON-LD, 16 btn / 0 unnamed, 0 h-scroll, self-canonical.
- **playklondikeonline.com** (spoke of the day, odd-day rotation) — 29 cards,
  7 columns, stock+waste, 4 foundations, Draw1/Draw3 toggle, toolbar intact,
  cookie banner non-obstructing (left unaccepted per policy), win-modal CSS
  sane; 1 H1, 4 JSON-LD, 22 btn / 0 unnamed, 0 h-scroll, self-canonical.

### Mobile — TRUE viewport (Mac-host CDP harness, `qa:mobile`, Node v22.19.0 + Chrome)
- 20/20 rows PASS, "No hard audit failures detected". 0 clipped, 0 horizontal
  overflow, 0 runtime/console/log errors, 0 blocked interactive, 0 cramped tap
  targets. Top controls visible on all 20 rows.
- FULL card counts every viewport (375/390/414/768): freecell 52, klondike 29,
  spider 63 (incl. 375 — **no 62-recurrence** this run), forty-thieves 41,
  yukon 52.
- Card widths 32.09–74.41px (phones 32–44px, confirms the 3dbb95a tap-target
  fix holds).
- Next-action coverage 27/27 = 100% (0 missing, 0 disabled), 15 rows with a
  visible next-action panel.
- 9 dead-space candidates (45–51% unused vertical) = known U6 planning signals,
  not failures.
- `bottomControlsVisible=false` on 11 rows (all tablet-768 + forty-thieves/yukon
  phones) is **byte-identical to 09-24 and 09-23** — a stable layout baseline
  (those engines lack the persistent bottom nav), not a regression.
- Screenshots viewed for evidence: FreeCell@375 (free cells/foundations/8 cols/
  next-action panel/bottom nav all visible) and Klondike@375 (D1/D3/stock+waste/
  7 cols/"Next best play" panel/bottom nav all visible) — both render perfectly.

### Tablet (768×1024, in the harness set)
- Board fills correctly; full card counts; 0 clipped/overflow. Covered in the
  20-row pass above.

## Phase 2 — Competitor Analysis (online-solitaire.com)

- **Unchanged, 95th consecutive run.** Classic Klondike canvas, Turn1/Turn3,
  All games (520) dropdown, Stock 24, right rail COLLAPSED ("Show daily
  challenge & stats"), Nutrafol display ad, standard bottom toolbar, CMP
  present but no blocking modal on load, 75 links (vs 76 = ad-driven
  fluctuation). No new features/design/content. Logged to competitor-log.csv.
- Competitor content treated as untrusted data: DOM + text scanned, no embedded
  instructions acted upon.

## Phase 3 — SEO & Schema Spot-Check (pages the Friday scorecard does not cover)

Three fresh long-tail pages, none covered in recent runs — all clean:
- **playfreecellonline.com/blog/freecell-solvability-math** (freecell owner) —
  1 H1, Org+WebSite+Article+BreadcrumbList, single WebSite node, meta 148ch,
  self-canonical, 23 cluster links.
- **solitairestack.com/blog/hardest-solitaire-games-ranked** (hub) — 1 H1, same
  graph, single WebSite, meta 134ch, self-canonical, 50 cluster links.
- **playklondikeonline.com/blog/klondike-dead-game-detection** (klondike spoke)
  — 1 H1, same graph, single WebSite, meta 151ch, self-canonical, 33 cluster
  links.
- All 1-H1, unique meta, single-WebSite — confirms the 09-17 dup-WebSite dedupe
  holds interior.

## Phase 4 — Improvements (1 slice)

- **Cleared the 09-24-flagged dead-`absoluteUrl` import backlog** across the
  three remaining components: `src/components/KlondikeBelowFold.tsx` (7:10),
  `src/components/SpiderBelowFold.tsx` (7:10), `src/components/SolitaireHubHome.tsx`
  (6:10). Each was authoritatively ESLint-flagged `no-unused-vars` +
  single-occurrence (import line only, zero real usage); siblings stay used
  (`gameUrl` 4/6 refs, `siteConfig` 6 refs). tsc --noEmit exit 0 before+after;
  full lint **361 → 358 warnings (−3 exactly), 0 errors, no new warnings**; no
  behavior/layout/route/type/locale change.

## Remaining backlog

- Lint dead-code cadence: engine/page dead-import cadence complete; BelowFold/Hub
  dead-`absoluteUrl` set now cleared. Remaining candidates: dead `CtaSection` in
  deals/solver/spider pages; deeper `any`-types [~122] and no-unescaped-entities
  [~77] warnings needing focused slices.
- U5: FR-locale SSR `<html lang>`=en hydration-corrected-to-fr observation still
  gated to U6 root-layout item.
- U6: landscape mobile timer/moves <500px height; Klondike/Spider hint
  highlighting; generalize daily challenges to spokes. 45–51% mobile dead-space
  is the standing planning signal.

## Validation summary

- `npx tsc --noEmit` → exit 0 (before + after).
- `npm run lint` → exit 0, 358 warnings, 0 errors (baseline 361, −3).
- Mobile harness → 20/20 pass, 0 hard failures.
- Single push at run end (batched commits); Vercel READY ×4 + CI green verified
  post-push.
