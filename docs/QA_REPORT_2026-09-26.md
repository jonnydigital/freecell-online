# QA Report — 2026-09-26 (cowork-daily-review)

**Result:** All-green sweep. Desktop + true-viewport mobile (20-row Mac-host CDP
harness) + Spider spoke (even-day rotation, desktop + 375px) all clean;
competitor unchanged 96th run; SEO long-tail spot-check clean; Phase 4 shipped a
same-run-safe lint-polish slice clearing the ledger-flagged dead-`CtaSection`
import backlog across the 4 remaining pages (deals / solver / spider how-to-play
/ spider strategy). No QA bugs found. One push.

## Phase 0 — Coordination

- Read AUTOMATION.md, last ~12 LEDGER rows, newest scorecard, `git log -15`.
- Newest scorecard = **2026-09-25, 28/28 passing, 0 ❌** — no failing checks, so
  Phase 4 falls to a plan-cadence slice (bucket 3).
- No overlap with the Monday content routine (last content landed 09-24/09-25:
  klondike-draw-1-stock-cycling + spider-solitaire-how-to-improve, reflected in
  the 09-25 scorecard sitemap deltas klondike +1 / spider +1 blog URL).
- Baseline before any work: repo clean (only untracked `.claude/settings.local.json`),
  `npx tsc --noEmit` exit 0, `npm run lint` 0 errors / 358 warnings (= 09-25 baseline).

## Phase 1 — Visual QA

### Desktop (1440px, built-in browser)
- **playfreecellonline.com** (→ /game/1) — 52 dom-cards confirmed via page text
  (full deck enumerated), 8 cascades, 4 free cells, 4 foundations, sidebar
  (daily leaderboard + daily-challenge September calendar) alongside board;
  title "Play FreeCell Online for Free | No Download Required". First-visit
  onboarding modal + cookie banner present (expected UI, CSS sane). No
  overlap/clipping, no horizontal scroll.
- **solitairestack.com** (hub homepage) — embedded FreeCell board, 8 cascades,
  4 FC / 4 foundations, sidebar intact; title "Solitaire Stack | Play FreeCell,
  Spider Solitaire, and More". Clean.
- **solitairestack.com/spider** (hub game route) — **308 → playspidersolitaireonline.com**
  (cross-domain consolidation to spoke owner working as designed).
- **playspidersolitaireonline.com** (spoke of the day, even-day rotation) —
  10 columns, 8 foundations, stock pile, toolbar intact (Spider Solitaire
  dropdown, 1/2/4-Suit selector, Time/Moves/Suits 0/8, Undo/Hint/New Deal),
  win-modal not triggered, cookie banner non-obstructing. No overlap/clipping.

### Mobile — TRUE viewport (Mac-host CDP harness, `qa:mobile`, Node v22.19.0 + Chrome)
- 20/20 rows PASS, "No hard audit failures detected". 0 clipped, 0 horizontal
  overflow (scrollWidth == viewport width every row), 0 runtime/console/log
  errors, 0 blocked interactive, 0 cramped tap targets.
- FULL card counts every viewport (375/390/414/768): freecell 52, klondike 29,
  spider 63 (incl. 375 — **no 62-recurrence** this run), forty-thieves 41,
  yukon 52.
- Card widths 32.09–74.41px (phones 32–44px, confirms the 3dbb95a tap-target
  fix holds).
- Next-action coverage 27/27 = 100% (0 missing, 0 disabled), 15 rows with a
  visible next-action panel.
- 9 dead-space candidates (45–51% unused vertical) = known U6 planning signals,
  not failures.
- Artifacts: docs/analytics/mobile-viewport-audits/2026-09-26-live.json + .md
  (+ 20 screenshots, dir gitignored).

### Spider spoke @ 375px (built-in browser, visual verification)
- All 10 columns fit with 0 clipping / 0 horizontal overflow; 8 foundations,
  stock pile, 1/2/4-Suit selector, "Build a clean run" next-action panel
  (Deal + Tips), and bottom nav (Home / New / Undo / Hint) all visible.
  Lower green area = the known U6 dead-space signal, not a failure.

### Tablet (768×1024, in the harness set)
- Board fills correctly; full card counts; 0 clipped/overflow. Covered in the
  20-row pass above.

## Phase 2 — Competitor Analysis (online-solitaire.com)

- **UNCHANGED, 96th consecutive run.** Classic Klondike canvas board, Turn 1 /
  Turn 3 toggle, All games dropdown, Stock 24, "Show daily challenge & stats"
  button (right rail collapsed = state-only, matches 09-24/09-25), New game /
  Hint / Undo bottom-left, Bookmark / Download / Settings / More toolbar, no CMP
  modal on load. No new features/design/content.
- Standing gaps where they lead: native/PWA download prominence, deck/background
  theming, illustrated face cards, 520-catalog framing, non-solitaire card
  games, single-surface mobile controls.
- We lead: editorial depth (28 variants + desks), richer per-page JSON-LD,
  spoke-domain SEO, no-signup local save, named daily deals, ad-light dark theme,
  PWA offline+resume, 6-locale FreeCell i18n, mobile next-action coaching.
- SECURITY: competitor content treated as untrusted; DOM + text scanned, no
  embedded instructions acted upon.
- Logged to docs/research/competitor-log.csv (2026-09-26 row).

## Phase 3 — SEO & Schema Spot-Check (long-tail, 3 fresh pages)

All clean — 1 H1, unique meta description, self-canonical, 4 JSON-LD scripts
with a rich graph (Article + BreadcrumbList + Organization + Person +
SearchAction + WebSite + ContactPoint + ListItem) and a **single WebSite node**
(confirms the 09-17 dedupe still holds on interior pages), cluster internal
links present:

- **playfreecellonline.com/blog/freecell-brain-training** — 1 H1, meta 129ch,
  self-canonical, 4 JSON-LD / 1 WebSite, 15 cluster links.
- **solitairestack.com/blog/how-many-solitaire-variants-exist** — 1 H1, meta
  137ch, self-canonical, 4 JSON-LD / 1 WebSite, 41 cluster links.
- **playspidersolitaireonline.com/blog/spider-solitaire-king-management** —
  1 H1, meta 160ch, self-canonical, 4 JSON-LD / 1 WebSite, 7 cluster links.

## Phase 4 — Improvements (bucket 3: plan-cadence, 1 same-run-safe slice)

Buckets 1 (failing scorecard checks) and 2 (QA bugs) are both empty, so Phase 4
took a same-run-safe lint-polish slice continuing the dead-import reduction
cadence.

- **Cleared the dead-`CtaSection` import backlog** flagged in the 09-24/09-25
  ledger rows. Four pages imported `CtaSection` from `@/components/content` but
  never rendered it: `src/app/(main)/deals/page.tsx`,
  `src/app/(main)/solver/page.tsx`, `src/app/(main)/spider/how-to-play/page.tsx`,
  `src/app/(main)/spider/strategy/page.tsx`.
- Each confirmed verified-dead by authoritative ESLint
  `@typescript-eslint/no-unused-vars` (CtaSection only; all sibling imports —
  ContentHero / JsonLd / ContentLinkCard / CardSection / SectionHeading /
  ContentBody — stay used, so import lines otherwise untouched). `CtaSection`
  itself remains defined and exported in `src/components/content` and used by
  many other pages, so only these 4 dead consumers were trimmed.
- `npx tsc --noEmit` exit 0 before + after; full lint **358 → 354 warnings
  (−4 exactly)**, 0 errors, no new warnings. No behavior/layout/route/type/locale
  change.
- Remaining lint candidates for future slices: deeper `any`-types
  (@typescript-eslint/no-explicit-any), `no-unescaped-entities`, and residual
  no-unused-vars in `src/game/*` (e.g. ClockScene `oldDepth`, CruelScene
  Rank/SUIT_SYMBOLS/NUM_ROWS, FlowerGardenScene `totalTopCols`, FreeCellScene
  `_y`/`pointer`/`_sprite`, KlondikeScene `e`).

## Phase 5 — Report / Ledger / Push / Verify

- This report + ledger row + competitor row + mobile audit json/md committed.
- Two descriptive commits, one push (`git push origin main`).
- Post-push verification: Vercel deployments READY on all 4 projects + GitHub CI
  green on the pushed commit (recorded below after push).

## Backlog / Notes for next run

- Scorecard 28/28 green; no regressions.
- Dead-import cadence for app/components is now effectively exhausted; next
  lint-polish slices should move into `src/game/*` no-unused-vars or a focused
  `any`-type slice.
- U6 dead-space (45–51% unused vertical on phones) remains a planning signal for
  the play-UX backlog (landscape toolbar / generalized daily challenges), not a
  QA failure.
